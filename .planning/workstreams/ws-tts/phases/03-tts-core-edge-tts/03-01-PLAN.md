---
phase: 03-tts-core-edge-tts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/error.rs
  - src/tts/mod.rs
  - src/lib.rs
autonomous: true
requirements:
  - TTS-01
  - TTS-02

must_haves:
  truths:
    - "TTSError 枚举定义 5 个变体（UnknownEngine/ConnectionFailed/AuthenticationFailed/SynthesisFailed/RetryExhausted），中文错误消息"
    - "WordBoundary 结构体包含 start_offset: u64 / end_offset: u64 / text: String 字段"
    - "TtsOutput 结构体包含 audio_file_path: PathBuf / word_boundaries: Vec<WordBoundary> / duration: f64 字段"
    - "TtsProvider trait 用 #[async_trait] 宏标记，有 synthesize 异步方法"
    - "synthesize() 路由器对未知引擎返回 TTSError::UnknownEngine"
    - "module-level pub async fn synthesize 导出，不包装在结构体中"
  artifacts:
    - path: "src/error.rs"
      provides: "TTSError 枚举"
      min_lines: 15
      contains: "pub enum TTSError"
    - path: "src/tts/mod.rs"
      provides: "WordBoundary, TtsOutput, TtsProvider 类型定义, synthesize 路由器"
      min_lines: 80
      contains: "pub struct WordBoundary | pub struct TtsOutput | pub trait TtsProvider | pub async fn synthesize"
    - path: "src/lib.rs"
      provides: "tts 模块声明"
      contains: "pub mod tts"
    - path: "Cargo.toml"
      provides: "async-trait 依赖"
      contains: "async-trait"
  key_links:
    - from: "src/lib.rs"
      to: "src/tts/mod.rs"
      via: "pub mod tts"
      pattern: "pub mod tts"
    - from: "synthesize()"
      to: "TTSError::UnknownEngine"
      via: "match engine dispatch"
      pattern: "UnknownEngine"
    - from: "TtsProvider trait"
      to: "TtsOutput"
      via: "synthesize return type"
      pattern: "Result<TtsOutput"
---

<objective>
构建 TTS 核心抽象层：定义 TTSError、WordBoundary、TtsOutput 数据类型，TtsProvider async trait 和 synthesize 路由器函数骨架。

Purpose: 为所有 TTS 引擎（Phase 3 Edge-TTS + Phase 12 其余引擎）提供统一的接口契约。Phase 6 纪录片流水线通过 synthesize() 调用 TTS。

Output:
- src/error.rs: 新增 TTSError 枚举（5 变体）
- src/tts/mod.rs: 新建模块，包含 WordBoundary、TtsOutput 结构体，TtsProvider trait，synthesize 路由器函数
- src/lib.rs: 添加 pub mod tts 声明
- Cargo.toml: 添加 async-trait 依赖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-CONTEXT.md
@.planning/REQUIREMENTS.md
@src/error.rs
@src/lib.rs
@Cargo.toml

<interfaces>
<!-- 现有错误枚举模式，Task 1 需遵循 -->
From src/error.rs:
```rust
#[derive(Error, Debug)]
pub enum ConfigError { /* 5 variants with Chinese messages */ }
#[derive(Error, Debug)]
pub enum FFmpegError { /* 5 variants with Chinese messages */ }
impl From<notify::Error> for ConfigError { /* ... */ }
// Tests follow pattern: test_<type>_error_<variant>_message_chinese
```

<!-- 现有模块声明模式，需遵循 -->
From src/lib.rs:
```rust
pub mod config;
pub mod ffmpeg;
pub mod error;
```

<!-- 现有依赖格式 -->
From Cargo.toml:
```toml
thiserror = "2.0.18"  # 精确版本号
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 TTSError 枚举 + WordBoundary/TtsOutput 数据类型</name>

  <files>
    src/error.rs
    src/tts/mod.rs
  </files>

  <read_first>
    src/error.rs（现有 ConfigError 和 FFmpegError 模式，Chinese error message 约定）
    src/tts/mod.rs（新文件，不存在则创建目录 src/tts/）
    config.example.toml（proxy 和 ui 配置段字段名）
  </read_first>

  <action>
    1. 在 src/error.rs 末尾（在 `impl From<notify::Error> for ConfigError` 之后）添加 TTSError 枚举，包含 5 个变体：

    ```rust
    /// TTS 领域错误
    #[derive(Error, Debug)]
    pub enum TTSError {
        #[error("未知 TTS 引擎: {engine}")]
        UnknownEngine { engine: String },

        #[error("TTS 连接失败: {0}")]
        ConnectionFailed(String),

        #[error("TTS 认证失败: {0}")]
        AuthenticationFailed(String),

        #[error("TTS 合成失败: {0}")]
        SynthesisFailed(String),

        #[error("TTS 重试耗尽: {0}")]
        RetryExhausted(String),
    }
    ```

    每个变体使用 `#[error("...")]` 中文消息，遵循 Phase 1 D-18（用户可见错误用中文）。

    2. 在 src/error.rs tests 模块末尾，为 TTSError 的 5 个变体各添加一个测试：

    测试模式（与 ConfigError/FFmpegError 一致）：
    ```rust
    #[test]
    fn test_tts_error_unknown_engine_message_chinese() {
        let err = TTSError::UnknownEngine { engine: "test".to_string() };
        let msg = err.to_string();
        assert!(msg.contains("未知 TTS 引擎"), "消息应包含中文: {}", msg);
    }
    #[test]
    fn test_tts_error_connection_failed_message_chinese() { ... }
    #[test]
    fn test_tts_error_authentication_failed_message_chinese() { ... }
    #[test]
    fn test_tts_error_synthesis_failed_message_chinese() { ... }
    #[test]
    fn test_tts_error_retry_exhausted_message_chinese() { ... }
    ```

    3. 创建 src/tts/ 目录和 src/tts/mod.rs 文件，首先添加 WordBoundary 和 TtsOutput 类型定义：

    ```rust
    use std::path::PathBuf;
    use crate::error::TTSError;

    /// 词边界，单位 100 纳秒（与 Python 版一致，Phase 6 字幕生成使用）
    ///
    /// 对应 D-08: start_offset/end_offset 单位 100 纳秒，与 Python 版 edge-tts 的 SubMaker 行为一致
    #[derive(Debug, Clone)]
    pub struct WordBoundary {
        pub start_offset: u64,
        pub end_offset: u64,
        pub text: String,
    }

    /// TTS 合成输出结果
    ///
    /// 对应 D-06: 调用方从返回值获取所有必要数据，无需额外查询
    /// 对应 D-09: 词边界直接内嵌在 word_boundaries 字段中，无独立 SubMaker 结构体
    #[derive(Debug, Clone)]
    pub struct TtsOutput {
        /// 生成的音频文件路径（MP3 格式）
        pub audio_file_path: PathBuf,
        /// 词边界时间序列，为空表示引擎不支持词边界
        pub word_boundaries: Vec<WordBoundary>,
        /// 音频总时长（秒）
        pub duration: f64,
    }
    ```

    使用 `std::path::PathBuf`（不要用 `String` 表示路径），遵循 D-06 精确字段名。
  </action>

  <verify>
    <automated>cargo build 2>&1 | grep -E "^error" && echo "BUILD_FAILED" || echo "BUILD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - src/error.rs 包含 `pub enum TTSError` 且有 5 个变体
    - src/error.rs 包含 `test_tts_error_unknown_engine_message_chinese` 等 5 个测试
    - src/tts/mod.rs 包含 `pub struct WordBoundary` 且有 start_offset/end_offset/text 字段
    - src/tts/mod.rs 包含 `pub struct TtsOutput` 且有 audio_file_path/word_boundaries/duration 字段
    - cargo build 零错误
    - `cargo test error::tests::test_tts_error_unknown_engine_message_chinese` 通过
  </acceptance_criteria>

  <done>
    TTSError 枚举 + WordBoundary/TtsOutput 数据类型编译通过，5 个 TTSError 错误消息测试全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TtsProvider trait 定义 + synthesize 路由器函数 + 模块导出</name>

  <files>
    Cargo.toml
    src/tts/mod.rs
    src/lib.rs
  </files>

  <read_first>
    src/tts/mod.rs（Task 1 已创建的 WordBoundary/TtsOutput 定义）
    src/error.rs（TTSError 枚举）
    src/lib.rs（现有 pub mod 声明模式）
    Cargo.toml（现有依赖格式和版本号约定）
  </read_first>

  <behavior>
    - TtsProvider trait 用 #[async_trait] 宏，标记为 Send + Sync
    - trait 方法签名（per D-07）: async fn synthesize(&self, text: &str, voice_name: &str, rate: f64, pitch: f64, output_path: &Path) -> Result<TtsOutput, TTSError>
    - synthesize() 路由器是模块级 pub async fn，不包装在结构体中（per D-12）
    - synthesize() match engine 对未知引擎返回 TTSError::UnknownEngine（per D-13）
    - 测试 1: 创建一个 mock 结构体，实现 TtsProvider trait，验证 trait 可被 impl（编译测试）
    - 测试 2: synthesize("nonexistent_engine", ...) 返回 Err(TTSError::UnknownEngine { engine: "nonexistent_engine".to_string() })
    - 测试 3: synthesize("nonexistent_engine", ...) 的错误消息包含引擎名
  </behavior>

  <action>
    1. 在 Cargo.toml `[dependencies]` 末尾添加：
    ```toml
    async-trait = "0.1.89"
    ```

    2. 在 src/tts/mod.rs 末尾（WordBoundary/TtsOutput 定义之后）添加：

    ```rust
    use async_trait::async_trait;
    use std::path::Path;

    /// TTS 引擎统一接口
    ///
    /// 对应 D-05: 使用 #[async_trait] 宏
    /// 对应 D-07: 独立参数 text/voice_name/rate/pitch/output_path
    /// 每个具体引擎（Edge-TTS、Azure 等）实现此 trait
    #[async_trait]
    pub trait TtsProvider: Send + Sync {
        /// 将文本合成为语音音频文件
        ///
        /// # 参数
        /// - text: 要合成的文本
        /// - voice_name: TTS 音色名
        /// - rate: 语速（1.0 为标准速度）
        /// - pitch: 音调（0 为标准音调）
        /// - output_path: 输出音频文件路径
        ///
        /// # 返回值
        /// - Ok(TtsOutput): 包含音频文件路径、词边界时序和时长
        /// - Err(TTSError): 合成失败
        async fn synthesize(
            &self,
            text: &str,
            voice_name: &str,
            rate: f64,
            pitch: f64,
            output_path: &Path,
        ) -> Result<TtsOutput, TTSError>;
    }

    /// TTS 路由器：按引擎名字符串分发到对应实现
    ///
    /// 对应 D-11: 字符串匹配分发
    /// 对应 D-12: 模块级 pub async fn，不包装为结构体
    /// 对应 D-13: 未知引擎返回 TTSError::UnknownEngine
    /// 对应 D-14: Phase 3 只实现 Edge-TTS 分支
    ///
    /// # 参数
    /// - engine: 引擎名称（如 "edge_tts"）
    /// - 其余参数同 TtsProvider::synthesize
    pub async fn synthesize(
        engine: &str,
        text: &str,
        voice_name: &str,
        rate: f64,
        pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        match engine {
            // Plan 03-02 将添加 "edge_tts" => EdgeTtsEngine::new(...).synthesize(...) 分支
            _ => Err(TTSError::UnknownEngine {
                engine: engine.to_string(),
            }),
        }
    }
    ```

    3. 在 src/lib.rs 的 pub mod 列表中添加：
    ```rust
    pub mod tts;
    ```
    放在 `pub mod error;` 之后，保持字母排序。

    4. 在 src/tts/mod.rs 中添加测试模块：
    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        use async_trait::async_trait;
        use std::path::Path;

        /// Mock TTS 引擎——验证 TtsProvider trait 可被实现
        struct MockTtsEngine;

        #[async_trait]
        impl TtsProvider for MockTtsEngine {
            async fn synthesize(
                &self,
                text: &str,
                _voice_name: &str,
                _rate: f64,
                _pitch: f64,
                output_path: &Path,
            ) -> Result<TtsOutput, TTSError> {
                // 返回最小有效 TtsOutput
                Ok(TtsOutput {
                    audio_file_path: output_path.to_path_buf(),
                    word_boundaries: vec![],
                    duration: 1.0,
                })
            }
        }

        #[tokio::test]
        async fn test_mock_engine_implements_trait() {
            let engine = MockTtsEngine;
            let result = engine
                .synthesize("hello", "voice", 1.0, 0.0, Path::new("test.mp3"))
                .await;
            assert!(result.is_ok());
            let output = result.unwrap();
            assert_eq!(output.duration, 1.0);
            assert!(output.word_boundaries.is_empty());
        }

        #[tokio::test]
        async fn test_synthesize_unknown_engine() {
            let result = synthesize(
                "nonexistent_engine",
                "hello",
                "voice",
                1.0,
                0.0,
                Path::new("test.mp3"),
            )
            .await;
            assert!(result.is_err());
            match result.unwrap_err() {
                TTSError::UnknownEngine { engine } => {
                    assert_eq!(engine, "nonexistent_engine");
                }
                _ => panic!("应为 UnknownEngine 错误"),
            }
        }

        #[tokio::test]
        async fn test_synthesize_unknown_engine_message() {
            let result = synthesize(
                "bad_engine",
                "hello",
                "voice",
                1.0,
                0.0,
                Path::new("test.mp3"),
            )
            .await;
            let err = result.unwrap_err();
            let msg = err.to_string();
            assert!(msg.contains("未知 TTS 引擎"), "消息应包含中文: {}", msg);
            assert!(msg.contains("bad_engine"), "消息应包含引擎名: {}", msg);
        }
    }
    ```

    注意：测试使用 `#[tokio::test]`，需要 Cargo.toml 中 tokio 已启用 "test" feature（当前 "features = ["full"]" 已包含 rt + macros，tokio::test 可用）。
  </action>

  <verify>
    <automated>cargo test --lib tts::tests 2>&1 | tail -5</automated>
    <automated>cargo build 2>&1 | grep -E "^error" && echo "BUILD_FAILED" || echo "BUILD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - Cargo.toml 包含 `async-trait = "0.1.89"`
    - src/tts/mod.rs 包含 `pub trait TtsProvider` 且使用 `#[async_trait]` 宏
    - src/tts/mod.rs 包含 `pub async fn synthesize` 路由器函数
    - synthesize() 对未知引擎返回 `Err(TTSError::UnknownEngine { .. })`
    - src/lib.rs 包含 `pub mod tts;`
    - 3 个测试全部通过:
      - test_mock_engine_implements_trait
      - test_synthesize_unknown_engine
      - test_synthesize_unknown_engine_message
    - cargo build 零警告零错误
  </acceptance_criteria>

  <done>
    TtsProvider trait 定义、synthesize 路由器函数、模块导出全部完成，3 个 TTS 核心测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthesize() 参数输入 | 调用方传入的 engine/text/voice_name/rate/pitch/output_path 参数 |
| TTSError 输出 | 错误消息中包含引擎名可能暴露内部路由信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S (Spoofing) | synthesize() router | mitigate | router 只做字符串 match，不执行任何认证逻辑；认证由具体引擎实现 |
| T-03-02 | I (Info Disclosure) | TTSError::UnknownEngine | accept | 引擎名字符串是公开信息（配置项），不涉及敏感数据 |
</threat_model>

<verification>
1. cargo build 零错误零警告
2. cargo test --lib tts::tests: 3 个测试全部通过
3. cargo test --lib error::tests: 5 个 TTSError 测试全部通过
4. cargo test --lib: 全部已有测试 + 新增 TTS 测试通过
</verification>

<success_criteria>
- [ ] cargo build 零错误零警告
- [ ] 3 个 tts 模块测试全部通过（mock trait impl, unknown engine dispatch, error message）
- [ ] 5 个 TTSError 测试全部通过（中文错误消息验证）
- [ ] 全部已有 37 个测试 + 8 个新增测试 = 45 个测试通过
- [ ] Cargo.toml 包含 async-trait 依赖
- [ ] src/error.rs 包含 pub enum TTSError
- [ ] src/tts/mod.rs 包含 WordBoundary、TtsOutput、TtsProvider trait、synthesize 路由器
- [ ] src/lib.rs 包含 pub mod tts;
</success_criteria>

<output>
After completion, create `.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-01-SUMMARY.md`
</output>
