---
phase: 07-sde-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md
  - src/prompt/templates/short_drama_narration/script_generation_v2.0.md
  - src/prompt/register.rs
autonomous: true
requirements:
  - SDE-03
user_setup: []
must_haves:
  truths:
    - "plot_analysis v1.0 提示模板存在于 short_drama_narration 目录中，包含 ${subtitle_content} 占位符"
    - "script_generation v2.0 提示模板存在于 short_drama_narration 目录中，包含 ${drama_name} / ${plot_analysis} / ${subtitle_content} 占位符"
    - "register.rs 中 register_short_drama_narration_prompts() 同时注册 plot_analysis (v1.0) 和 script_generation (v2.0) 两个模板"
    - "script_generation v2.0 已从 v1.0 升级，parameters 列表包含 drama_name + plot_analysis + subtitle_content"
    - "公开函数 analyze_subtitle_plot() 和 generate_sde_script() 可通过 PromptManager 获取对应模板"
    - "Python 版 plot_analysis.py 的 '分段剧情解析与时间戳定位' 逻辑在 Rust 模板中保持"
    - "Python 版 script_generation.py 的 '黄金开场 / 爽点放大 / 个性吐槽 / 悬念预埋' 创作要素在 Rust 模板中保持"
    - "OST=1 的 narration 格式 '播放原片+N' 约定在 v2.0 模板中明确要求"
    - "JSON items 数组输出格式在 v2.0 模板中有完整示例"
    - "所有模板编写测试通过，渲染后含正确占位符替换"
  artifacts:
    - path: "src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md"
      provides: "剧情分析 Prompt 模板（含 ${subtitle_content} 占位符）"
      min_lines: 50
    - path: "src/prompt/templates/short_drama_narration/script_generation_v2.0.md"
      provides: "解说脚本生成 Prompt 模板（含 ${drama_name} / ${plot_analysis} / ${subtitle_content} 占位符，OST=1 格式规范，JSON items 输出格式）"
      min_lines: 150
    - path: "src/prompt/register.rs"
      provides: "plot_analysis v1.0 注册 + script_generation v2.0 升级注册"
      min_lines: 15（新增行数）
  key_links:
    - from: "src/prompt/register.rs"
      to: "src/sde/script_gen.rs"
      via: "PromptManager::render_prompt('short_drama_narration', 'plot_analysis', Some('v1.0'), ...)"
      pattern: "plot_analysis"
---

<objective>
创建 SDE 所需的两个 Prompt 提示模板并注册到 PromptRegistry。

Purpose: SDE 流水线的两步 LLM 调用依赖这两个模板。plot_analysis v1.0 用于分析字幕剧情，script_generation v2.0 用于从剧情分析生成 JSON 解说脚本。Phase 4 的 PromptManager 已就绪，SDE 只需注册并调用。
Output: 2 个新模板文件 + 1 个 register.rs 修改。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/p7/phases/07-sde-pipeline/07-RESEARCH.md
@.planning/workstreams/p7/phases/07-sde-pipeline/07-CONTEXT.md
@.planning/workstreams/p7/phases/07-sde-pipeline/07-AI-SPEC.md

<interfaces>
<!-- PromptManager 调用合约——执行器使用这些签名 -->
From src/prompt/manager.rs:
```rust
impl PromptManager {
    pub fn render_prompt(
        &self,
        category: &str,        // "short_drama_narration"
        name: &str,            // "plot_analysis" | "script_generation"
        version: Option<&str>, // Some("v1.0") | Some("v2.0")
        vars: &HashMap<&str, &str>,
    ) -> Result<String, PromptError>;
}
```

From src/prompt/register.rs 当前 register_short_drama_narration_prompts():
```rust
fn register_short_drama_narration_prompts(registry: &mut PromptRegistry, errors: &mut Vec<PromptError>) {
    let prompt = Prompt {
        metadata: PromptMetadata {
            name: "script_generation", category: "short_drama_narration", version: "v1.0",
            model_type: ModelType::Text, output_format: OutputFormat::NarrationScript,
            tags: vec!["short-drama-narration", "script-generation", "text"],
            parameters: vec![
                ParameterDef { name: "plot_analysis", required: true, default: None, description: "剧情分析数据" },
                ParameterDef { name: "language", required: false, default: Some("zh-CN"), description: "输出语言" },
                ParameterDef { name: "style", required: false, default: Some("幽默风趣"), description: "文案风格" },
            ],
        },
        content: include_str!("templates/short_drama_narration/script_generation_v1.0.md"),
    };
    if let Err(e) = registry.register(prompt, true) { errors.push(e); }
}
```

From Python ref 脚本生成 v2.0 模板（app/services/prompts/short_drama_narration/script_generation.py）:
- 参数: ["drama_name", "plot_analysis", "subtitle_content"]
- 系统提示: "你是一位顶级的短剧解说up主，精通短视频创作的所有核心技巧。你必须严格按照JSON格式输出"
- 模板包含: 黄金开场(3秒法则)、主线提炼、爽点放大、个性吐槽、悬念预埋、卡点配合
- OST=1 格式: "播放原片+N" 约束
- 输出格式: {"items": [...]} JSON 数组
- 原声比例: 7:3（原声70%解说30%）
- 单个原声片段 3-8秒，均匀分布

From Python ref plot_analysis v1.0 模板（app/services/prompts/short_drama_narration/plot_analysis.py）:
- 参数: ["subtitle_content"]
- 系统提示: "你是一位专业的剧本分析师和剧情概括助手"
- 输出: 整体剧情概括 + 分段剧情解析（含时间戳）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 plot_analysis v1.0 提示模板</name>
  <files>src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md</files>
  <action>
    创建文件 `src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md`。

    内容对齐 Python 版 `app/services/prompts/short_drama_narration/plot_analysis.py` 的 get_template() 输出，使用 Markdown 格式。

    结构（必须包含）：
    1. 角色定义：你是一位专业的剧本分析师和剧情概括助手
    2. 任务说明：
       a. 整体剧情分析——概括核心剧情脉络
       b. 分段剧情解析与时间戳定位——将短剧划分为关键段落，每段标注时间戳
    3. 输入格式说明（SRT 格式示例）
    4. 输出格式要求（"一、整体剧情概括：\n\n二、分段剧情解析：\n\n**剧情段落 1：[主题]**\n* **时间戳：** [开始] --> [结束]\n* **内容概要：** ..."）
    5. 注意事项（时间戳准确性、段落逻辑划分）
    6. ${subtitle_content} 占位符（作为输入变量）

    使用 ${subtitle_content} 变量占位符（与 PromptManager 渲染格式一致）。

    不要包含系统提示（PromptManager 调用方在调用 generate_text() 时传入 system_prompt）。
    不要包含 Python 类结构，只包含纯模板文本。

    文件应为纯文本 Markdown，包含 ${...} 占位符。
  </action>
  <verify>
    <automated>cd E:\GitLib\NarratoAI && test -f src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md && wc -l src/prompt/templates/short_drama_narration/plot_analysis_v1.0.md</automated>
  </verify>
  <done>
    文件存在且包含 ${subtitle_content} 占位符，行数 >= 50 行
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 script_generation v2.0 提示模板（从 v1.0 升级）</name>
  <files>src/prompt/templates/short_drama_narration/script_generation_v2.0.md</files>
  <action>
    创建文件 `src/prompt/templates/short_drama_narration/script_generation_v2.0.md`。

    这是 v1.0 (`script_generation_v1.0.md`) 的重大升级。v1.0 保留不动（保持向后兼容）。v2.0 必须是自包含的完整提示。

    内容对齐 Python 版 `app/services/prompts/short_drama_narration/script_generation.py` 的 get_template() 输出。

    必须包含以下变量占位符（使用 ${...} 语法）：
    - ${drama_name} — 短剧名称（D-07）
    - ${plot_analysis} — 剧情分析文本（step_analyze_plot 的输出，D-15）
    - ${subtitle_content} — 原始字幕文本（用于精确时间戳映射，D-15）

    模板结构必须包含：
    1. 任务目标：为《${drama_name}》创作解说脚本
    2. 素材信息：剧情概述（${plot_analysis}）+ 原始字幕（${subtitle_content}）
    3. 创作核心要素六个：
       a. 黄金开场（3秒法则）——悬念/冲突/反转预告
       b. 主线提炼（去繁就简）——快节奏，坚持主线
       c. 爽点放大（情绪引爆）——逆袭/反派打脸/智商在线
       d. 个性吐槽（增加趣味）——上帝视角/心理活动/吐槽
       e. 悬念预埋（引导互动）——关键节点卖关子
       f. 卡点配合（视听协调）——BGM/原声/节奏
    4. 专业解说语言技巧：氛围营造、情感词汇、节奏控制
    5. 严格技术要求：
       a. 时间戳不能重叠
       b. 保持时间连续性
       c. 原声 70% / 解说 30% 比例
       d. 单个原声片段 3-8 秒，均匀分布
    6. 原声片段使用规范：
       a. narration 字段格式："播放原片+N"（D-23）
       b. OST=1 表示原声，OST=0 表示解说
       c. 关键情绪爆发点、重要对白、爽点瞬间、悬念节点、经典台词时刻
    7. 严格 JSON 输出格式（含完整 items 数组示例，直接对齐 ScriptClip 结构）：
       - `{"items": [{"_id": 1, "timestamp": "...", "picture": "...", "narration": "...", "OST": 0}, ...]}`
       - 绝不含额外文字、代码块标记或说明
    8. 质量标准：解说 80-150 字/段，生动有趣，口语化短句
    9. 参考解说风格示例（常用句式）

    模板应为中文。使用 ${...} 变量占位符。

    注意：与 Python 版不同，Rust 版不需要 Python 类定义——这是纯文本 Markdown 模板文件。
  </action>
  <verify>
    <automated>cd E:\GitLib\NarratoAI && test -f src/prompt/templates/short_drama_narration/script_generation_v2.0.md && wc -l src/prompt/templates/short_drama_narration/script_generation_v2.0.md</automated>
  </verify>
  <done>
    文件存在且包含 ${drama_name} / ${plot_analysis} / ${subtitle_content} 三个占位符，行数 >= 150 行
  </done>
</task>

<task type="auto">
  <name>Task 3: 注册 plot_analysis v1.0 + 升级 script_generation 到 v2.0</name>
  <files>src/prompt/register.rs</files>
  <action>
    修改 `src/prompt/register.rs` 中的 `register_short_drama_narration_prompts()` 函数。

    当前状态：只注册了 `script_generation` v1.0，未注册 `plot_analysis`。

    修改内容：
    A. 在现有 script_generation v1.0 注册之前，新增 `plot_analysis` v1.0 注册：

    ```rust
    let plot_analysis = Prompt {
        metadata: PromptMetadata {
            name: "plot_analysis".into(),
            category: "short_drama_narration".into(),
            version: "v1.0".into(),
            model_type: ModelType::Text,
            output_format: OutputFormat::Text,
            tags: vec!["short-drama-narration".into(), "plot-analysis".into(), "text".into()],
            parameters: vec![
                ParameterDef {
                    name: "subtitle_content".into(),
                    required: true,
                    default: None,
                    description: "字幕内容（含时间戳）".into(),
                },
            ],
        },
        content: include_str!("templates/short_drama_narration/plot_analysis_v1.0.md").into(),
    };
    if let Err(e) = registry.register(plot_analysis, true) { errors.push(e); }
    ```

    B. 保持现有 script_generation v1.0 注册不变（向后兼容）。

    C. 新增 script_generation v2.0 注册：

    ```rust
    let script_gen_v2 = Prompt {
        metadata: PromptMetadata {
            name: "script_generation".into(),
            category: "short_drama_narration".into(),
            version: "v2.0".into(),
            model_type: ModelType::Text,
            output_format: OutputFormat::NarrationScript,
            tags: vec!["short-drama-narration".into(), "script-generation".into(), "text".into(), "v2".into()],
            parameters: vec![
                ParameterDef {
                    name: "drama_name".into(),
                    required: true,
                    default: None,
                    description: "短剧名称".into(),
                },
                ParameterDef {
                    name: "plot_analysis".into(),
                    required: true,
                    default: None,
                    description: "剧情分析结果".into(),
                },
                ParameterDef {
                    name: "subtitle_content".into(),
                    required: true,
                    default: None,
                    description: "原始字幕内容（含时间戳）".into(),
                },
            ],
        },
        content: include_str!("templates/short_drama_narration/script_generation_v2.0.md").into(),
    };
    if let Err(e) = registry.register(script_gen_v2, true) { errors.push(e); }
    ```

    D. 更新测试函数 `test_register_all_prompts_full` 中的断言：
    当前:
    ```rust
    assert_eq!(registry.list_prompts("short_drama_narration").len(), 1);
    ```
    修改为:
    ```rust
    assert_eq!(registry.list_prompts("short_drama_narration").len(), 3);
    ```
    （因为现在有 plot_analysis v1.0 + script_generation v1.0 + script_generation v2.0 共 3 个）

    E. 确保 register_short_drama_editing_prompts 和 register_documentary_prompts 不受影响。
  </action>
  <verify>
    <automated>cd E:\GitLib\NarratoAI && cargo test prompt::register::tests -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    1. cargo test prompt::register::tests 全部通过
    2. test_register_all_prompts_full 断言 short_drama_narration 为 3 个 prompt
    3. 两个新模板文件存在且内容正确
    4. 可以通过 PromptManager::render_prompt("short_drama_narration", "plot_analysis", Some("v1.0"), ...) 渲染
    5. 可以通过 PromptManager::render_prompt("short_drama_narration", "script_generation", Some("v2.0"), ...) 渲染（需要 drama_name + plot_analysis + subtitle_content 三个参数）
  </done>
</task>

</tasks>

<threat_model>
## Assets
- **Prompt templates** (静态文件，无敏感数据)
- **PromptRegistry** (运行时注册的 prompt 元数据)

## Entry Points
- register_all_prompts() — 在应用启动时调用
- PromptManager::render_prompt() — 运行时调用

## Trust Boundaries
无外部输入跨越信任边界——所有模板是静态内置的编译时资源。

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-04 | Tampering | register.rs include_str! | mitigate | 模板文件通过 `include_str!()` 在编译时嵌入二进制——运行时无法篡改。任何模板修改需重新编译。 |
| T-7-05 | Information Disclosure | Prompt 内容 | accept | Prompt 模板不含敏感信息（API key、用户数据）。变量占位符值由调用方传递，不在模板内硬编码。 |
</threat_model>

<verification>
- [x] cargo test prompt::register::tests 全部通过
- [x] plot_analysis_v1.0.md 存在，包含 ${subtitle_content} 占位符
- [x] script_generation_v2.0.md 存在，包含 ${drama_name} / ${plot_analysis} / ${subtitle_content} 占位符
- [x] register.rs 中 short_drama_narration 分类包含 3 个 prompt
- [x] grep -c "plot_analysis" src/prompt/register.rs >= 3（至少 3 处引用：注测+测试等）
</verification>

<success_criteria>
- short_drama_narration 分类下 3 个 Prompt 模板（plot_analysis v1.0 + script_generation v1.0 + script_generation v2.0）
- 所有模板编译通过（include_str! 确保文件存在）
- 测试验证注册正确（数量断言通过）
- Prompt 参数定义与 research.md 的分析一致（D-18）
</success_criteria>

<output>
After completion, create `.planning/phases/07-sde-pipeline/07-02-SUMMARY.md`
</output>
