---
phase: 13-gap-closure
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - narratoai-core/src/sde/pipeline.rs
  - narratoai-core/src/sdp/pipeline.rs
  - narratoai-core/src/sdp/types.rs
  - src-tauri/src/commands/pipeline.rs
autonomous: true
requirements: ["EXTD-01", "EXTD-04"]
user_setup: []

must_haves:
  truths:
    - SDE 流水线的音频合并步骤完成后调用 normalize_merged_audio 做标准化
    - SDE 流水线的最终合成步骤开头调用 resolve_volumes 覆盖音量值
    - SDP 流水线的最终合成步骤对视频原声音频做 loudnorm 预标准化
    - SDP 流水线的最终合成步骤开头调用 resolve_volumes 覆盖音量值
    - SDP 流水线的 run_sdp 函数接收 config 参数
  artifacts:
    - path: "narratoai-core/src/sde/pipeline.rs"
      provides: "音频标准化 + 智能音量集成"
      contains: "normalize_merged_audio"
    - path: "narratoai-core/src/sdp/pipeline.rs"
      provides: "音频标准化（raw音频） + 智能音量集成"
      contains: "normalize_audio_for_mixing"
    - path: "narratoai-core/src/sdp/types.rs"
      provides: "SdpPipelineState 含 normalized_audio_path 字段（可选）"
      contains: "normalized_audio_path"
    - path: "src-tauri/src/commands/pipeline.rs"
      provides: "run_sdp 命令传递 config 到 core"
      contains: "run_sdp"
  key_links:
    - from: "sde/pipeline.rs run_sde"
      to: "audio/pipeline::normalize_merged_audio"
      via: "merge_audio 步骤后"
      pattern: "normalize_merged_audio"
    - from: "sde/pipeline.rs run_sde"
      to: "audio/pipeline::resolve_volumes"
      via: "composite 步骤前"
      pattern: "resolve_volumes"
    - from: "sdp/pipeline.rs sdp_step_composite"
      to: "audio/normalizer::normalize_audio_for_mixing"
      via: "composite 步骤内 raw audio 预处理"
      pattern: "normalize_audio_for_mixing"
    - from: "sdp/pipeline.rs sdp_step_composite"
      to: "audio/pipeline::resolve_volumes"
      via: "composite 步骤前"
      pattern: "resolve_volumes"
---

<objective>
将音频标准化和智能音量集成到 SDE 和 SDP 两条流水线。SDE 对齐 Documentary 模式（merge_audio 后标准化 + composite 前智能音量），SDP 对齐 raw 音频预处理（无 merge_audio 步骤，需在 composite 中对视频原声做 loudnorm 预标准化）。

Purpose: 三条流水线全部获得音频标准化和智能音量能力。AudioSection 的统一功能性覆盖。
Output: 更新后的 sde/pipeline.rs、sdp/pipeline.rs、sdp/types.rs、Tauri 命令层
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gap-closure/13-CONTEXT.md
@.planning/phases/13-gap-closure/13-01-SUMMARY.md (after execution)
@.planning/phases/13-gap-closure/13-02-SUMMARY.md (after execution)
@.planning/phases/13-gap-closure/13-04-SUMMARY.md (after execution, Documentary 的集成模式做参考)

<interfaces>
Per D-01~D-13, D-23.

**SDE pipeline.rs patterns:**
- `run_sde(request, config, proxy, registry, prompt_manager, progress)` — 已接收 config
- merge_audio 在第 280-298 行内联（非独立函数）
- step_composite 在第 390-604 行内联
- 使用 `request.tts_volume`, `request.original_volume`, `request.bgm_volume`（SdeRequest 字段）

**SDP pipeline.rs patterns:**
- `run_sdp(request, progress)` — **未**接收 config（需要修改）
- `sdp_step_composite(state, request)` — 内联 FFmpeg 调用，使用 `request.original_volume` 和 `request.bgm_volume`
- SDP 无 TTS/merge_audio 步骤（全部 OST=1）
- SDP 的 composite 步骤中：[0:a] 为视频原声，[1:a] 为可选的 BGM

**Tauri 命令层 run_sdp（src-tauri/src/commands/pipeline.rs line 175）：**
- 当前签名：`pub async fn run_sdp(app, request, _config: tauri::State<'_, AppConfig>)`
- `_config` 已有但未使用 — 只需去除 `_` 前缀并传递给 core

**SdpPipelineState（sdp/types.rs）：**
- 当前无 `normalized_audio_path` 字段
- 需要在 SDP 中添加 `normalized_audio_path` 以支持 raw 音频预标准化后传递给 composite 步骤

**AppConfig field path:** `config.audio` → AudioSection

**Documentary 的集成模式（参考 Plan 04）：**
- merge 后：`crate::audio::pipeline::normalize_merged_audio(merged_path, &config.audio).map_err(PipelineError::from)?;`
- composite 前：构造 VolumeConfig → `crate::audio::pipeline::resolve_volumes(&req_volumes, "documentary", &config.audio)` → 使用 resolved 值

SDE/SDP 的区别：
- SDE 用 `crate::audio::pipeline::normalize_merged_audio`（模式同 Documentary）
- SDP 需要 `crate::audio::normalizer::normalize_audio_for_mixing`（对原始音频做 2-pass loudnorm 后单独传入 composite）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SDE 流水线音频标准化 + 智能音量集成</name>
  <files>
    narratoai-core/src/sde/pipeline.rs
  </files>
  <read_first>
    narratoai-core/src/sde/pipeline.rs (第 280-298 行 merge_audio 步骤，第 390-416 行 composite 步骤开头，第 151-155 行 run_sde 调用)
    narratoai-core/src/audio/pipeline.rs (normalize_merged_audio 和 resolve_volumes 签名 — Plan 02)
    narratoai-core/src/sde/types.rs (SdeRequest 音量字段 — tts_volume, original_volume, bgm_volume)
  </read_first>
  <action>
    对 `src/sde/pipeline.rs` 做两处修改，对齐 Documentary 的集成模式：

    **修改 1 — merge_audio 后新增音频标准化：**
    在第 290 行 `state.merged_audio_path = Some(merged_audio);` 之后（第 291 行 `state.emit_progress("merge_audio", 85.0 ...)` 之前），新增：
    ```rust
    // 音频标准化：合并完成后立即标准化（D-01~D-06）
    if let Some(ref merged_path) = state.merged_audio_path {
        let _ = crate::audio::pipeline::normalize_merged_audio(merged_path, &config.audio)
            .map_err(|e| SdeError::VideoProcess(PipelineError::AudioNormalization {
                details: e.to_string(),
            }))?;
    }
    ```

    **修改 2 — composite 步骤开头新增智能音量覆盖：**
    在第 390 行 `state.emit_progress("composite", 95.0, "正在最终合成...");` 之后（第 392 行 `let merged_video = ...` 之前），新增：
    ```rust
    // 智能音量覆盖（D-08~D-13, D-15）
    let request_volumes = crate::audio::volume::VolumeConfig {
        tts_volume: request.tts_volume,
        original_volume: request.original_volume,
        bgm_volume: request.bgm_volume,
    };
    let resolved = crate::audio::pipeline::resolve_volumes(
        &request_volumes, "sde", &config.audio,
    );
    ```
    然后将第 412-415 行原有的 `let tts_vol = request.tts_volume;` `let bgm_vol = request.bgm_volume;` `let orig_vol = request.original_volume;` 三行替换为：
    ```rust
    let tts_vol = resolved.tts_volume;
    let bgm_vol = resolved.bgm_volume;
    let orig_vol = resolved.original_volume;
    ```

    **注意：** 确认 `use crate::documentary::error::PipelineError;` 在第 6 行已导入（用于构造 SdeError::VideoProcess(PipelineError::AudioNormalization {...})）。如果 `use crate::documentary::error::PipelineError;` 不存在，添加该导入。
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check -p narratoai-core 2>&1 | grep -E "^error" | head -10</automated>
  </verify>
  <done>
    - SDE merge_audio 步骤后调用 `normalize_merged_audio`
    - SDE composite 步骤开头调用 `resolve_volumes`
    - 编译器通过
  </done>
</task>

<task type="auto">
  <name>Task 2: SDP 流水线 raw 音频标准化 + 智能音量集成</name>
  <files>
    narratoai-core/src/sdp/types.rs
    narratoai-core/src/sdp/pipeline.rs
  </files>
  <read_first>
    narratoai-core/src/sdp/types.rs (SdpPipelineState 定义第 119-155 行)
    narratoai-core/src/sdp/pipeline.rs (run_sdp 第 17-121 行，sdp_step_composite 第 210-318 行)
    narratoai-core/src/audio/normalizer.rs (normalize_audio_for_mixing 签名 — 用于 SDP raw 音频预处理)
    narratoai-core/src/audio/pipeline.rs (normalize_merged_audio 和 resolve_volumes 签名 — Plan 02)
    narratoai-core/src/audio/volume.rs (VolumeConfig 定义)
  </read_first>
  <action>
    对 SDP 流水线做三处修改：

    **修改 1 — SdpPipelineState 新增 normalized_audio_path 字段（D-07, D-21）：**
    在 `src/sdp/types.rs` 的 `SdpPipelineState` 结构体中新增字段：
    ```rust
    pub normalized_audio_path: Option<PathBuf>,
    ```
    在 `SdpPipelineState::new()` 中新增初始化：
    ```rust
    normalized_audio_path: None,
    ```

    **修改 2 — run_sdp 新增 config 参数 + concat 后 raw 音频预标准化（D-07, D-23）：**
    
    a) `run_sdp` 签名新增 `config: &AppConfig` 参数（放在 `request` 之后，`progress` 之前）：
    ```rust
    pub async fn run_sdp(
        request: SdpRequest,
        config: &AppConfig,
        progress: Option<SdpProgressCallback>,
    ) -> Result<PathBuf, SdpError>
    ```
    
    b) 在第 107 行 concat 步骤完成之后（`state.emit_progress("concat", 60.0, ...)` 之后，composite 步骤之前），新增 raw 音频预标准化：
    ```rust
    // Raw 音频预标准化（D-07）：对拼接视频的原声音轨做 loudnorm 标准化
    if config.audio.enable_audio_normalization {
        state.emit_progress("composite", 61.0, "正在标准化原声音频...");
        if let Some(ref combined_path) = state.combined_video_path {
            let raw_audio = state.task_dir.join("sdp_raw_audio.wav");
            let combined_str = combined_path.to_string_lossy().to_string();
            let raw_str = raw_audio.to_string_lossy().to_string();
    
            // 步骤 A: 从拼接视频中提取音频
            crate::ffmpeg::command::run_ffmpeg({
                let combined_str = combined_str.clone();
                let raw_str = raw_str.clone();
                move || {
                    let mut cmd = ffmpeg_sidecar::command::FfmpegCommand::new();
                    cmd.arg("-i").arg(&combined_str)
                        .arg("-vn")
                        .arg("-acodec").arg("pcm_s16le")
                        .arg("-y")
                        .arg(&raw_str);
                    let mut child = cmd.spawn()
                        .map_err(|e| crate::error::FFmpegError::SpawnFailed(e.to_string()))?;
                    let status = child.wait()
                        .map_err(|e| crate::error::FFmpegError::ExecutionError(e.to_string()))?;
                    if !status.success() {
                        return Err(crate::error::FFmpegError::ExecutionError("SDP 音频提取失败".into()));
                    }
                    Ok(())
                }
            }).await.map_err(|e: crate::error::FFmpegError| {
                SdpError::VideoProcess(PipelineError::FFmpeg { source: e })
            })?;
    
            // 步骤 B: 标准化提取的音频
            match crate::audio::normalizer::normalize_audio_for_mixing(
                &raw_audio,
                &state.task_dir,
                config.audio.target_lufs,
                config.audio.sample_rate,
                config.audio.channels,
            ) {
                Ok(normalized) => {
                    state.normalized_audio_path = Some(normalized);
                    tracing::info!("SDP raw 音频标准化完成 (target_lufs={})", config.audio.target_lufs);
                }
                Err(e) => {
                    tracing::warn!("SDP raw 音频标准化失败（非致命，将使用原始音频）: {}", e);
                    state.normalized_audio_path = None;
                }
            }
        }
    }
    ```
    (D-07 定义为强制性操作，但如果 loudnorm 备用 RMS 都失败，降级到使用原始音频也不中断流水线)

    c) 更新 `sdp_step_composite` 调用处（第 113 行附近）：传递 config
    ```rust
    sdp_step_composite(&mut state, &request, config).await?;
    ```

    **修改 3 — sdp_step_composite 集成智能音量 + raw 音频输入调整（D-07~D-13）：**
    
    a) 函数签名新增 `config: &AppConfig` 参数：
    ```rust
    async fn sdp_step_composite(
        state: &mut SdpPipelineState,
        request: &SdpRequest,
        config: &AppConfig,
    ) -> Result<(), SdpError>
    ```
    
    b) 在函数体开头（第 214 行 `let merged_video = ...` 之后，第 226 行 `let original_volume = ...` 之前），新增智能音量覆盖：
    ```rust
    // 智能音量覆盖（D-08~D-13）
    let request_volumes = crate::audio::volume::VolumeConfig {
        tts_volume: 0.0,  // SDP 无 TTS
        original_volume: request.original_volume,
        bgm_volume: request.bgm_volume,
    };
    let resolved = crate::audio::pipeline::resolve_volumes(
        &request_volumes, "sdp", &config.audio,
    );
    let original_volume = resolved.original_volume;
    let bgm_volume = resolved.bgm_volume;
    ```
    移除第 226-227 行原有的 `let original_volume = request.original_volume;` 和 `let bgm_volume = request.bgm_volume;`（由上述 resolved 替代）。

    c) 如果 `state.normalized_audio_path` 有值，调整 FFmpeg 命令：在 `-i` 视频输入之后增加一个 `-i normalized_path` 输入，然后将 filter_complex 中的 `[0:a]` 改为 `[1:a]`，BGM 输入索引从 `[1:a]` 改为 `[2:a]`。代码逻辑：
    ```rust
    let has_normalized_audio = state.normalized_audio_path.is_some();
    if let Some(ref norm_path) = state.normalized_audio_path {
        cmd.arg("-i").arg(norm_path.to_string_lossy().to_string());
    }
    // ... 后续 filter_complex 中：
    // if has_normalized_audio { [1:a] } else { [0:a] }
    // if has_normalized_audio { [2:a] } else { [1:a] }  // BGM
    ```
    具体实现中需要调整 `amix_count` 计数和 filter_complex 中的输入标签。确保当 normalized_audio 存在时，原始音频来源从 `[0:a]` 切换到 `[1:a]`（第一个额外输入），BGM 索引相应后移。

    注意：`sdp_step_composite` 的 `move ||` 闭包捕获需要调整以包含 `has_normalized_audio` 和可能的 `normalized_audio_path` 值。
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check -p narratoai-core 2>&1 | grep -E "^error" | head -10</automated>
  </verify>
  <done>
    - `SdpPipelineState` 含 `normalized_audio_path: Option<PathBuf>`
    - `run_sdp` 接收 `config: &AppConfig` 参数
    - `sdp_step_composite` 接收 `config: &AppConfig` 参数
    - SDP composite 步骤开头调用 `resolve_volumes`
    - SDP composite 步骤使用标准化音频（如果可用）或回退到原始音频
  </done>
</task>

<task type="auto">
  <name>Task 3: Tauri 命令 run_sdp 传递 config 参数</name>
  <files>
    src-tauri/src/commands/pipeline.rs
  </files>
  <read_first>
    src-tauri/src/commands/pipeline.rs (第 171-216 行 run_sdp 命令 — 注意 _config 已通过 State 注入但未使用)
    src-tauri/src/commands/pipeline.rs (第 1-4 行 use 声明)
  </read_first>
  <action>
    修改 `src-tauri/src/commands/pipeline.rs` 中的 run_sdp 命令（第 175-215 行）：

    1. 将函数签名中的 `_config: tauri::State<'_, AppConfig>` 改为 `config: tauri::State<'_, AppConfig>`（去掉 `_` 前缀）

    2. 修改第 208-211 行的 core 调用，新增 `&config` 参数：
    ```rust
    let output_path = narratoai_core::sdp::pipeline::run_sdp(
        request,
        &config,
        Some(progress_callback),
    ).await?;
    ```
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check 2>&1 | grep -E "^error" | head -10</automated>
  </verify>
  <done>
    - run_sdp Tauri 命令传递 `&config` 到 core
    - workspace 级别 `cargo check` 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SDP pipeline → filesystem | 音频提取/标准化产生临时文件（sdp_raw_audio.wav, *_normalized.mp3） |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | T | sdp/pipeline.rs raw 音频提取 | mitigate | 标准化失败时降级使用原始音频（非中断），已有 tracing::warn 日志 |
| T-13-05 | T | sde/pipeline.rs normalize_merged_audio | mitigate | 错误通过 SdeError::VideoProcess(PipelineError::AudioNormalization) 传播，触发 fail-fast |
</threat_model>

<verification>
- `cargo check -p narratoai-core` 通过
- `cargo check`（workspace 级别，含 Tauri）通过
- 验证 SDE 和 SDP 的现有单元测试不受影响
</verification>

<success_criteria>
- SDE 流水线 merge_audio 后调用 normalize_merged_audio
- SDE 流水线 composite 前调用 resolve_volumes
- SDP 流水线 composite 前对 raw 音频做 loudnorm 预标准化（非中断降级）
- SDP 流水线 composite 前调用 resolve_volumes
- Tauri 命令 run_sdp 传递 config
- 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-05-SUMMARY.md`
</output>
