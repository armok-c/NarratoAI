---
phase: 13-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - narratoai-core/src/config/types.rs
  - narratoai-core/src/config/defaults.rs
  - config.example.toml
autonomous: true
requirements: ["PRMP-03", "EXTD-01", "EXTD-04"]
user_setup: []

must_haves:
  truths:
    - AudioSection 新增 volume_profile 字段，序列化/反序列化兼容旧 config.toml
    - AudioSection::default() 的 target_lufs 为 -14.0，包含 volume_profile = "balanced"
    - config.example.toml 的 [audio] 段反映 -14.0 LUFS 标准和 volume_profile 字段
  artifacts:
    - path: "narratoai-core/src/config/types.rs"
      provides: "AudioSection 定义包含 volume_profile: String"
      contains: "volume_profile: String"
    - path: "narratoai-core/src/config/defaults.rs"
      provides: "AudioSection::default() target_lufs=-14.0, volume_profile=balanced"
      contains: "-14.0"
    - path: "config.example.toml"
      provides: "[audio] 段含 volume_profile 和更新注释"
      contains: "volume_profile"
  key_links:
    - from: "config/types.rs AudioSection"
      to: "config/defaults.rs AudioSection::default"
      via: "Default impl"
      pattern: "impl Default for AudioSection"
---

<objective>
更新配置系统：AudioSection 新增 volume_profile 字段，默认 target_lufs 统一为 -14.0，config.example.toml 同步更新。

Purpose: 为音频标准化和智能音量提供配置基础。Phase 11 模块已实现但默认值未对齐目标平台（YouTube/流媒体），且缺少 volume_profile 字段入口。
Output: 更新后的 config/types.rs、config/defaults.rs、config.example.toml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gap-closure/13-CONTEXT.md

<interfaces>
Per D-14, D-24, D-25, D-26, D-27:

**D-14:** AudioSection 新增 volume_profile: String 字段，默认 "balanced"
**D-25:** AudioSection::default() target_lufs -23.0 → -14.0，新增 volume_profile: "balanced".into()
**D-26:** config.example.toml [audio] 段更新：target_lufs 注释改为 YouTube/流媒体标准，新增 volume_profile = "balanced"
**D-27:** config/types.rs 测试中 target_lufs 断言从 -23.0 更新为 -14.0，新增 volume_profile 断言
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AudioSection 新增 volume_profile 字段 + 测试断言更新</name>
  <files>
    narratoai-core/src/config/types.rs
  </files>
  <read_first>
    narratoai-core/src/config/types.rs (当前 AudioSection 结构体)
    narratoai-core/src/config/defaults.rs (AudioSection::default() 当前值)
  </read_first>
  <action>
    1. 在 `AudioSection` 结构体的 `bgm_fade_out` 字段之后新增 `volume_profile: String`，标注 `#[serde(default)]`。注意 `#[serde(deny_unknown_fields)]` 在结构体级别，新字段有 default 所以旧 TOML 不包含此字段时静默使用默认值。
    2. 定位到同一文件中 `#[cfg(test)] mod tests` 下的 `test_load_full_config` 函数：
       - 将第 366 行附近 `config.audio.target_lufs` 断言值从 `-23.0` 改为 `-14.0`
       - 在 `config.audio.enable_audio_normalization` 断言之后新增一行：`assert_eq!(config.audio.volume_profile, "");`（空 TOML 未提供，serde default 为 String::default() 即空字符串）
    3. 定位到同一文件中 `#[cfg(test)] mod tests` 下的 `test_default_values` 函数：
       - 在 `assert_eq!(config.frames.vision_max_concurrency, 2);` 之后新增一行验证 AudioSection 默认值：`assert_eq!(config.audio.volume_profile, "");`
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo test -p narratoai-core -- config::types::tests 2>&1 | grep -E "PASS|OK|FAIL|error"</automated>
  </verify>
  <done>
    - narratoai-core/src/config/types.rs 中 AudioSection 包含 `volume_profile: String` 字段，标注 `#[serde(default)]`
    - `test_load_full_config` 中 target_lufs 断言为 -14.0，含 volume_profile 断言
    - `test_default_values` 含 volume_profile 空字符串断言
  </done>
</task>

<task type="auto">
  <name>Task 2: AudioSection::default() 更新 target_lufs + volume_profile</name>
  <files>
    narratoai-core/src/config/defaults.rs
  </files>
  <read_first>
    narratoai-core/src/config/defaults.rs (impl Default for AudioSection)
  </read_first>
  <action>
    1. 定位到 `impl Default for AudioSection {` 块：
       - 将 `target_lufs: -23.0` 改为 `target_lufs: -14.0` (D-25: YouTube/流媒体标准)
       - 在 `bgm_fade_out: 3.0` 之后新增一行 `volume_profile: "balanced".into()`
    2. 确认默认值对齐：D-28 要求 `enable_audio_normalization` 和 `enable_smart_volume` 保持 `false`（当前已经是），不做更改。
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo test -p narratoai-core -- config::defaults 2>&1 | grep -E "PASS|OK|FAIL|error"</automated>
  </verify>
  <done>
    - `AudioSection::default()` 的 `target_lufs` 值为 `-14.0`
    - `AudioSection::default()` 包含 `volume_profile: "balanced".into()`
    - `enable_audio_normalization` 和 `enable_smart_volume` 保持 `false`
  </done>
</task>

<task type="auto">
  <name>Task 3: config.example.toml [audio] 段更新</name>
  <files>
    config.example.toml
  </files>
  <read_first>
    config.example.toml (当前 [audio] 段，第 183-218 行)
  </read_first>
  <action>
    将 `config.example.toml` 第 183 行开始的 `[audio]` 段整体替换为以下内容（D-26）：

    ```toml
    [audio]
        # LUFS 响度标准化目标值（-14.0 符合 YouTube/流媒体标准，-23.0 符合 EBU R128 广播标准）
        target_lufs = -14.0

        # 最大峰值（dBFS）
        max_peak = -1.0

        # 是否启用音频标准化（默认关闭，需手动开启）
        enable_audio_normalization = false

        # 是否启用智能音量调整（默认关闭，需手动开启）
        enable_smart_volume = false

        # 音量 profile（balanced / voice_focused / original_focused / quiet_background，配合智能音量使用）
        volume_profile = "balanced"

        # TTS 解说音量（0.0-2.0）
        tts_volume = 0.8

        # 原声音量（0.0-2.0）
        original_volume = 1.3

        # BGM 背景音乐音量（0.0-2.0）
        bgm_volume = 0.3

        # 采样率
        sample_rate = 44100

        # 声道数
        channels = 2

        # 比特率
        bitrate = 192000

        # 交叉淡化时长（秒）
        crossfade_duration = 0.1

        # BGM 淡出时长（秒）
        bgm_fade_out = 3.0
    ```
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && python -c "import toml; c=toml.load('config.example.toml'); assert c['audio']['target_lufs'] == -14.0; assert c['audio']['volume_profile'] == 'balanced'; assert c['audio']['enable_audio_normalization'] == False; print('OK')"</automated>
  </verify>
  <done>
    - `config.example.toml` 的 `[audio]` 段 `target_lufs = -14.0`
    - `config.example.toml` 的 `[audio]` 段包含 `volume_profile = "balanced"`
    - 注释反映 YouTube/流媒体标准
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| config.toml → AppConfig | 用户提供的配置数据反序列化为类型化结构体 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | T | config/types.rs AudioSection | mitigate | volume_profile 带 #[serde(default)]，旧 config.toml 无需修改也可加载。deny_unknown_fields 在新字段发布后不会触发因为 TOML 文件不含该字段 |
</threat_model>

<verification>
- `cargo test -p narratoai-core` 全线通过（重点关注 config::types::tests 和 config::defaults）
- config.example.toml 可被 toml crate 正常解析
</verification>

<success_criteria>
- AudioSection 含 volume_profile: String 字段
- AudioSection::default() target_lufs = -14.0, volume_profile = "balanced"
- config.example.toml [audio] 段已更新
- 所有配置测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-01-SUMMARY.md`
</output>
