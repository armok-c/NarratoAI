---
phase: 13-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - narratoai-core/src/documentary/error.rs
  - narratoai-core/src/audio/pipeline.rs
  - narratoai-core/src/audio/mod.rs
autonomous: true
requirements: ["EXTD-01", "EXTD-04"]
user_setup: []

must_haves:
  truths:
    - PipelineError 可表达音频标准化失败的场景 (PipelineError::AudioNormalization)
    - 共享辅助函数 normalize_merged_audio() 在 src/audio/pipeline.rs 中可被三流水线调用
    - 共享辅助函数 resolve_volumes() 在 src/audio/pipeline.rs 中可被三流水线调用
    - audio::pipeline 模块可从 crate::audio::pipeline 导入
  artifacts:
    - path: "narratoai-core/src/documentary/error.rs"
      provides: "PipelineError::AudioNormalization 变体 + From&lt;AudioError&gt;"
      contains: "AudioNormalization"
    - path: "narratoai-core/src/audio/pipeline.rs"
      provides: "normalize_merged_audio + resolve_volumes 两个共享辅助函数"
      exists: true
    - path: "narratoai-core/src/audio/mod.rs"
      provides: "pub mod pipeline 导出"
      contains: "pub mod pipeline"
  key_links:
    - from: "audio/pipeline.rs"
      to: "audio/normalizer::normalize_audio_for_mixing"
      via: "normalize_merged_audio 调用"
      pattern: "normalize_audio_for_mixing"
    - from: "audio/pipeline.rs"
      to: "audio/volume::get_optimized_volumes"
      via: "resolve_volumes 调用"
      pattern: "get_optimized_volumes"
    - from: "documentary/error.rs"
      to: "audio/normalizer::AudioError"
      via: "From&lt;AudioError&gt; impl"
      pattern: "From<AudioError>"
---

<objective>
新增 PipelineError::AudioNormalization 变体 + From&lt;AudioError&gt; 自动转换，创建 src/audio/pipeline.rs 共享辅助函数层。

Purpose: 音频标准化错误的专用类型表达（而非复用 AudioMerge 或 Composite），以及三个流水线共享的 normalize_merged_audio() / resolve_volumes() 函数，避免重复代码。
Output: 更新后的 documentary/error.rs、新建 audio/pipeline.rs、更新 audio/mod.rs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gap-closure/13-CONTEXT.md

<interfaces>
Per D-21, D-22, D-23, D-34:

**D-21:** 新建 src/audio/pipeline.rs，提供两个共享辅助函数：
  - `normalize_merged_audio(input: &Path, config: &AudioSection) -> Result<PathBuf, AudioError>` — 检查 enable_audio_normalization 开关 → 调用 normalize_audio_for_mixing → 原地覆盖原文件
  - `resolve_volumes(request_volumes: &VolumeConfig, video_type: &str, config: &AudioSection) -> VolumeConfig` — 检查 enable_smart_volume 开关 → 调用 get_optimized_volumes 或返回原值 → tracing::info! 日志

**D-22:** PipelineError 新增 AudioNormalization { details: String } 变体，实现 From&lt;AudioError&gt; 自动转换
**D-23:** step 函数 +config 参数会在下游计划实现，本计划只需定义好辅助函数签名
**D-34:** 单元测试覆盖两种辅助函数的开关开启/关闭场景

SDE/SDP 有自己的错误类型（SdeError, SdpError），它们也通过 VideoProcess 变体包裹 PipelineError，所以 PipelineError::AudioNormalization 通过 `SdeError::VideoProcess(PipelineError::AudioNormalization {..})` 传递到 SDE/SDP 域。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PipelineError 新增 AudioNormalization 变体 + From&lt;AudioError&gt;</name>
  <files>
    narratoai-core/src/documentary/error.rs
  </files>
  <read_first>
    narratoai-core/src/documentary/error.rs (完整文件 — 当前 PipelineError 枚举和 From impl)
    narratoai-core/src/audio/normalizer.rs (AudioError 枚举定义)
  </read_first>
  <action>
    1. 在 `PipelineError` 枚举的 `Llm` 变体之前（`FFmpeg` 与 `Llm` 之间）新增变体：
       ```rust
       #[error("音频标准化失败: {details}")]
       AudioNormalization { details: String },
       ```
    2. 在 `From<LLMError> for PipelineError` 的 `impl` 之后新增 `From<AudioError>` impl：
       ```rust
       impl From<crate::audio::normalizer::AudioError> for PipelineError {
           fn from(source: crate::audio::normalizer::AudioError) -> Self {
               PipelineError::AudioNormalization {
                   details: source.to_string(),
               }
           }
       }
       ```
       （将 AudioError 的 Display 消息通过 `.to_string()` 提取为 String，因为 PipelineError::AudioNormalization 不是 #[source] 变体）

    3. 在 `#[cfg(test)]` 测试模块中新增测试函数（对齐现有中文 Display 测试模式）：
       ```rust
       #[test]
       fn test_pipeline_error_audio_normalization_chinese() {
           let err = PipelineError::AudioNormalization {
               details: "loudnorm 分析失败".into(),
           };
           let msg = err.to_string();
           assert!(msg.contains("音频标准化失败"), "消息应包含中文: {}", msg);
       }
       ```
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo test -p narratoai-core -- documentary::error::tests 2>&1 | grep -E "PASS|OK|FAIL|error"</automated>
  </verify>
  <done>
    - `PipelineError` 包含 `AudioNormalization { details: String }` 变体
    - 存在 `From<crate::audio::normalizer::AudioError> for PipelineError` 的 impl
    - 测试 `test_pipeline_error_audio_normalization_chinese` 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 src/audio/pipeline.rs 共享辅助函数 + 单元测试</name>
  <files>
    narratoai-core/src/audio/pipeline.rs
    narratoai-core/src/audio/mod.rs
  </files>
  <read_first>
    narratoai-core/src/audio/normalizer.rs (normalize_audio_for_mixing 签名，AudioError 类型)
    narratoai-core/src/audio/volume.rs (get_optimized_volumes 签名，VolumeConfig 类型)
    narratoai-core/src/config/types.rs (AudioSection 定义)
  </read_first>
  <action>
    1. 创建 `narratoai-core/src/audio/pipeline.rs` 文件，内容如下：

    ```rust
    use std::path::{Path, PathBuf};

    use crate::audio::normalizer::{normalize_audio_for_mixing, AudioError};
    use crate::audio::volume::{get_optimized_volumes, VolumeConfig};
    use crate::config::types::AudioSection;

    /// 标准化合并后的音频文件
    ///
    /// 1. 检查 `config.enable_audio_normalization` — 关闭时直接返回 input
    /// 2. 开启时调用 `normalize_audio_for_mixing()` 生成标准化文件
    /// 3. 原地覆盖（std::fs::rename）原始文件
    ///
    /// # 参数
    ///
    /// * `input` — 合并后的音频文件路径（如 merger_audio.mp3）
    /// * `config` — AudioSection 配置（target_lufs, sample_rate, channels 等）
    pub fn normalize_merged_audio(
        input: &Path,
        config: &AudioSection,
    ) -> Result<PathBuf, AudioError> {
        if !config.enable_audio_normalization {
            tracing::info!("音频标准化已关闭，跳过");
            return Ok(input.to_path_buf());
        }

        let output_dir = input.parent().unwrap_or(Path::new("."));
        let temp = normalize_audio_for_mixing(
            input,
            output_dir,
            config.target_lufs,
            config.sample_rate,
            config.channels,
        )?;

        // 原地覆盖（D-03）
        std::fs::rename(&temp, input)
            .map_err(|e| AudioError::IoError(format!("标准化文件重命名失败: {}", e)))?;

        tracing::info!(
            "音频标准化完成 (target_lufs={})，原地覆盖: {}",
            config.target_lufs,
            input.display()
        );

        Ok(input.to_path_buf())
    }

    /// 解析智能音量覆盖
    ///
    /// 1. 检查 `config.enable_smart_volume` — 关闭时返回 request_volumes 原值
    /// 2. 开启时调用 `get_optimized_volumes(video_type)` 获取优化值
    /// 3. 通过 `tracing::info!` 输出最终生效的音量值和来源（D-13）
    ///
    /// # 参数
    ///
    /// * `request_volumes` — 请求结构体中的原始音量值（DocumentaryRequest / SdeRequest / SdpRequest）
    /// * `video_type` — 视频类型：`"documentary"` / `"sde"` / `"sdp"`
    /// * `config` — AudioSection 配置（含 enable_smart_volume 开关）
    pub fn resolve_volumes(
        request_volumes: &VolumeConfig,
        video_type: &str,
        config: &AudioSection,
    ) -> VolumeConfig {
        if !config.enable_smart_volume {
            tracing::info!(
                "智能音量已关闭，使用请求值: tts={:.2}, original={:.2}, bgm={:.2} (来源=manual)",
                request_volumes.tts_volume,
                request_volumes.original_volume,
                request_volumes.bgm_volume,
            );
            return *request_volumes;
        }

        let optimized = get_optimized_volumes(video_type);

        tracing::info!(
            "智能音量生效: video_type={}, tts={:.2}, original={:.2}, bgm={:.2} (来源=smart)",
            video_type,
            optimized.tts_volume,
            optimized.original_volume,
            optimized.bgm_volume,
        );

        optimized
    }

    #[cfg(test)]
    mod tests {
        use super::*;

        fn make_audio_section(
            enable_normalization: bool,
            enable_smart_volume: bool,
        ) -> AudioSection {
            AudioSection {
                enable_audio_normalization: enable_normalization,
                enable_smart_volume,
                target_lufs: -14.0,
                sample_rate: 44100,
                channels: 2,
                ..Default::default()
            }
        }

        #[test]
        fn test_normalize_merged_audio_disabled() {
            let config = make_audio_section(false, false);
            // 当开关关闭时，应直接返回原路径（不报错）
            let input = Path::new("/tmp/nonexistent_audio.mp3");
            let result = normalize_merged_audio(input, &config);
            assert!(result.is_ok());
            assert_eq!(result.unwrap(), input.to_path_buf());
        }

        #[test]
        fn test_resolve_volumes_disabled_returns_request() {
            let config = make_audio_section(false, false);
            let req = VolumeConfig {
                tts_volume: 0.5,
                original_volume: 1.0,
                bgm_volume: 0.1,
            };
            let result = resolve_volumes(&req, "documentary", &config);
            assert!((result.tts_volume - 0.5).abs() < 0.01);
            assert!((result.original_volume - 1.0).abs() < 0.01);
            assert!((result.bgm_volume - 0.1).abs() < 0.01);
        }

        #[test]
        fn test_resolve_volumes_enabled_returns_optimized() {
            let config = make_audio_section(false, true);
            let req = VolumeConfig {
                tts_volume: 0.5,
                original_volume: 1.0,
                bgm_volume: 0.1,
            };
            let result = resolve_volumes(&req, "documentary", &config);
            // get_optimized_volumes("documentary") — D-09 新增的分支，确认返回的是优化值
            assert!((result.tts_volume - 0.8).abs() < 0.01, "documentary 默认 tts 应为 0.8");
        }

        #[test]
        fn test_resolve_volumes_unknown_type_uses_default() {
            let config = make_audio_section(false, true);
            let req = VolumeConfig::default();
            let result = resolve_volumes(&req, "unknown_type", &config);
            // 应返回 get_optimized_volumes 的 fallback 分支
            assert!((result.bgm_volume - 0.3).abs() < 0.01);
        }
    }
    ```

    2. 在 `narratoai-core/src/audio/mod.rs` 中新增 `pub mod pipeline;`（在 `pub mod volume;` 之后）
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo test -p narratoai-core -- audio::pipeline 2>&1 | grep -E "PASS|OK|FAIL|error"</automated>
  </verify>
  <done>
    - `narratoai-core/src/audio/pipeline.rs` 存在，导出 `normalize_merged_audio` 和 `resolve_volumes`
    - `normalize_merged_audio` 在开关关闭时直接返回 input path
    - `resolve_volumes` 在开关关闭时返回请求值，开启时返回优化值
    - 4 个单元测试全部通过
    - `audio/mod.rs` 包含 `pub mod pipeline;`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| normalize_merged_audio → filesystem | 重命名操作覆盖原音频文件 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | T | audio/pipeline.rs normalize_merged_audio | mitigate | normalize_audio_for_mixing 内部已校验输入文件存在；rename 失败通过 AudioError::IoError 向上传播，流水线 fail-fast 终止 |
</threat_model>

<verification>
- `cargo test -p narratoai-core` 全线通过
- `audio::pipeline` 模块的 4 个单元测试全部通过
- `documentary::error::tests` 新增的 AudioNormalization 中文 Display 测试通过
</verification>

<success_criteria>
- documentary/error.rs: PipelineError::AudioNormalization + From&lt;AudioError&gt;
- audio/pipeline.rs: normalize_merged_audio + resolve_volumes + 单元测试
- audio/mod.rs: pub mod pipeline
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-02-SUMMARY.md`
</output>
