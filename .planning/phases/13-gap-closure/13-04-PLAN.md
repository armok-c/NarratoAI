---
phase: 13-gap-closure
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - narratoai-core/src/audio/volume.rs
  - narratoai-core/src/documentary/pipeline.rs
autonomous: true
requirements: ["EXTD-01", "EXTD-04"]
user_setup: []

must_haves:
  truths:
    - get_optimized_volumes() 新增 "documentary" 分支返回针对纪录片优化的音量值
    - ProcessingConfig::default() 的 target_lufs 为 -14.0
    - 纪录片流水线 step_merge_audio_subtitle 完成后调用 normalize_merged_audio 做标准化
    - 纪录片流水线 step_composite 开头调用 resolve_volumes 覆盖音量值
    - 最终生效的音量值通过 tracing::info! 日志输出
  artifacts:
    - path: "narratoai-core/src/audio/volume.rs"
      provides: "get_optimized_volumes 含 documentary/sde/sdp 分支 + ProcessingConfig::default() target_lufs=-14.0"
      contains: "\"documentary\""
    - path: "narratoai-core/src/documentary/pipeline.rs"
      provides: "音频标准化集成于 step 4 + 智能音量集成于 step 6"
      contains: "normalize_merged_audio"
  key_links:
    - from: "documentary/pipeline.rs:step_merge_audio_subtitle"
      to: "audio/pipeline.rs:normalize_merged_audio"
      via: "merge_audio_files 完成后调用"
      pattern: "normalize_merged_audio"
    - from: "documentary/pipeline.rs:step_composite"
      to: "audio/pipeline.rs:resolve_volumes"
      via: "合成步骤开头调用"
      pattern: "resolve_volumes"
---

<objective>
更新 ProcessingConfig 默认值、扩展 get_optimized_volumes() 新增流水线分支、将音频标准化和智能音量集成到纪录片流水线。

Purpose: Phase 11 音频模块已在但零流水线引用。本计划更新默认值对齐 YouTube 标准并作为第一个集成者建立模式。
Output: 更新后的 volume.rs 和 documentary/pipeline.rs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gap-closure/13-CONTEXT.md
@.planning/phases/13-gap-closure/13-01-SUMMARY.md (after execution)
@.planning/phases/13-gap-closure/13-02-SUMMARY.md (after execution)

<interfaces>
Per D-01~D-15, D-23, D-24, D-27.

**Current volume.rs patterns:**
- `ProcessingConfig::default()`: `target_lufs: -20.0` — D-24 要求改为 `-14.0`
- `get_optimized_volumes(video_type)` — 现有分支: educational/entertainment/news/fallback
- `test_processing_config_default` — 断言 `-20.0` — D-27 要求改为 `-14.0`

**Current documentary pipeline.rs patterns:**
- `step_merge_audio_subtitle(state, _request)` — 不接收 config
- `step_composite(state, request)` — 使用 request.tts_volume/request.original_volume/request.bgm_volume
- `run_documentary(request, config, proxy, progress)` — 已接收 config

**audio/pipeline.rs helpers (Plan 02):**
- `normalize_merged_audio(input: &Path, config: &AudioSection) -> Result<PathBuf, AudioError>`
- `resolve_volumes(request_volumes: &VolumeConfig, video_type: &str, config: &AudioSection) -> VolumeConfig`

**D-09 new branches (Claude's discretion):**
- "documentary": tts=1.0, original=0.5, bgm=0.2
- "sde": tts=0.9, original=0.8, bgm=0.3
- "sdp": tts=0.0, original=1.0, bgm=0.3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProcessingConfig 默认值更新 + get_optimized_volumes 新增分支</name>
  <files>
    narratoai-core/src/audio/volume.rs
  </files>
  <read_first>
    narratoai-core/src/audio/volume.rs (ProcessingConfig::default 第 53-62 行，get_optimized_volumes 第 109-132 行，test_processing_config_default 第 315-320 行)
  </read_first>
  <action>
    对 `narratoai-core/src/audio/volume.rs` 做四部分修改：

    **Part A — ProcessingConfig::default() target_lufs 更新（D-24）：**
    - 找到 `impl Default for ProcessingConfig`（约第 53 行）
    - 将 `target_lufs: -20.0` 改为 `target_lufs: -14.0`

    **Part B — test_processing_config_default 断言更新（D-27）：**
    - 找到 `test_processing_config_default` 测试函数（约第 315 行）
    - 将 `assert!((cfg.target_lufs - (-20.0)).abs() < 0.01);` 改为 `assert!((cfg.target_lufs - (-14.0)).abs() < 0.01);`

    **Part C — get_optimized_volumes() 新增三个分支（D-09）：**
    - 在 match 块 "educational" 分支之后新增 "documentary" 分支
    - 在 "entertainment" 之后新增 "sde" 分支
    - 在 "news" 之后新增 "sdp" 分支，保持在其他分支之后 `_` fallback 之前
    - 精确值：
      ```rust
      "documentary" => VolumeConfig { tts_volume: 1.0, original_volume: 0.5, bgm_volume: 0.2 },
      "sde" => VolumeConfig { tts_volume: 0.9, original_volume: 0.8, bgm_volume: 0.3 },
      "sdp" => VolumeConfig { tts_volume: 0.0, original_volume: 1.0, bgm_volume: 0.3 },
      ```

    **Part D — 三个新测试函数：**
    ```rust
    #[test]
    fn test_get_optimized_volumes_documentary() {
        let v = get_optimized_volumes("documentary");
        assert!((v.tts_volume - 1.0).abs() < 0.01);
        assert!((v.original_volume - 0.5).abs() < 0.01);
        assert!((v.bgm_volume - 0.2).abs() < 0.01);
    }
    #[test]
    fn test_get_optimized_volumes_sde() {
        let v = get_optimized_volumes("sde");
        assert!((v.tts_volume - 0.9).abs() < 0.01);
        assert!((v.original_volume - 0.8).abs() < 0.01);
        assert!((v.bgm_volume - 0.3).abs() < 0.01);
    }
    #[test]
    fn test_get_optimized_volumes_sdp() {
        let v = get_optimized_volumes("sdp");
        assert!((v.tts_volume - 0.0).abs() < 0.01);
        assert!((v.original_volume - 1.0).abs() < 0.01);
        assert!((v.bgm_volume - 0.3).abs() < 0.01);
    }
    ```
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo test -p narratoai-core -- audio::volume::tests 2>&1 | grep -E "test_get_optimized_volumes_(documentary|sde|sdp)|test_processing_config_default|PASS|FAIL"</automated>
  </verify>
  <done>
    - ProcessingConfig::default() target_lufs 为 -14.0，对应测试断言更新
    - "documentary"/"sde"/"sdp" 三个新分支有正确的音量返回值
    - 5 个 volume::tests 测试通过（3 新 + 2 现有更新）
  </done>
</task>

<task type="auto">
  <name>Task 2: step_merge_audio_subtitle + step_composite 集成音频标准化和智能音量</name>
  <files>
    narratoai-core/src/documentary/pipeline.rs
  </files>
  <read_first>
    narratoai-core/src/documentary/pipeline.rs (step_merge_audio_subtitle 第 149-181 行，step_composite 第 269-463 行，run_documentary 第 466-519 行)
    narratoai-core/src/audio/pipeline.rs (normalize_merged_audio 和 resolve_volumes 签名 — 由 Plan 02 创建)
  </read_first>
  <action>
    对 `src/documentary/pipeline.rs` 做以下三处修改：

    **修改 1 — step_merge_audio_subtitle 集成音频标准化（D-01~D-06, D-23）：**
    - 将签名从 `_request: &DocumentaryRequest` 改为 `config: &AppConfig`
    - 在第 167 行 `state.merged_audio_path = Some(merged_audio);` 之后，第 170 行 `// Merge subtitles` 注释之前，新增标准化调用：
      ```rust
      // D-01: 合并后立即标准化（原地覆盖）
      if let Some(ref merged_path) = state.merged_audio_path {
          let _ = crate::audio::pipeline::normalize_merged_audio(merged_path, &config.audio)
              .map_err(PipelineError::from)?;
      }
      ```
    - 需要确保 `use crate::config::types::AppConfig;` 已导入（或使用完整路径 `&crate::config::types::AppConfig`）

    **修改 2 — step_composite 集成智能音量（D-08~D-13, D-15, D-23）：**
    - 签名新增参数：`config: &AppConfig`（放在 `request: &DocumentaryRequest` 之后）
    - 在函数体开头 `tracing::info!("## 6. 最终合成");` 之后（第 273 行），`let merged_video = state.combined_video_path...` 之前，新增：
      ```rust
      // 智能音量覆盖（D-08~D-13）
      let resolved = crate::audio::pipeline::resolve_volumes(
          &crate::audio::volume::VolumeConfig {
              tts_volume: request.tts_volume,
              original_volume: request.original_volume,
              bgm_volume: request.bgm_volume,
          },
          "documentary", &config.audio,
      );
      ```
    - 删除第 289-291 行原有的 `let tts_vol = request.tts_volume;` `let bgm_vol = request.bgm_volume;` `let orig_vol = request.original_volume;` 三行
    - 在第 289 行原位置（`let bgm_path_opt = ...` 之后）插入：
      ```rust
      let tts_vol = resolved.tts_volume;
      let bgm_vol = resolved.bgm_volume;
      let orig_vol = resolved.original_volume;
      let has_original_audio = ...
      ```
    - 后续 FFmpeg filter 组装代码保持不变（变量名一致）

    **修改 3 — run_documentary 传递 config（D-23）：**
    - 第 506 行：`step_merge_audio_subtitle(&mut state, &request)` → `step_merge_audio_subtitle(&mut state, config)`
    - 第 512 行：`step_composite(&mut state, &request)` → `step_composite(&mut state, &request, config)`
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check -p narratoai-core 2>&1 | grep -E "^error" | head -10</automated>
  </verify>
  <done>
    - step_merge_audio_subtitle 接收 config 参数，合并后调用 normalize_merged_audio
    - step_composite 接收 config 参数，开头调用 resolve_volumes 并使用其返回值覆盖音量变量
    - run_documentary 传递 config 到两个 step 函数
    - cargo check 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| step_merge_audio_subtitle → filesystem | normalize_merged_audio 通过 rename 覆盖 merger_audio.mp3 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | T | documentary/pipeline.rs | mitigate | normalize_merged_audio 有开关保护；失败时通过 PipelineError::AudioNormalization 传播，触发 fail-fast |
</threat_model>

<verification>
- `cargo test -p narratoai-core -- audio::volume::tests` 全部通过
- `cargo check -p narratoai-core` 无错误
- 验证 documentary::pipeline::tests 中现有单元测试仍通过
</verification>

<success_criteria>
- ProcessingConfig::default() target_lufs = -14.0
- get_optimized_volumes 含 documentary/sde/sdp 分支
- Documentary 流水线 step 4 合并后调用 normalize_merged_audio
- Documentary 流水线 step 6 合成前调用 resolve_volumes
- 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-04-SUMMARY.md`
</output>
