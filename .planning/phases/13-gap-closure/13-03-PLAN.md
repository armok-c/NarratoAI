---
phase: 13-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - narratoai-core/src/documentary/script_gen.rs
  - narratoai-core/src/prompt/templates/documentary/narration_generation_v2.0.md
  - src-tauri/src/commands/pipeline.rs
autonomous: true
requirements: ["PRMP-03"]
user_setup: []

must_haves:
  truths:
    - generate_documentary_script() 接收 prompt_manager 参数并传递给 analyze_video 和 generate_narration
    - analyze_video() 通过 PromptManager::render_prompt("documentary", "frame_analysis", Some("v1.0"), &vars) 获取 prompt
    - generate_narration() 通过 PromptManager::render_prompt("documentary", "narration_generation", Some("v2.0"), &vars) 获取 prompt
    - narration_generation_v2.0.md 模板明确要求 JSON 格式、items 数组结构、每 item 的必填字段
    - Tauri 命令 generate_documentary_script 从 State 获取 PromptManager 并传递
  artifacts:
    - path: "narratoai-core/src/documentary/script_gen.rs"
      provides: "PromptManager 集成后的 generate_documentary_script / analyze_video / generate_narration"
      contains: "prompt_manager.render_prompt"
    - path: "narratoai-core/src/prompt/templates/documentary/narration_generation_v2.0.md"
      provides: "增强版模板含 JSON items 数组格式说明"
      contains: "\"items\""
    - path: "src-tauri/src/commands/pipeline.rs"
      provides: "generate_documentary_script 命令传递 prompt_manager 到 core 函数"
      contains: "prompt_manager"
  key_links:
    - from: "documentary/script_gen.rs"
      to: "prompt/manager.rs::render_prompt"
      via: "analyze_video() 和 generate_narration() 调用"
      pattern: "prompt_manager.render_prompt"
    - from: "documentary/script_gen.rs"
      to: "prompt/templates/documentary/frame_analysis_v1.0.md"
      via: "render_prompt -> frame_analysis v1.0"
      pattern: "frame_analysis"
    - from: "documentary/script_gen.rs"
      to: "prompt/templates/documentary/narration_generation_v2.0.md"
      via: "render_prompt -> narration_generation v2.0"
      pattern: "narration_generation"
---

<objective>
修复纪录片流水线的 Prompt 调用链：将 generate_narration() 和 analyze_video() 的硬编码 prompt 替换为 PromptManager::render_prompt() 调用，对齐 SDE/SDP 的已验证模式。

Purpose: 模板已注册（Phase 4）但 Documentary 流水线绕过 PromptManager，导致模板变更不影响流水线行为。修复后所有三类流水线一致使用 PromptManager。
Output: 更新后的 script_gen.rs、narration_generation_v2.0.md、Tauri 命令层
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-gap-closure/13-CONTEXT.md

<interfaces>
Per D-16, D-17, D-18, D-19.

SDE 已验证的 PromptManager 调用模式（来自 `src/sde/script_gen.rs` `step_analyze_plot`）：
```rust
let mut vars = HashMap::new();
vars.insert("subtitle_content", &state.subtitle_text as &str);
let prompt = prompt_manager
    .render_prompt("short_drama_narration", "plot_analysis", Some("v1.0"), &vars)
    .map_err(|e| SdeError::PlotAnalysis { details: format!("渲染 prompt 失败: {}", e) })?;
let result = provider.generate_text(&prompt, ...).await?;
```

Documentary 需要对齐此模式。关键区别：
- `analyze_video()` 使用 category="documentary", name="frame_analysis", version="v1.0"
- `generate_narration()` 使用 category="documentary", name="narration_generation", version="v2.0"
- frame_analysis 模板参数：`video_description`, `language`
- narration_generation 模板参数：`video_title`, `frame_analysis_json`, `language`, `style`

当前函数签名（参考自 script_gen.rs）：
```rust
pub async fn analyze_video(request: &ScriptGenRequest, provider: &dyn LlmProvider) -> Result<...>
pub async fn generate_narration(analysis_markdown: &str, provider: &dyn LlmProvider, video_theme: Option<&str>) -> Result<...>
pub async fn generate_documentary_script(request: ScriptGenRequest, vision_provider: &dyn LlmProvider, text_provider: &dyn LlmProvider) -> Result<...>
```

Tauri 命令调用（参考自 src-tauri/src/commands/pipeline.rs line 270）：
```rust
let clips = narratoai_core::documentary::script_gen::generate_documentary_script(
    request,
    vision_provider.as_ref(),
    text_provider.as_ref(),
).await?;
```

PromptManager 在 Tauri State 中的类型：
```rust
prompt_manager: tauri::State<'_, std::sync::Arc<tokio::sync::RwLock<narratoai_core::prompt::manager::PromptManager>>>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 narration_generation_v2.0.md 模板 — 明确 JSON items 数组格式</name>
  <files>
    narratoai-core/src/prompt/templates/documentary/narration_generation_v2.0.md
  </files>
  <read_first>
    narratoai-core/src/prompt/templates/documentary/narration_generation_v2.0.md (当前模板内容，只有自由文本格式要求)
    narratoai-core/src/documentary/script_gen.rs (parse_script_clips 函数 — 它期望的 JSON 结构)
    narratoai-core/src/script/types.rs (ScriptClip 结构体的字段定义)
  </read_first>
  <action>
    将文档 `narration_generation_v2.0.md` 的整体内容替换为以下增强版模板（D-19）：

    ```markdown
    你是一个专业的纪录片解说文案撰写者。根据提供的帧分析数据，生成与画面匹配的解说文案。

    视频标题: ${video_title}
    文案风格: ${style}
    输出语言: ${language}

    帧分析数据:
    ${frame_analysis_json}

    ## 写作要求
    1. 文案必须与帧分析中的场景描述内容一致——不要虚构画面中不存在的内容
    2. 自然地串联相邻帧的内容，形成流畅的叙事
    3. 使用${style}风格的语调——正式但不僵硬
    4. 每段解说对应一个帧组（约3-5个连续帧），段落之间用空行分隔
    5. 在合适位置加入过渡词语，避免生硬的场景切换
    6. 总时长与帧分析覆盖的视频长度匹配

    ## 输出格式
    请严格按照以下 JSON 格式输出，不要包含其他内容：

    ```json
    {
      "items": [
        {
          "picture": "画面描述文字",
          "narration": "对应的解说文案",
          "timestamp": "HH:MM:SS-HH:MM:SS",
          "OST": 2
        }
      ]
    }
    ```

    字段说明：
    - `picture`: 该片段对应的画面内容描述（字符串）
    - `narration`: 详细的解说文案，与画面内容相关（字符串）
    - `timestamp`: 时间范围，格式 HH:MM:SS-HH:MM:SS（字符串）
    - `OST`: 音频类型，固定为 2（混合模式，整数）

    确保：
    - 输出是合法的 JSON，items 数组的每个元素包含上述四个字段
    - items 数组按时间顺序排列
    - 不添加 JSON 标准格式之外的任何注释或标记
    ```

    变更要点：
    - 移除旧版"请在输出中仅包含解说文案正文，不要添加格式标记"——旧指令与代码强制 JSON 输出的矛盾
    - 明确要求 JSON 格式 + items 数组 + 四个字段
    - 字段说明对齐 ScriptClip 结构体和 parse_script_clips() 的预期
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && grep -c '"items"' narratoai-core/src/prompt/templates/documentary/narration_generation_v2.0.md</automated>
  </verify>
  <done>
    - 模板内容包含 `"items"` JSON 数组格式说明
    - 模板内容包含 `picture`、`narration`、`timestamp`、`OST` 四个字段说明
    - 模板不再包含"仅包含解说文案正文"等与 JSON 输出矛盾的指令
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 analyze_video() 和 generate_narration() 使用 PromptManager</name>
  <files>
    narratoai-core/src/documentary/script_gen.rs
  </files>
  <read_first>
    narratoai-core/src/documentary/script_gen.rs (完整文件 — analyze_video 第 60-161 行，generate_narration 第 183-216 行，generate_documentary_script 第 223-245 行)
    narratoai-core/src/prompt/manager.rs (PromptManager API — 重点是 render_prompt 签名)
    narratoai-core/src/prompt/register.rs (documentary prompt 的参数定义 — frame_analysis 需要 video_description/language，narration_generation 需要 video_title/frame_analysis_json/language/style)
    narratoai-core/src/sde/script_gen.rs (已验证的 PromptManager 调用模式，step_analyze_plot 第 22-70 行)
  </read_first>
  <action>
    对 `src/documentary/script_gen.rs` 做以下三处修改：

    **修改 1 — `analyze_video()` 签名 + 实现（D-17）：**
    - 签名添加 `prompt_manager: &PromptManager` 参数
    - 移除第 105-108 行的硬编码 `analysis_prompt` 字符串
    - 新增 `use std::collections::HashMap;`（如果尚未导入）
    - 构造 vars HashMap：`video_description` 从 `request.custom_prompt.as_deref().unwrap_or("根据视频关键帧生成帧分析报告")` 提供，`language` 为 `"zh-CN"`
    - 调用 `prompt_manager.render_prompt("documentary", "frame_analysis", Some("v1.0"), &vars)` — 将错误映射为 `PipelineError::Llm { source: crate::error::LLMError::Validation(e.to_string()) }`
    - 将渲染结果赋值给局部变量 `analysis_prompt`，后续 `provider.analyze_images()` 调用保持不变
    - 注意：`render_prompt()` 返回 `Result<String, PromptError>`，需用 `.map_err()` 转换

    **修改 2 — `generate_narration()` 签名 + 实现（D-16）：**
    - 签名添加 `prompt_manager: &PromptManager` 参数
    - 移除第 188-198 行的硬编码 prompt 构建代码（`let mut prompt = format!(...)`）
    - 构造 vars HashMap：`video_title` 从 `video_theme.unwrap_or("未命名视频")` 提供，`frame_analysis_json` 从 `analysis_markdown` 提供，`language` 为 `"zh-CN"`，`style` 为 `"正式"`
    - 调用 `prompt_manager.render_prompt("documentary", "narration_generation", Some("v2.0"), &vars)` — 使用 `.map_err(PipelineError::from)` 转换错误（PromptError 已实现 Into&lt;PipelineError&gt;，但需确认是否有 From；若无则用 `.map_err(|e| PipelineError::Llm { source: crate::error::LLMError::Validation(e.to_string()) })`）
    - 将渲染结果赋值给 `prompt`，后续 `provider.generate_text()` 调用保持不变

    **修改 3 — `generate_documentary_script()` 签名 + 传递（D-18）：**
    - 签名新增 `prompt_manager: &PromptManager` 参数（放在 `text_provider` 之后或 `vision_provider` 之前，对齐 SDE `step_analyze_plot` 的 `provider, prompt_manager` 参数排列）
    - 调用 `analyze_video(&request, vision_provider, prompt_manager).await?;` — 传递新参数
    - 调用 `generate_narration(&markdown, text_provider, prompt_manager, request.video_theme.as_deref()).await?;` — 传递新参数
    - 函数签名顺序建议：`generate_documentary_script(request, vision_provider, text_provider, prompt_manager)` — 将 prompt_manager 放在最后

    **注意编译检查：** 修改完成后需要确认 `mod.rs` 中的 `pub use script_gen::{generate_documentary_script, ScriptGenRequest};` 无需变更（泛型/函数签名导出不影响 use 声明）。
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check -p narratoai-core 2>&1 | grep -E "error|warning\[" | head -5</automated>
  </verify>
  <done>
    - `generate_documentary_script` 接收 4 个参数（含 prompt_manager）
    - `analyze_video` 通过 `prompt_manager.render_prompt("documentary", "frame_analysis", Some("v1.0"), &vars)` 获取 prompt
    - `generate_narration` 通过 `prompt_manager.render_prompt("documentary", "narration_generation", Some("v2.0"), &vars)` 获取 prompt
    - 无硬编码 prompt 字符串残留（除了回退值用于构造 vars）
    - `cargo check` 通过
  </done>
</task>

<task type="auto">
  <name>Task 3: Tauri 命令 generate_documentary_script 传递 PromptManager</name>
  <files>
    src-tauri/src/commands/pipeline.rs
  </files>
  <read_first>
    src-tauri/src/commands/pipeline.rs (第 219-277 行 generate_documentary_script 命令)
    src-tauri/src/commands/pipeline.rs (第 107-168 行 run_sde 命令 — 已验证的 prompt_manager 传递模式)
  </read_first>
  <action>
    修改 `src-tauri/src/commands/pipeline.rs` 中 `generate_documentary_script` 命令：

    1. 在函数签名中新增 `prompt_manager` state 参数（放在 `registry` 之后，对齐 `run_sde` 的模式）：
       ```rust
       prompt_manager: tauri::State<'_, std::sync::Arc<tokio::sync::RwLock<narratoai_core::prompt::manager::PromptManager>>>,
       ```

    2. 在调用 `narratoai_core::documentary::script_gen::generate_documentary_script` 之前，锁定 prompt_manager：
       ```rust
       let pm = prompt_manager.read().await;
       ```

    3. 修改调用行（第 270-274 行），将 `&pm` 作为第 4 个参数传入：
       ```rust
       let clips = narratoai_core::documentary::script_gen::generate_documentary_script(
           request,
           vision_provider.as_ref(),
           text_provider.as_ref(),
           &pm,
       ).await?;
       ```

    4. 确保在 `.await` 之前 `drop` 所有非 Send guard（prompt_manager 使用 `tokio::sync::RwLock`，其 `ReadGuard` 已实现 `Send`，所以不需要作用域化；参考 `run_sde` 第 152 行的模式）

    5. 确保 `use` 语句不需要额外添加（prompt_manager 类型已在 `run_sde` 函数所在的同一文件中使用，因此类型已在作用域内）
  </action>
  <verify>
    <automated>cd /E/GitLib/NarratoAI && cargo check 2>&1 | grep -E "error" | head -5</automated>
  </verify>
  <done>
    - generate_documentary_script Tauri 命令接收 prompt_manager State 参数
    - 调用 core 函数时传递 `&pm`
    - `cargo check` 全线通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| LLM response → script parser | LLM 可能输出格式不正确的 JSON，已有 parse_script_clips 修复层 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | T | script_gen.rs parse_script_clips | mitigate | 已有 strip_and_repair_json + 尾逗号清理 + 单引号转双引号 + 默认 OST 值等多层防御。新增模板 JSON 格式要求可降低 LLM 输出格式错误的概率 |
</threat_model>

<verification>
- `cargo check -p narratoai-core` 通过
- `cargo check`（workspace 级别，含 Tauri）通过
- 验证 script_gen.rs 中不再有纯字符串拼接的硬编码 prompt
</verification>

<success_criteria>
- Documentary 流水线的 analyze_video 和 generate_narration 均通过 PromptManager 获取 prompt
- Tauri 命令层正确传递 prompt_manager 到 core
- 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/13-gap-closure/13-03-SUMMARY.md`
</output>
