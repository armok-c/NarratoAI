---
phase: 05-script-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/script/mod.rs
  - src/script/types.rs
  - src/script/error.rs
autonomous: true
requirements:
  - SCRP-01
  - SCRP-03

must_haves:
  truths:
    - "能加载 Python 版生成的 JSON 脚本文件，所有字段正确反序列化为 Rust 结构体"
    - "加载时自动校验，缺失必要字段或无效值返回包含中文描述的校验错误"
    - "校验收集所有错误一次性返回，不第一个错误即停"
    - "保存输出美化 JSON (indent=2)，非 ASCII 字符原样输出，Option 字段不出现在输出中"
    - "OstType 枚举正确序列化为 0/1/2 整数、从 0/1/2 正确反序列化"
  artifacts:
    - path: "src/script/types.rs"
      provides: "ScriptClip, OstType, Script 类型别名"
      contains: "pub struct ScriptClip"
    - path: "src/script/error.rs"
      provides: "ScriptError 枚举 + ValidationError 结构体"
      contains: "pub enum ScriptError"
    - path: "src/script/mod.rs"
      provides: "load_script, save_script, validate 公开函数"
      exports: ["load_script", "save_script", "validate"]
    - path: "Cargo.toml"
      provides: "serde_repr 依赖"
      contains: "serde_repr"
    - path: "src/lib.rs"
      provides: "pub mod script 模块导出"
      contains: "pub mod script"
  key_links:
    - from: "src/script/mod.rs"
      to: "src/script/types.rs"
      via: "use crate::script::types::{Script, ScriptClip, OstType}"
      pattern: "use crate::script::types"
    - from: "src/script/mod.rs"
      to: "src/script/error.rs"
      via: "use crate::script::error::ScriptError"
      pattern: "use crate::script::error"
    - from: "src/lib.rs"
      to: "src/script/mod.rs"
      via: "pub mod script"
      pattern: "pub mod script"
---

<objective>
创建 script 模块的数据模型 (ScriptClip, OstType)、错误类型 (ScriptError, ValidationError)、以及加载/保存/校验函数。系统能加载 Python 版 JSON 脚本文件，校验所有字段完整性，保存回美化 JSON。

Purpose: 建立 Script 模块的核心基础设施，为后续编辑 API 和流水线集成提供数据模型和 I/O 操作。
Output: src/script/ 模块（types.rs, error.rs, mod.rs）+ Cargo.toml 依赖更新 + lib.rs 模块导出
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-script-management/05-CONTEXT.md
@.planning/phases/05-script-management/05-RESEARCH.md
@.planning/phases/05-script-management/05-PATTERNS.md

<interfaces>
<!-- Phase 1 已有模式 — executor 必须遵循这些约定 -->

From src/lib.rs:
```rust
pub mod config;
pub mod ffmpeg;
pub mod error;
```

From src/error.rs — thiserror 错误枚举模式:
```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum ConfigError {
    #[error("配置文件读取失败: {0}")]
    IoError(String),
    #[error("配置文件解析失败: {0}")]
    ParseError(String),
    // ...
}
```

From src/config/types.rs — serde derive 模式:
```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, Default)]
#[serde(deny_unknown_fields)]
pub struct AppConfig {
    #[serde(default)]
    pub app: AppSection,
    // ...
}
```

From src/config/mod.rs — 文件 I/O + 错误映射模式:
```rust
pub fn load(path: &Path) -> Result<Self, ConfigError> {
    let content = std::fs::read_to_string(path)
        .map_err(|e| ConfigError::IoError(e.to_string()))?;
    let config: AppConfig = toml::from_str(&content)
        .map_err(|e| ConfigError::ParseError(e.to_string()))?;
    Ok(Self { ... })
}
```
</interfaces>

<!-- Python 参考文件 — 校验规则的权威来源 -->
@app/utils/check_script.py
@resource/scripts/2026-0416-112716.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 数据模型 + 错误类型 + Cargo 依赖</name>
  <files>Cargo.toml, src/lib.rs, src/script/types.rs, src/script/error.rs</files>
  <read_first>
    - Cargo.toml (当前依赖列表)
    - src/lib.rs (当前模块导出)
    - src/error.rs (thiserror 错误枚举模式)
    - src/config/types.rs (serde derive 模式)
    - .planning/phases/05-script-management/05-CONTEXT.md (D-01 到 D-19 决策)
    - resource/scripts/2026-0416-112716.json (实际 JSON 样例)
  </read_first>
  <behavior>
    - Test 1: serde_json 反序列化包含 5 个核心字段的 JSON 为 ScriptClip，验证 _id=1, OST=OstType::NarrationOnly
    - Test 2: serde_json 反序列化包含 6 个管道扩展字段的 JSON，验证 duration/source_time_range 等字段有值
    - Test 3: 缺少管道扩展字段的 JSON 反序列化后，Option 字段为 None
    - Test 4: OstType 序列化为整数 0/1/2，反序列化从 0/1/2 正确还原
    - Test 5: ScriptClip 序列化时 Option::None 字段不出现在 JSON 输出中
    - Test 6: ScriptClip 序列化时中文字符原样输出（不转义为 \uXXXX）
    - Test 7: ScriptError 各变体的中文错误消息
    - Test 8: ValidationError 的 Display 输出包含中文
    - Test 9: cargo build 成功，src/lib.rs 包含 pub mod script
  </behavior>
  <action>
    **RED 阶段 — 先写测试，再实现：**

    1. 修改 `Cargo.toml`，在 `[dependencies]` 添加 `serde_repr = "0.1.20"`。

    2. 修改 `src/lib.rs`，添加 `pub mod script;` 行（在 `pub mod error;` 之后）。

    3. 创建 `src/script/types.rs`：
       - `use serde::{Deserialize, Serialize};` 和 `use serde_repr::{Deserialize_repr, Serialize_repr};`
       - `OstType` 枚举（per D-02）：
         ```rust
         #[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize_repr, Deserialize_repr)]
         #[repr(u8)]
         pub enum OstType {
             NarrationOnly = 0,
             OriginalSound = 1,
             Mixed = 2,
         }
         ```
       - `ScriptClip` 结构体（per D-01, D-05, D-06）：
         ```rust
         #[derive(Debug, Clone, Serialize, Deserialize)]
         pub struct ScriptClip {
             pub _id: i64,
             pub timestamp: String,
             pub picture: String,
             pub narration: String,
             pub OST: OstType,
             #[serde(skip_serializing_if = "Option::is_none")]
             pub duration: Option<f64>,
             #[serde(rename = "sourceTimeRange", skip_serializing_if = "Option::is_none")]
             pub source_time_range: Option<String>,
             #[serde(rename = "editedTimeRange", skip_serializing_if = "Option::is_none")]
             pub edited_time_range: Option<String>,
             #[serde(skip_serializing_if = "Option::is_none")]
             pub audio: Option<std::path::PathBuf>,
             #[serde(skip_serializing_if = "Option::is_none")]
             pub video: Option<std::path::PathBuf>,
             #[serde(skip_serializing_if = "Option::is_none")]
             pub subtitle: Option<std::path::PathBuf>,
         }
         ```
       - `Script` 类型别名（per D-04）：`pub type Script = Vec<ScriptClip>;`
       - `ValidationError` 结构体（per D-13, D-14）：
         ```rust
         #[derive(Debug, Clone)]
         pub struct ValidationError {
             pub clip_index: usize,
             pub field: String,
             pub message: String,
         }
         impl std::fmt::Display for ValidationError {
             fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
                 write!(f, "第 {} 段 {}: {}", self.clip_index + 1, self.field, self.message)
             }
         }
         ```
       - 内联 `#[cfg(test)] mod tests` 块包含上述 9 个测试用例

    4. 创建 `src/script/error.rs`（per D-19）：
       ```rust
       use thiserror::Error;
       use crate::script::types::ValidationError;

       #[derive(Error, Debug)]
       pub enum ScriptError {
           #[error("JSON 解析失败: {0}")]
           JsonParse(#[from] serde_json::Error),
           #[error("脚本校验失败")]
           Validation(Vec<ValidationError>),
           #[error("文件操作失败: {0}")]
           Io(#[from] std::io::Error),
           #[error("索引越界")]
           IndexOutOfBounds,
           #[error("时间戳格式无效: {0}")]
           InvalidTimestamp(String),
       }
       ```
       - 内联测试验证每个变体的中文消息

    5. 创建 `src/script/mod.rs` 占位（仅子模块声明，edit.rs 由 Plan 02 添加）：
       ```rust
       pub mod error;
       pub mod types;
       ```

    **关键 serde 属性说明：**
    - `#[serde(rename = "sourceTimeRange")]` — 映射 camelCase JSON key（per RESEARCH.md Pitfall 2）
    - `#[serde(skip_serializing_if = "Option::is_none")]` — 保存时省略 None 字段（per D-08）
    - `_id` 字段直接使用下划线前缀（per RESEARCH.md Open Question 3 推荐，减少 rename 认知负担）
    - `serde_repr` 将 OstType 枚举序列化为整数（per D-02）
  </action>
  <verify>
    <automated>cargo test --lib script::types && cargo test --lib script::error</automated>
  </verify>
  <done>
    - ScriptClip 从 JSON 反序列化正确，所有 5 个核心字段 + 6 个 Option 管道字段映射正确
    - OstType 枚举序列化为 0/1/2 整数，反序列化从整数正确还原
    - 保存时 Option::None 字段不出现在 JSON 输出中
    - 非 ASCII 字符（中文）原样输出，不转义
    - ScriptError 5 个变体的中文错误消息正确
    - ValidationError Display 输出中文格式
    - cargo build 零警告
    - `grep -c "serde_repr" Cargo.toml` 返回 1
    - `grep -c "pub mod script" src/lib.rs` 返回 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 加载/保存/校验函数</name>
  <files>src/script/mod.rs</files>
  <read_first>
    - src/script/mod.rs (当前占位状态)
    - src/script/types.rs (刚创建的 ScriptClip, OstType, Script 类型)
    - src/script/error.rs (刚创建的 ScriptError, ValidationError)
    - src/config/mod.rs (文件 I/O + 错误映射模式参考)
    - app/utils/check_script.py (Python 版校验规则的权威来源)
    - resource/scripts/2026-0416-112716.json (实际脚本样例)
  </read_first>
  <behavior>
    - Test 1: load_script 从有效 JSON 文件加载，返回正确的 Script（用 tempfile 创建临时文件）
    - Test 2: load_script 从不存在的文件加载，返回 ScriptError::Io
    - Test 3: load_script 从无效 JSON 文件加载，返回 ScriptError::JsonParse
    - Test 4: load_script 从 JSON 数组中 OST 为无效值（如 5）的文件加载，serde_repr 反序列化失败或 validate 捕获
    - Test 5: load_script 对空数组返回校验错误
    - Test 6: load_script 对 _id <= 0 的脚本返回校验错误
    - Test 7: load_script 对无效 timestamp 格式返回校验错误
    - Test 8: load_script 对空 picture/narration 返回校验错误
    - Test 9: validate 收集多个错误一次性返回（如同时有 _id 和 timestamp 问题）
    - Test 10: save_script 输出美化 JSON（indent=2），Option 字段不出现
    - Test 11: save_script 后 load_script 可以正确重新加载（round-trip）
    - Test 12: validate_timestamp 辅助函数正确识别有效/无效 timestamp 格式
  </behavior>
  <action>
    **更新 `src/script/mod.rs`，实现 load_script, save_script, validate 函数：**

    1. 模块声明保留 `pub mod error;` 和 `pub mod types;`（暂不声明 edit）。

    2. 导入：
       ```rust
       use std::path::Path;
       use crate::script::error::ScriptError;
       use crate::script::types::{Script, ScriptClip, ValidationError};
       ```

    3. 实现 `validate_timestamp(ts: &str) -> bool` 手写校验函数（per RESEARCH.md 推荐，避免引入 regex）：
       - 格式：`HH:MM:SS,mmm-HH:MM:SS,mmm`
       - 按 `-` 分割为 2 部分，每部分按 `,` 分割为 time 和 millis
       - time 按 `:` 分割为 3 部分（HH, MM, SS），每部分长度为 2 且全为数字
       - millis 长度为 3 且全为数字

    4. 实现 `validate(script: &Script) -> Result<(), ScriptError>` （per D-11, D-12, D-13）：
       - 检查非空（对应 Python 版 `len(data) == 0` 检查）
       - 遍历每个 clip (enumerate)，收集校验错误：
         - `_id > 0`（per Python 版 `clip['_id'] <= 0` 检查）
         - `timestamp` 格式（调用 validate_timestamp）
         - `picture.trim()` 非空
         - `narration.trim()` 非空
       - 使用 Vec<ValidationError> 收集所有错误，循环结束后如果非空返回 Err(ScriptError::Validation(errors))
       - 注意：serde + serde_repr 已自动处理 JSON 数组类型、dict 类型、5 个核心字段存在性、OST 为整数——Rust 版 validate 不需要重复检查这些

    5. 实现 `load_script(path: &Path) -> Result<Script, ScriptError>` （per D-12）：
       ```rust
       pub fn load_script(path: &Path) -> Result<Script, ScriptError> {
           let content = std::fs::read_to_string(path)
               .map_err(ScriptError::Io)?;
           let script: Script = serde_json::from_str(&content)
               .map_err(ScriptError::JsonParse)?;
           validate(&script)?;
           Ok(script)
       }
       ```

    6. 实现 `save_script(script: &Script, path: &Path) -> Result<(), ScriptError>` （per D-08, D-09, D-10）：
       ```rust
       pub fn save_script(script: &Script, path: &Path) -> Result<(), ScriptError> {
           let json = serde_json::to_string_pretty(script)
               .map_err(|e| ScriptError::JsonParse(e))?;
           std::fs::write(path, json.as_bytes())
               .map_err(ScriptError::Io)?;
           Ok(())
       }
       ```
       注意：serde_json::to_string_pretty 默认使用 2 空格缩进，不转义非 ASCII 字符（与 Python 版 `json.dumps(indent=2, ensure_ascii=False)` 行为一致）。ScriptClip 的 `#[serde(skip_serializing_if = "Option::is_none")]` 确保 None 字段不输出。

    7. 内联 `#[cfg(test)] mod tests` 块，使用 tempfile::TempDir 创建临时 JSON 文件，覆盖上述 12 个测试用例。

    **校验规则对齐说明（参考 app/utils/check_script.py lines 5-110）：**
    - Python 第 14-21 行 (JSON 数组检查): serde 自动处理
    - Python 第 24-30 行 (非空检查): validate 中 `script.is_empty()` 检查
    - Python 第 33-40 行 (元素类型检查): serde 自动处理
    - Python 第 43-50 行 (必需字段检查): serde 自动处理（缺字段报错）
    - Python 第 52-58 行 (_id 正整数检查): validate 中 `_id > 0`
    - Python 第 61-67 行 (timestamp 格式): validate 中 validate_timestamp()
    - Python 第 70-75 行 (picture 非空): validate 中 `picture.trim().is_empty()`
    - Python 第 78-83 行 (narration 非空): validate 中 `narration.trim().is_empty()`
    - Python 第 86-91 行 (OST 整数检查): serde_repr 自动处理
  </action>
  <verify>
    <automated>cargo test --lib script::tests</automated>
  </verify>
  <done>
    - load_script 从有效 JSON 文件加载并校验通过
    - load_script 从无效文件返回正确的 ScriptError 变体
    - validate 对空数组、无效 _id、无效 timestamp、空 picture/narration 各返回对应错误
    - validate 收集多个错误一次性返回
    - save_script 输出美化 JSON，Option 字段不出现，中文原样输出
    - save + load round-trip 一致
    - 所有测试通过: cargo test --lib script
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文件系统 -> serde | 用户提供的 JSON 文件内容不可信，可能包含畸形 JSON、无效类型、恶意大文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | load_script | accept | 桌面应用场景，用户自行选择文件，超大文件风险极低 |
| T-05-02 | Tampering | validate | mitigate | serde 强类型 + validate() 双重校验，拒绝无效 _id/timestamp/空字符串 |
| T-05-03 | Tampering | serde_repr (OstType) | mitigate | serde_repr 对无效整数值（非 0/1/2）返回反序列化错误 |
</threat_model>

<verification>
1. `cargo build` 零警告零错误
2. `cargo test --lib script` 全部通过
3. `grep -c "serde_repr" Cargo.toml` 返回 1
4. `grep -c "pub mod script" src/lib.rs` 返回 1
5. `grep -c "pub fn load_script" src/script/mod.rs` 返回 1
6. `grep -c "pub fn save_script" src/script/mod.rs` 返回 1
7. `grep -c "pub fn validate" src/script/mod.rs` 返回 1
</verification>

<success_criteria>
- ScriptClip 从 Python 版 JSON 正确反序列化，OST 为 0/1/2 整数映射到 OstType 枚举
- load_script 加载 Python 版实际脚本文件（2026-0416-112716.json）所有字段正确
- validate 检测缺失字段、无效 _id、无效 timestamp、空字符串，收集所有错误
- save_script 输出美化 JSON，中文字符原样输出，Option 字段省略
- cargo test --lib script 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-script-management/05-01-SUMMARY.md`
</output>
