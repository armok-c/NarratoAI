---
phase: 05-script-management
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/script/mod.rs
  - src/script/edit.rs
  - tests/script_test.rs
autonomous: true
requirements:
  - SCRP-02

must_haves:
  truths:
    - "update_narration 修改指定下标的 narration 字段，返回新的 Script 实例，原始 Script 不变"
    - "set_ost 修改指定下标的 OstType，返回新的 Script 实例"
    - "update_timestamp 修改指定下标的 timestamp 字段，返回新的 Script 实例"
    - "update_picture 修改指定下标的 picture 字段，返回新的 Script 实例"
    - "索引越界时返回 ScriptError::IndexOutOfBounds"
  artifacts:
    - path: "src/script/edit.rs"
      provides: "不可变编辑方法 (update_narration, set_ost, update_timestamp, update_picture)"
      contains: "pub fn update_narration"
    - path: "src/script/mod.rs"
      provides: "pub mod edit 子模块声明"
      contains: "pub mod edit"
    - path: "tests/script_test.rs"
      provides: "端到端集成测试 (load -> edit -> save -> reload round-trip)"
      contains: "use narratoai_core::script"
  key_links:
    - from: "src/script/edit.rs"
      to: "src/script/types.rs"
      via: "use crate::script::types::{Script, OstType}"
      pattern: "use crate::script::types"
    - from: "src/script/edit.rs"
      to: "src/script/error.rs"
      via: "use crate::script::error::ScriptError"
      pattern: "use crate::script::error"
    - from: "src/script/mod.rs"
      to: "src/script/edit.rs"
      via: "pub mod edit"
      pattern: "pub mod edit"
---

<objective>
实现 Script 的不可变编辑 API 和端到端集成测试。四个编辑方法各返回新的 Script 实例。集成测试验证 load -> edit -> save -> reload 的完整 round-trip。

Purpose: 提供 Phase 6 流水线和 Phase 10 Tauri 命令层需要的脚本编辑能力。
Output: src/script/edit.rs (编辑方法) + tests/script_test.rs (集成测试)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-script-management/05-CONTEXT.md
@.planning/phases/05-script-management/05-01-SUMMARY.md

<interfaces>
<!-- Plan 01 创建的类型 — executor 必须使用这些接口 -->

From src/script/types.rs:
```rust
pub type Script = Vec<ScriptClip>;

pub struct ScriptClip {
    pub _id: i64,
    pub timestamp: String,
    pub picture: String,
    pub narration: String,
    pub OST: OstType,
    pub duration: Option<f64>,
    pub source_time_range: Option<String>,
    pub edited_time_range: Option<String>,
    pub audio: Option<std::path::PathBuf>,
    pub video: Option<std::path::PathBuf>,
    pub subtitle: Option<std::path::PathBuf>,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize_repr, Deserialize_repr)]
#[repr(u8)]
pub enum OstType {
    NarrationOnly = 0,
    OriginalSound = 1,
    Mixed = 2,
}
```

From src/script/error.rs:
```rust
pub enum ScriptError {
    JsonParse(#[from] serde_json::Error),
    Validation(Vec<ValidationError>),
    Io(#[from] std::io::Error),
    IndexOutOfBounds,
    InvalidTimestamp(String),
}
```

From src/script/mod.rs:
```rust
pub fn load_script(path: &Path) -> Result<Script, ScriptError>;
pub fn save_script(script: &Script, path: &Path) -> Result<(), ScriptError>;
pub fn validate(script: &Script) -> Result<(), ScriptError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 不可变编辑方法</name>
  <files>src/script/edit.rs, src/script/mod.rs</files>
  <read_first>
    - src/script/mod.rs (当前状态，需添加 pub mod edit)
    - src/script/types.rs (Script, ScriptClip, OstType 定义)
    - src/script/error.rs (ScriptError 定义)
    - .planning/phases/05-script-management/05-CONTEXT.md (D-15, D-16, D-17, D-18 决策)
  </read_first>
  <behavior>
    - Test 1: update_narration(script, 0, "新文案") 返回新 Script，script[0].narration == "新文案"，原始 script 不变
    - Test 2: update_narration(script, 999, "text") 返回 Err(ScriptError::IndexOutOfBounds)
    - Test 3: set_ost(script, 0, OstType::OriginalSound) 返回新 Script，script[0].OST == OriginalSound
    - Test 4: set_ost(script, 999, OstType::Mixed) 返回 Err(ScriptError::IndexOutOfBounds)
    - Test 5: update_timestamp(script, 0, "00:01:00,000-00:02:00,000") 返回新 Script，timestamp 已更新
    - Test 6: update_timestamp(script, 999, "...") 返回 Err(ScriptError::IndexOutOfBounds)
    - Test 7: update_picture(script, 0, "新画面描述") 返回新 Script，picture 已更新
    - Test 8: update_picture(script, 999, "...") 返回 Err(ScriptError::IndexOutOfBounds)
    - Test 9: 连续编辑——先 update_narration 再 set_ost，第二个操作基于第一个结果，两个修改都保留
    - Test 10: 不可变性验证——编辑后原始 Script 的所有字段值不变
  </behavior>
  <action>
    **RED 阶段 — 先写测试：**

    1. 创建 `src/script/edit.rs`：
       ```rust
       use crate::script::error::ScriptError;
       use crate::script::types::{OstType, Script};

       /// 修改指定下标的 narration，返回新的 Script 实例 (per D-15, D-16)
       pub fn update_narration(script: &Script, index: usize, text: &str) -> Result<Script, ScriptError> {
           if index >= script.len() {
               return Err(ScriptError::IndexOutOfBounds);
           }
           let mut new_script = script.clone();
           new_script[index].narration = text.to_string();
           Ok(new_script)
       }

       /// 修改指定下标的 OST 类型，返回新的 Script 实例 (per D-15)
       pub fn set_ost(script: &Script, index: usize, ost: OstType) -> Result<Script, ScriptError> {
           if index >= script.len() {
               return Err(ScriptError::IndexOutOfBounds);
           }
           let mut new_script = script.clone();
           new_script[index].OST = ost;
           Ok(new_script)
       }

       /// 修改指定下标的 timestamp，返回新的 Script 实例 (per D-15)
       pub fn update_timestamp(script: &Script, index: usize, ts: &str) -> Result<Script, ScriptError> {
           if index >= script.len() {
               return Err(ScriptError::IndexOutOfBounds);
           }
           let mut new_script = script.clone();
           new_script[index].timestamp = ts.to_string();
           Ok(new_script)
       }

       /// 修改指定下标的 picture，返回新的 Script 实例 (per D-15)
       pub fn update_picture(script: &Script, index: usize, pic: &str) -> Result<Script, ScriptError> {
           if index >= script.len() {
               return Err(ScriptError::IndexOutOfBounds);
           }
           let mut new_script = script.clone();
           new_script[index].picture = pic.to_string();
           Ok(new_script)
       }
       ```
       - 内联 `#[cfg(test)] mod tests` 包含上述 10 个测试用例

    2. 修改 `src/script/mod.rs`：
       - 添加 `pub mod edit;` 子模块声明
       - 在 mod.rs 的公开 API 中 re-export 编辑函数（可选，保持调用方 `narratoai_core::script::edit::update_narration` 或通过 mod.rs re-export）

    **关键实现说明：**
    - 所有函数接收 `&Script` 引用，返回 `Result<Script, ScriptError>`——不可变更新模式（per D-15）
    - 使用 `script.clone()` 克隆整个 Vec，然后修改指定下标的字段——Vec<ScriptClip> 的 clone 是深拷贝（String 和 Option 都实现了 Clone）
    - 索引检查用 `index >= script.len()` 判断越界（per D-18）
    - update_timestamp 不在内部校验格式（per D-03 保留原始字符串格式，校验由 validate 在 load 时处理）
    - 不实现 insert/remove/reorder（per D-17，Phase 5 只做单字段更新）
  </action>
  <verify>
    <automated>cargo test --lib script::edit</automated>
  </verify>
  <done>
    - 4 个编辑方法正确实现，每个返回新的 Script 实例
    - 索引越界统一返回 ScriptError::IndexOutOfBounds
    - 原始 Script 在编辑后不变
    - 连续编辑基于前一次结果正确累积
    - cargo test --lib script::edit 全部通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 端到端集成测试</name>
  <files>tests/script_test.rs</files>
  <read_first>
    - tests/script_test.rs (如已存在则读取当前状态)
    - src/script/mod.rs (公开 API 签名)
    - src/script/edit.rs (编辑方法签名)
    - resource/scripts/2026-0416-112716.json (实际 Python 版脚本样例)
    - tests/config_test.rs (集成测试模式参考，如存在)
  </read_first>
  <behavior>
    - Test 1: 端到端 round-trip——创建 JSON 文件 -> load_script -> save_script -> load_script -> 验证数据一致
    - Test 2: 编辑 round-trip——加载脚本 -> update_narration -> save -> reload -> 验证新 narration
    - Test 3: 多次编辑 round-trip——加载 -> update_narration -> set_ost -> update_timestamp -> save -> reload -> 验证全部修改
    - Test 4: 加载 Python 版实际脚本样例文件（resource/scripts/2026-0416-112716.json），验证所有片段正确反序列化
    - Test 5: 保存后的 JSON 可被 serde_json::from_str 重新解析，不丢失任何核心字段
  </behavior>
  <action>
    **创建 `tests/script_test.rs` 集成测试文件：**

    ```rust
    use narratoai_core::script::{
        load_script, save_script, validate,
        edit,
        types::{OstType, Script},
    };
    use std::path::Path;
    use tempfile::TempDir;
    ```

    实现 5 个集成测试用例：

    1. `test_load_save_roundtrip` — 用 serde_json 手工构建一个包含 3 个片段的 JSON 字符串（覆盖三种 OST 类型），写入临时文件，load_script 加载，save_script 保存到另一个临时文件，再次 load_script 加载，比较两次加载结果完全一致（每个字段逐一 assert_eq）。

    2. `test_edit_save_reload` — 从 JSON 字符串加载脚本，调用 `edit::update_narration(&script, 0, "新的中文文案")`，保存结果，重新加载，验证 `script[0].narration == "新的中文文案"`，其他字段不变。

    3. `test_multiple_edits` — 加载脚本，连续调用 update_narration、set_ost(OstType::OriginalSound)、update_timestamp，保存，重新加载，验证所有修改都正确反映。

    4. `test_load_python_sample` — 加载 `resource/scripts/2026-0416-112716.json`，验证加载成功，片段数量 >= 6，第一个片段 _id == 1，第三个片段 OST == OstType::OriginalSound（对应 JSON 中 OST=1）。

    5. `test_save_produces_valid_json` — 加载脚本，编辑，保存，用 `serde_json::from_str::<serde_json::Value>` 验证保存的文件是合法 JSON，验证不存在 `"audio": null` 等 null 字段。

    **JSON 测试数据（手工构建，用于测试 1-3, 5）：**
    ```json
    [
        {
            "_id": 1,
            "timestamp": "00:00:00,000-00:00:10,000",
            "picture": "测试画面描述一",
            "narration": "这是第一段测试解说文案",
            "OST": 0
        },
        {
            "_id": 2,
            "timestamp": "00:00:10,000-00:00:20,000",
            "picture": "测试画面描述二",
            "narration": "播放原片2",
            "OST": 1
        },
        {
            "_id": 3,
            "timestamp": "00:00:20,000-00:00:30,000",
            "picture": "测试画面描述三",
            "narration": "这是第三段混合音频的解说",
            "OST": 2
        }
    ]
    ```
  </action>
  <verify>
    <automated>cargo test --test script_test</automated>
  </verify>
  <done>
    - 5 个集成测试全部通过
    - load -> save -> reload round-trip 数据一致
    - 编辑 -> 保存 -> 重新加载验证修改持久化
    - Python 版实际脚本文件加载成功
    - 保存的 JSON 无 null 字段，中文字符原样输出
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增信任边界 | 编辑方法在内存中操作，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | edit functions | accept | 索引越界已保护，编辑方法为纯函数无副作用 |

</threat_model>

<verification>
1. `cargo test --lib script::edit` 全部通过
2. `cargo test --test script_test` 全部通过
3. `cargo test` (完整测试套件) 全部通过
4. `grep -c "pub fn update_narration" src/script/edit.rs` 返回 1
5. `grep -c "pub fn set_ost" src/script/edit.rs` 返回 1
6. `grep -c "pub mod edit" src/script/mod.rs` 返回 1
</verification>

<success_criteria>
- 4 个不可变编辑方法正确实现
- 所有编辑方法返回新 Script 实例，原始 Script 不变
- 索引越界返回 ScriptError::IndexOutOfBounds
- 集成测试覆盖 load -> edit -> save -> reload 完整 round-trip
- Python 版实际脚本文件加载和验证正确
- cargo test 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/05-script-management/05-02-SUMMARY.md`
</output>
