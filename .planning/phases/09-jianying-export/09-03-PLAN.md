---
phase: 09-jianying-export
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/jianying/builder.rs
  - src/jianying/mod.rs
  - src/ffmpeg/probe.rs
autonomous: true
requirements:
  - JYNG-01
  - JYNG-02

must_haves:
  truths:
    - "DraftFolder::create_draft 在指定路径创建草稿文件夹，写入 draft_content.json 和 draft_meta_info.json"
    - "ExportRequest 接收 Script + 视频源路径 + 草稿配置参数"
    - "export_draft 按 OST 类型正确分支——OST=0 加视频+音频，OST=1 仅视频，OST=2 加视频+音频"
    - "视频片段来源智能回退——有 clip.video 用它，没有则用原始路径 + source_timerange"
    - "音频时长通过 ffprobe 精确获取，safe_duration = min(音频时长, 视频时长)"
    - "草稿命名用户指定 + 自动回退 NarratoAI_{timestamp}"
    - "导出时严格校验所有 Option 字段有值"
    - "分辨率配置化，默认 1920x1080"
  artifacts:
    - path: "src/jianying/builder.rs"
      provides: "DraftFolder, ScriptFile, ExportRequest, export_draft 函数"
      contains: "pub fn export_draft"
    - path: "src/ffmpeg/probe.rs"
      provides: "probe_audio 函数——获取音频时长"
      contains: "pub fn probe_audio"
  key_links:
    - from: "src/jianying/builder.rs"
      to: "src/jianying/segment.rs"
      via: "export_draft 中构造 VideoSegment/AudioSegment"
      pattern: "use.*segment"
    - from: "src/jianying/builder.rs"
      to: "src/jianying/track.rs"
      via: "ScriptFile add_track / add_segment"
      pattern: "use.*track"
    - from: "src/jianying/builder.rs"
      to: "src/jianying/template.rs"
      via: "DraftFolder 加载 JSON 模板"
      pattern: "DRAFT_CONTENT_TEMPLATE"
    - from: "src/jianying/builder.rs"
      to: "src/ffmpeg/probe.rs"
      via: "probe_audio 获取音频时长（per D-10）"
      pattern: "probe_audio"
    - from: "src/jianying/builder.rs"
      to: "src/script/types.rs"
      via: "ExportRequest 引用 Script, ScriptClip, OstType"
      pattern: "use.*script::types"
---

<objective>
实现 builder API 层（DraftFolder + ScriptFile）和导出主函数 export_draft。扩展 FFmpeg probe 支持 audio 时长获取。这是整个 Phase 9 的核心逻辑层，将 Script 数据转换为剪映草稿文件夹。

Purpose: 实现从 NarratoAI Script 到剪映草稿的完整导出流程，包括 OST 分支处理、视频来源回退、音频安全时长、草稿文件写入。
Output: builder.rs（DraftFolder, ScriptFile, ExportRequest, export_draft）+ probe.rs 扩展（probe_audio）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jianying-export/09-CONTEXT.md
@.planning/phases/09-jianying-export/09-RESEARCH.md
@.planning/phases/09-jianying-export/09-02-SUMMARY.md

<interfaces>
<!-- Wave 2 产出的类型 -->

From src/jianying/segment.rs:
```rust
pub struct VideoSegment { /* material, speed, target_timerange, source_timerange */ }
impl VideoSegment {
    pub fn new(path: &Path, target: Timerange, width: u32, height: u32) -> Result<Self, JianYingError>;
    pub fn with_source_timerange(path: &Path, target: Timerange, source: Timerange, width: u32, height: u32) -> Result<Self, JianYingError>;
    pub fn to_json(&self) -> VideoSegmentJson;
    pub fn material_id(&self) -> &str;
    pub fn speed_id(&self) -> &str;
}
pub struct AudioSegment { /* material, speed, target_timerange */ }
impl AudioSegment {
    pub fn new(path: &Path, target: Timerange) -> Result<Self, JianYingError>;
    pub fn to_json(&self) -> AudioSegmentJson;
    pub fn material_id(&self) -> &str;
    pub fn speed_id(&self) -> &str;
}
```

From src/jianying/track.rs:
```rust
pub enum TrackType { Video, Audio }
pub struct Track { /* track_type, name, id, segments */ }
impl Track {
    pub fn new(track_type: TrackType, name: &str) -> Self;
    pub fn add_video_segment(&mut self, seg: VideoSegment);
    pub fn add_audio_segment(&mut self, seg: AudioSegment);
    pub fn to_json(&self) -> TrackJson;
    pub fn collect_materials(&self) -> (Vec<VideoMaterialJson>, Vec<AudioMaterialJson>, Vec<SpeedJson>);
}
```

From src/jianying/template.rs:
```rust
pub const DRAFT_CONTENT_TEMPLATE: &str;
pub const DRAFT_META_INFO_TEMPLATE: &str;
```

From src/jianying/time.rs:
```rust
pub fn trange_from_secs(start_secs: f64, duration_secs: f64) -> Timerange;
```

<!-- 已有代码接口 -->

From src/script/types.rs:
```rust
pub struct ScriptClip {
    pub _id: i64,
    pub timestamp: String,
    pub picture: String,
    pub narration: String,
    pub ost: OstType,
    pub duration: Option<f64>,
    pub source_time_range: Option<String>,
    pub edited_time_range: Option<String>,
    pub audio: Option<std::path::PathBuf>,
    pub video: Option<std::path::PathBuf>,
    pub subtitle: Option<std::path::PathBuf>,
}
pub type Script = Vec<ScriptClip>;
pub enum OstType { NarrationOnly = 0, OriginalSound = 1, Mixed = 2 }
```

From src/ffmpeg/probe.rs:
```rust
pub struct VideoInfo { pub duration_secs: f64, pub width: u32, pub height: u32, pub codec_name: String, pub format_name: String }
pub fn probe_video(path: &Path) -> Result<VideoInfo, FFmpegError>;
```

From app/services/jianying_task.py (Python 版参考):
```python
# OST 分支逻辑:
if item['OST'] in [0, 2]:  # 需要TTS的片段
    safe_duration = min(actual_audio_duration, duration)
    audio_segment = AudioSegment(audio_file, trange(f"{current_time}s", f"{safe_duration}s"))

# 视频回退逻辑:
if video_file:
    video_segment = VideoSegment(video_file, trange(f"{current_time}s", f"{duration}s"))
else:
    video_segment = VideoSegment(origin_path, trange(f"{current_time}s", f"{duration}s"), source_timerange=trange(f"{start_time}s", f"{duration}s"))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FFmpeg probe_audio + DraftFolder/ScriptFile builder</name>
  <files>src/ffmpeg/probe.rs, src/jianying/builder.rs, src/jianying/mod.rs</files>
  <read_first>
    - src/ffmpeg/probe.rs (现有 probe_video 实现模式)
    - src/jianying/template.rs (DRAFT_CONTENT_TEMPLATE, DRAFT_META_INFO_TEMPLATE)
    - src/jianying/track.rs (Track, TrackType)
    - src/jianying/segment.rs (VideoSegment, AudioSegment)
    - src/jianying/time.rs (trange_from_secs)
    - .planning/phases/09-jianying-export/09-RESEARCH.md (Code Examples 草稿文件夹结构)
    - app/services/jianying_task.py (Python 版导出逻辑)
  </read_first>
  <behavior>
    - Test 1: probe_audio 从真实 mp3 文件返回正确的 duration_secs（需要 fixture 或 mock）
    - Test 2: probe_audio 对不存在的文件返回 FFmpegError
    - Test 3: DraftFolder::create_draft 创建草稿目录和两个 JSON 文件
    - Test 4: draft_content.json 包含 canvas_config (1920x1080), fps=30.0, version=360000
    - Test 5: draft_meta_info.json 包含 draft_id (UUID 格式)
    - Test 6: ScriptFile::new 创建空 tracks 列表
    - Test 7: ScriptFile::add_track 添加视频轨道和音频轨道
    - Test 8: ScriptFile::save 后 draft_content.json 可被 serde_json 解析
    - Test 9: ExportRequest 缺少 draft_path 时返回 JianYingError
    - Test 10: ExportRequest 的默认分辨率为 1920x1080
  </behavior>
  <action>
    **1. 扩展 `src/ffmpeg/probe.rs`（per D-10 音频时长使用 ffprobe）：**

    添加 `probe_audio` 函数，复用 `probe_video` 的 ffprobe 调用模式：
    ```rust
    /// 通过 ffprobe 获取音频文件时长（秒）
    pub fn probe_audio(path: &Path) -> Result<f64, FFmpegError> {
        let ffprobe_bin = ffmpeg_sidecar::ffprobe::ffprobe_path();
        let output = Command::new(&ffprobe_bin)
            .args(["-v", "error", "-show_entries", "format=duration", "-of", "csv=p=0"])
            .arg(path.as_os_str())
            .output()
            .map_err(|e| {
                if e.kind() == std::io::ErrorKind::NotFound {
                    FFmpegError::BinaryNotFound
                } else {
                    FFmpegError::SpawnFailed(e.to_string())
                }
            })?;
        if !output.status.success() {
            return Err(FFmpegError::ExecutionError(/* stderr */));
        }
        let stdout = String::from_utf8_lossy(&output.stdout);
        stdout.trim().parse::<f64>()
            .map_err(|e| FFmpegError::OutputParseError(format!("音频时长解析失败: {}", e)))
    }
    ```

    同时添加 `probe_audio_async` 使用 spawn_blocking 包装（与 probe_video_async 模式一致）。

    **2. 创建 `src/jianying/builder.rs`（per D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-12, D-14）：**

    **ExportRequest 结构体**（per D-05, D-11, D-14）：
    ```rust
    /// 导出请求——包含导出所需的所有数据（per D-05）
    pub struct ExportRequest {
        /// 处理后的脚本（所有 Option 字段应有值）
        pub script: Script,
        /// 原始视频文件路径（用于 source_timerange 回退）
        pub video_origin_path: PathBuf,
        /// 草稿保存路径（per D-14）
        pub draft_path: PathBuf,
        /// 草稿名称（per D-12，空则自动生成 NarratoAI_{timestamp}）
        pub draft_name: String,
        /// 输出视频宽度（per D-11，默认 1920）
        pub width: u32,
        /// 输出视频高度（per D-11，默认 1080）
        pub height: u32,
    }

    impl ExportRequest {
        pub fn default_resolution() -> (u32, u32) { (1920, 1080) }

        fn draft_name_or_default(&self) -> String {
            if self.draft_name.trim().is_empty() {
                format!("NarratoAI_{}", std::time::SystemTime::now()
                    .duration_since(std::time::UNIX_EPOCH)
                    .unwrap_or_default()
                    .as_secs())
            } else {
                self.draft_name.clone()
            }
        }
    }
    ```

    **DraftFolder**（per pyJianYingDraft draft_folder.py create_draft）：
    ```rust
    pub struct DraftFolder {
        draft_path: PathBuf,
        draft_name: String,
    }

    impl DraftFolder {
        /// 创建草稿文件夹 + 元信息文件（per RESEARCH Code Examples 草稿文件夹结构）
        pub fn create_draft(
            draft_path: &Path,
            draft_name: &str,
            width: u32,
            height: u32,
        ) -> Result<(Self, ScriptFile), JianYingError> {
            let draft_dir = draft_path.join(draft_name);
            std::fs::create_dir_all(&draft_dir)?;

            // 写入 draft_meta_info.json（per RESEARCH Pitfall 7）
            let meta_content = DRAFT_META_INFO_TEMPLATE
                .replace("{draft_id}", &uuid::Uuid::new_v4().to_string().replace("-", ""));
            std::fs::write(draft_dir.join("draft_meta_info.json"), meta_content)?;

            let script_file = ScriptFile::new(width, height);

            Ok((Self { draft_path: draft_path.to_path_buf(), draft_name: draft_name.to_string() }, script_file))
        }
    }
    ```

    **ScriptFile**（per pyJianYingDraft script_file.py）：
    ```rust
    pub struct ScriptFile {
        width: u32,
        height: u32,
        tracks: Vec<Track>,
        draft_id: String,
    }

    impl ScriptFile {
        pub fn new(width: u32, height: u32) -> Self { /* ... */ }
        pub fn add_track(&mut self, track_type: TrackType, name: &str) { /* ... */ }
        pub fn add_segment_to_track(&mut self, seg: impl Into<SegmentOutput>, track_name: &str) { /* ... */ }

        /// 保存 draft_content.json（per pyJianYingDraft ScriptFile.save/dumps）
        pub fn save(&self, folder: &DraftFolder) -> Result<PathBuf, JianYingError> {
            let draft_dir = folder.draft_path.join(&folder.draft_name);
            let content_path = draft_dir.join("draft_content.json");

            // 构建完整的 draft_content JSON
            // 1. 从模板加载基础结构
            // 2. 替换 canvas_config 的 width/height
            // 3. 替换 materials（收集所有轨道的素材）
            // 4. 替换 tracks（按 render_index 排序）
            // 5. 替换 duration（最后一个片段的 end 时间）
            // 6. 替换 id (UUID)
            // 7. serde_json::to_string_pretty 写入文件
        }
    }
    ```

    **export_draft 导出主函数**（per D-04, D-06, D-07, D-08, D-09）：
    ```rust
    /// 导出脚本为剪映草稿（per Python 版 jianying_task.py 逻辑）
    pub fn export_draft(req: &ExportRequest) -> Result<PathBuf, JianYingError> {
        // 1. 校验 ExportRequest（per D-06）
        validate_export_request(req)?;

        let draft_name = req.draft_name_or_default();
        let (folder, mut script_file) = DraftFolder::create_draft(
            &req.draft_path, &draft_name, req.width, req.height,
        )?;

        // 2. 添加双轨（per D-07）
        script_file.add_track(TrackType::Video, "视频轨道");
        script_file.add_track(TrackType::Audio, "音频轨道");

        // 3. 遍历 ScriptClip 构建时间线（per D-08, D-09）
        let mut current_time_secs: f64 = 0.0;

        for (i, clip) in req.script.iter().enumerate() {
            let duration = clip.duration.ok_or_else(|| JianYingError::MissingField {
                field: "duration".to_string(),
                clip_index: i,
            })?;
            let target = trange_from_secs(current_time_secs, duration);

            // 视频片段（per D-09 智能回退）
            if let Some(ref video_path) = clip.video {
                let video_seg = VideoSegment::new(video_path, target.clone(), req.width, req.height)?;
                script_file.add_segment_to_track(video_seg, "视频轨道");
            } else {
                let source_start = parse_source_start_time(clip, i)?;
                let source = trange_from_secs(source_start, duration);
                let video_seg = VideoSegment::with_source_timerange(
                    &req.video_origin_path, target.clone(), source, req.width, req.height,
                )?;
                script_file.add_segment_to_track(video_seg, "视频轨道");
            }

            // 音频片段（per D-08 OST 映射）
            if clip.ost == OstType::NarrationOnly || clip.ost == OstType::Mixed {
                if let Some(ref audio_path) = clip.audio {
                    let audio_duration = probe_audio(audio_path)
                        .map_err(|e| JianYingError::ProbeError(e.to_string()))?;
                    let safe_duration = duration.min(audio_duration);  // per D-10
                    let audio_target = trange_from_secs(current_time_secs, safe_duration);
                    let audio_seg = AudioSegment::new(audio_path, audio_target)?;
                    script_file.add_segment_to_track(audio_seg, "音频轨道");
                }
            }

            current_time_secs += duration;
        }

        // 4. 保存草稿
        let draft_path = script_file.save(&folder)?;
        Ok(draft_path)
    }

    /// 校验导出请求（per D-06）
    fn validate_export_request(req: &ExportRequest) -> Result<(), JianYingError> {
        if req.script.is_empty() {
            return Err(JianYingError::Validation { details: "脚本不能为空".to_string() });
        }
        if req.draft_path.as_os_str().is_empty() {
            return Err(JianYingError::Validation { details: "草稿保存路径不能为空".to_string() });
        }
        if req.width == 0 || req.height == 0 {
            return Err(JianYingError::Validation { details: "分辨率宽高必须大于 0".to_string() });
        }
        Ok(())
    }

    /// 解析 source_time_range 获取起始秒数（格式: "HH:MM:SS,mmm-HH:MM:SS,mmm"）
    fn parse_source_start_time(clip: &ScriptClip, index: usize) -> Result<f64, JianYingError> {
        let range = clip.source_time_range.as_ref().ok_or_else(|| JianYingError::MissingField {
            field: "source_time_range".to_string(),
            clip_index: index,
        })?;
        // 按 '-' 分割取前半部分，解析为秒数
        // 复用 src/script/mod.rs 的 timestamp_to_millis 逻辑（或内联实现）
        parse_timestamp_start(range).ok_or_else(|| JianYingError::Validation {
            details: format!("第 {} 段 source_time_range 格式无效: {}", index + 1, range),
        })
    }
    ```

    3. 更新 `src/jianying/mod.rs` 添加 `pub mod builder;`。

    **测试策略：**
    - DraftFolder/ScriptFile 测试使用 tempfile 创建临时目录
    - probe_audio 测试需要 mock 或跳过（标记 #[ignore] 因为需要 ffprobe 二进制）
    - ExportRequest 校验测试不需要外部依赖
    - export_draft 端到端测试在 Plan 04 集成测试中进行
  </action>
  <verify>
    <automated>cargo test --lib jianying::builder && cargo test --lib ffmpeg::probe</automated>
  </verify>
  <done>
    - probe_audio 通过 ffprobe 获取音频文件时长（秒）
    - DraftFolder::create_draft 创建草稿文件夹，包含 draft_content.json 和 draft_meta_info.json
    - ScriptFile 可添加轨道和 segment，save() 写入完整 draft_content.json
    - export_draft 按 OST 类型正确分支（OST=0/2 加音频，OST=1 仅视频）
    - export_draft 视频片段有 clip.video 时用它，没有时回退到原始路径 + source_timerange
    - 音频安全时长 = min(ffprobe 时长, 视频时长)
    - 草稿名称空时自动回退为 NarratoAI_{timestamp}
    - 导出前校验脚本非空、路径非空、分辨率有效
    - cargo test --lib jianying 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExportRequest -> 文件系统 | 调用方提供的路径不可信，可能不存在或包含特殊字符 |
| 文件系统 -> ffprobe | 音频文件可能不存在或格式不支持 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | export_draft draft_path | mitigate | validate_export_request 校验路径非空，DraftFolder 使用安全的路径拼接 |
| T-09-05 | Tampering | export_draft video/audio paths | mitigate | VideoMaterial::new 使用 canonicalize，probe_audio 失败返回错误 |
| T-09-06 | Denial of Service | probe_audio | accept | ffprobe 调用有系统级超时保护，桌面场景风险低 |
| T-09-07 | Elevation of Privilege | DraftFolder 文件创建 | accept | 桌面应用以用户权限运行，无提权可能 |

</threat_model>

<verification>
1. `cargo build` 零警告零错误
2. `cargo test --lib jianying` 全部通过
3. `grep -c "pub fn export_draft" src/jianying/builder.rs` 返回 1
4. `grep -c "pub struct ExportRequest" src/jianying/builder.rs` 返回 1
5. `grep -c "pub fn probe_audio" src/ffmpeg/probe.rs` 返回 1
6. `grep -c "pub mod builder" src/jianying/mod.rs` 返回 1
</verification>

<success_criteria>
- DraftFolder 创建的草稿文件夹包含 draft_content.json + draft_meta_info.json
- draft_content.json 包含 canvas_config (1920x1080), fps=30.0, version=360000
- export_draft 按 OST=0/1/2 正确分支处理视频和音频
- 视频来源智能回退：有 clip.video 用它，无则用 origin + source_timerange
- 音频安全时长 = min(ffprobe 时长, 视频时长)
- ExportRequest 校验拒绝空脚本、空路径、零分辨率
- cargo test --lib jianying 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-jianying-export/09-03-SUMMARY.md`
</output>
