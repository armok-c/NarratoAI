---
phase: 09-jianying-export
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - tests/jianying_export.rs
autonomous: true
requirements:
  - JYNG-01
  - JYNG-02

must_haves:
  truths:
    - "3 种 OST 分支各生成正确的时间线结构（OST=0 视频段+音频段，OST=1 仅视频段，OST=2 视频段+音频段）"
    - "视频回退到原始路径 + source_timerange 时 source_timerange 字段存在于 JSON"
    - "音频安全时长不超过视频时长"
    - "生成的 draft_content.json 结构完整：canvas_config, fps, version, materials, tracks"
    - "materials 包含 videos, audios, speeds 三类素材，ID 引用一致"
    - "tracks 按 render_index 排序，video 轨道在 audio 轨道之前"
    - "每个 segment 的 extra_material_refs 包含对应 Speed 的 ID"
  artifacts:
    - path: "tests/jianying_export.rs"
      provides: "端到端集成测试——覆盖 JYNG-01 和 JYNG-02"
      contains: "fn test_ost_narration_only"
  key_links:
    - from: "tests/jianying_export.rs"
      to: "src/jianying/builder.rs"
      via: "调用 export_draft"
      pattern: "export_draft"
    - from: "tests/jianying_export.rs"
      to: "src/script/types.rs"
      via: "构造测试用 ScriptClip"
      pattern: "ScriptClip"
---

<objective>
编写集成测试，端到端验证 export_draft 生成的剪映草稿 JSON 格式正确性。覆盖 JYNG-01（JSON 格式）和 JYNG-02（时间线轨道映射）两个需求的所有场景。

Purpose: 确保生成的剪映草稿 JSON 可被剪映专业版正确识别——通过验证 JSON 结构的完整性、ID 引用的一致性、OST 分支的正确性。
Output: tests/jianying_export.rs 集成测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jianying-export/09-CONTEXT.md
@.planning/phases/09-jianying-export/09-RESEARCH.md
@.planning/phases/09-jianying-export/09-03-SUMMARY.md

<interfaces>
<!-- Wave 3 产出的 API -->

From src/jianying/builder.rs:
```rust
pub struct ExportRequest {
    pub script: Script,
    pub video_origin_path: PathBuf,
    pub draft_path: PathBuf,
    pub draft_name: String,
    pub width: u32,
    pub height: u32,
}

pub fn export_draft(req: &ExportRequest) -> Result<PathBuf, JianYingError>;
```

From src/jianying/mod.rs:
```rust
pub mod builder;
pub mod error;
pub mod material;
pub mod segment;
pub mod template;
pub mod time;
pub mod track;
pub mod types;
```

From src/jianying/error.rs:
```rust
pub enum JianYingError {
    Io(std::io::Error),
    JsonSerialize(serde_json::Error),
    Validation { details: String },
    MissingField { field: String, clip_index: usize },
    ProbeError(String),
}
```

From src/script/types.rs:
```rust
pub struct ScriptClip {
    pub _id: i64,
    pub timestamp: String,
    pub picture: String,
    pub narration: String,
    pub ost: OstType,
    pub duration: Option<f64>,
    pub source_time_range: Option<String>,
    pub edited_time_range: Option<String>,
    pub audio: Option<PathBuf>,
    pub video: Option<PathBuf>,
    pub subtitle: Option<PathBuf>,
}
pub type Script = Vec<ScriptClip>;
pub enum OstType { NarrationOnly = 0, OriginalSound = 1, Mixed = 2 }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OST 分支 + 视频回退 + JSON 结构完整性集成测试</name>
  <files>tests/jianying_export.rs</files>
  <read_first>
    - src/jianying/builder.rs (export_draft 函数签名和 ExportRequest)
    - src/jianying/error.rs (JianYingError 变体)
    - src/script/types.rs (ScriptClip, OstType)
    - .planning/phases/09-jianying-export/09-RESEARCH.md (draft_content.json 结构, Pattern 2-7)
    - app/services/jianying_task.py (Python 版导出逻辑参考)
  </read_first>
  <behavior>
    - Test 1 (OST=0): 3 个 NarrationOnly 片段生成 3 个视频段 + 3 个音频段，tracks.len() == 2
    - Test 2 (OST=1): 2 个 OriginalSound 片段仅生成 2 个视频段，无音频段，audio track segments.len() == 0
    - Test 3 (OST=2): 混合脚本（0/1/2 各一个），OST=0 和 OST=2 的片段有音频，OST=1 的没有
    - Test 4 (视频回退): clip.video 为 None 时，视频段的 source_timerange 有值
    - Test 5 (JSON 结构): draft_content.json 包含 version=360000, fps=30.0, canvas_config { width, height }
    - Test 6 (materials): materials.videos + materials.audios + materials.speeds 非空，每个 material 有唯一 id
    - Test 7 (ID 引用): 每个 segment 的 material_id 在 materials 中有对应素材，extra_material_refs[0] 在 speeds 中
    - Test 8 (时间连续): 视频段 target_timerange 连续，第一段 start=0，后续段 start = 前一段 start + duration
    - Test 9 (校验错误): 缺少 duration 的脚本返回 JianYingError::MissingField
    - Test 10 (校验错误): 空脚本返回 JianYingError::Validation
    - Test 11 (草稿命名): draft_name 为空时自动生成 NarratoAI_ 前缀的名称
    - Test 12 (draft_meta_info): draft_meta_info.json 包含 draft_id 字段
  </behavior>
  <action>
    **创建 `tests/jianying_export.rs`——端到端集成测试：**

    **测试工具函数：**
    ```rust
    /// 创建测试用的 ScriptClip（per D-04 接收处理后的数据）
    fn make_clip(
        id: i64,
        ost: OstType,
        duration: f64,
        video: Option<&str>,
        audio: Option<&str>,
        source_time_range: Option<&str>,
    ) -> ScriptClip {
        ScriptClip {
            _id: id,
            timestamp: format!("00:00:{:02},000-00:00:{:02},000", id * 5, id * 5 + 5),
            picture: format!("测试画面{}", id),
            narration: format!("测试解说{}", id),
            ost,
            duration: Some(duration),
            source_time_range: source_time_range.map(|s| s.to_string()),
            edited_time_range: None,
            audio: audio.map(PathBuf::from),
            video: video.map(PathBuf::from),
            subtitle: None,
        }
    }

    /// 创建临时音频文件（0.5 秒静音 mp3），用于 probe_audio 测试
    fn create_dummy_audio(dir: &Path, name: &str) -> PathBuf {
        // 创建一个最小的 mp3 文件头（或使用 ffmpeg 生成静音）
        // 如果 ffmpeg 不可用，则跳过需要 probe_audio 的测试
    }

    /// 验证导出的 draft_content.json 结构
    fn load_draft_content(draft_path: &Path) -> serde_json::Value {
        let content = std::fs::read_to_string(draft_path.join("draft_content.json")).unwrap();
        serde_json::from_str(&content).unwrap()
    }
    ```

    **测试实现注意点：**
    - 所有测试使用 tempfile::TempDir 创建临时目录作为草稿保存路径
    - 视频/音频文件路径使用占位路径（不实际创建视频文件——probe_audio 测试单独处理）
    - 需要实际 ffprobe 的测试（probe_audio）标记 `#[ignore]`，CI 环境可能有 ffmpeg
    - 不需要 ffprobe 的测试（使用已有 video 路径、不触发 probe_audio 的 OST=1 场景）不标记 ignore

    **Test 1 (OST=0) 实现策略：**
    ```rust
    #[test]
    fn test_ost_narration_only_timeline() {
        // 创建 3 个 OST=0 片段，有 video + audio 路径
        // export_draft -> 检查 tracks[0] (video) 有 3 个 segments
        // 检查 tracks[1] (audio) 有 3 个 segments
        // 检查每个 audio segment 的 target_timerange.duration <= video duration
    }
    ```
    注意：OST=0 需要 probe_audio 获取音频时长。由于测试环境可能没有实际音频文件，这个测试需要：
    (a) 创建一个最小音频文件（通过 ffmpeg 生成静音），或
    (b) 使用 mock（但 Rust 没有 mock 库），或
    (c) 标记 #[ignore] 并提供注释说明
    推荐方案 (c)——标记 #[ignore]，在本地有 ffmpeg 时运行。

    **Test 2 (OST=1) 不需要 probe_audio：**
    ```rust
    #[test]
    fn test_ost_original_sound_video_only() {
        // OST=1 不添加音频段，不需要 probe_audio
        // 创建 2 个 OST=1 片段
        // export_draft -> tracks[1] (audio) segments 为空
    }
    ```

    **Test 4 (视频回退) 不需要 probe_audio：**
    ```rust
    #[test]
    fn test_video_fallback_source_range() {
        // OST=1 片段，video=None
        // export_draft -> 视频段的 source_timerange 有值
        // 视频段的 material.path 指向 video_origin_path
    }
    ```

    **Test 5-8 (JSON 结构验证)：**
    解析生成的 draft_content.json 为 serde_json::Value，逐字段验证：
    - `json["version"] == 360000`
    - `json["fps"] == 30.0`
    - `json["canvas_config"]["width"] == 1920`
    - `json["materials"]["videos"]` 是数组，长度匹配
    - `json["materials"]["speeds"]` 是数组，长度 = segments 总数
    - 每个 segment 的 `extra_material_refs[0]` 在 speeds 中有对应 id

    **Test 9-10 (校验错误)：**
    ```rust
    #[test]
    fn test_export_missing_duration() {
        // 创建 duration=None 的片段
        // export_draft -> Err(JianYingError::MissingField { field: "duration", .. })
    }

    #[test]
    fn test_export_empty_script() {
        // script = vec![]
        // export_draft -> Err(JianYingError::Validation { .. })
    }
    ```

    **Test 12 (draft_meta_info)：**
    ```rust
    #[test]
    fn test_draft_meta_info_json() {
        // export_draft -> 读取 draft_meta_info.json
        // 验证包含 draft_id 字段（32 位 hex 或 UUID 格式）
    }
    ```
  </action>
  <verify>
    <automated>cargo test --test jianying_export</automated>
  </verify>
  <done>
    - 12 个测试用例覆盖 OST=0/1/2 分支、视频回退、JSON 结构、ID 引用、校验错误、草稿命名
    - OST=0 片段正确生成视频段 + 音频段
    - OST=1 片段仅生成视频段，无音频段
    - OST=2 片段正确生成视频段 + 音频段
    - 视频回退时 source_timerange 字段存在
    - draft_content.json 包含完整结构（version, fps, canvas_config, materials, tracks）
    - 每个 segment 的 material_id 和 extra_material_refs 引用一致
    - 缺少 duration 返回 MissingField 错误
    - 空脚本返回 Validation 错误
    - 需要 ffmpeg 的测试标记 #[ignore]
    - cargo test --test jianying_export 通过（除 #[ignore] 测试）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 集成测试仅验证已有功能，不引入新的信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | 测试临时文件 | accept | tempfile 自动清理，测试不使用真实数据 |

</threat_model>

<verification>
1. `cargo test --test jianying_export` 通过（#[ignore] 测试除外）
2. `grep -c "fn test_ost_" tests/jianying_export.rs` 返回 >= 3
3. `grep -c "fn test_video_fallback" tests/jianying_export.rs` 返回 >= 1
4. `grep -c "fn test_draft_content" tests/jianying_export.rs` 返回 >= 1
5. `grep -c "fn test_export_missing" tests/jianying_export.rs` 返回 >= 1
</verification>

<success_criteria>
- OST=0/1/2 三种类型的片段在导出的 draft_content.json 中轨道映射正确
- 视频回退到原始路径时 source_timerange 字段正确
- draft_content.json 结构完整，materials 中 videos/audios/speeds 三类素材齐全
- 每个 segment 的 material_id 在 materials 中有对应条目
- 每个 segment 的 extra_material_refs[0] 在 speeds 中有对应条目
- 时间线连续：第一段 start=0，后续段 start 累加
- 校验拒绝空脚本和缺少 duration 的片段
- cargo test --test jianying_export 通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-jianying-export/09-04-SUMMARY.md`
</output>
