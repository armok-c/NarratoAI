---
phase: 09-jianying-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/jianying/mod.rs
  - src/jianying/error.rs
  - src/jianying/time.rs
  - src/jianying/types.rs
  - src/jianying/template.rs
autonomous: true
requirements:
  - JYNG-01

must_haves:
  truths:
    - "Timerange 以微秒整数序列化为 JSON {start, duration}，trange() 可从秒字符串构造"
    - "JianYingError 枚举覆盖 IO/校验/格式三类错误，Display 输出中文"
    - "SegmentJson 包含 BaseSegment + MediaSegment 所有字段，serde 序列化字段名正确"
    - "draft_content 和 draft_meta_info 模板常量完整可用"
    - "cargo build 零警告，uuid crate 已添加"
  artifacts:
    - path: "src/jianying/mod.rs"
      provides: "模块入口，pub 导出子模块"
      contains: "pub mod"
    - path: "src/jianying/error.rs"
      provides: "JianYingError 枚举"
      contains: "pub enum JianYingError"
    - path: "src/jianying/time.rs"
      provides: "Timerange 结构体、trange() 函数、SEC 常量"
      contains: "pub struct Timerange"
    - path: "src/jianying/types.rs"
      provides: "剪映 JSON serde 结构体（SegmentJson, ClipTransform, HdrSettings 等）"
      contains: "pub struct SegmentJson"
    - path: "src/jianying/template.rs"
      provides: "draft_content_template 和 draft_meta_info_template JSON 常量"
      contains: "DRAFT_CONTENT_TEMPLATE"
    - path: "Cargo.toml"
      provides: "uuid v4 依赖"
      contains: "uuid"
    - path: "src/lib.rs"
      provides: "pub mod jianying 模块导出"
      contains: "pub mod jianying"
  key_links:
    - from: "src/jianying/mod.rs"
      to: "src/jianying/error.rs"
      via: "pub mod error"
      pattern: "pub mod error"
    - from: "src/jianying/time.rs"
      to: "src/jianying/types.rs"
      via: "Timerange 被 SegmentJson 引用"
      pattern: "use.*Timerange"
    - from: "src/lib.rs"
      to: "src/jianying/mod.rs"
      via: "pub mod jianying"
      pattern: "pub mod jianying"
---

<objective>
创建 jianying 模块的基础设施层——错误类型、时间工具、JSON 类型定义、模板常量。这些是后续 material/segment/track/builder 层的依赖基础。

Purpose: 建立剪映 JSON 格式的类型系统和工具函数，为 Wave 2 的核心构建块提供 serde 结构体和时间转换能力。
Output: src/jianying/ 模块（mod.rs, error.rs, time.rs, types.rs, template.rs）+ Cargo.toml uuid 依赖 + lib.rs 模块导出
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-jianying-export/09-CONTEXT.md
@.planning/phases/09-jianying-export/09-RESEARCH.md

<interfaces>
<!-- 已有代码接口 — executor 必须遵循这些约定 -->

From src/lib.rs — 模块导出模式:
```rust
pub mod config;
pub mod error;
pub mod ffmpeg;
pub mod script;
```

From src/error.rs — thiserror 错误枚举模式:
```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum ConfigError {
    #[error("配置文件读取失败: {0}")]
    IoError(String),
    // ...
}
```

From src/script/types.rs — serde + serde_repr 模式:
```rust
use serde::{Deserialize, Serialize};
use serde_repr::{Deserialize_repr, Serialize_repr};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize_repr, Deserialize_repr)]
#[repr(u8)]
pub enum OstType {
    NarrationOnly = 0,
    OriginalSound = 1,
    Mixed = 2,
}
```

From src/script/error.rs — 领域错误模式:
```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum ScriptError {
    #[error("JSON 解析失败: {0}")]
    JsonParse(#[from] serde_json::Error),
    #[error("脚本校验失败")]
    Validation(Vec<ValidationError>),
    #[error("文件操作失败: {0}")]
    Io(#[from] std::io::Error),
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 错误类型 + 时间工具 + Cargo 依赖</name>
  <files>Cargo.toml, src/lib.rs, src/jianying/mod.rs, src/jianying/error.rs, src/jianying/time.rs</files>
  <read_first>
    - Cargo.toml (当前依赖列表)
    - src/lib.rs (当前模块导出)
    - src/error.rs (thiserror 错误枚举模式)
    - .planning/phases/09-jianying-export/09-RESEARCH.md (Pitfall 1 时间单位)
  </read_first>
  <behavior>
    - Test 1: Timerange { start: 0, duration: 1_000_000 } 序列化为 {"start":0,"duration":1000000}
    - Test 2: trange("1.5s", "7.441s") 生成 Timerange { start: 1_500_000, duration: 7_441_000 }
    - Test 3: trange("0s", "0.1s") 生成 Timerange { start: 0, duration: 100_000 }
    - Test 4: Timerange 可被 serde_json 反序列化还原
    - Test 5: JianYingError::Io 的中文消息
    - Test 6: JianYingError::Validation 的中文消息
    - Test 7: JianYingError::MissingField 的中文消息
    - Test 8: cargo build 成功，src/lib.rs 包含 pub mod jianying
  </behavior>
  <action>
    **RED -> GREEN -> REFACTOR:**

    1. 修改 `Cargo.toml`，在 `[dependencies]` 添加 `uuid = { version = "1", features = ["v4"] }`。

    2. 修改 `src/lib.rs`，在 `pub mod script;` 之后添加 `pub mod jianying;`。

    3. 创建 `src/jianying/mod.rs`：
       ```rust
       pub mod error;
       pub mod time;
       pub mod types;
       pub mod template;
       ```

    4. 创建 `src/jianying/error.rs`（per Phase 1 D-16/D-17 模式，D-18 中文消息）：
       ```rust
       use thiserror::Error;

       #[derive(Error, Debug)]
       pub enum JianYingError {
           #[error("文件操作失败: {0}")]
           Io(#[from] std::io::Error),

           #[error("JSON 序列化失败: {0}")]
           JsonSerialize(#[from] serde_json::Error),

           #[error("导出数据校验失败: {details}")]
           Validation { details: String },

           #[error("缺少必要字段: {field}，片段索引: {clip_index}")]
           MissingField { field: String, clip_index: usize },

           #[error("FFmpeg 探测失败: {0}")]
           ProbeError(String),
       }
       ```
       - 内联 `#[cfg(test)] mod tests` 验证每个变体的中文错误消息

    5. 创建 `src/jianying/time.rs`（per RESEARCH Pattern 1）：
       ```rust
       use serde::{Deserialize, Serialize};

       /// 一秒对应的微秒数（per pyJianYingDraft time_util.py SEC 常量）
       pub const SEC: i64 = 1_000_000;

       /// 时间范围——start 和 duration 均为微秒（per RESEARCH Pitfall 1）
       #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
       pub struct Timerange {
           pub start: i64,
           pub duration: i64,
       }

       /// 从秒字符串构造 Timerange（per pyJianYingDraft trange() 函数）
       /// 接受 "1.5s" 格式，也接受纯数字字符串（视为秒）
       pub fn trange(start: &str, duration: &str) -> Timerange {
           Timerange {
               start: parse_seconds(start),
               duration: parse_seconds(duration),
           }
       }

       /// 从 f64 秒数构造 Timerange（Rust API 友好版本）
       pub fn trange_from_secs(start_secs: f64, duration_secs: f64) -> Timerange {
           Timerange {
               start: (start_secs * SEC as f64).round() as i64,
               duration: (duration_secs * SEC as f64).round() as i64,
           }
       }

       /// 解析时间字符串为微秒——支持 "1.5s" 和纯数字
       fn parse_seconds(input: &str) -> i64 {
           let input = input.trim().to_lowercase();
           let secs: f64 = if input.ends_with('s') {
               input.trim_end_matches('s').parse().unwrap_or(0.0)
           } else {
               input.parse().unwrap_or(0.0)
           };
           (secs * SEC as f64).round() as i64
       }
       ```
       - 内联 `#[cfg(test)] mod tests` 覆盖上述测试用例
       - 注意：Timerange 派生 PartialEq 方便测试断言
  </action>
  <verify>
    <automated>cargo test --lib jianying::time && cargo test --lib jianying::error</automated>
  </verify>
  <done>
    - Timerange 序列化为 {"start":N,"duration":N} 微秒整数格式
    - trange("1.5s", "7.441s") 生成正确的微秒 Timerange
    - trange_from_secs(0.0, 6.959) 生成正确的微秒 Timerange
    - JianYingError 5 个变体的中文错误消息正确
    - cargo build 零警告
    - grep -c "uuid" Cargo.toml 返回非零
    - grep -c "pub mod jianying" src/lib.rs 返回 1
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JSON 类型定义 + 模板常量</name>
  <files>src/jianying/types.rs, src/jianying/template.rs</files>
  <read_first>
    - src/jianying/time.rs (刚创建的 Timerange 类型)
    - .planning/phases/09-jianying-export/09-RESEARCH.md (Pattern 2-7: 完整 JSON 结构)
    - .planning/phases/09-jianying-export/09-RESEARCH.md (draft_content_template.json 结构)
  </read_first>
  <behavior>
    - Test 1: SegmentJson 序列化输出包含所有 BaseSegment 字段（enable_adjust, visible, id, material_id, target_timerange 等）
    - Test 2: SegmentJson 的 extra_material_refs 包含 Speed UUID
    - Test 3: ClipTransform 序列化为 {"alpha":1.0,"flip":{"horizontal":false,"vertical":false},"rotation":0.0,"scale":{"x":1.0,"y":1.0},"transform":{"x":0.0,"y":0.0}}
    - Test 4: HdrSettings 序列化为 {"intensity":1.0,"mode":1,"nits":1000}
    - Test 5: VideoMaterialJson 包含所有 pyJianYingDraft VideoMaterial 字段（path, width, height, duration 等）
    - Test 6: AudioMaterialJson 包含所有 pyJianYingDraft AudioMaterial 字段
    - Test 7: SpeedJson 序列化为 {"curve_speed":null,"id":"...","mode":0,"speed":1.0,"type":"speed"}
    - Test 8: TrackJson 序列化输出包含 attribute, id, name, segments, type 字段
    - Test 9: draft_content_template 可被 serde_json::from_str 解析为 serde_json::Value
    - Test 10: draft_meta_info_template 可被 serde_json::from_str 解析
  </behavior>
  <action>
    **创建 `src/jianying/types.rs`——所有剪映 JSON 对应的 serde 结构体（per RESEARCH Pattern 2-7）：**

    所有结构体使用 `#[derive(Debug, Clone, Serialize, Deserialize)]` 和 `#[serde(rename_all = "snake_case")]`。

    1. **ClipTransform** — VideoSegment 的 clip 字段（per RESEARCH Pattern 5）：
       ```rust
       #[derive(Debug, Clone, Serialize, Deserialize)]
       pub struct ClipTransform {
           pub alpha: f64,
           pub flip: FlipState,
           pub rotation: f64,
           pub scale: ScaleValue,
           pub transform: TransformValue,
       }
       ```
       - FlipState { horizontal: bool, vertical: bool }
       - ScaleValue { x: f64, y: f64 }
       - TransformValue { x: f64, y: f64 }
       - UniformScale { on: bool, value: f64 }

    2. **HdrSettings** — VideoSegment 的 hdr_settings 字段：
       ```rust
       #[derive(Debug, Clone, Serialize, Deserialize)]
       pub struct HdrSettings {
           pub intensity: f64,
           pub mode: u32,
           pub nits: u32,
       }
       ```

    3. **CropSettings** — VideoMaterial 的 crop 字段（per RESEARCH Pattern 2）：
       ```rust
       #[derive(Debug, Clone, Serialize, Deserialize)]
       pub struct CropSettings {
           pub upper_left_x: f64, pub upper_left_y: f64,
           pub upper_right_x: f64, pub upper_right_y: f64,
           pub lower_left_x: f64, pub lower_left_y: f64,
           pub lower_right_x: f64, pub lower_right_y: f64,
       }
       ```

    4. **SegmentJson** — 合并 BaseSegment + MediaSegment 字段（per RESEARCH Pattern 4）：
       - 包含 enable_adjust, enable_color_correct_adjust, enable_color_curves 等布尔字段
       - id (String), material_id (String), target_timerange (Timerange)
       - common_keyframes: Vec<Value>, keyframe_refs: Vec<Value>
       - source_timerange: Option<Timerange>, speed: f64, volume: f64
       - extra_material_refs: Vec<String>, is_tone_modify: bool

    5. **VideoSegmentJson** — SegmentJson 扩展（per RESEARCH Pattern 5）：
       - clip: ClipTransform
       - uniform_scale: UniformScale
       - hdr_settings: HdrSettings

    6. **AudioSegmentJson** — SegmentJson 扩展（per RESEARCH Pattern 6）：
       - clip: Option<()>（序列化为 null）
       - hdr_settings: Option<()>（序列化为 null）

    7. **VideoMaterialJson** — 视频素材 JSON（per RESEARCH Pattern 2）：
       - 所有字段：audio_fade, category_id, category_name, check_flag, crop, crop_ratio, crop_scale, duration, height, id, local_material_id, material_id, material_name, media_path, path, type (rename to "type"), width

    8. **AudioMaterialJson** — 音频素材 JSON（per RESEARCH Pattern 2）：
       - 所有字段：app_id, category_id, category_name, check_flag, copyright_limit_type, duration, effect_id, formula_id, id, local_material_id, music_id, name, path, source_platform, type, wave_points

    9. **SpeedJson** — Speed 素材（per RESEARCH Pattern 7）：
       ```rust
       #[derive(Debug, Clone, Serialize, Deserialize)]
       pub struct SpeedJson {
           pub curve_speed: Option<()>,
           pub id: String,
           pub mode: u32,
           pub speed: f64,
           #[serde(rename = "type")]
           pub type_field: String,
       }
       ```

    10. **TrackJson** — 轨道输出（per RESEARCH Pattern 3）：
        - attribute: u32, flag: u32, id: String, is_default_name: bool, name: String
        - segments: Vec<serde_json::Value>, type: String (rename)

    11. **DraftContentJson** — draft_content.json 顶层结构（per RESEARCH Code Examples）：
        - canvas_config, color_space, config, duration, fps, id, materials, tracks, version

    每个 struct 提供 `default()` 工厂方法返回 pyJianYingDraft 默认值（如 ClipTransform::default_video() 返回 alpha=1.0, scale x/y=1.0 等）。

    **创建 `src/jianying/template.rs`——JSON 模板常量（per RESEARCH Code Examples 和 Pitfall 7）：**

    1. `DRAFT_CONTENT_TEMPLATE: &str` — 包含完整的 draft_content.json 模板，字段：
       - version: 360000
       - canvas_config: { height: 1080, ratio: "original", width: 1920 }
       - color_space: 0
       - config: { maintrack_adsorb: true, video_mute: false }
       - fps: 30.0（per D-13）
       - materials: 所有空数组（audios, speeds, videos, audio_effects, audio_fades, canvases, effects, masks, material_animations, transitions, video_effects 等）
       - tracks: []
       - platform: { app_id: 3704, app_version: "5.9.0", device_id: "", hard_disk_id: "", mac_address: "" }
       - id: 将在 builder 中替换为 UUID

    2. `DRAFT_META_INFO_TEMPLATE: &str` — 包含 draft_meta_info.json 模板（per pyJianYingDraft draft_meta_info.json）

    内联 `#[cfg(test)] mod tests` 验证模板可解析 + 所有 serde 结构体序列化正确。
  </action>
  <verify>
    <automated>cargo test --lib jianying::types && cargo test --lib jianying::template</automated>
  </verify>
  <done>
    - 所有剪映 JSON serde 结构体可正确序列化，字段名与 pyJianYingDraft 输出一致
    - SegmentJson 包含 BaseSegment + MediaSegment 全部字段
    - VideoSegmentJson 额外包含 clip + hdr_settings
    - AudioSegmentJson 的 clip 和 hdr_settings 序列化为 null
    - SpeedJson curve_speed 序列化为 null
    - draft_content_template 可被 serde_json::from_str 解析
    - draft_meta_info_template 可被 serde_json::from_str 解析
    - cargo build 零警告
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无外部输入 | 本 plan 仅创建类型定义和常量，不处理外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | template.rs | accept | 固定模板常量不包含敏感信息（app_id 是公开值） |

</threat_model>

<verification>
1. `cargo build` 零警告零错误
2. `cargo test --lib jianying` 全部通过
3. `grep -c "uuid" Cargo.toml` 返回非零
4. `grep -c "pub mod jianying" src/lib.rs` 返回 1
5. `grep -c "pub struct Timerange" src/jianying/time.rs` 返回 1
6. `grep -c "pub enum JianYingError" src/jianying/error.rs` 返回 1
7. `grep -c "DRAFT_CONTENT_TEMPLATE" src/jianying/template.rs` 返回 1
</verification>

<success_criteria>
- Timerange 以微秒整数正确序列化，trange() 从秒字符串正确转换
- JianYingError 枚举覆盖 IO/序列化/校验/缺失字段/探测 5 类错误，中文消息
- 所有剪映 JSON serde 结构体字段名与 pyJianYingDraft 输出一一对应
- draft_content 和 draft_meta_info 模板常量可被 serde_json 解析
- cargo test --lib jianying 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-jianying-export/09-01-SUMMARY.md`
</output>
