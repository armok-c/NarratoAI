---
phase: 11-extended-features
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/material/searcher.rs
  - src/material/downloader.rs
autonomous: true
requirements: [EXTD-03]
user_setup:
  - why: "Pexels 和 Pixabay API 需要用户申请 key 并填入 config.toml"
    steps:
      - "1. 注册 Pexels: https://www.pexels.com/api/ → 获取 API key"
      - "2. 注册 Pixabay: https://pixabay.com/api/docs/ → 获取 API key"
      - "3. 填入 config.toml [app] 段的 pexels_api_keys / pixabay_api_keys 数组"
    env_vars:
      - name: "PEXELS_API_KEYS"
        source: "Pexels API Dashboard"
      - name: "PIXABAY_API_KEYS"
        source: "Pixabay API Documentation"

must_haves:
  truths:
    - "MaterialError 枚举包含 4+ 变体，所有 Display 信息为中文"
    - "search_videos_pexels() 通过 reqwest 调用 Pexels API /videos/search"
    - "search_videos_pixabay() 通过 reqwest 调用 Pixabay API /api/videos/"
    - "get_api_key() 从 Vec<String> 轮询 API key（AtomicUsize 原子自增）"
    - "Pexels/Pixabay API 响应类型用 serde 反序列化"
    - "save_video() 用 MD5(url_无query) 做缓存 key，文件已存在则跳过"
    - "download_videos() 编排搜索+下载+时长汇总，超 audio_duration 后停止"
    - "下载的 video 通过 ffprobe 验证（检查 duration>0）"
  artifacts:
    - path: "src/material/searcher.rs"
      provides: "MaterialError + API 响应类型 + Pexels/Pixabay 搜索函数 + key 轮询"
      min_lines: 180
    - path: "src/material/downloader.rs"
      provides: "save_video + download_videos + MD5 缓存 + ffprobe 验证"
      min_lines: 120
  key_links:
    - from: "src/material/searcher.rs::search_videos_pexels"
      to: "reqwest::Client"
      via: "HTTP GET Pexels API"
      pattern: "pexels.com/videos/search"
    - from: "src/material/downloader.rs::save_video"
      to: "reqwest::get | reqwest::Client"
      via: "HTTP download video file"
      pattern: "reqwest::"
    - from: "src/material/downloader.rs::save_video"
      to: "md-5::Md5"
      via: "MD5 URL hash 作为缓存 key"
      pattern: "Md5"

---

<objective>
实现 Pexels/Pixabay 素材搜索和下载功能（EXTD-03），通过 reqwest HTTP 客户端调用 REST API 搜索视频素材，下载到本地缓存，MD5 URL 去重。

与 Python 版 `material.py` 1:1 功能对齐，包含多 API key 轮换、Pexels + Pixabay 双源搜索、MD5 URL 缓存。下载视频通过 ffprobe 验证。

Purpose: 提供素材库搜索和下载能力，供视频剪辑流水线使用。
Output: 完整可测试的 material 模块（searcher + downloader）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-extended-features/11-CONTEXT.md
@.planning/phases/11-extended-features/11-RESEARCH.md
@.planning/phases/11-extended-features/11-PATTERNS.md
@src/material/mod.rs
@src/material/searcher.rs
@src/material/downloader.rs
@src/ffmpeg/probe.rs
@src/config/types.rs

<interfaces>
<!-- 从代码库提取的现有类型 -->

From src/ffmpeg/probe.rs (ffprobe audio validation reuse):
```rust
pub fn probe_audio(path: &Path) -> Result<f64, FFmpegError> {
    // 返回音频时长（秒），用于验证下载的 video 文件有效性
}
```

From src/config/types.rs:
```rust
pub struct ProxySection {
    pub http: String,
    pub https: String,
    pub enabled: bool,
}

// pexels_api_keys 和 pixabay_api_keys 在 AppSection 中
pub struct AppSection {
    // ...
    pub pexels_api_keys: Vec<String>,
    pub pixabay_api_keys: Vec<String>,
}
```

From PATTERNS.md (Pexels/Pixabay API response types):
```rust
#[derive(Debug, Deserialize)]
#[serde(rename_all = "snake_case")]
pub struct PexelsSearchResponse {
    pub page: u32,
    pub per_page: u32,
    pub total_results: u32,
    pub videos: Vec<PexelsVideo>,
}

#[derive(Debug, Deserialize)]
pub struct PexelsVideo {
    pub id: u64,
    pub width: u32,
    pub height: u32,
    pub duration: u32,
    pub url: String,
    pub video_files: Vec<PexelsVideoFile>,
}

#[derive(Debug, Deserialize)]
pub struct PexelsVideoFile {
    pub id: u64,
    pub quality: String,
    pub file_type: String,
    pub width: u32,
    pub height: u32,
    pub link: String,
}

#[derive(Debug, Deserialize)]
pub struct PixabaySearchResponse {
    pub total: u32,
    pub total_hits: u32,
    pub hits: Vec<PixabayHit>,
}

#[derive(Debug, Deserialize)]
pub struct PixabayHit {
    pub id: u64,
    pub duration: u32,
    pub videos: PixabayVideos,
}

#[derive(Debug, Deserialize)]
pub struct PixabayVideos {
    pub large: Option<PixabayVideoVariant>,
    pub medium: Option<PixabayVideoVariant>,
    pub small: Option<PixabayVideoVariant>,
    pub tiny: Option<PixabayVideoVariant>,
}

#[derive(Debug, Deserialize)]
pub struct PixabayVideoVariant {
    pub url: String,
    pub width: u32,
    pub height: u32,
    pub size: u64,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Material searcher (searcher.rs) — MaterialError + API 响应类型 + Pexels/Pixabay 搜索 + key 轮询</name>
  <files>src/material/searcher.rs</files>
  <read_first>
    .planning/phases/11-extended-features/11-CONTEXT.md (D-12~D-14, D-25~D-29, D-31)
    .planning/phases/11-extended-features/11-RESEARCH.md (Material Search Flow, Pexels API Response, Pixabay API Response, Pitfall 3: key rotation race)
    .planning/phases/11-extended-features/11-PATTERNS.md (API response struct pattern, error enum pattern, reqwest HTTP pattern)
    app/services/material.py (Python 参考：search_videos_pexels, search_videos_pixabay, get_api_key)
    src/config/types.rs (ProxySection, AppSection)
  </read_first>
  <action>
    用完整实覆盖写 `src/material/searcher.rs` 存根。

    **1. 必要导入：**
    ```rust
    use std::sync::atomic::{AtomicUsize, Ordering};
    use std::time::Duration;
    
    use serde::Deserialize;
    use thiserror::Error;
    use reqwest::Client;
    use reqwest::Proxy;
    ```

    **2. MaterialError 枚举**（per D-25, D-26, D-27）：
    ```rust
    #[derive(Error, Debug)]
    pub enum MaterialError {
        #[error("API 请求失败: {0}")]
        ApiRequest(String),
        
        #[error("API 认证失败: {0}")]
        AuthenticationFailed(String),
        
        #[error("下载失败: {0}")]
        DownloadFailed(String),
        
        #[error("文件验证失败: {0}")]
        ValidationFailed(String),
        
        #[error("API key 未配置: {0}")]
        MissingApiKey(String),
        
        #[error("IO 错误: {0}")]
        IoError(String),
    }
    ```

    **3. API 响应类型**（全量，含 serde 反序列化，值从 RESEARCH.md Pattern 复制）：

    包含以下所有 struct（serde 版本，rename_all snake_case）：
    - `PexelsSearchResponse` (page, per_page, total_results, videos: Vec<PexelsVideo>)
    - `PexelsVideo` (id, width, height, duration, url, video_files: Vec<PexelsVideoFile>)
    - `PexelsVideoFile` (id, quality, file_type, width, height, link)
    - `PixabaySearchResponse` (total, total_hits, hits: Vec<PixabayHit>)
    - `PixabayHit` (id, duration, videos: PixabayVideos)
    - `PixabayVideos` (large/medium/small/tiny: Option<PixabayVideoVariant>)
    - `PixabayVideoVariant` (url, width, height, size)

    **4. MaterialInfo 结构体**（搜索结果的统一表示）：
    ```rust
    #[derive(Debug, Clone)]
    pub struct MaterialInfo {
        pub id: u64,
        pub duration: u32,
        pub width: u32,
        pub height: u32,
        pub video_url: String,
        pub source: String, // "pexels" | "pixabay"
    }
    ```

    **5. get_api_key(keys: &[String], counter: &AtomicUsize) -> Result<String, MaterialError>**（per D-13, Pitfall 3）：

    ```rust
    /// 从 key 数组中轮询返回 API key（AtomicUsize 原子自增，防止异步竞态）
    pub fn get_api_key(keys: &[String], counter: &AtomicUsize) -> Result<String, MaterialError> {
        if keys.is_empty() {
            return Err(MaterialError::MissingApiKey("API key 列表为空".into()));
        }
        if keys.len() == 1 {
            return Ok(keys[0].clone());
        }
        let idx = counter.fetch_add(1, Ordering::Relaxed) % keys.len();
        Ok(keys[idx].clone())
    }
    ```

    **6. search_videos_pexels(search_term: &str, minimum_duration: u32, target_width: u32, target_height: u32, api_keys: &[String], key_counter: &AtomicUsize, proxy_section: &config::types::ProxySection) -> Result<Vec<MaterialInfo>, MaterialError>**

    使用 reqwest Client 调用 Pexels API：
    - `GET https://api.pexels.com/videos/search?query={search_term}&per_page=20`
    - Header: `Authorization: <api_key>`
    - 解析 JSON 响应为 `PexelsSearchResponse`
    - 过滤：`duration >= minimum_duration` + 匹配 `width==target_width && height==target_height`
    - 构建 MaterialInfo 列表
    - 使用 reqwest::Client 配置 proxy（per D-31）：如果 `proxy_section.enabled` 为 true 且 `proxy_section.http` 非空，则构建 `Proxy::http(...)` 注入 Client

    ```rust
    pub fn search_videos_pexels(
        search_term: &str,
        minimum_duration: u32,
        target_width: u32,
        target_height: u32,
        api_keys: &[String],
        key_counter: &AtomicUsize,
        proxy_section: &crate::config::types::ProxySection,
    ) -> Result<Vec<MaterialInfo>, MaterialError> {
        let api_key = get_api_key(api_keys, key_counter)?;
        let api_url = format!(
            "https://api.pexels.com/videos/search?query={}&per_page=20&orientation={}",
            urlencoding(search_term),
            if target_width >= target_height { "landscape" } else { "portrait" }
        );
        
        let client = build_client(proxy_section)?;
        let response = client.get(&api_url)
            .header("Authorization", &api_key)
            .timeout(Duration::from_secs(60))
            .send()
            .map_err(|e| MaterialError::ApiRequest(e.to_string()))?;
        
        if response.status().as_u16() == 401 {
            return Err(MaterialError::AuthenticationFailed("Pexels API key 无效".into()));
        }
        if !response.status().is_success() {
            return Err(MaterialError::ApiRequest(format!("HTTP {}", response.status())));
        }
        
        let body: PexelsSearchResponse = response.json()
            .map_err(|e| MaterialError::ApiRequest(e.to_string()))?;
        
        let results: Vec<MaterialInfo> = body.videos.into_iter()
            .filter(|v| v.duration >= minimum_duration)
            .flat_map(|v| {
                v.video_files.into_iter()
                    .filter(|f| f.width == target_width && f.height == target_height)
                    .map(move |f| MaterialInfo {
                        id: v.id,
                        duration: v.duration,
                        width: f.width,
                        height: f.height,
                        video_url: f.link,
                        source: "pexels".into(),
                    })
            })
            .collect();
        
        Ok(results)
    }
    ```

    **7. search_videos_pixabay(search_term: &str, minimum_duration: u32, target_width: u32, target_height: u32, api_keys: &[String], key_counter: &AtomicUsize, proxy_section: &config::types::ProxySection) -> Result<Vec<MaterialInfo>, MaterialError>**

    类似 Pexels 但是调用 Pixabay API：
    - `GET https://pixabay.com/api/videos/?q={search_term}&video_type=all&per_page=50&key={api_key}`
    - 使用 `proxy_section` 构建 Client
    - 解析 `PixabaySearchResponse`
    - 选择最匹配目标分辨率的 variant（large/medium/small 逐级尝试）
    - 过滤 duration >= minimum_duration

    **8. 辅助函数 pub(crate) fn build_client(proxy_section: &config::types::ProxySection) -> Result<Client, MaterialError>**（per D-31；pub(crate) 使得 downloader.rs 可导入）：

    ```rust
    pub(crate) fn build_client(proxy_section: &crate::config::types::ProxySection) -> Result<Client, MaterialError> {
        let mut builder = Client::builder()
            .timeout(Duration::from_secs(60))
            .danger_accept_invalid_certs(false);
        
        if proxy_section.enabled {
            if !proxy_section.http.is_empty() {
                let proxy = Proxy::http(&proxy_section.http)
                    .map_err(|e| MaterialError::ApiRequest(format!("代理配置无效: {}", e)))?;
                builder = builder.proxy(proxy);
            }
            if !proxy_section.https.is_empty() {
                let proxy = Proxy::https(&proxy_section.https)
                    .map_err(|e| MaterialError::ApiRequest(format!("代理配置无效: {}", e)))?;
                builder = builder.proxy(proxy);
            }
        }
        
        builder.build()
            .map_err(|e| MaterialError::ApiRequest(format!("HTTP 客户端创建失败: {}", e)))
    }
    ```

    **9. URL 编码辅助函数：**
    ```rust
    fn urlencoding(s: &str) -> String {
        // 简单 URL 编码（仅编码空格和特殊字符）
        s.replace(' ', "+")
    }
    ```

    **10. 单元测试：**

    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        
        #[test]
        fn test_material_error_chinese_display() {
            let err = MaterialError::ApiRequest("timeout".into());
            assert!(err.to_string().contains("API 请求失败"));
            
            let err = MaterialError::AuthenticationFailed("bad key".into());
            assert!(err.to_string().contains("API 认证失败"));
            
            let err = MaterialError::DownloadFailed("network".into());
            assert!(err.to_string().contains("下载失败"));
            
            let err = MaterialError::MissingApiKey("pexels".into());
            assert!(err.to_string().contains("API key 未配置"));
        }
        
        #[test]
        fn test_get_api_key_single() {
            let keys = vec!["key1".to_string()];
            let counter = AtomicUsize::new(0);
            let key = get_api_key(&keys, &counter).unwrap();
            assert_eq!(key, "key1");
        }
        
        #[test]
        fn test_get_api_key_rotation() {
            let keys = vec!["key1".to_string(), "key2".to_string(), "key3".to_string()];
            let counter = AtomicUsize::new(0);
            
            assert_eq!(get_api_key(&keys, &counter).unwrap(), "key1");
            assert_eq!(get_api_key(&keys, &counter).unwrap(), "key2");
            assert_eq!(get_api_key(&keys, &counter).unwrap(), "key3");
            assert_eq!(get_api_key(&keys, &counter).unwrap(), "key1"); // wrap
        }
        
        #[test]
        fn test_get_api_key_empty() {
            let keys: Vec<String> = vec![];
            let counter = AtomicUsize::new(0);
            let result = get_api_key(&keys, &counter);
            assert!(result.is_err());
            assert!(result.unwrap_err().to_string().contains("API key 未配置"));
        }
        
        #[test]
        fn test_pexels_response_deserialize() {
            let json = r#"{
                "page": 1,
                "per_page": 20,
                "total_results": 1,
                "videos": [{
                    "id": 12345,
                    "width": 1920,
                    "height": 1080,
                    "duration": 15,
                    "url": "https://www.pexels.com/video/test/",
                    "video_files": [{
                        "id": 1,
                        "quality": "hd",
                        "file_type": "video/mp4",
                        "width": 1920,
                        "height": 1080,
                        "link": "https://www.pexels.com/video/download/12345/"
                    }]
                }]
            }"#;
            let resp: PexelsSearchResponse = serde_json::from_str(json).unwrap();
            assert_eq!(resp.total_results, 1);
            assert_eq!(resp.videos.len(), 1);
            assert_eq!(resp.videos[0].id, 12345);
            assert_eq!(resp.videos[0].video_files[0].quality, "hd");
        }
        
        #[test]
        fn test_pixabay_response_deserialize() {
            let json = r#"{
                "total": 1,
                "total_hits": 1,
                "hits": [{
                    "id": 67890,
                    "duration": 10,
                    "videos": {
                        "large": {
                            "url": "https://pixabay.com/video/download/67890_large.mp4",
                            "width": 1920,
                            "height": 1080,
                            "size": 5000000
                        },
                        "medium": null,
                        "small": null,
                        "tiny": null
                    }
                }]
            }"#;
            let resp: PixabaySearchResponse = serde_json::from_str(json).unwrap();
            assert_eq!(resp.total_hits, 1);
            assert_eq!(resp.hits[0].id, 67890);
            assert!(resp.hits[0].videos.large.is_some());
            assert!(resp.hits[0].videos.medium.is_none());
        }
        
        #[test]
        fn test_urlencoding() {
            assert_eq!(urlencoding("nature"), "nature");
            assert_eq!(urlencoding("mountain river"), "mountain+river");
        }
        
        #[test]
        fn test_material_info_structure() {
            let info = MaterialInfo {
                id: 1,
                duration: 30,
                width: 1920,
                height: 1080,
                video_url: "https://example.com/video.mp4".into(),
                source: "pexels".into(),
            };
            assert_eq!(info.source, "pexels");
            assert_eq!(info.duration, 30);
        }
    }
    ```

    **约束：**
    - `get_api_key` 使用 `AtomicUsize` 而非 `Mutex<usize>`（per Pitfall 3 防护，AtomicUsize 是改进，Python 版未做线程安全）
    - HTTP 客户端使用 `reqwest::Client`（已在 Cargo.toml 中）
    - 所有 API 调用超时 60 秒
    - 认证失败时返回 MaterialError::AuthenticationFailed（非 ApiRequest），供调用方区分
  </action>
  <verify>
    <automated>cargo test --lib material::searcher::tests -- --nocapture 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 所有单元测试通过（`cargo test --lib material::searcher::tests`）
    - `grep -c "pub enum MaterialError" src/material/searcher.rs` 返回 1
    - `grep -c "pub fn get_api_key" src/material/searcher.rs` 返回 1
    - `grep -c "pub fn search_videos_pexels" src/material/searcher.rs` 返回 1
    - `grep -c "pub fn search_videos_pixabay" src/material/searcher.rs` 返回 1
    - `grep -c "pexels.com/videos/search" src/material/searcher.rs` 返回 1
    - `grep -c "pixabay.com/api/videos" src/material/searcher.rs` 返回 1
    - `grep -c "AtomicUsize" src/material/searcher.rs` 返回 1
    - `grep -c "pub struct PexelsSearchResponse" src/material/searcher.rs` 返回 1
    - `grep -c "pub struct PixabaySearchResponse" src/material/searcher.rs` 返回 1
  </acceptance_criteria>
  <done>Material searcher 完整实现：MaterialError、PexelsSearch/PixabaySearch 响应类型、get_api_key（AtomicUsize）、两个搜索函数、单元测试全部通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Material downloader (downloader.rs) — save_video + download_videos + MD5 缓存 + ffprobe 验证</name>
  <files>src/material/downloader.rs</files>
  <read_first>
    .planning/phases/11-extended-features/11-CONTEXT.md (D-14: download_videos orchestration, D-15: MD5 URL cache, reqwest)
    .planning/phases/11-extended-features/11-RESEARCH.md (Material Search Flow: save_video MD5 cache, download_videos orchestration)
    .planning/phases/11-extended-features/11-PATTERNS.md (temp file + atomic rename pattern, ffprobe validation pattern)
    app/services/material.py (Python 参考：save_video, download_videos)
    src/ffmpeg/probe.rs (probe_audio 函数用于验证下载文件)
  </read_first>
  <action>
    用完整实覆盖写 `src/material/downloader.rs` 存根。

    注意：downloader.rs 是独立文件（不在 searcher.rs 中），但它使用 searcher.rs 中的 MaterialError 和 MaterialInfo / 搜索函数。

    **1. 必要导入：**
    ```rust
    use std::io::Write;
    use std::path::{Path, PathBuf};
    use std::sync::atomic::AtomicUsize;
    
    use md5::{Md5, Digest};
    use reqwest::Client;
    use tokio::task::spawn_blocking;
    
    use crate::ffmpeg::probe::probe_audio;
    use crate::material::searcher::{
        self, MaterialError, MaterialInfo,
        search_videos_pexels, search_videos_pixabay, build_client,
    };
    use crate::config::types::ProxySection;
    ```

    **2. compute_cache_key(url: &str) -> String**（per D-15，MD5 URL 去重）：

    ```rust
    /// 计算 URL 的 MD5 缓存 key（去掉 query string，与 Python 版行为一致）
    pub fn compute_cache_key(url: &str) -> String {
        let url_without_query = url.split('?').next().unwrap_or(url);
        let mut hasher = Md5::new();
        hasher.update(url_without_query.as_bytes());
        let result = hasher.finalize();
        format!("vid-{:x}", result)
    }
    ```

    **3. save_video(video_url: &str, save_dir: &Path, proxy_section: &ProxySection) -> Result<PathBuf, MaterialError>**（per D-15）：

    ```rust
    pub fn save_video(
        video_url: &str,
        save_dir: &Path,
        proxy_section: &ProxySection,
    ) -> Result<PathBuf, MaterialError> {
        // 1. 创建保存目录
        std::fs::create_dir_all(save_dir)
            .map_err(|e| MaterialError::IoError(e.to_string()))?;
        
        // 2. 计算缓存 key
        let cache_key = compute_cache_key(video_url);
        let video_path = save_dir.join(format!("{}.mp4", cache_key));
        
        // 3. 缓存命中检查
        if video_path.exists() {
            let file_size = std::fs::metadata(&video_path)
                .map(|m| m.len())
                .unwrap_or(0);
            if file_size > 0 {
                tracing::info!("素材缓存命中: {}", video_path.display());
                return Ok(video_path);
            }
        }
        
        // 4. HTTP 下载
        tracing::info!("下载素材: {}", video_url);
        let client = build_client(proxy_section)?;
        let response = client.get(video_url)
            .timeout(std::time::Duration::from_secs(300))
            .send()
            .map_err(|e| MaterialError::DownloadFailed(e.to_string()))?;
        
        if !response.status().is_success() {
            return Err(MaterialError::DownloadFailed(
                format!("HTTP {}", response.status())
            ));
        }
        
        // 5. 先写入临时文件，防止下载中断产生破损文件
        let temp_path = save_dir.join(format!("{}.tmp", cache_key));
        let bytes = response.bytes()
            .map_err(|e| MaterialError::DownloadFailed(e.to_string()))?;
        
        let mut file = std::fs::File::create(&temp_path)
            .map_err(|e| MaterialError::IoError(e.to_string()))?;
        file.write_all(&bytes)
            .map_err(|e| MaterialError::IoError(e.to_string()))?;
        drop(file);
        
        // 6. 原子重命名
        std::fs::rename(&temp_path, &video_path)
            .map_err(|e| MaterialError::IoError(e.to_string()))?;
        
        // 7. ffprobe 验证（复用 src/ffmpeg/probe.rs 的 probe_audio）
        match probe_audio(&video_path) {
            Ok(duration) => {
                if duration <= 0.0 {
                    let _ = std::fs::remove_file(&video_path);
                    return Err(MaterialError::ValidationFailed("下载的视频时长为 0".into()));
                }
            }
            Err(e) => {
                let _ = std::fs::remove_file(&video_path);
                return Err(MaterialError::ValidationFailed(
                    format!("ffprobe 验证失败: {}", e)
                ));
            }
        }
        
        tracing::info!("素材下载完成: {}", video_path.display());
        Ok(video_path)
    }
    ```

    **4. download_videos(task_id: &str, search_terms: &[String], source: &str, target_width: u32, target_height: u32, audio_duration: f64, max_clip_duration: u32, api_keys_pexels: &[String], api_keys_pixabay: &[String], key_counter_pexels: &AtomicUsize, key_counter_pixabay: &AtomicUsize, proxy_section: &ProxySection) -> Result<Vec<PathBuf>, MaterialError>**（per D-14，编排函数）：

    ```rust
    /// 编排搜索 + 下载 + 时长汇总（Python 版 1:1）
    ///
    /// 1. 对每个 search_term 调用搜索函数获取所有匹配素材
    /// 2. 去重（按 URL）
    /// 3. 逐个下载
    /// 4. 累积总时长，超过 audio_duration 后停止
    pub fn download_videos(
        task_id: &str,
        search_terms: &[String],
        source: &str,
        target_width: u32,
        target_height: u32,
        audio_duration: f64,
        max_clip_duration: u32,
        api_keys_pexels: &[String],
        api_keys_pixabay: &[String],
        key_counter_pexels: &AtomicUsize,
        key_counter_pixabay: &AtomicUsize,
        proxy_section: &ProxySection,
    ) -> Result<Vec<PathBuf>, MaterialError> {
        let save_dir = PathBuf::from("storage").join("temp").join(task_id);
        
        // 1. 收集所有搜索结果
        let mut all_items: Vec<MaterialInfo> = Vec::new();
        let mut seen_urls: std::collections::HashSet<String> = std::collections::HashSet::new();
        
        for term in search_terms {
            let items = match source {
                "pexels" => search_videos_pexels(
                    term, max_clip_duration, target_width, target_height,
                    api_keys_pexels, key_counter_pexels, proxy_section,
                )?,
                "pixabay" => search_videos_pixabay(
                    term, max_clip_duration, target_width, target_height,
                    api_keys_pixabay, key_counter_pixabay, proxy_section,
                )?,
                _ => return Err(MaterialError::ApiRequest(format!("未知素材源: {}", source))),
            };
            
            for item in items {
                if seen_urls.insert(item.video_url.clone()) {
                    all_items.push(item);
                }
            }
        }
        
        // 2. 下载，直到累积时长达到 audio_duration
        let mut downloaded_paths: Vec<PathBuf> = Vec::new();
        let mut total_duration: f64 = 0.0;
        
        for item in &all_items {
            if total_duration >= audio_duration {
                break;
            }
            
            match save_video(&item.video_url, &save_dir, proxy_section) {
                Ok(path) => {
                    total_duration += item.duration as f64;
                    downloaded_paths.push(path);
                }
                Err(e) => {
                    tracing::warn!("素材下载失败 (跳过): {}", e);
                    continue;
                }
            }
        }
        
        Ok(downloaded_paths)
    }
    ```

    **5. 单元测试：**

    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;
        use std::sync::atomic::AtomicUsize;
        
        #[test]
        fn test_compute_cache_key() {
            // MD5("https://example.com/video.mp4") = 已知值
            let key = compute_cache_key("https://example.com/video.mp4");
            assert!(key.starts_with("vid-"), "缓存 key 应以 vid- 开头: {}", key);
            assert_eq!(key.len(), 36, "vid- + 32 hex chars = 36: {}", key);
        }
        
        #[test]
        fn test_compute_cache_key_removes_query_string() {
            // 两个 URL 只有 query 不同，缓存 key 应相同
            let key1 = compute_cache_key("https://example.com/video.mp4?token=abc");
            let key2 = compute_cache_key("https://example.com/video.mp4?token=xyz");
            assert_eq!(key1, key2, "query string 不应影响缓存 key");
        }
        
        #[test]
        fn test_compute_cache_key_different_urls() {
            let key1 = compute_cache_key("https://example.com/video1.mp4");
            let key2 = compute_cache_key("https://example.com/video2.mp4");
            assert_ne!(key1, key2, "不同 URL 应产生不同缓存 key");
        }
        
        #[test]
        fn test_save_video_invalid_url() {
            // 无效 URL 下载应返回错误
            let save_dir = std::env::temp_dir().join("narratoai_test_material");
            let proxy = ProxySection::default();
            
            let result = save_video("https://invalid.example.com/nonexistent.mp4", &save_dir, &proxy);
            // 预期出错（网络不通或 404）
            assert!(result.is_err());
        }
        
        #[test]
        fn test_download_videos_no_terms() {
            let result = download_videos(
                "test",
                &[],
                "pexels",
                1920, 1080,
                30.0, 10,
                &[], &[],
                &AtomicUsize::new(0), &AtomicUsize::new(0),
                &ProxySection::default(),
            );
            assert!(result.is_ok());
            assert!(result.unwrap().is_empty(), "无搜索词应返回空列表");
        }
    }
    ```

    **约束：**
    - 素材下载首先检查缓存，MD5 key 去掉 URL 的 query string（Python 版 1:1）
    - 视频写入先写到 `.tmp` 临时文件，再原子重命名为最终路径（per 已有 Pattern: script/mod.rs save_script）
    - 下载后使用 `probe_audio()` 验证（ffprobe 检查 duration > 0），无效则删除文件
    - `download_videos` 的 `total_duration > audio_duration` break 条件防止无限下载（per Research Anti-Pattern 5）
  </action>
  <verify>
    <automated>cargo test --lib material::downloader::tests -- --nocapture 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 所有单元测试通过（`cargo test --lib material::downloader::tests`）
    - `grep -c "pub fn compute_cache_key" src/material/downloader.rs` 返回 1
    - `grep -c "pub fn save_video" src/material/downloader.rs` 返回 1
    - `grep -c "pub fn download_videos" src/material/downloader.rs` 返回 1
    - `grep -c "Md5" src/material/downloader.rs` 返回 1
    - `grep -c "probe_audio" src/material/downloader.rs` 返回 1（ffprobe 验证）
    - `grep -c "atomic rename\|rename.*\.tmp\|rename(&temp" src/material/downloader.rs` 至少 1 次（temp→final 重命名）
    - `grep -c "total_duration >= audio_duration" src/material/downloader.rs` 返回 1（防止无限下载）
  </acceptance_criteria>
  <done>Material downloader 完整实现：MD5 URL 缓存（compute_cache_key）、save_video（HTTP 下载 + temp rename + ffprobe 验证）、download_videos（编排 + 时长限制），单元测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → Pexels/Pixabay API | untrusted API responses parsed into Rust structs |
| HTTP client → video CDN URL | untrusted binary downloaded to filesystem |
| ffprobe subprocess → validation | file path passed to CLI subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | `save_video()` download origin | mitigate | Only download URLs returned by Pexels/Pixabay API. No arbitrary URL injection. API response validated via serde Deserialize (rejects malformed responses). References: ASVS V8.1.1 (file upload/download) |
| T-11-02 | Information Disclosure | proxy credentials in config | accept | Proxy URL from [proxy] section may contain credentials. Documented that env-var-based proxy is preferred. reqwest::Proxy accepts URL as-is. |
| T-11-03 | Denial of Service | large file download | mitigate | `save_video()` timeout at 300s. Downloaded bytes held in memory (response.bytes()). For typical stock video clips (<50MB) acceptable. Large downloads fail via timeout before OOM. |
| T-11-04 | Tampering | ffprobe file path validation | accept | File path is `save_dir + cache_key.mp4`, constructed internally (not from user input). probe_audio from src/ffmpeg/probe.rs uses args array, no shell. |
</threat_model>

<verification>
1. `cargo test --lib material::searcher::tests` 全部通过
2. `cargo test --lib material::downloader::tests` 全部通过
3. `cargo test --lib material::` 全部通过
4. `cargo test --lib` 全部通过（所有 4 个模块无冲突）
5. `cargo check` 零 error
</verification>

<success_criteria>
- MaterialError 6 个变体，所有 Display 为中文
- Pexels + Pixabay API 响应类型完整定义（serde Deserialize）
- `search_videos_pexels()` 和 `search_videos_pixabay()` 实现搜索功能
- `get_api_key()` 使用 `AtomicUsize` 轮询（防 Pitfall 3 竞态）
- `compute_cache_key()` 使用 md-5 crate，去掉 URL query string（Python 版 1:1）
- `save_video()` 使用 temp 文件 → 原子重命名 → ffprobe 验证三步流程
- `download_videos()` 编排搜索→下载→时长汇总，设 break 条件防无限下载
- 所有外部 URL 调用的超时时间合理（API: 60s, 下载: 300s）
</success_criteria>

<output>
After completion, create `.planning/phases/11-extended-features/11-04-SUMMARY.md`
</output>
