---
phase: 11-extended-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - config.example.toml
  - src/config/types.rs
  - src/config/defaults.rs
  - src/lib.rs
  - src/audio/mod.rs
  - src/audio/normalizer.rs
  - src/audio/volume.rs
  - src/youtube/mod.rs
  - src/youtube/downloader.rs
  - src/material/mod.rs
  - src/material/searcher.rs
  - src/material/downloader.rs
autonomous: true
requirements: [EXTD-01, EXTD-02, EXTD-03, EXTD-04]
user_setup: []

must_haves:
  truths:
    - "Cargo.toml 包含 symphonia + md-5 依赖"
    - "config 可以解析 [audio] 配置段和目标字段"
    - "config 可以解析 pexels_api_keys 和 pixabay_api_keys"
    - "lib.rs 导出 pub mod audio / youtube / material"
    - "所有 3 个模块子目录有 mod.rs 和子模块存根，cargo check 通过"
  artifacts:
    - path: "Cargo.toml"
      provides: "symphonia + md-5 依赖声明"
      contains: "symphonia"
    - path: "src/config/types.rs"
      provides: "AudioSection struct + pexels_api_keys/pixabay_api_keys in AppSection"
      contains: "AudioSection"
    - path: "src/config/defaults.rs"
      provides: "AudioSection 默认值"
      contains: "impl Default for AudioSection"
    - path: "src/audio/mod.rs"
      provides: "pub mod normalizer; pub mod volume;"
    - path: "src/youtube/mod.rs"
      provides: "pub mod downloader;"
    - path: "src/material/mod.rs"
      provides: "pub mod searcher; pub mod downloader;"
    - path: "config.example.toml"
      provides: "[audio] 配置段示例"
      contains: "[audio]"
  key_links:
    - from: "src/config/types.rs"
      to: "Cargo.toml"
      via: "serde, toml 包"
      pattern: "derive.*Deserialize"
    - from: "src/lib.rs"
      to: "src/audio/mod.rs"
      via: "pub mod audio"
---

<objective>
为 Phase 11 四个扩展功能模块建立基础设施：添加 symphonia + md-5 依赖，扩展配置类型（AudioSection、Pexels/Pixabay API key 数组），创建所有模块目录/存根/ mod.rs，更新 lib.rs 导出声明，保证 cargo check 通过。

Purpose: 后续 Plan 2/3/4 可以专注于核心实现逻辑，无需处理基础设施搭建。一次搭建，多处使用。
Output: 可编译的 Rust 模块骨架 + 可加载的配置结构。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extended-features/11-CONTEXT.md
@.planning/phases/11-extended-features/11-RESEARCH.md
@.planning/phases/11-extended-features/11-PATTERNS.md

<interfaces>
<!-- 以下是从代码库提取的现有类型和模式，执行器直接使用，无需探索代码库 -->

From src/config/types.rs:
```rust
// 现有 section 模式：serde + deny_unknown_fields + #[serde(default)]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(deny_unknown_fields)]
pub struct FramesSection {
    #[serde(default)]
    pub frame_interval_input: u64,
    #[serde(default)]
    pub vision_batch_size: u32,
    #[serde(default)]
    pub vision_max_concurrency: u32,
}

// AppConfig 结构
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
#[serde(deny_unknown_fields)]
pub struct AppConfig {
    pub app: AppSection,
    pub ui: UiSection,
    pub azure: AzureSection,
    // ... 其他 section 字段
    pub proxy: ProxySection,
    pub frames: FramesSection,
}

// AppSection 现有字段（需要添加 pexels_api_keys, pixabay_api_keys）
pub struct AppSection {
    pub project_version: String,
    pub vision_llm_provider: String,
    // ... 其他字段
    pub hide_config: bool,
}
```

From src/config/defaults.rs:
```rust
// 现有 Default impl 模式
impl Default for FramesSection {
    fn default() -> Self {
        Self {
            frame_interval_input: 3,
            vision_batch_size: 10,
            vision_max_concurrency: 2,
        }
    }
}
```

From src/lib.rs:
```rust
pub mod config;
pub mod error;
pub mod tts;
pub mod llm;
pub mod ffmpeg;
pub mod jianying;
pub mod script;
pub mod prompt;
pub mod visual;
// Add: pub mod audio; pub mod youtube; pub mod material;
```

From src/ffmpeg/probe.rs:
```rust
// subprocess 模式：std::process::Command + args array（never shell string）
let output = match Command::new(&ffprobe_bin)
    .args(["-v", "quiet", "-print_format", "json"])
    .arg(path.as_os_str())
    .output()
```

From src/error.rs:
```rust
// thiserror + 中文 Display 模式
#[derive(Error, Debug)]
pub enum FFmpegError {
    #[error("FFmpeg 进程启动失败: {0}")]
    SpawnFailed(String),
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加依赖 + 扩展配置类型 + 更新 example 配置</name>
  <files>
    Cargo.toml
    src/config/types.rs
    src/config/defaults.rs
    config.example.toml
  </files>
  <read_first>
    Cargo.toml (现有依赖块)
    src/config/types.rs (AppConfig struct 和所有 section struct)
    src/config/defaults.rs (现有 Default impl 模式)
    config.example.toml (现有 section 示例格式)
    .planning/phases/11-extended-features/11-CONTEXT.md (D-19: [audio] 11 fields, D-24: AppSection ext)
    .planning/phases/11-extended-features/11-RESEARCH.md (AudioSection 字段定义)
  </read_first>
  <action>
    **1. Cargo.toml — 在 [dependencies] 末尾添加：**
    ```toml
    symphonia = { version = "0.5", features = ["mp3", "flac", "pcm", "wav", "vorbis"] }
    md-5 = "0.10"
    ```

    **2. src/config/types.rs — 添加 AudioSection struct + 扩展 AppSection：**

    在 FramesSection 后面（第 203 行后）、#[cfg(test)] 之前，添加新 struct：

    ```rust
    /// [audio] 配置段
    #[derive(Debug, Clone, Serialize, Deserialize)]
    #[serde(deny_unknown_fields)]
    pub struct AudioSection {
        #[serde(default)]
        pub target_lufs: f64,
        #[serde(default)]
        pub max_peak: f64,
        #[serde(default)]
        pub enable_audio_normalization: bool,
        #[serde(default)]
        pub enable_smart_volume: bool,
        #[serde(default)]
        pub tts_volume: f64,
        #[serde(default)]
        pub original_volume: f64,
        #[serde(default)]
        pub bgm_volume: f64,
        #[serde(default)]
        pub sample_rate: u32,
        #[serde(default)]
        pub channels: u32,
        #[serde(default)]
        pub bitrate: u32,
        #[serde(default)]
        pub crossfade_duration: f64,
        #[serde(default)]
        pub bgm_fade_out: f64,
    }
    ```

    在 AppConfig struct（第 6-27 行）的 `frames: FramesSection,` 后面添加：
    ```rust
        #[serde(default)]
        pub audio: AudioSection,
    ```

    在 AppSection struct（第 32-59 行）的 `hide_config: bool,` 后面添加：
    ```rust
        #[serde(default)]
        pub pexels_api_keys: Vec<String>,
        #[serde(default)]
        pub pixabay_api_keys: Vec<String>,
    ```

    同时更新 test_full_config 测试用例：在 TOML 字符串的 `[frames]` section 后添加 `[audio]` section：
    ```toml
    [audio]
    target_lufs = -23.0
    max_peak = -1.0
    enable_audio_normalization = true
    enable_smart_volume = true
    tts_volume = 0.8
    original_volume = 1.3
    bgm_volume = 0.3
    sample_rate = 44100
    channels = 2
    bitrate = 192000
    crossfade_duration = 0.1
    bgm_fade_out = 3.0
    ```

    并在测试断言中添加：
    ```rust
    assert_eq!(config.audio.target_lufs, -23.0);
    assert_eq!(config.audio.enable_audio_normalization, true);
    assert_eq!(config.audio.tts_volume, 0.8);
    ```

    在 AppSection 断言中也添加 pexels/pixabay 的断言：
    ```rust
    assert!(config.app.pexels_api_keys.is_empty());
    assert!(config.app.pixabay_api_keys.is_empty());
    ```

    **3. src/config/defaults.rs — 添加 AudioSection 默认值：**

    在 FramesSection Default 之后（第 111 行后）添加：
    ```rust
    impl Default for AudioSection {
        fn default() -> Self {
            Self {
                target_lufs: -23.0,
                max_peak: -1.0,
                enable_audio_normalization: false,
                enable_smart_volume: false,
                tts_volume: 0.8,
                original_volume: 1.3,
                bgm_volume: 0.3,
                sample_rate: 44100,
                channels: 2,
                bitrate: 192_000,
                crossfade_duration: 0.1,
                bgm_fade_out: 3.0,
            }
        }
    }
    ```

    **4. config.example.toml — 在 [frames] 段后添加 [audio] 段：**
    ```toml
    ##########################################
    # 音频处理配置
    ##########################################
    
    [audio]
        # LUFS 响度标准化目标值（-23.0 符合 EBU R128 广播标准，-14.0 符合 YouTube 标准）
        target_lufs = -23.0
    
        # 最大峰值（dBFS）
        max_peak = -1.0
    
        # 是否启用音频标准化
        enable_audio_normalization = false
    
        # 是否启用智能音量调整
        enable_smart_volume = false
    
        # TTS 解说音量（0.0-2.0）
        tts_volume = 0.8
    
        # 原声音量（0.0-2.0）
        original_volume = 1.3
    
        # BGM 背景音乐音量（0.0-2.0）
        bgm_volume = 0.3
    
        # 采样率
        sample_rate = 44100
    
        # 声道数
        channels = 2
    
        # 比特率
        bitrate = 192000
    
        # 交叉淡化时长（秒，Phase 6 使用）
        crossfade_duration = 0.1
    
        # BGM 淡出时长（秒，Phase 6 使用）
        bgm_fade_out = 3.0
    ```

    **重要：** 所有新字段上的 `#[serde(default)]` 保证旧 config.toml 用户不会因缺少 `[audio]` 段而报错。
  </action>
  <verify>
    <automated>cargo check 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 通过，无错误
    - `cargo test --lib config::types::tests` 全部通过
    - `grep -c "symphonia" Cargo.toml` 返回 1
    - `grep -c "pub struct AudioSection" src/config/types.rs` 返回 1
    - `grep -c "pexels_api_keys" src/config/types.rs` 返回 1
    - `grep -c "impl Default for AudioSection" src/config/defaults.rs` 返回 1
    - `grep -c "\[audio\]" config.example.toml` 返回 1
  </acceptance_criteria>
  <done>Cargo.toml, config/types.rs, config/defaults.rs, config.example.toml 都已更新，cargo check 通过，配置测试全部通过</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建模块存根 + lib.rs 导出 + 验证编译</name>
  <files>
    src/lib.rs
    src/audio/mod.rs
    src/audio/normalizer.rs
    src/audio/volume.rs
    src/youtube/mod.rs
    src/youtube/downloader.rs
    src/material/mod.rs
    src/material/searcher.rs
    src/material/downloader.rs
  </files>
  <read_first>
    src/lib.rs (现有 pub mod 列表)
    .planning/phases/11-extended-features/11-PATTERNS.md (模块结构模式：visual/mod.rs)
    .planning/phases/11-extended-features/11-CONTEXT.md (D-23: 四个顶层模块)
  </read_first>
  <action>
    **1. 在 src/lib.rs 第 9 行 visual 声明后面添加三行：**
    ```rust
    pub mod audio;
    pub mod youtube;
    pub mod material;
    ```

    **2. 创建 src/audio/mod.rs：**
    ```rust
    pub mod normalizer;
    pub mod volume;
    ```

    **3. 创建 src/audio/normalizer.rs（最小存根）：**
    ```rust
    //! 音频响度标准化模块
    //!
    //! 实际实现见 11-02-PLAN.md。
    //! 本存根仅用于 cargo check 通过。
    
    /// 占位函数——后续 Plan 2 实现
    pub fn normalize_placeholder() -> bool { true }
    ```
    存根必须包含至少一个导出项，否则 Rust 会警告"unused module"。

    **4. 创建 src/audio/volume.rs（最小存根）：**
    ```rust
    //! 智能音量控制模块
    //!
    //! 实际实现见 11-02-PLAN.md。
    
    /// 占位函数——后续 Plan 2 实现
    pub fn volume_placeholder() -> bool { true }
    ```

    **5. 创建 src/youtube/mod.rs：**
    ```rust
    pub mod downloader;
    ```

    **6. 创建 src/youtube/downloader.rs（最小存根）：**
    ```rust
    //! YouTube 视频下载模块
    //!
    //! 实际实现见 11-03-PLAN.md。
    
    /// 占位函数——后续 Plan 3 实现
    pub fn youtube_placeholder() -> bool { true }
    ```

    **7. 创建 src/material/mod.rs：**
    ```rust
    pub mod searcher;
    pub mod downloader;
    ```

    **8. 创建 src/material/searcher.rs（最小存根）：**
    ```rust
    //! 素材搜索模块（Pexels + Pixabay）
    //!
    //! 实际实现见 11-04-PLAN.md。
    
    /// 占位函数——后续 Plan 4 实现
    pub fn search_placeholder() -> bool { true }
    ```

    **9. 创建 src/material/downloader.rs（最小存根）：**
    ```rust
    //! 素材下载与缓存模块
    //!
    //! 实际实现见 11-04-PLAN.md。
    
    /// 占位函数——后续 Plan 4 实现
    pub fn download_placeholder() -> bool { true }
    ```

    创建所有文件后，运行 `cargo check` 确保编译通过。
  </action>
  <verify>
    <automated>cargo check 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check` 无错误、无警告
    - `grep -c "pub mod audio" src/lib.rs` 返回 1
    - `grep -c "pub mod normalizer" src/audio/mod.rs` 返回 1
    - `grep -c "pub mod volume" src/audio/mod.rs` 返回 1
    - `grep -c "pub mod downloader" src/youtube/mod.rs` 返回 1
    - `grep -c "pub mod searcher" src/material/mod.rs` 返回 1
    - `grep -c "pub mod downloader" src/material/mod.rs` 返回 1
  </acceptance_criteria>
  <done>8 个模块存根文件创建完毕，lib.rs 更新，cargo check 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.toml → parser | 用户提供的 TOML 配置文件被解析为强类型结构 |
| file system → module | 模块文件系统路径操作（仅占位实现，无实际 I/O） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | config.toml parse | mitigate | serde 强类型解析 + `deny_unknown_fields` 拒绝未知字段；`#[serde(default)]` 保证缺失字段不会导致 panic |
| T-11-02 | Information Disclosure | Cargo.toml new deps | accept | symphonia 和 md-5 是成熟的开源库，版本固定为 0.5 和 0.10，无已知 CVE 影响音频解码和哈希 |
</threat_model>

<verification>
1. `cargo check` 无错误
2. `cargo test --lib config::types::tests` 全部通过 —— 验证 TOML 解析正确（含 [audio] section + pexels_api_keys/pixabay_api_keys）
3. 所有 8 个模块存根文件存在
</verification>

<success_criteria>
- `cargo check` 零 error 零 warning
- `cargo test --lib` 全部通过（含配置解析测试的 [audio] 段断言和新 AppSection 字段断言）
- 3 个新模块的 mod.rs 都正确声明了子模块
- lib.rs 导出 audio / youtube / material 三个顶层模块
- config.example.toml 包含完整的 [audio] 段注释和默认值
</success_criteria>

<output>
After completion, create `.planning/phases/11-extended-features/11-01-SUMMARY.md`
</output>
