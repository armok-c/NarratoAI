---
phase: 12
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - Cargo.toml
  - src/tts/qwen_tts.rs
  - src/tts/indextts2.rs
  - src/tts/mod.rs
autonomous: true
requirements: [TTS-07, TTS-08]
user_setup: []
must_haves:
  truths:
    - "Qwen TTS 引擎通过 DashScope REST API 生成语音音频（含二次 URL 下载）"
    - "IndexTTS2 引擎通过 multipart/form-data 上传参考音频生成语音"
    - "synthesize() 路由器新增 qwen_tts 和 indextts2 两个 match 分支"
    - "语音名前缀 qwen3: 和 indextts2: 被正确剥离"
    - "IndexTTS2 使用 reqwest::multipart 表单上传参考音频"
    - "IndexTTS2 超时 120 秒（推理时间长）"
    - "Cargo.toml 中 reqwest 添加 multipart feature"
  artifacts:
    - path: "src/tts/qwen_tts.rs"
      provides: "Qwen TTS 引擎实现"
      min_lines: 120
    - path: "src/tts/indextts2.rs"
      provides: "IndexTTS2 引擎实现（multipart）"
      min_lines: 140
    - path: "src/tts/mod.rs"
      provides: "synthesize() 路由器的 qwen_tts + indextts2 分支"
      contains: "qwen_tts|indextts2"
  key_links:
    - from: "src/tts/qwen_tts.rs"
      to: "src/tts/common.rs"
      via: "build_client / retry_loop / parse_engine_prefix / write_audio_bytes"
      pattern: "common::"
    - from: "src/tts/indextts2.rs"
      to: "src/tts/common.rs"
      via: "build_client / retry_loop / parse_engine_prefix / write_audio_bytes"
      pattern: "common::"
    - from: "src/tts/mod.rs"
      to: "src/tts/common.rs"
      via: "ProxyConfig::from_proxy"
      pattern: "common::ProxyConfig"
---

<objective>
实现 Qwen TTS 和 IndexTTS2 两个中等复杂度的引擎。

Purpose: Qwen TTS 验证「二次 URL 下载」模式（先 POST 获取 audio URL，再 GET 下载音频文件）。IndexTTS2 验证 multipart/form-data 文件上传模式（参考音频作为表单字段）。两者复用 Wave 1 的 common.rs 基础设施。

Output:
- `src/tts/qwen_tts.rs` — Qwen TTS 引擎（DashScope REST API + 二次下载）
- `src/tts/indextts2.rs` — IndexTTS2 引擎（multipart 参考音频上传）
- `src/tts/mod.rs` — 新增 qwen_tts/indextts2 mod 声明 + match 分支
- `Cargo.toml` — reqwest 添加 multipart feature
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
From src/tts/mod.rs (已更新 by Wave 1):
```rust
pub async fn synthesize(
    engine: &str, text: &str, voice_name: &str, rate: f64, pitch: f64,
    output_path: &Path, proxy: Option<&ProxySection>,
    app_config: Option<&AppConfig>,
) -> Result<TtsOutput, TTSError>
```

From src/tts/common.rs (Wave 1 已交付):
```rust
pub struct ProxyConfig { pub http: String, pub https: String, pub enabled: bool }
impl ProxyConfig { pub fn from_proxy(Option<&ProxySection>) -> Self; pub fn apply_to_client(reqwest::ClientBuilder) -> reqwest::ClientBuilder }
pub fn build_client(&ProxyConfig) -> reqwest::Client;
pub async fn retry_loop<F, Fut>(F) -> Result<TtsOutput, TTSError>;
pub async fn write_audio_bytes(&Path, &[u8]) -> Result<(), TTSError>;
pub fn parse_engine_prefix<'a>(&'a str, &[&str]) -> &'a str;
```

From src/config/types.rs:
```rust
pub struct TtsQwenSection { pub api_key: String, pub model_name: String }
pub struct IndexTTS2Section { pub api_url: String, pub reference_audio: String, pub infer_mode: String,
    pub temperature: f64, pub top_p: f64, pub top_k: u32, pub do_sample: bool,
    pub num_beams: u32, pub repetition_penalty: f64 }
```
</interfaces>

Python 参考:
- Qwen: voice.py 1840-1928 行 (qwen3_tts), 1831-1837 行 (parse_qwen3_voice)
- IndexTTS2: voice.py 2162-2258 行 (indextts2_tts), 2151-2158 行 (parse_indextts2_voice)
</context>

<tasks>

<task type="auto">
<name>Task 1: 实现 Qwen TTS 引擎 + 更新 Cargo.toml</name>
<files>
- Cargo.toml
- src/tts/qwen_tts.rs (NEW)
- src/tts/mod.rs
</files>

<read_first>
- Cargo.toml (需为 reqwest 添加 multipart feature — 本任务仅添加 feature，实际 multipart 在 Task 2 使用)
- src/tts/mod.rs (Wave 1 更新后的 synthesize())
- src/tts/common.rs (build_client / retry_loop / parse_engine_prefix / write_audio_bytes)
- src/config/types.rs (TtsQwenSection)
- app/services/voice.py (qwen3_tts 1840-1928 行, parse_qwen3_voice 1831-1837 行)
</read_first>

<action>
1. 修改 Cargo.toml：将 reqwest 改為 `reqwest = { version = "0.13", features = ["json", "multipart"] }`（在 Wave 1 的 json 基础上添加 multipart）

2. 创建 `src/tts/qwen_tts.rs`，包含以下关键逻辑：

```rust
use crate::error::TTSError;
use crate::tts::{TtsOutput, TtsProvider};
use crate::config::types::TtsQwenSection;
use crate::tts::common;
use async_trait::async_trait;
use std::path::Path;
use std::time::Duration;

/// Qwen TTS 引擎 (DashScope REST API)
///
/// API 流程:
/// 1. POST https://dashscope.aliyuncs.com/api/v1/services/audio/tts/customization
///    Authorization: Bearer {api_key}
///    请求体: {"model": "qwen3-tts-flash", "input": {"text": "...", "voice": "..."}}
///    响应: {"output": {"audio": {"url": "...", "duration": 12.5}}}
/// 2. GET audio_url → 下载音频字节
///
/// 对齐 Python 版 qwen3_tts() — voice.py:1840-1928
pub(super) struct QwenTtsEngine {
    client: reqwest::Client,
    config: TtsQwenSection,
}

impl QwenTtsEngine {
    pub(super) fn new(config: TtsQwenSection, proxy_config: &common::ProxyConfig) -> Self {
        let client = common::build_client(proxy_config);
        Self { client, config }
    }

    /// 单次合成（无重试）
    /// 步骤: 1) 构建并发送 TTS 请求  2) 从响应解析 audio_url  3) 下载音频  4) 写入文件
    async fn synthesize_once(&self, text: &str, voice_name: &str, output_path: &Path) -> Result<TtsOutput, TTSError> {
        // 解析 voice_name: 去除 "qwen3:" 前缀 (Python 版 parse_qwen3_voice — voice.py:1831)
        let parsed_voice = common::parse_engine_prefix(voice_name, &["qwen3:"]);

        let api_key = &self.config.api_key;
        if api_key.is_empty() {
            return Err(TTSError::AuthenticationFailed("Qwen TTS API key 未配置".to_string()));
        }

        let model_name = if self.config.model_name.is_empty() {
            "qwen3-tts-flash"
        } else {
            &self.config.model_name
        };

        // Step 1: 发送 TTS 合成请求到 DashScope
        // Python 版使用 dashscope SDK 的 MultiModalConversation.call()
        // Rust 版直接调用底层 REST API (D-04: 无第三方 SDK)
        const DASHSCOPE_TTS_URL: &str =
            "https://dashscope.aliyuncs.com/api/v1/services/audio/tts/customization";

        let payload = serde_json::json!({
            "model": model_name,
            "input": {
                "text": text.trim(),
                "voice": parsed_voice,
            },
        });

        let response = self.client
            .post(DASHSCOPE_TTS_URL)
            .header("Authorization", format!("Bearer {}", api_key))
            .header("Content-Type", "application/json")
            .json(&payload)
            .timeout(Duration::from_secs(60))
            .send()
            .await
            .map_err(|e| TTSError::ConnectionFailed(format!("Qwen 请求失败: {}", e)))?;

        let status = response.status();
        if !status.is_success() {
            return Err(TTSError::SynthesisFailed(format!(
                "Qwen API 返回 {} (期望 200)", status.as_u16()
            )));
        }

        // Step 2: 解析响应，提取 audio URL 和 duration
        let result: serde_json::Value = response.json().await
            .map_err(|e| TTSError::SynthesisFailed(format!("Qwen 响应 JSON 解析失败: {}", e)))?;

        let audio_url = result["output"]["audio"]["url"].as_str()
            .ok_or_else(|| TTSError::SynthesisFailed("Qwen 响应中无 output.audio.url".to_string()))?;
        let duration = result["output"]["audio"]["duration"].as_f64().unwrap_or(0.0);

        // Step 3: 下载音频文件
        // Python 版: requests.get(audio_url, timeout=30)
        let audio_response = self.client
            .get(audio_url)
            .timeout(Duration::from_secs(60))
            .send()
            .await
            .map_err(|e| TTSError::SynthesisFailed(format!("Qwen 音频下载失败: {}", e)))?;

        let audio_status = audio_response.status();
        if !audio_status.is_success() {
            return Err(TTSError::SynthesisFailed(format!(
                "Qwen 音频 URL 返回 {} (期望 200)", audio_status.as_u16()
            )));
        }

        let audio_bytes = audio_response.bytes().await
            .map_err(|e| TTSError::SynthesisFailed(format!("Qwen 读取音频响应失败: {}", e)))?;

        // Step 4: 写入文件
        common::write_audio_bytes(output_path, &audio_bytes).await?;

        Ok(TtsOutput {
            audio_file_path: output_path.to_path_buf(),
            word_boundaries: vec![],
            duration,
        })
    }
}

#[async_trait]
impl TtsProvider for QwenTtsEngine {
    async fn synthesize(
        &self,
        text: &str,
        voice_name: &str,
        _rate: f64,
        _pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        if text.is_empty() {
            return Err(TTSError::SynthesisFailed("text 不能为空".to_string()));
        }
        common::retry_loop(|| self.synthesize_once(text, voice_name, output_path)).await
    }
}
```

包含 `#[cfg(test)] mod tests`：
- `test_qwen_engine_new` —— 验证 struct 创建
- `test_qwen_success` —— wiremock 分两步 mock：先 mock POST 返回 `{"output": {"audio": {"url": "http://mock/audio.mp3", "duration": 12.5}}}`，再 mock GET 端点返回音频字节。验证 TtsOutput.duration 为 12.5，音频文件写入正确
- `test_qwen_empty_text` —— 空 text 返回 SynthesisFailed
- `test_qwen_no_audio_url` —— POST 响应缺少 output.audio.url，返回 SynthesisFailed
- `test_qwen_parse_prefix` —— "qwen3:Cherry" → parsed_voice 为 "Cherry"

3. 修改 `src/tts/mod.rs`：
   - 添加 `mod qwen_tts;`
   - 在 synthesize() 的 match 中添加 qwen_tts 分支：
   ```rust
   "qwen_tts" => {
       let cfg = app_config.ok_or_else(|| TTSError::SynthesisFailed("需要 AppConfig".to_string()))?;
       let proxy_config = common::ProxyConfig::from_proxy(proxy);
       let engine = qwen_tts::QwenTtsEngine::new(cfg.tts_qwen.clone(), &proxy_config);
       engine.synthesize(text, voice_name, rate, pitch, output_path).await
   }
   ```
</action>

<verify>
<automated>cargo test --lib tts::qwen_tts::tests -- --nocapture</automated>
</verify>

<acceptance_criteria>
- `Cargo.toml` 中 reqwest 包含 features = ["json", "multipart"]
- `src/tts/qwen_tts.rs` 包含 `pub(super) struct QwenTtsEngine` 和 `#[async_trait] impl TtsProvider`
- `src/tts/qwen_tts.rs` 中 POST 端点使用 `"https://dashscope.aliyuncs.com/api/v1/services/audio/tts/customization"`
- `src/tts/qwen_tts.rs` 中成功路径包含二次 GET 下载 audio_url
- `src/tts/qwen_tts.rs` 中 `parse_engine_prefix` 用于 `"qwen3:"` 前缀
- `src/tts/mod.rs` 包含 `mod qwen_tts;` 和 match 分支 `"qwen_tts" =>`
- `cargo test --lib tts::qwen_tts::tests` 全部通过
</acceptance_criteria>

<done>Qwen TTS 引擎实现完成，单元测试通过（含二次 URL 下载模拟），synthesize() 路由包含 qwen_tts 分支</done>
</task>

<task type="auto">
<name>Task 2: 实现 IndexTTS2 引擎</name>
<files>
- src/tts/indextts2.rs (NEW)
- src/tts/mod.rs
</files>

<read_first>
- src/tts/mod.rs (Wave 1 + Task 1 更新后的 synthesize())
- src/tts/common.rs (共享工具)
- src/config/types.rs (IndexTTS2Section 结构体)
- app/services/voice.py (indextts2_tts 2162-2258 行, parse_indextts2_voice 2151-2158 行)
</read_first>

<action>
1. 创建 `src/tts/indextts2.rs`：

```rust
use crate::error::TTSError;
use crate::tts::{TtsOutput, TtsProvider};
use crate::config::types::IndexTTS2Section;
use crate::tts::common;
use async_trait::async_trait;
use reqwest::multipart;
use std::path::Path;
use std::time::Duration;

/// IndexTTS2 语音克隆引擎。
///
/// 本地自托管服务 (Docker/本地部署)。
/// API: POST {api_url} (默认 http://127.0.0.1:8081/tts)
/// 请求: multipart/form-data 含 prompt_audio(参考音频文件) + 文本参数
/// 响应: 原始音频字节
/// 超时: 120s (推理时间长)
///
/// 对齐 Python 版 indextts2_tts() — voice.py:2162-2258
pub(super) struct IndexTts2Engine {
    client: reqwest::Client,
    config: IndexTTS2Section,
}

impl IndexTts2Engine {
    pub(super) fn new(config: IndexTTS2Section, proxy_config: &common::ProxyConfig) -> Self {
        let client = common::build_client(proxy_config);
        Self { client, config }
    }

    async fn synthesize_once(&self, text: &str, voice_name: &str, output_path: &Path) -> Result<TtsOutput, TTSError> {
        // 解析 voice_name: 去除 "indextts2:" 前缀 = 参考音频路径
        // Python 版 parse_indextts2_voice — voice.py:2151
        let ref_audio_path_str = common::parse_engine_prefix(voice_name, &["indextts2:"]);
        let ref_audio_path = Path::new(ref_audio_path_str);

        // 如果 voice_name 不包含前缀，使用配置中的默认 reference_audio
        let ref_audio_path = if ref_audio_path_str == voice_name && !self.config.reference_audio.is_empty() {
            Path::new(&self.config.reference_audio)
        } else {
            ref_audio_path
        };

        // 读取参考音频到内存（重试安全：每次重新读取，不持有文件句柄）
        // 对齐 Python 版: files={'prompt_audio': open(reference_audio_path, 'rb')}
        // Rust 版使用 Part::bytes() 避免路径编码问题 (Pitfall 4)
        let reference_audio_bytes = tokio::fs::read(ref_audio_path).await
            .map_err(|e| TTSError::SynthesisFailed(format!("读取参考音频失败: {}", e)))?;

        let api_url = if self.config.api_url.is_empty() {
            "http://127.0.0.1:8081/tts"
        } else {
            &self.config.api_url
        };

        // 构建 multipart 表单
        // 对齐 Python 版 data 字典 (voice.py:2197-2206):
        //   {'text': text, 'infer_mode': infer_mode, 'temperature': ..., 'top_p': ...}
        let form = multipart::Form::new()
            .part("prompt_audio", multipart::Part::bytes(reference_audio_bytes)
                .file_name("reference.wav")
                .mime_str("audio/wav")
                .map_err(|e| TTSError::SynthesisFailed(format!("MIME 设置失败: {}", e)))?)
            .text("text", text.trim().to_string())
            .text("infer_mode", self.config.infer_mode.clone())
            .text("temperature", self.config.temperature.to_string())
            .text("top_p", self.config.top_p.to_string())
            .text("top_k", self.config.top_k.to_string())
            .text("do_sample", self.config.do_sample.to_string())
            .text("num_beams", self.config.num_beams.to_string())
            .text("repetition_penalty", self.config.repetition_penalty.to_string());

        // 超时 120s — IndexTTS2 推理时间可能较长 (Python 版 timeout=120)
        let response = self.client
            .post(api_url)
            .multipart(form)
            .timeout(Duration::from_secs(120))
            .send()
            .await
            .map_err(|e| TTSError::ConnectionFailed(format!("IndexTTS2 请求失败: {}", e)))?;

        let status = response.status();
        if !status.is_success() {
            return Err(TTSError::SynthesisFailed(format!(
                "IndexTTS2 API 返回 {}", status.as_u16()
            )));
        }

        let audio_bytes = response.bytes().await
            .map_err(|e| TTSError::SynthesisFailed(format!("IndexTTS2 读取响应失败: {}", e)))?;

        common::write_audio_bytes(output_path, &audio_bytes).await?;

        // IndexTTS2 不支持词边界
        Ok(TtsOutput {
            audio_file_path: output_path.to_path_buf(),
            word_boundaries: vec![],
            duration: 0.0,
        })
    }
}

#[async_trait]
impl TtsProvider for IndexTts2Engine {
    async fn synthesize(
        &self,
        text: &str,
        voice_name: &str,
        _rate: f64,
        _pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        if text.is_empty() {
            return Err(TTSError::SynthesisFailed("text 不能为空".to_string()));
        }
        // 检查 voice_name 或配置中是否有参考音频路径
        let has_prefix = voice_name.starts_with("indextts2:");
        if !has_prefix && self.config.reference_audio.is_empty() {
            return Err(TTSError::SynthesisFailed(
                "IndexTTS2 需要参考音频路径（在 voice_name 中或 indextts2.reference_audio 配置中提供）".to_string()
            ));
        }
        common::retry_loop(|| self.synthesize_once(text, voice_name, output_path)).await
    }
}
```

包含 `#[cfg(test)] mod tests`：
- `test_indextts2_engine_new` —— 验证 struct 创建
- `test_indextts2_success` —— wiremock 模拟 multipart POST 返回音频字节。使用临时目录创建测试参考音频文件。验证响应正确写入文件
- `test_indextts2_empty_text` —— 空 text 返回 SynthesisFailed
- `test_indextts2_missing_ref_audio` —— voice_name 无前缀且配置中 reference_audio 为空，返回 SynthesisFailed
- `test_indextts2_parse_prefix` —— "indextts2:/path/to/ref.wav" → 解析后的 ref path 为 "/path/to/ref.wav"

2. 修改 `src/tts/mod.rs`：
   - 添加 `mod indextts2;`
   - 在 synthesize() 的 match 中添加 indextts2 分支：
   ```rust
   "indextts2" => {
       let cfg = app_config.ok_or_else(|| TTSError::SynthesisFailed("需要 AppConfig".to_string()))?;
       let proxy_config = common::ProxyConfig::from_proxy(proxy);
       let engine = indextts2::IndexTts2Engine::new(cfg.indextts2.clone(), &proxy_config);
       engine.synthesize(text, voice_name, rate, pitch, output_path).await
   }
   ```
</action>

<verify>
<automated>cargo test --lib tts::indextts2::tests -- --nocapture && cargo test --lib tts</automated>
</verify>

<acceptance_criteria>
- `src/tts/indextts2.rs` 包含 `pub(super) struct IndexTts2Engine` 和 `#[async_trait] impl TtsProvider`
- `src/tts/indextts2.rs` 中使用 `reqwest::multipart::Form::new().part("prompt_audio", ...)`
- `src/tts/indextts2.rs` 中使用 `.timeout(Duration::from_secs(120))`
- `src/tts/indextts2.rs` 中 `parse_engine_prefix` 用于 `"indextts2:"` 前缀
- `src/tts/mod.rs` 包含 `mod indextts2;` 和 match 分支 `"indextts2" =>`
- `cargo test --lib tts::indextts2::tests` 全部通过
- `cargo test --lib tts` 全部通过（Wave 1 + Wave 2 所有测试绿）
</acceptance_criteria>

<done>IndexTTS2 引擎实现完成，单元测试通过（含 multipart 模拟），synthesize() 路由包含 indextts2 分支，Wave 2 全部完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NarratoAI -> DashScope API | API key 经 HTTPS 传输到阿里云 |
| NarratoAI -> IndexTTS2 (local) | 本地 HTTP 服务，无认证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Spoofing | Qwen request | mitigate | Authorization Bearer 头 + HTTPS，key 从配置读取 (config.toml gitignored) |
| T-12-06 | Information Disclosure | Qwen 响应日志 | mitigate | API key 不记录到日志（仅记录 key[..4]... 前缀用于调试） |
| T-12-07 | Tampering | IndexTTS2 multipart upload | accept | 本地服务 (localhost/127.0.0.1)，不跨网络传输。路径不存在时返回 SynthesisFailed |
| T-12-08 | Denial of Service | IndexTTS2 超时 | mitigate | 120s 超时防止长时间阻塞，retry_loop 确保重试有限制 |
</threat_model>

<verification>
- `cargo build` 编译成功
- `cargo test --lib tts::qwen_tts::tests -- --nocapture` 全部通过
- `cargo test --lib tts::indextts2::tests -- --nocapture` 全部通过
- `cargo test --lib tts` 全部通过（Wave 1 + Wave 2 所有测试）
</verification>

<success_criteria>
1. Qwen TTS 引擎通过 `"qwen_tts"` 路由可调用，包含二次 URL 下载逻辑
2. IndexTTS2 引擎通过 `"indextts2"` 路由可调用，包含 multipart 文件上传
3. synthesize() 路由包含 qwen_tts 和 indextts2 两个新分支
4. 所有单元测试通过
5. `qwen3:` 前缀被正确解析为语音名称
6. `indextts2:` 前缀被正确解析为参考音频路径
</success_criteria>

<output>
After completion, create `.planning/phases/12-additional-tts-engines/12-02-SUMMARY.md`
</output>
