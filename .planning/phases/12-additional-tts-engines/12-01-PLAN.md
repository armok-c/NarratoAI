---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/tts/common.rs
  - src/tts/soulvoice.rs
  - src/tts/doubaotts.rs
  - src/tts/mod.rs
autonomous: true
requirements: [TTS-06, TTS-09]
user_setup: []
must_haves:
  truths:
    - "common.rs 提供 retry_loop / apply_proxy / write_audio_bytes / parse_engine_prefix 四个核心函数"
    - "SoulVoice 引擎通过 reqwest POST 生成音频文件"
    - "Doubao TTS 引擎通过 reqwest POST + base64 解码生成音频文件"
    - "synthesize() 路由器新增 soulvoice 和 doubaotts 两个 match 分支"
    - "语音名前缀 soulvoice: 被正确剥离"
    - "Doubao Authorization 头使用 Bearer;{token} 格式（分号，不是空格）"
    - "Cargo.toml 新增 reqwest json feature"
  artifacts:
    - path: "src/tts/common.rs"
      provides: "共享工具函数"
      min_lines: 80
    - path: "src/tts/soulvoice.rs"
      provides: "SoulVoice TTS 引擎完整实现"
      min_lines: 80
    - path: "src/tts/doubaotts.rs"
      provides: "Doubao TTS 引擎完整实现（含 base64 解码）"
      min_lines: 100
    - path: "src/tts/mod.rs"
      provides: "synthesize() 路由器的 soulvoice + doubaotts 分支"
      contains: "soulvoice|doubaotts"
  key_links:
    - from: "src/tts/soulvoice.rs"
      to: "src/tts/common.rs"
      via: "retry_loop / build_client / parse_engine_prefix"
      pattern: "common::"
    - from: "src/tts/doubaotts.rs"
      to: "src/tts/common.rs"
      via: "retry_loop / build_client"
      pattern: "common::"
    - from: "src/tts/mod.rs"
      to: "src/tts/common.rs"
      via: "ProxyConfig::from_proxy"
      pattern: "common::ProxyConfig"
---

<objective>
搭建共享工具模块 common.rs，并实现 SoulVoice 和 Doubao 两个最简单的 TTS 引擎。

Purpose: 先建立共享基础设施（重试循环、代理配置、音频写入、前缀解析），然后用两个简单引擎验证模式正确。SoulVoice 和 Doubao 都是标准 REST API 调用，无复杂认证，适合 Wave 1 快速验证。

Output: 
- `src/tts/common.rs` — 共享工具模块
- `src/tts/soulvoice.rs` — SoulVoice 引擎
- `src/tts/doubaotts.rs` — Doubao TTS 引擎
- `src/tts/mod.rs` — 更新后含 common/soulvoice/doubaotts mod 声明 + match 分支
- `Cargo.toml` — reqwest 添加 json feature
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- 关键接口 — 执行器使用，无需探索代码库 -->

From src/tts/mod.rs:
```rust
pub struct TtsOutput {
    pub audio_file_path: PathBuf,
    pub word_boundaries: Vec<WordBoundary>,
    pub duration: f64,
}

#[async_trait]
pub trait TtsProvider: Send + Sync {
    async fn synthesize(&self, text: &str, voice_name: &str, rate: f64, pitch: f64, output_path: &Path)
        -> Result<TtsOutput, TTSError>;
}

pub async fn synthesize(
    engine: &str, text: &str, voice_name: &str, rate: f64, pitch: f64,
    output_path: &Path, proxy: Option<&ProxySection>,
    app_config: Option<&AppConfig>,  // 由本计划新增
) -> Result<TtsOutput, TTSError>
```

From src/error.rs:
```rust
pub enum TTSError {
    UnknownEngine { engine: String },
    ConnectionFailed(String),
    AuthenticationFailed(String),
    SynthesisFailed(String),
    RetryExhausted(String),
}
```

From src/config/types.rs:
```rust
pub struct SoulVoiceSection { pub api_key: String, pub voice_uri: String, pub api_url: String, pub model: String }
pub struct DoubaoTTSSection { pub ak: String, pub sk: String, pub appid: String, pub token: String, pub cluster: String, pub volume: f64, pub pitch: f64, pub silence_duration: f64 }
pub struct ProxySection { pub http: String, pub https: String, pub enabled: bool }
pub struct AppConfig { pub app: AppSection, pub ui: UiSection, pub azure: AzureSection, ... }
```
</interfaces>

主要参考:
- `src/tts/edge_tts.rs` — 引擎实现模板（struct 定义、TtsProvider impl、new()、synthesize_with_retry 模式）
- `src/tts/mod.rs` — 路由器当前状态（仅 edge_tts 分支）
- `src/error.rs` — TTSError 5 变体
- `src/config/types.rs` — 配置结构体（SoulVoiceSection、DoubaoTTSSection、ProxySection）
- `app/services/voice.py` — Python 版权威参考（soulvoice_tts 函数 2039-2127 行、doubaotts_tts 函数 1119-1235 行）
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 common.rs 共享工具模块 + 更新 Cargo.toml 和 synthesize() 签名</name>
<files>
- Cargo.toml
- src/tts/common.rs (NEW)
- src/tts/mod.rs
</files>

<read_first>
- Cargo.toml (当前 reqwest 为裸版本，需添加 json feature)
- src/tts/mod.rs (现有 synthesize() 签名、TtsProvider trait、TtsOutput 类型)
- src/error.rs (TTSError 枚举定义)
- src/config/types.rs (ProxySection、AppConfig 结构体)
</read_first>

<action>
1. 修改 Cargo.toml：
   - 将 `reqwest = "0.13"` 改为 `reqwest = { version = "0.13", features = ["json"] }`
   - 保持其他所有依赖不变

2. 创建 `src/tts/common.rs`，包含以下内容：

   a) `ProxyConfig` 结构体：
   ```rust
   #[derive(Debug, Clone)]
   pub struct ProxyConfig {
       pub http: String,
       pub https: String,
       pub enabled: bool,
   }
   ```

   b) `ProxyConfig` 方法：
   - `pub fn from_proxy(proxy: Option<&crate::config::types::ProxySection>) -> Self` —— 从 Option<&ProxySection> 创建。None 时返回 disabled 的默认值
   - `pub fn apply_to_client(&self, builder: reqwest::ClientBuilder) -> reqwest::ClientBuilder` —— 如果 enabled，根据 http/https 设置 reqwest::Proxy。两者都配置时都添加

   c) `pub fn build_client(proxy_config: &ProxyConfig) -> reqwest::Client`：
   - 调用 `reqwest::Client::builder()` 再 `proxy_config.apply_to_client(builder)` 最后 `.build().unwrap_or_else(|_| reqwest::Client::new())`
   - 不使用 timeout() 等额外配置——超时由各引擎的 `.timeout()` 在具体请求上设置

   d) `pub async fn retry_loop<F, Fut>(f: F) -> Result<TtsOutput, TTSError>`：
   - 签名：`where F: Fn() -> Fut, Fut: std::future::Future<Output = Result<TtsOutput, TTSError>>`
   - 4 次尝试（1 initial + 3 retries），与 edge_tts.rs 的 synthesize_with_retry() 模式一致
   - AuthenticationFailed 直接返回不重试
   - 重试间隔 1 秒
   - 重试耗尽返回 `TTSError::RetryExhausted(...)` 带最后一次错误信息
   - 在 retry_loop 外不清理文件（调用方负责）

   e) `pub async fn write_audio_bytes(path: &Path, data: &[u8]) -> Result<(), TTSError>`：
   - 调用 `tokio::fs::write(path, data).await`
   - 失败时 map 到 `TTSError::SynthesisFailed("写入音频文件失败: ...")`

   f) `pub fn parse_engine_prefix<'a>(voice_name: &'a str, prefixes: &[&str]) -> &'a str`：
   - 遍历 prefixes 检查 voice_name 是否以某个 prefix 开头
   - 匹配则返回去除前缀后的子串
   - 不匹配则返回原始 voice_name

   g) `#[cfg(test)] mod tests` 包含：
   - `test_parse_engine_prefix_matches_soulvoice` —— "soulvoice:speech:model:voice:id" 去掉 "soulvoice:" 前缀后应为 "speech:model:voice:id"
   - `test_parse_engine_prefix_matches_tencent` —— "tencent:101001" 去掉 "tencent:" 前缀后应为 "101001"
   - `test_parse_engine_prefix_no_match` —— "zh-CN-XiaoyiNeural" 不匹配任何前缀时返回自身
   - `test_proxy_config_from_none` —— proxy=None 时 enabled=false
   - `test_proxy_config_from_some` —— proxy=Some(...) 时正确复制 http/https/enabled
   - `test_write_audio_bytes` —— 写入临时文件后验证文件存在且内容正确

3. 修改 `src/tts/mod.rs`：
   - 在 `mod edge_tts;` 之后添加 `mod common;`
   - 在 `synthesize()` 函数签名末尾添加参数：`, app_config: Option<&crate::config::types::AppConfig>`（最后参数）
   - 在 synthesize() 函数体内，edge_tts 分支之前不变
   - 保持所有现有测试不变（它们不需要 app_config，传 None 即可）
   - 在文件顶部添加 `use crate::config::types::AppConfig;` 或使用全路径
</action>

<verify>
<automated>cargo test --lib tts::common::tests -- --nocapture</automated>
</verify>

<acceptance_criteria>
- `Cargo.toml` 中 reqwest 包含 features = ["json"]
- `src/tts/common.rs` 包含 `pub struct ProxyConfig` 定义
- `src/tts/common.rs` 包含 `pub fn parse_engine_prefix` 函数
- `src/tts/common.rs` 包含 `pub async fn retry_loop` 函数
- `src/tts/common.rs` 包含 `pub fn build_client` 函数
- `src/tts/mod.rs` 包含 `mod common;` 声明
- `src/tts/mod.rs` 的 `synthesize()` 签名末尾包含 `app_config: Option<&AppConfig>`
- `cargo test --lib tts::common::tests` 全部通过
- `cargo test --lib tts::mod::tests` 全部通过（现有测试仍然有效）
</acceptance_criteria>

<done>common.rs 的所有测试通过，synthesize() 签名已扩展，reqwest 已启用 json feature</done>
</task>

<task type="auto">
<name>Task 2: 实现 SoulVoice TTS 引擎</name>
<files>
- src/tts/soulvoice.rs (NEW)
- src/tts/mod.rs
</files>

<read_first>
- src/tts/mod.rs (更新后的 synthesize() 签名)
- src/tts/common.rs (共享工具：build_client、retry_loop、parse_engine_prefix、write_audio_bytes)
- src/tts/edge_tts.rs (引擎模板参考)
- src/config/types.rs (SoulVoiceSection 结构体)
- app/services/voice.py (soulvoice_tts 函数 2039-2127 行 + parse_soulvoice_voice 1810-1819 行 — Python 版权威参考)
</read_first>

<action>
1. 创建 `src/tts/soulvoice.rs`：

```rust
use crate::error::TTSError;
use crate::tts::{TtsOutput, TtsProvider};
use crate::config::types::SoulVoiceSection;
use crate::tts::common;
use async_trait::async_trait;
use std::path::Path;
use std::time::Duration;

pub(super) struct SoulVoiceEngine {
    client: reqwest::Client,
    config: SoulVoiceSection,
}

impl SoulVoiceEngine {
    pub(super) fn new(config: SoulVoiceSection, proxy_config: &common::ProxyConfig) -> Self {
        let client = common::build_client(proxy_config);
        Self { client, config }
    }

    async fn synthesize_once(&self, text: &str, voice_name: &str, output_path: &Path) -> Result<TtsOutput, TTSError> {
        // 解析 voice_name: 去除 "soulvoice:" 前缀
        // 对齐 Python 版 parse_soulvoice_voice() 函数
        let parsed_voice = common::parse_engine_prefix(voice_name, &["soulvoice:"]);
        
        let api_key = &self.config.api_key;
        if api_key.is_empty() {
            return Err(TTSError::AuthenticationFailed("SoulVoice API key 未配置".to_string()));
        }
        
        let api_url = if self.config.api_url.is_empty() {
            "https://tts.scsmtech.cn/tts"
        } else {
            &self.config.api_url
        };
        
        let model = if self.config.model.is_empty() {
            "FunAudioLLM/CosyVoice2-0.5B"
        } else {
            &self.config.model
        };

        // 请求体: {"text": "...", "model": "...", "voice": "...", "speed": 1.0}
        // 对齐 Python 版: headers={'Authorization': f'Bearer {api_key}', 'Content-Type': 'application/json'}
        // data = {'text': text.strip(), 'model': default_model, 'voice': parsed_voice, 'speed': speed}
        let request_body = serde_json::json!({
            "text": text.trim(),
            "model": model,
            "voice": parsed_voice,
            "speed": 1.0,  // rate 参数当前不由调用方透传（Python 版 soulvoice 使用 speed 参数）
        });

        let response = self.client
            .post(api_url)
            .header("Authorization", format!("Bearer {}", api_key))
            .header("Content-Type", "application/json")
            .json(&request_body)
            .timeout(Duration::from_secs(60))
            .send()
            .await
            .map_err(|e| TTSError::ConnectionFailed(format!("SoulVoice 请求失败: {}", e)))?;

        let status = response.status();
        if !status.is_success() {
            let body_text = response.text().await.unwrap_or_default();
            return Err(TTSError::SynthesisFailed(format!(
                "SoulVoice API 返回 {}: {}", status.as_u16(), body_text
            )));
        }

        let audio_bytes = response.bytes().await
            .map_err(|e| TTSError::SynthesisFailed(format!("SoulVoice 读取响应失败: {}", e)))?;

        common::write_audio_bytes(output_path, &audio_bytes).await?;

        // SoulVoice 不支持词边界
        Ok(TtsOutput {
            audio_file_path: output_path.to_path_buf(),
            word_boundaries: vec![],
            duration: 0.0,
        })
    }
}

#[async_trait]
impl TtsProvider for SoulVoiceEngine {
    async fn synthesize(
        &self,
        text: &str,
        voice_name: &str,
        _rate: f64,
        _pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        if text.is_empty() {
            return Err(TTSError::SynthesisFailed("text 不能为空".to_string()));
        }
        common::retry_loop(|| self.synthesize_once(text, voice_name, output_path)).await
    }
}
```

包含 `#[cfg(test)] mod tests`：
- `test_soulvoice_engine_new` —— 验证 struct 创建
- `test_soulvoice_success` —— 使用 wiremock 模拟 HTTP 200 + 音频字节，验证返回 TtsOutput
- `test_soulvoice_empty_text` —— 空 text 返回 SynthesisFailed
- `test_soulvoice_http_error` —— wiremock 模拟 401，验证返回对应错误
- `test_soulvoice_parse_prefix` —— "soulvoice:speech:model:voice:id" → parsed_voice 为 "speech:model:voice:id"

2. 修改 `src/tts/mod.rs`：
   - 添加 `mod soulvoice;`
   - 在 synthesize() 的 match 中添加 soulvoice 分支：
   ```rust
   "soulvoice" => {
       let cfg = app_config.ok_or_else(|| TTSError::SynthesisFailed("需要 AppConfig".to_string()))?;
       let proxy_config = common::ProxyConfig::from_proxy(proxy);
       let engine = soulvoice::SoulVoiceEngine::new(cfg.soulvoice.clone(), &proxy_config);
       engine.synthesize(text, voice_name, rate, pitch, output_path).await
   }
   ```
</action>

<verify>
<automated>cargo test --lib tts::soulvoice::tests -- --nocapture</automated>
</verify>

<acceptance_criteria>
- `src/tts/soulvoice.rs` 包含 `pub(super) struct SoulVoiceEngine` 定义和 `#[async_trait] impl TtsProvider for SoulVoiceEngine`
- `src/tts/soulvoice.rs` 的 `synthesize_once` 中 POST 请求使用 `api_url`（来自配置或默认值）
- `src/tts/soulvoice.rs` 中 `parse_engine_prefix` 用于去除 "soulvoice:" 前缀
- `src/tts/mod.rs` 包含 `mod soulvoice;` 和 match 分支 `"soulvoice" =>`
- `cargo test --lib tts::soulvoice::tests` 全部通过
- `cargo test --lib tts::mod::tests` 全部通过（现有测试不受影响）
</acceptance_criteria>

<done>SoulVoice 引擎实现完成，单元测试通过，synthesize() 路由包含 soulvoice 分支</done>
</task>

<task type="auto">
<name>Task 3: 实现 Doubao TTS 引擎</name>
<files>
- src/tts/doubaotts.rs (NEW)
- src/tts/mod.rs
</files>

<read_first>
- src/tts/mod.rs (更新后的 synthesize() 签名)
- src/tts/common.rs (共享工具)
- src/config/types.rs (DoubaoTTSSection 结构体)
- app/services/voice.py (doubaotts_tts 函数 1119-1235 行 — Python 版权威参考)
</read_first>

<action>
1. 创建 `src/tts/doubaotts.rs`：

```rust
use crate::error::TTSError;
use crate::tts::{TtsOutput, TtsProvider};
use crate::config::types::DoubaoTTSSection;
use crate::tts::common;
use async_trait::async_trait;
use base64::Engine;
use std::path::Path;
use std::time::Duration;

/// 豆包语音 TTS 引擎。
///
/// API 端点: POST https://openspeech.bytedance.com/api/v1/tts
/// 认证: Authorization: Bearer;{token}  (⚠ 分号不是空格 — 非标准但必要)
/// 请求体: 嵌套 JSON 含 app/user/audio/request 四段
/// 响应: {"code": 3000, "message": "Success", "data": "<base64 audio>"}
///
/// 对齐 Python 版 doubaotts_tts() — voice.py:1119-1235
pub(super) struct DoubaoTtsEngine {
    client: reqwest::Client,
    config: DoubaoTTSSection,
}

impl DoubaoTtsEngine {
    pub(super) fn new(config: DoubaoTTSSection, proxy_config: &common::ProxyConfig) -> Self {
        let client = common::build_client(proxy_config);
        Self { client, config }
    }

    /// 单次合成（无重试）
    async fn synthesize_once(&self, text: &str, voice_name: &str, output_path: &Path) -> Result<TtsOutput, TTSError> {
        // Doubao 不使用 voice_name 前缀解析，voice_name 直接作为 voice_type
        // 配置检查
        if self.config.appid.is_empty() || self.config.token.is_empty() {
            return Err(TTSError::AuthenticationFailed("豆包语音 TTS 配置未完成（缺少 appid 或 token）".to_string()));
        }

        let speed_ratio = 1.0;  // rate 暂不使用（Python 版实际使用固定值或 ui.doubaotts_rate）
        
        // 构建请求体 (对齐 Python 版 payload — voice.py:1149-1176)
        let mut payload = serde_json::json!({
            "app": {
                "appid": self.config.appid,
                "token": self.config.token,
                "cluster": self.config.cluster,
            },
            "user": {
                "uid": "NarratoAI"
            },
            "audio": {
                "voice_type": voice_name,
                "encoding": "mp3",
                "rate": 24000,
                "speed_ratio": speed_ratio,
                "volume_ratio": self.config.volume,
                "pitch_ratio": self.config.pitch,
            },
            "request": {
                "reqid": uuid::Uuid::new_v4().to_string(),
                "text": text.trim(),
                "text_type": "plain",
                "operation": "query",
            },
        });

        // 如果设置了句尾静音时长，添加 (Python 版: voice.py 1175-1176)
        if self.config.silence_duration > 0.0 {
            payload["audio"]["silence_duration"] = serde_json::json!(self.config.silence_duration);
        }

        const API_URL: &str = "https://openspeech.bytedance.com/api/v1/tts";

        let response = self.client
            .post(API_URL)
            // ⚠ 分号不是空格 — 对齐 Python 版: headers = {"Authorization": f"Bearer;{token}"}
            .header("Authorization", format!("Bearer;{}", self.config.token))
            .header("Content-Type", "application/json")
            .json(&payload)
            .timeout(Duration::from_secs(60))
            .send()
            .await
            .map_err(|e| TTSError::ConnectionFailed(format!("Doubao 请求失败: {}", e)))?;

        let status = response.status();
        if !status.is_success() {
            return Err(TTSError::SynthesisFailed(format!("Doubao API 返回 {}", status.as_u16())));
        }

        let result: serde_json::Value = response.json().await
            .map_err(|e| TTSError::SynthesisFailed(format!("Doubao 响应 JSON 解析失败: {}", e)))?;

        // 检查业务状态码 (Python 版: result.get("code") == 3000)
        let code = result["code"].as_i64().unwrap_or(0);
        if code != 3000 {
            let msg = result["message"].as_str().unwrap_or("未知错误");
            return Err(TTSError::SynthesisFailed(format!("Doubao TTS 错误 ({}): {}", code, msg)));
        }

        // 解码 base64 audio data (Python 版: base64.b64decode(result.get("data", "")))
        let audio_base64 = result["data"].as_str()
            .ok_or_else(|| TTSError::SynthesisFailed("Doubao 响应中无 data 字段".to_string()))?;

        let audio_bytes = base64::engine::general_purpose::STANDARD
            .decode(audio_base64)
            .map_err(|e| TTSError::SynthesisFailed(format!("Base64 解码失败: {}", e)))?;

        common::write_audio_bytes(output_path, &audio_bytes).await?;

        // Doubao 不支持词边界
        Ok(TtsOutput {
            audio_file_path: output_path.to_path_buf(),
            word_boundaries: vec![],
            duration: 0.0,
        })
    }
}

#[async_trait]
impl TtsProvider for DoubaoTtsEngine {
    async fn synthesize(
        &self,
        text: &str,
        voice_name: &str,
        _rate: f64,
        _pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        if text.is_empty() {
            return Err(TTSError::SynthesisFailed("text 不能为空".to_string()));
        }
        common::retry_loop(|| self.synthesize_once(text, voice_name, output_path)).await
    }
}
```

包含 `#[cfg(test)] mod tests`：
- `test_doubaotts_engine_new` —— 验证 struct 创建
- `test_doubaotts_success` —— wiremock 模拟返回 {"code": 3000, "message": "Success", "data": "<base64 编码的音频>"}，验证音频文件正确写入
- `test_doubaotts_empty_text` —— 空 text 返回 SynthesisFailed
- `test_doubaotts_business_error` —— wiremock 模拟返回 {"code": 4000, "message": "error"}，验证返回 SynthesisFailed
- `test_doubaotts_auth_header_format` —— 验证 Authorization 头包含 "Bearer;"（分号），而不是 "Bearer "（空格）

2. 修改 `src/tts/mod.rs`：
   - 添加 `mod doubaotts;`
   - 在 synthesize() 的 match 中添加 doubaotts 分支：
   ```rust
   "doubaotts" => {
       let cfg = app_config.ok_or_else(|| TTSError::SynthesisFailed("需要 AppConfig".to_string()))?;
       let proxy_config = common::ProxyConfig::from_proxy(proxy);
       let engine = doubaotts::DoubaoTtsEngine::new(cfg.doubaotts.clone(), &proxy_config);
       engine.synthesize(text, voice_name, rate, pitch, output_path).await
   }
   ```
</action>

<verify>
<automated>cargo test --lib tts::doubaotts::tests -- --nocapture && cargo test --lib tts::mod::tests -- --nocapture</automated>
</verify>

<acceptance_criteria>
- `src/tts/doubaotts.rs` 包含 `pub(super) struct DoubaoTtsEngine` 定义和 `#[async_trait] impl TtsProvider`
- `src/tts/doubaotts.rs` 中 Authorization 头使用 `"Bearer;{}"` 格式（分号）
- `src/tts/doubaotts.rs` 中成功路径使用 `base64::engine::general_purpose::STANDARD.decode()`
- `src/tts/doubaotts.rs` 中检查 `result["code"] == 3000`
- `src/tts/mod.rs` 包含 `mod doubaotts;` 和 match 分支 `"doubaotts" =>`
- `cargo test --lib tts::doubaotts::tests` 全部通过
- `cargo test --lib tts::mod::tests` 全部通过
- `cargo test --lib tts` 全部通过（tts 模块所有测试绿）
</acceptance_criteria>

<done>Doubao TTS 引擎实现完成，单元测试通过，synthesize() 路由包含 doubaotts 分支，Wave 1 全部完成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NarratoAI -> SoulVoice API | API key 经 HTTPS 传输到第三方 TTS 服务 |
| NarratoAI -> Doubao API | Token 经 HTTPS 传输到火山引擎 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | reqwest client (all engines) | mitigate | 所有 API 调用使用 HTTPS (reqwest default-tls)；完整 key 不记录日志 |
| T-12-02 | Spoofing | SoulVoice request | mitigate | Authorization Bearer 头 + HTTPS，key 从配置读取 (config.toml gitignored) |
| T-12-03 | Spoofing | Doubao request | mitigate | Authorization `Bearer;{token}` + HTTPS，token 读取自 config.toml |
| T-12-04 | Information Disclosure | Common: `write_audio_bytes` | accept | 音频输出路径由调用方（流水线/用户）指定，不包含 PII |
</threat_model>

<verification>
- `cargo build` 编译成功
- `cargo test --lib tts::common::tests -- --nocapture` 全部通过
- `cargo test --lib tts::soulvoice::tests -- --nocapture` 全部通过
- `cargo test --lib tts::doubaotts::tests -- --nocapture` 全部通过
- `cargo test --lib tts` 全部通过（包括 Phase 3 的现有测试）
</verification>

<success_criteria>
1. common.rs 提供 ProxyConfig、retry_loop、build_client、write_audio_bytes、parse_engine_prefix 五个公开项
2. SoulVoice 引擎通过 `"soulvoice"` 路由可调用，生成立即返回 TtsOutput
3. Doubao 引擎通过 `"doubaotts"` 路由可调用，含 base64 解码，返回 TtsOutput
4. synthesize() 签名已扩展 app_config 参数，不影响现有 edge_tts 分支
5. 所有单元测试通过
6. 未知引擎错误保持不变
</success_criteria>

<output>
After completion, create `.planning/phases/12-additional-tts-engines/12-01-SUMMARY.md`
</output>
