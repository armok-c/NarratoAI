---
phase: 15-layout-shell-3-mode-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01-PLAN.md
files_modified:
  - src/components/SettingSection.vue
  - src/components/SettingsDrawer.vue
autonomous: true
requirements:
  - LOUT-04
user_setup: []

must_haves:
  truths:
    - "SettingSection renders title and icon in header, slot content in body"
    - "SettingSection supports collapsible mode (header click toggles expand/collapse with v-expand-transition)"
    - "SettingSection collapsible header shows chevron icon that rotates on expand"
    - "SettingSection supports variant prop ('outlined' | 'flat' | 'elevated') for card style"
    - "SettingsDrawer opens from right as v-navigation-drawer temporary with 420px width"
    - "SettingsDrawer header shows gear icon + '设置' title + close button"
    - "SettingsDrawer content scrolls with custom 8px scrollbar (rgba(var(--v-theme-on-surface), 0.2) idle, 0.3 hover)"
    - "SettingsDrawer footer shows version string (via getVersion() or fallback 'v0.1.0')"
    - "SettingsDrawer content has SettingSection: 外观 — mdi-brightness-6 icon + '深色模式' label + v-switch toggle"
    - "SettingsDrawer switches between three close methods: header close button, scrim click, gear toggle from AppHeader"
  artifacts:
    - path: src/components/SettingSection.vue
      provides: Reusable collapsible settings card component
      contains: "collapsible"
    - path: src/components/SettingsDrawer.vue
      provides: 420px right-side settings drawer with header/content/footer
      contains: "v-navigation-drawer"
  key_links:
    - from: SettingsDrawer.vue
      to: SettingSection.vue
      via: Import and render inside drawer content
      pattern: "SettingSection"
    - from: SettingsDrawer.vue
      to: @tauri-apps/api/app
      via: getVersion() call in onMounted
      pattern: "getVersion"
    - from: SettingsDrawer.vue
      to: useTheme.ts
      via: toggleTheme call from v-switch
      pattern: "useTheme"
    - from: SettingsDrawer.vue
      to: DefaultLayout.vue (Wave 3 consumer)
      via: v-model binding for showSettings
      pattern: "modelValue"
---

<objective>
右侧设置抽屉组件 — SettingSection（可复用可折叠设置卡片）和 SettingsDrawer（420px 右侧临时抽屉，三段式结构：Header/Content/Footer）。

Purpose: 构建 Phase 16 所有配置面板的容器（SettingsDrawer）和可复用分组卡片（SettingSection）。Phase 15 中仅在 Content 区放置主题切换；Phase 16 扩展 LLM/TTS/BGM/Export 面板。
Output: 2 个组件（SettingSection.vue + SettingsDrawer.vue）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-layout-shell-3-mode-ux/15-CONTEXT.md (D-27~D-31 SettingsDrawer decisions, D-29 SettingSection, D-30 scrollbar)
@.planning/phases/15-layout-shell-3-mode-ux/15-RESEARCH.md (Pitfall 1 Vuetify drawer width bug, Pitfall 5 scroll overflow, Pattern 4 v-model + control, Code Examples: SettingSection pattern)
@.planning/phases/15-layout-shell-3-mode-ux/15-UI-SPEC.md (Section 2.1 SettingsDrawer/SettingSection, Section 3.2 layout tokens, Section 7.5~7.6 interaction contracts, Section 6.2 copywriting, Section 8.4 SettingSection state)

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/composables/useTheme.ts (consumed by SettingsDrawer):
```typescript
export function useTheme() {
  const appStore = useAppStore()
  const vuetifyTheme = useVuetifyTheme()
  vuetifyTheme.global.name.value = appStore.theme
  function toggleTheme() { appStore.toggleTheme(); vuetifyTheme.global.name.value = appStore.theme }
  function setTheme(val: 'light' | 'dark') { appStore.setTheme(val); vuetifyTheme.global.name.value = val }
  return { theme: computed(() => appStore.theme), toggleTheme, setTheme }
}
```

From src/composables/useTauri.ts (consumed by SettingsDrawer for getVersion fallback):
```typescript
export async function tauriInvoke<T>(command: string, args?: Record<string, unknown>): Promise<T>
```

SettingSection props contract (from UI-SPEC Section 8.4):
| Prop | Type | Default |
|------|------|---------|
| title | string | required |
| icon | string (optional) | undefined |
| collapsible | boolean | false |
| defaultExpanded | boolean | true |
| variant | 'outlined' | 'flat' | 'elevated' | 'outlined' |
| loading | boolean | false |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingSection.vue (reusable collapsible settings card)</name>
  <files>
    - src/components/SettingSection.vue (CREATE)
  </files>

  <read_first>
    - D-29 (SettingSection 设计约束)
    - UI-SPEC Section 8.4 (Props definition: title, icon, collapsible, defaultExpanded, variant, loading)
    - UI-SPEC Section 7.6 (Interaction contracts: collapsible header click, chevron animation, hover)
  </read_first>

  <action>
    创建 `src/components/SettingSection.vue`（Vue 3 `<script setup lang="ts">` + `<template>` 单文件组件）:

    **Props**（用 `defineProps` 定义，TypeScript 类型标注）:
    - `title: string` — 必填
    - `icon?: string` — 可选，mdi 图标名
    - `collapsible?: boolean` — 默认 false
    - `defaultExpanded?: boolean` — 默认 true
    - `variant?: 'outlined' | 'flat' | 'elevated'` — 默认 'outlined'
    - `loading?: boolean` — 默认 false

    **Template 结构:**
    - 外层 div: `class="setting-section"` + `:class="['setting-section--' + variant]"`
    - Header: `class="setting-section-header d-flex align-center justify-space-between pa-3"` + `:class="{ 'setting-section-header--clickable': collapsible }"` + `@click="toggleExpand"`
      - 左侧: `<div class="d-flex align-center">` + `<v-icon v-if="icon" :icon="icon" size="20" class="mr-2" />` + `<span class="setting-section-title text-subtitle-2 font-weight-medium">{{ title }}</span>` + `</div>`
      - 右侧: `<div class="d-flex align-center">` + `<slot name="header-actions" />` + `<v-icon v-if="collapsible" :icon="isExpanded ? 'mdi-chevron-up' : 'mdi-chevron-down'" size="20" class="ml-2 setting-section-chevron" />` + `</div>`
    - Content: `<v-expand-transition>` + `<div v-show="isExpanded" class="setting-section-content pa-3 pt-0">` + `<slot />` + `</div>` + `</v-expand-transition>`
    - Loading overlay: 当 loading=true 时在 content 内显示 `<v-progress-linear indeterminate color="primary" class="mt-2" />`

    **Script setup:**
    - `const isExpanded = ref(props.defaultExpanded)`
    - `function toggleExpand() { if (props.collapsible) isExpanded.value = !isExpanded.value }`
    - 使用 `withDefaults` 设置默认值

    **Scoped style:**
    - `.setting-section` — width: 100%
    - `.setting-section--outlined` — border: `1px solid rgba(var(--v-theme-on-surface), 0.12)`, border-radius: 8px
    - `.setting-section--flat` — border: none, no background
    - `.setting-section--elevated` — box-shadow with elevation
    - `.setting-section-header--clickable` — cursor: pointer
    - `.setting-section-header--clickable:hover` — background: `rgba(var(--v-theme-on-surface), 0.04)`
    - `.setting-section-header` — min-height: 48px, padding: 12px 16px（UI-SPEC 3.2）
    - `.setting-section-content` — padding: 0 16px 16px 16px（UI-SPEC 3.2）
    - `.setting-section-title` — opacity: 0.87（UI-SPEC 5.2）
    - `.setting-section-chevron` — opacity: 0.6（UI-SPEC 5.2），transition: transform 0.2s ease
    
    Slot 说明：default slot 供 Area/Phase 16 的配置表单使用（LLM 双模型配置、TTS 引擎选择等）。header-actions slot 供需要额外操作（如刷新按钮）的场景使用。
  </action>

  <acceptance_criteria>
    - src/components/SettingSection.vue 存在
    - defineProps 包含 title (string, required)、icon、collapsible、defaultExpanded、variant、loading
    - Template 包含 v-expand-transition 和 v-show 控制的 content 区域
    - collapsible=true 时 header 可点击切换展开/折叠，并显示 chevron 图标
    - variant='outlined' 时显示边框样式
    - loading=true 时显示 v-progress-linear
    - TypeScript 编译通过
  </acceptance_criteria>

  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | head -10</automated>
  </verify>

  <done>
    SettingSection 可复用组件创建完成，支持 collapsible/outlined/loading
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SettingsDrawer.vue (420px right temporary drawer)</name>
  <files>
    - src/components/SettingsDrawer.vue (CREATE)
  </files>

  <read_first>
    - D-27~D-31 (SettingsDrawer 全部决策)
    - UI-SPEC Section 7.5 (Interaction contracts: open, close, scroll, theme switch, version)
    - UI-SPEC Section 6.2 copywriting (header: 设置, theme: 外观/深色模式, version prefix: v)
  </read_first>

  <action>
    创建 `src/components/SettingsDrawer.vue`（Vue 3 `<script setup lang="ts">` + `<template>` 单文件组件）:

    **模板结构:**
    - `<v-navigation-drawer :model-value="modelValue" location="right" temporary width="420" @update:model-value="emit('update:modelValue', $event)" class="settings-drawer">`
    - 内部三段式 flex 列布局（height: 100%, display: flex, flex-direction: column）:
      
      **Header（固定顶部，flex-shrink: 0）:** `<div class="settings-drawer__header d-flex align-center pa-4">` — 左侧：`<v-icon class="mr-2">mdi-cog</v-icon>` + `<span class="text-h6 font-weight-semibold">设置</span>` + `<v-spacer />` + `<v-btn icon variant="text" size="small" @click="handleClose" aria-label="关闭设置"><v-icon>mdi-close</v-icon></v-btn>` — `</div>` + border-bottom 分隔线
      
      **Content（可滚动，flex: 1, overflow-y: auto）:** `<div class="settings-drawer__content pa-4 pt-2">`
      - `<SettingSection icon="mdi-brightness-6" title="外观" :collapsible="false">`
        - `<div class="d-flex align-center justify-space-between">`
          - `<div>` — `<span class="text-body-2">深色模式</span>` + `<br />` + `<span class="text-caption text-medium-emphasis">` — `{{ isDark ? '深色主题已启用' : '浅色主题已启用' }}` — `</span>`
          - `</div>`
          - `<v-switch :model-value="isDark" color="primary" hide-details density="compact" @update:model-value="toggleTheme" />`
        - `</div>`
      - `</SettingSection>`
      - `</div>`
      
      **Footer（固定底部，flex-shrink: 0）:** `<div class="settings-drawer__footer d-flex align-center justify-center pa-4">` — `<span class="text-caption" style="opacity: 0.6">{{ versionText }}</span>` — `</div>` + border-top 分隔线. 无保存按钮（Phase 16 添加，D-28）。
      
    - `</v-navigation-drawer>`

    **Script setup:**
    - Import: `ref, computed, onMounted` from vue, `SettingSection` from `@/components/SettingSection.vue`, `useTheme` from `@/composables/useTheme`, `useAppStore` from `@/stores/app`
    - Props: `modelValue: boolean`（v-model 双向绑定，供 DefaultLayout 使用）
    - Emits: `update:modelValue: [value: boolean]`
    - `const { theme, toggleTheme } = useTheme()`
    - `const isDark = computed(() => theme.value === 'dark')`
    - `const versionText = ref('v0.1.0')`
    - `onMounted(async () => { try { const { getVersion } = await import('@tauri-apps/api/app'); versionText.value = 'v' + (await getVersion()) } catch { versionText.value = 'v0.1.0' } })`
    - `function handleClose() { emit('update:modelValue', false) }`

    **Scoped style:**
    - `.settings-drawer` — flex 列容器
    - `.settings-drawer__header` — border-bottom: `1px solid rgba(var(--v-theme-on-surface), 0.12)`, padding: 16px 20px（UI-SPEC 3.2）
    - `.settings-drawer__content` — flex: 1, overflow-y: auto, padding: 16px 20px
    - `.settings-drawer__footer` — border-top: `1px solid rgba(var(--v-theme-on-surface), 0.08)`, padding: 16px 20px
    - 自定义滚动条：`.settings-drawer__content::-webkit-scrollbar` — width: 8px（UI-SPEC 3.2 将 D-30 的 6px 修正为 8px 以对齐 4px 网格；UI-SPEC 是设计合约，覆盖 D-30 的初始值）。`.settings-drawer__content::-webkit-scrollbar-thumb` — background: `rgba(var(--v-theme-on-surface), 0.2)`, border-radius: 4px。`::-webkit-scrollbar-thumb:hover` — background: `rgba(var(--v-theme-on-surface), 0.3)`。`::-webkit-scrollbar-track` — background: transparent
    
    **避免 Pitfall 1（Vuetify drawer width bug）**：如果 v-navigation-drawer 关闭时右侧残留可见部分，添加 `:width="null"` 属性作为 workaround。Phase 15 先用标准 `width="420"`。
    
    **避免 Pitfall 5（scroll overflow）**：使用 `display: flex; flex-direction: column; height: 100%` 布局，header/footer 使用 flex-shrink: 0，content 使用 flex: 1; overflow-y: auto。

    注意：SettingsDrawer 接收 v-model 作为 prop（modelValue），由 DefaultLayout 控制开关。v-navigation-drawer 自带 scrim 点击关闭行为。
  </action>

  <acceptance_criteria>
    - src/components/SettingsDrawer.vue 存在
    - Template 包含 v-navigation-drawer: location="right" temporary width="420"
    - 三段式结构：header（齿轮+设置+关闭）、content（可滚动，含 SettingSection 主题切换）、footer（版本号）
    - header 有 border-bottom 分隔线，footer 有 border-top 分隔线
    - 自定义滚动条样式（8px、rgba 0.2/0.3）
    - onMounted 动态导入 @tauri-apps/api/app 获取 getVersion
    - v-switch 绑定 theme toggle
    - 组件通过 modelValue/update:modelValue 实现 v-model 双向绑定
    - TypeScript 编译通过
  </acceptance_criteria>

  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | head -10</automated>
  </verify>

  <done>
    SettingsDrawer 组件创建完成，三段式结构就绪，包含主题切换和版本显示
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsDrawer modelValue prop → parent | Component state flow via v-model, no trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | I | SettingsDrawer getVersion | accept | 返回的是 tauri.conf.json 中定义的应用版本号，无敏感信息。fallback 'v0.1.0' 在非 Tauri 环境使用。 |
</threat_model>

<verification>
- npx vue-tsc --noEmit 编译通过
- npx vitest run --reporter=verbose 全部测试通过
</verification>

<success_criteria>
- SettingSection 可复用组件就绪（collapsible + outlined + loading）
- SettingsDrawer 右侧抽屉就绪（header/content/footer 三段式 + 主题切换 + 版本号）
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/15-layout-shell-3-mode-ux/15-03-SUMMARY.md`
</output>
