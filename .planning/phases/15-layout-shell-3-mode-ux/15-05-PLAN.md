---
phase: 15-layout-shell-3-mode-ux
plan: 05
type: execute
wave: 3
depends_on:
  - 15-01-PLAN.md
  - 15-02-PLAN.md
files_modified:
  - src/views/HomeView.vue
autonomous: true
requirements:
  - LOUT-03
  - MODE-02
  - MODE-03
user_setup: []

must_haves:
  truths:
    - "HomeView renders upper section (60%) and lower section (40%) with 16px vertical gap"
    - "Upper section has left column (VideoTable placeholder, 60% width) and right column (ModeSelector card, 40% width) with 16px horizontal gap"
    - "Lower section has left column (操作按钮 placeholder, 60% width) and right column (LogPanel placeholder, 40% width) with 16px horizontal gap"
    - "VideoTable, 操作按钮, LogPanel placeholders show labeled dashed-border area with region title and 'Phase 17 实现' sub-label"
    - "ModeSelector card shows title '工作模式' with mdi-folder-multiple icon, outlined variant"
    - "ModeSelector v-select offers 3 options: 纪录片解说 / 短剧解说 / 短剧混剪"
    - "ModeSelector v-select onChange calls ModeStore.setMode() + ModeStore.resetPipeline()"
    - "v-select initial value matches ModeStore.currentMode"
    - "Layout is full-width (no max-width), uses flex:6/flex:4 ratios for column splits"
  artifacts:
    - path: src/views/HomeView.vue
      provides: 60/40 split home page with placeholders and ModeSelector
      contains: "v-select"
  key_links:
    - from: HomeView.vue
      to: stores/mode.ts
      via: useModeStore() for mode selection
      pattern: "useModeStore"
    - from: HomeView.vue
      to: router/index.ts
      via: Route '/' renders DefaultLayout > HomeView (already configured in Phase 14)
      pattern: "HomeView"
---

<objective>
主页视图和模式选择器 — HomeView 60/40 上下分区布局、灰色虚线占位卡片（VideoTable / 操作按钮 / LogPanel）、ModeSelector 工作模式 v-select 下拉选择。

Purpose: 构建 Phase 15 的应用主页。上区左列是 VideoTable 占位（Phase 17 替换为真实组件），右列是 ModeSelector 工作模式选择卡片。下区左列是操作按钮占位，右列是 LogPanel 占位。三模式切换基础在本 Phase 完成。
Output: 1 个重写的文件（HomeView.vue）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-layout-shell-3-mode-ux/15-CONTEXT.md (D-15~D-26 HomeView + ModeSelector 全部决策)
@.planning/phases/15-layout-shell-3-mode-ux/15-RESEARCH.md (Insights on HomeView placeholders and ModeSelector)
@.planning/phases/15-layout-shell-3-mode-ux/15-UI-SPEC.md (Section 2.1 ModeSelector, Section 3.2 layout tokens, Section 6.2 copywriting, Section 7.4 HomeView interactions, Section 8.1 ModeStore extensions)

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/stores/mode.ts (Phase 14 + Phase 15 Plan 02 extended):
```typescript
export type WorkMode = 'documentary' | 'sde' | 'sdp'
export const useModeStore = defineStore('mode', () => {
  const currentMode = ref<WorkMode>(/* init from localStorage(lastMode) or 'documentary' */)
  function setMode(val: WorkMode) { /* sets + persists to localStorage */ }
  function resetPipeline() { usePipelineStore().reset() }
  return { currentMode, setMode, resetPipeline }
})
```

Copywriting (from UI-SPEC Section 6.2):
| Element | Copy |
|---------|------|
| Card title | 工作模式 |
| Option 1 | 纪录片解说 |
| Option 2 | 短剧解说 |
| Option 3 | 短剧混剪 |
| Default | 纪录片解说 |
| VideoTable placeholder | 视频列表 / Phase 17 实现 |
| 操作按钮 placeholder | 操作按钮 / Phase 17 实现 |
| LogPanel placeholder | 日志面板 / Phase 17 实现 |

Layout tokens (from UI-SPEC Section 3.2):
| Token | Value |
|-------|-------|
| Upper/lower split | 60% / 40% (flex: 6 / flex: 4) |
| Column split (upper) | VideoTable flex: 6 / ModeSelector flex: 4 |
| Column split (lower) | 按钮 flex: 6 / LogPanel flex: 4 |
| Section gap (vertical) | 16px (gap-4) |
| Column gap (horizontal) | 16px (gap-4) |
| Full width | No max-width, fills v-main |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HomeView.vue with 60/40 split layout, labeled placeholder zones, and ModeSelector card</name>
  <files>
    - src/views/HomeView.vue (REWRITE)
  </files>

  <read_first>
    - src/views/HomeView.vue (current Phase 14 welcome placeholder — must read before rewriting)
    - D-15~D-26 (HomeView + ModeSelector 全部决策)
    - src/stores/mode.ts (ModeStore interface to consume)
  </read_first>

  <action>
    重写 `src/views/HomeView.vue`（Vue 3 `<script setup lang="ts">` + `<template>` 单文件组件）:

    **根布局:**
    - 根 `<div class="home-view d-flex flex-column pa-4" style="height: 100%">` — 使用 `pa-4`（16px 内边距）

    **上区 flex: 6（60%）:**
    - `<div class="d-flex ga-4 upper-section" style="flex: 6">` — 双列 flex 行，gap-4（16px gap）
      - **左列 flex: 6（60%）:** `<div class="placeholder-card d-flex flex-column align-center justify-center" style="flex: 6; min-height: 200px">` — 灰色虚线框占位
        - `<v-icon size="48" class="mb-2" style="opacity: 0.3">mdi-video-outline</v-icon>`
        - `<span class="text-body-2 font-weight-medium" style="opacity: 0.6">视频列表</span>`
        - `<span class="text-caption" style="opacity: 0.4">Phase 17 实现</span>`
      - **右列 flex: 4（40%）:** `<div style="flex: 4">` — ModeSelector v-card
        - `<v-card variant="outlined" class="pa-4">`
          - `<v-card-title class="pa-0 mb-3 d-flex align-center text-subtitle-2 font-weight-medium">` — `<v-icon class="mr-2">mdi-folder-multiple</v-icon>工作模式` — `</v-card-title>`
          - `<v-select v-model="selectedMode" :items="modeOptions" variant="outlined" density="compact" hide-details @update:model-value="handleModeChange" />`
        - `</v-card>`
    - `</div>`

    **下区 flex: 4（40%）:**
    - `<div class="d-flex ga-4 lower-section" style="flex: 4">` — 双列 flex 行
      - **左列 flex: 6（60%）:** `<div class="placeholder-card d-flex flex-column align-center justify-center" style="flex: 6; min-height: 120px">`
        - `<v-icon size="48" class="mb-2" style="opacity: 0.3">mdi-play-circle-outline</v-icon>`
        - `<span class="text-body-2 font-weight-medium" style="opacity: 0.6">操作按钮</span>`
        - `<span class="text-caption" style="opacity: 0.4">Phase 17 实现</span>`
      - **右列 flex: 4（40%）:** `<div class="placeholder-card d-flex flex-column align-center justify-center" style="flex: 4; min-height: 120px">`
        - `<v-icon size="48" class="mb-2" style="opacity: 0.3">mdi-text-box-outline</v-icon>`
        - `<span class="text-body-2 font-weight-medium" style="opacity: 0.6">日志面板</span>`
        - `<span class="text-caption" style="opacity: 0.4">Phase 17 实现</span>`
    - `</div>`

    **Script setup:**
    - Import: `ref, watch` from vue, `useModeStore` from `@/stores/mode`, `WorkMode` type from `@/stores/mode`
    - `const modeStore = useModeStore()`
    - `const selectedMode = ref<WorkMode>(modeStore.currentMode)`
    - `const modeOptions = ref([{ title: '纪录片解说', value: 'documentary' }, { title: '短剧解说', value: 'sde' }, { title: '短剧混剪', value: 'sdp' }])` — 纯中文标签（D-22）
    - `function handleModeChange(val: WorkMode) {` — `modeStore.setMode(val)` + `modeStore.resetPipeline()`（D-24）— `}`
    - watch 或 computed 确保 selectedMode 与 modeStore.currentMode 同步（当外部修改时）
      - `watch(() => modeStore.currentMode, (val) => { selectedMode.value = val })` — 当 modeStore 外部变化时同步 UI

    **全局样式（非 scoped 或使用 deep 选择器 — 放在 `<style>` 块中）:**
    - `.home-view` — height: 100%, display: flex, flex-direction: column
    - `.upper-section` — 没有额外样式（flex: 6 由 style 属性控制）
    - `.lower-section` — 没有额外样式（flex: 4 由 style 属性控制）
    - `.placeholder-card` — border: `2px dashed rgba(var(--v-theme-on-surface), 0.2)`, border-radius: 8px, background: `rgba(var(--v-theme-on-surface), 0.02)`, min-height 由子元素 type 决定

    注意：
    - D-21 明确上区右列仅包含 ModeSelector 设置卡片，不放其他配置项（LLM/TTS/BGM/Export 均属于 Phase 16）
    - D-26 明确 MODE-03（面板动态显隐）在 Phase 15 不生效
    - 三个占位区域分别用不同图标（mdi-video-outline / mdi-play-circle-outline / mdi-text-box-outline）帮助开发者区分
    - 内容区无最大宽度（D-18 全宽布局）
  </action>

  <acceptance_criteria>
    - src/views/HomeView.vue 已重写（不再是 welcome 占位内容）
    - 上区 flex:6 下区 flex:4，上下间距 16px
    - 上区左列 flex:6（VideoTable 占位），右列 flex:4（ModeSelector 卡片），列间 16px gap
    - 下区左列 flex:6（操作按钮占位），右列 flex:4（LogPanel 占位），列间 16px gap
    - 三个占位卡片显示灰色虚线框 + 区域标题 + "Phase 17 实现" 标注
    - ModeSelector v-card 标题 "工作模式" + mdi-folder-multiple 图标
    - v-select 三个选项：纪录片解说 / 短剧解说 / 短剧混剪
    - v-select @update:model-value 调用 modeStore.setMode() 和 modeStore.resetPipeline()
    - TypeScript 编译通过
  </acceptance_criteria>

  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | head -10</automated>
  </verify>

  <done>
    HomeView 重写完成 — 60/40 上下分区、占位卡片、ModeSelector 模式切换就绪
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v-select dropdown → ModeStore | User selection triggers Pinia state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | T (Tampering) | v-select mode options | mitigate | options 是硬编码数组，不可由用户输入修改。v-select 的 items 使用固定选项列表，不渲染用户输入。 |
| T-15-10 | I (Information Disclosure) | Placeholder cards | accept | placeholder 仅显示区域名称和 Phase 标注，无敏感信息。 |
</threat_model>

<verification>
- npx vue-tsc --noEmit 编译通过
- npx vitest run --reporter=verbose 全部测试通过
</verification>

<success_criteria>
- HomeView 60/40 上下分区布局就绪
- 3 个占位卡片就绪（视频列表、操作按钮、日志面板）
- ModeSelector 工作模式选择就绪（3 个模式选项、localStorage 持久化）
- 模式切换调用 setMode + resetPipeline
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/15-layout-shell-3-mode-ux/15-05-SUMMARY.md`
</output>
