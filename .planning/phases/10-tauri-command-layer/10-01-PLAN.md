---
phase: 10-tauri-command-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - narratoai-core/Cargo.toml
  - narratoai-core/src/documentary/types.rs
  - narratoai-core/src/documentary/pipeline.rs
  - narratoai-core/src/documentary/script_gen.rs
  - narratoai-core/src/sde/types.rs
  - narratoai-core/src/sde/pipeline.rs
  - narratoai-core/src/sdp/types.rs
  - narratoai-core/src/sdp/pipeline.rs
  - src-tauri/Cargo.toml
  - src-tauri/build.rs
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [TAURI-01, TAURI-02]
user_setup: []

must_haves:
  truths:
    - "D-09: Cargo 工作空间构建成功，narratoai-core 库和 src-tauri 二进制均在 workspace 中"
    - "D-09: DocumentaryRequest/SdeRequest/SdpRequest/ScriptGenRequest 实现 Deserialize，可被 Tauri IPC 反序列化"
    - "D-14: 三个流水线的 ProgressCallback 统一为字符串回调类型 Box<dyn Fn(&str, f32, &str) + Send + Sync>"
    - "D-14: run_documentary/run_sde/run_sdp 接受 Option<ProgressCallback> 参数，向后兼容（不传则无回调）"
    - "D-14: 所有 emit_progress 调用点使用新的 string-based 回调签名"
    - "D-08: Tauri 应用基础结构（tauri.conf.json、capabilities、main.rs）就绪，PathBuf 不做额外标准化"
  artifacts:
    - path: "Cargo.toml"
      provides: "工作空间根 manifest（members 含 narratoai-core 和 src-tauri）"
      contains: "[workspace]"
    - path: "narratoai-core/Cargo.toml"
      provides: "库 crate manifest（[lib] name = narratoai_core）"
      contains: "[lib]"
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri 二进制 crate manifest"
      contains: "narratoai-core"
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri 2.0 运行时配置"
      contains: "\"identifier\""
    - path: "src-tauri/capabilities/default.json"
      provides: "Tauri 权限配置（dialog/event/core）"
      contains: "permissions"
    - path: "src-tauri/src/main.rs"
      provides: "Tauri 应用入口"
      contains: "fn main"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri builder 骨架"
      contains: "tauri::Builder"
  key_links:
    - from: "Cargo.toml"
      to: "narratoai-core/Cargo.toml"
      via: "workspace members"
      pattern: "narratoai-core"
    - from: "src-tauri/Cargo.toml"
      to: "narratoai-core"
      via: "workspace path dependency"
      pattern: "narratoai-core"
---

<objective>
创建 Cargo 工作空间布局，将现有库代码迁移到 narratoai-core/ 子 crate，搭建 src-tauri Tauri 2.0 应用骨架。修改所有请求类型以支持 Tauri IPC 反序列化，统一三个流水线的进度回调类型为字符串基，为流水线入口函数添加进度回调参数。

Purpose: 为 Tauri 命令层提供工作空间隔离、IPC 兼容的数据类型、统一的进度回调接口
Output: 工作空间 Cargo.toml + narratoai-core/ + src-tauri/ 骨架 + 修改后的请求/回调/流水线类型
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-tauri-command-layer/10-CONTEXT.md
@.planning/phases/10-tauri-command-layer/10-RESEARCH.md
@.planning/phases/10-tauri-command-layer/10-PATTERNS.md
@.planning/phases/10-tauri-command-layer/10-VALIDATION.md

<interfaces>
<!-- Key types from existing code that will be modified -->

Current DocumentaryRequest (src/documentary/types.rs:6-25):
- #[derive(Debug, Clone)] → 需添加 Serialize, Deserialize
- 所有字段为 PathBuf, String, f64, u32, Option<T> — 均 Serde 兼容

Current ProgressStep enum (src/documentary/types.rs:87-95):
- #[derive(Debug, Clone, Copy, PartialEq, Eq)] → 不变
- 变体: LoadScript, Tts, Clip, MergeAudio, Concat, Composite

Current ProgressCallback (src/documentary/types.rs:98):
- pub type ProgressCallback = Box<dyn Fn(ProgressStep, f32, &str) + Send + Sync>;
- → 改为: Box<dyn Fn(&str, f32, &str) + Send + Sync> (string-based)

Current run_documentary (src/documentary/pipeline.rs:471-475):
- pub async fn run_documentary(request: DocumentaryRequest, config: &AppConfig, proxy: Option<&ProxySection>) -> Result<PathBuf, PipelineError>
- → 添加: progress: Option<ProgressCallback>

Current SDE request (src/sde/types.rs:7-28):
- #[derive(Debug, Clone)] → 需添加 Serialize, Deserialize

Current SDP request (src/sdp/types.rs:7-29):
- #[derive(Debug, Clone)] → 需添加 Serialize, Deserialize

Current ScriptGenRequest (src/documentary/script_gen.rs:10-19):
- #[derive(Debug, Clone)] → 需添加 Serialize, Deserialize

Current ExportRequest (src/jianying/builder.rs:19-32):
- pub struct ExportRequest { ... } — 无 derive → 需添加 Debug, Clone, Serialize, Deserialize
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Cargo 工作空间并迁移库代码到 narratoai-core/</name>

  <files>
    Cargo.toml (root, 新建 workspace manifest)
    narratoai-core/Cargo.toml (从根目录 Cargo.toml 迁移)
    narratoai-core/src/ (从根目录 src/ 迁移)
    src-tauri/Cargo.toml (新建)
    src-tauri/build.rs (新建)
    src-tauri/tauri.conf.json (新建)
    src-tauri/capabilities/default.json (新建)
    src-tauri/src/main.rs (新建)
    src-tauri/src/lib.rs (新建, 骨架)
  </files>

  <read_first>
    E:\GitLib\NarratoAI\Cargo.toml (当前单包布局，需转为 workspace)
    10-RESEARCH.md (Pattern 4: Config Injection, Page 308-328; Workspace Layout, Page 232-250)
    10-PATTERNS.md (src-tauri file patterns for main.rs/lib.rs at lines 540-570)
    10-CONTEXT.md (D-06: Tauri State + Arc/RwLock 混合模式)
  </read_first>

  <action>
    1. 创建 narratoai-core/ 目录 (mkdir -p narratoai-core)

    2. 复制根 Cargo.toml 到 narratoai-core/Cargo.toml，添加 [lib] 节：
       ```toml
       [package]
       name = "narratoai-core"
       version = "0.1.0"
       edition = "2021"

       [lib]
       name = "narratoai_core"
       path = "src/lib.rs"

       [features]
       default = []
       download-ffmpeg = ["ffmpeg-sidecar/download_ffmpeg"]

       [dependencies]
       # 保持原有所有依赖不变（tokio, serde, thiserror, ffmpeg-sidecar, tauri 等）
       # 注意：narratoai-core 本身不依赖 tauri 系列库

       [dev-dependencies]
       # 保持原有所有 dev-dependencies
       ```

    3. 移动源代码：`git mv src/ narratoai-core/src/`
       这会移动所有 .rs 文件到 narratoai-core/src/，git 保留历史记录
       所有 crate:: 引用路径不变，因为 lib.rs 的相对位置从 ./src/lib.rs 变为 narratoai-core/src/lib.rs

    4. 创建根目录 Cargo.toml（workspace 专属）：
       ```toml
       [workspace]
       members = ["narratoai-core", "src-tauri"]
       resolver = "2"
       ```
       工作空间根不包含 [package] — 纯 workspace manifest。

    5. 创建 src-tauri/Cargo.toml：
       ```toml
       [package]
       name = "narratoai-app"
       version = "0.1.0"
       edition = "2021"

       [lib]
       name = "narratoai_app"
       path = "src/lib.rs"

       [[bin]]
       name = "narratoai"
       path = "src/main.rs"

       [dependencies]
       narratoai-core = { path = "../narratoai-core" }
       tauri = { version = "2", features = [] }
       tauri-plugin-dialog = "2"
       serde = { version = "1", features = ["derive"] }
       serde_json = "1"
       uuid = { version = "1", features = ["v4"] }
       tokio = { version = "1", features = ["full"] }

       [build-dependencies]
       tauri-build = { version = "2", features = [] }
       ```
       注意：使用 lib + bin 双目标，lib 导出 pub fn run()，bin 调用 lib::run()

    6. 创建 src-tauri/build.rs：
       ```rust
       fn main() {
           tauri_build::build()
       }
       ```

    7. 创建 src-tauri/tauri.conf.json（最小 Tauri 2.0 配置）：
       ```json
       {
         "$schema": "https://raw.githubusercontent.com/tauri-apps/tauri/dev/crates/tauri-config-schema/schema.json",
         "productName": "NarratoAI",
         "version": "0.1.0",
         "identifier": "com.narratoai.app",
         "build": {
           "frontendDist": "../dist",
           "devUrl": "http://localhost:1420",
           "beforeDevCommand": "",
           "beforeBuildCommand": ""
         },
         "app": {
           "windows": [
             {
               "title": "NarratoAI",
               "width": 1200,
               "height": 800
             }
           ],
           "security": {
             "csp": null
           }
         },
         "plugins": {
           "dialog": {}
         }
       }
       ```
       说明：frontendDist/devUrl 为 Vue 3 前端预留（v2 里程碑），当前不会影响后端编译。
       依据：RESEARCH.md Lines 220-230 (Tauri 2.0 config schema)

    8. 创建 src-tauri/capabilities/default.json：
       ```json
       {
         "identifier": "default",
         "description": "NarratoAI 默认权限",
         "windows": ["main"],
         "permissions": [
           "core:default",
           "core:event:default",
           "core:event:allow-emit",
           "core:event:allow-listen",
           "dialog:default"
         ]
       }
       ```
       依据：RESEARCH.md Lines 636-642 (Tauri 2.x capabilities system)

    9. 创建 src-tauri/src/main.rs：
       ```rust
       #![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

       fn main() {
           narratoai_app::run();
       }
       ```

    10. 创建 src-tauri/src/lib.rs（骨架，仅导出 run()，Plan 02 填充实现）：
        ```rust
        use narratoai_core;

        /// Tauri 应用入口——启动桌面应用
        ///
        /// Phase 10 Plan 02 将在此填充完整的 Builder 配置：
        /// - 插件初始化 (dialog)
        /// - config.toml 加载和 State 管理
        /// - 命令注册
        pub fn run() {
            println!("NarratoAI Tauri app starting...");
            // Plan 02 将替换为完整 tauri::Builder::default() 调用
        }
        ```

    11. 验证：
        - `cargo check --workspace` 编译成功
        - `cargo check -p narratoai-core` 只编译库
        - `cargo check -p narratoai-app --lib` 编译 Tauri lib
        - 确认 git status 显示 narratoai-core/ 中保留了 src/ 的历史

    12. 注意：
        - 不要在 narratoai-core/Cargo.toml 中加入 tauri 依赖——库 crate 保持纯 Rust
        - src-tauri/Cargo.toml 通过 path = "../narratoai-core" 引用库
        - 工作空间成员名 "narratoai-core" 在 use 语句中转为 narratoai_core
        - Icons 目录可以后续添加，当前 tauri.conf.json 不要求图标
  </action>

  <verify>
    <automated>cargo check --workspace 2>&1 | head -5 && cargo check -p narratoai-core --lib 2>&1 | tail -3 && cargo check -p narratoai-app --lib 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
    1. 根目录 Cargo.toml 包含 [workspace] members = ["narratoai-core", "src-tauri"]
    2. narratoai-core/Cargo.toml 包含 [lib] name = "narratoai_core"
    3. narratoai-core/src/lib.rs 存在且导出所有 pub mod
    4. src-tauri/Cargo.toml 包含 narratoai-core 依赖
    5. src-tauri/tauri.conf.json 包含 identifier, build, app 节
    6. src-tauri/capabilities/default.json 包含 permissions 数组
    7. src-tauri/src/main.rs 包含 fn main() { narratoai_app::run(); }
    8. src-tauri/src/lib.rs 包含 pub fn run()
    9. src-tauri/build.rs 包含 tauri_build::build()
    10. cargo check --workspace 无错误
  </acceptance_criteria>

  <done>
    Cargo 工作空间结构就绪，narratoai-core 库和 src-tauri 骨架均可独立编译
  </done>
</task>

<task type="auto">
  <name>Task 2: 为请求类型添加 Deserialize，统一 ProgressCallback 为字符串回调</name>

  <files>
    narratoai-core/src/documentary/types.rs
    narratoai-core/src/sde/types.rs
    narratoai-core/src/sdp/types.rs
    narratoai-core/src/documentary/script_gen.rs
    narratoai-core/src/jianying/builder.rs
  </files>

  <read_first>
    narratoai-core/src/documentary/types.rs (当前 DocumentaryRequest + ProgressCallback 定义)
    narratoai-core/src/sde/types.rs (当前 SdeRequest + SdeProgressCallback + SdePipelineState)
    narratoai-core/src/sdp/types.rs (当前 SdpRequest + SdpProgressCallback + SdpPipelineState)
    narratoai-core/src/documentary/script_gen.rs (ScriptGenRequest 定义)
    narratoai-core/src/jianying/builder.rs (ExportRequest 定义)
  </read_first>

  <action>
    对以下每个文件，按 D-09（复用现有类型）要求添加/修改 derive 宏：

    A) narratoai-core/src/documentary/types.rs:
       - DocumentaryRequest: 将 `#[derive(Debug, Clone)]` 改为 `#[derive(Debug, Clone, Serialize, Deserialize)]`
       - ProgressStep: 保持不变（enum 不需要序列化到前端）
       - ProgressCallback: 将 `Box<dyn Fn(ProgressStep, f32, &str) + Send + Sync>` 改为
         `Box<dyn Fn(&str, f32, &str) + Send + Sync>` —— 第一个参数从 ProgressStep 枚举改为 &str step_name
         依据 D-14: "扩展现有回调类型——定义 TauriProgressCallback：Fn(ProgressStep)"

    B) narratoai-core/src/sde/types.rs:
       - SdeRequest: `#[derive(Debug, Clone, Serialize, Deserialize)]`
       - SdeProgressStep: 保持不变
       - SdeProgressCallback: 改为 `Box<dyn Fn(&str, f32, &str) + Send + Sync>`
       - SdePipelineState::progress: 字段类型自动变为 SdeProgressCallback
       - SdePipelineState::emit_progress: 签名不变（因为 SdeProgressCallback 已变）

    C) narratoai-core/src/sdp/types.rs:
       - SdpRequest: `#[derive(Debug, Clone, Serialize, Deserialize)]`
       - SdpProgressStep: 保持不变
       - SdpProgressCallback: 改为 `Box<dyn Fn(&str, f32, &str) + Send + Sync>`
       - SdpPipelineState::progress: 字段类型自动变为 SdpProgressCallback

    D) narratoai-core/src/documentary/script_gen.rs:
       - ScriptGenRequest: `#[derive(Debug, Clone, Serialize, Deserialize)]`
       - ScriptGenRequest.progress 字段的类型 `Box<dyn Fn(f32, &str) + Send + Sync>` 保持不变
         （这是不同的回调——帧分析进度，不是流水线进度）

    E) narratoai-core/src/jianying/builder.rs:
       - ExportRequest: 添加 `#[derive(Debug, Clone, Serialize, Deserialize)]`
       依据 D-09: 命令复用 ExportRequest，需要序列化支持

    重要说明：修改后所有 emit_progress 调用点需要将枚举变体改为字符串字面量。
    具体改变在 Task 3 中完成（因为 emit_progress 定义在 pipeline.rs 中）。
    但首先需要确认编译不会因类型别名改变而失败。

    验证方法：执行 `cargo check -p narratoai-core --lib` 确认编译通过。
    注意：此步骤后 emit_progress 的调用参数变为 string，但调用点尚未更新——
    编译会因类型不匹配而失败。这是预期的，Task 3 会修复所有调用点。
  </action>

  <verify>
    <automated>cargo check -p narratoai-core 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    1. DocumentaryRequest 有 Serialize + Deserialize derive
    2. SdeRequest 有 Serialize + Deserialize derive
    3. SdpRequest 有 Serialize + Deserialize derive
    4. ScriptGenRequest 有 Serialize + Deserialize derive
    5. ExportRequest 有 Debug + Clone + Serialize + Deserialize derive
    6. ProgressCallback/SdeProgressCallback/SdpProgressCallback 签名改为 Fn(&str, f32, &str)
    7. （临场）Type mismatch 错误在调用点出现——Task 3 修复
  </acceptance_criteria>

  <done>
    所有命令参数类型支持 Serde 序列化/反序列化，进度回调类型统一为字符串基
  </done>
</task>

<task type="auto">
  <name>Task 3: 为流水线入口函数添加 progress 参数，更新所有 emit_progress 调用点为 string 签名</name>

  <files>
    narratoai-core/src/documentary/pipeline.rs
    narratoai-core/src/sde/pipeline.rs
    narratoai-core/src/sdp/pipeline.rs
  </files>

  <read_first>
    narratoai-core/src/documentary/pipeline.rs (run_documentary 签名 + emit_progress 调用点)
    narratoai-core/src/sde/pipeline.rs (run_sde 签名 + emit_progress 调用点)
    narratoai-core/src/sdp/pipeline.rs (run_sdp 签名 + emit_progress 调用点)
  </read_first>

  <action>
    按 D-14（回调注入 + ProgressStep 结构体）+ D-11（回调注入 + AppHandle emit）要求修改：

    A) narratoai-core/src/documentary/pipeline.rs:
       - run_documentary 签名添加 progress 参数：
         ```rust
         pub async fn run_documentary(
             request: DocumentaryRequest,
             config: &crate::config::types::AppConfig,
             proxy: Option<&crate::config::types::ProxySection>,
             progress: Option<ProgressCallback>,  // NEW
         ) -> Result<PathBuf, PipelineError> {
         ```
       - 在 run_documentary 函数体中，创建 PipelineState 时将 progress 传入：
         ```rust
         state.progress = progress;
         ```
         替换原来的 `state.progress = None;`
       - 更新所有 emit_progress 调用点为字符串（保留原枚举变体名作为字符串，保持语义可读）：
         - `state.emit_progress(ProgressStep::LoadScript, 0.0, "加载视频脚本");` →
           `state.emit_progress("load_script", 0.0, "加载视频脚本");`
         - `state.emit_progress(ProgressStep::Tts, 20.0, "TTS 生成完成");` →
           `state.emit_progress("tts", 20.0, "TTS 生成完成");`
         - `state.emit_progress(ProgressStep::Clip, 60.0, "视频裁剪完成");` →
           `state.emit_progress("clip", 60.0, "视频裁剪完成");`
         - `state.emit_progress(ProgressStep::MergeAudio, 70.0, "音频字幕合并完成");` →
           `state.emit_progress("merge_audio", 70.0, "音频字幕合并完成");`
         - `state.emit_progress(ProgressStep::Concat, 80.0, "视频拼接完成");` →
           `state.emit_progress("concat", 80.0, "视频拼接完成");`
         - `state.emit_progress(ProgressStep::Composite, 100.0, "最终合成完成");` →
           `state.emit_progress("composite", 100.0, "最终合成完成");`
       - 更新 PipelineState 的 emit_progress 方法签名（函数体内无需改变——cb 的类型会自动匹配新的 Fn(&str, f32, &str)）

    B) narratoai-core/src/sde/pipeline.rs:
       - run_sde 签名添加 progress 参数：
         ```rust
         pub async fn run_sde(
             request: SdeRequest,
             config: &AppConfig,
             proxy: Option<&ProxySection>,
             registry: &Registry,
             prompt_manager: &PromptManager,
             progress: Option<SdeProgressCallback>,  // NEW
         ) -> Result<PathBuf, SdeError> {
         ```
       - 在 run_sde 函数体中，将 progress 赋给 state.progress（在 state 初始化后的合适位置）
       - 更新所有 emit_progress 调用点为字符串（共约 18 处）：
         - ParseSubtitle → "parse_subtitle"
         - PlotAnalysis → "plot_analysis"
         - ScriptGenerate → "script_generate"
         - LoadScript → "load_script"
         - Tts → "tts"
         - Clip → "clip"
         - MergeAudio → "merge_audio"
         - Concat → "concat"
         - Composite → "composite"
       - 具体每步的两次 emit 也统一更新

    C) narratoai-core/src/sdp/pipeline.rs:
       - run_sdp 签名添加 progress 参数：
         ```rust
         pub async fn run_sdp(
             request: SdpRequest,
             _config: &crate::config::types::AppConfig,
             progress: Option<SdpProgressCallback>,  // NEW
         ) -> Result<PathBuf, SdpError> {
         ```
       - 将 progress 赋给 state.progress
       - 更新 emit_progress 调用点：
         - SdpProgressStep::Clip → "clip"
         - SdpProgressStep::Concat → "concat"
         - SdpProgressStep::Composite → "composite"

    验证方法：`cargo check -p narratoai-core` 编译通过，无类型错误。
    注意：要使编译通过，Task 2 和 Task 3 必须一起生效。建议编写完所有修改后一次性运行 cargo check。
  </action>

  <verify>
    <automated>cargo check -p narratoai-core 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    1. run_documentary 签名包含 progress: Option<ProgressCallback>
    2. run_sde 签名包含 progress: Option<SdeProgressCallback>
    3. run_sdp 签名包含 progress: Option<SdpProgressCallback>
    4. 所有 emit_progress 调用使用小写字符串名（非枚举变体）
    5. cargo check -p narratoai-core 编译通过，无警告
  </acceptance_criteria>

  <done>
    三个流水线入口函数接受可选进度回调，所有进度上报点使用统一字符串签名
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC (JS→Rust) | 前端通过 Tauri invoke 传入的命令参数在此边界通过 serde 反序列化 |
| Tauri State (Config) | AppConfig 存储在 Tauri State 中，供所有命令读取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | DocumentaryRequest/SdeRequest/SdpRequest 反序列化 | mitigate | serde 自动限制类型（只接受已定义字段），且现有 validate() 方法在校验失败时返回错误 |
| T-10-02 | Information Disclosure | AppConfig State 中的 API key | accept | `#[serde(skip_serializing)]` 在 API key 字段上防止序列化泄漏，但命令读取时仍可访问 Arc<RwLock<AppConfig>> |
| T-10-03 | Information Disclosure | ProgressPayload 事件 payload | mitigate | ProgressPayload 只含 pipeline_type/step_name/percent/message 等公开信息，不含配置或密钥 |
| T-10-04 | Elevation of Privilege | run_sde/run_sdp 脚本路径参数 | accept | Tauri 文件对话框返回的路径由操作系统验证；手动传入路径时由 std::fs 操作的权限控制决定 |
</threat_model>

<verification>
1. `cargo check --workspace` — 工作空间整体编译
2. `cargo check -p narratoai-core` — 库 crate 编译
3. `cargo check -p narratoai-app` — Tauri 二进制编译（不含 setup 阶段）
4. `cargo test -p narratoai-core` — 已有测试全绿
</verification>

<success_criteria>
- 工作空间 Cargo.toml 包含两个 member crate，编译通过
- 所有请求类型（DocumentaryRequest/SdeRequest/SdpRequest/ScriptGenRequest/ExportRequest）实现 Serialize+Deserialize
- 三个流水线的 ProgressCallback 统一为 `Box<dyn Fn(&str, f32, &str) + Send + Sync>`
- run_documentary/run_sde/run_sdp 接受 Option<ProgressCallback> 参数
- 所有 emit_progress 调用点使用字符串 step_name
- src-tauri 骨架（Cargo.toml, build.rs, tauri.conf.json, capabilities, main.rs, lib.rs）就绪
- 已有测试全部通过无回归
</success_criteria>

<output>
After completion, create `.planning/phases/10-tauri-command-layer/10-01-SUMMARY.md`
</output>
