---
phase: 10-tauri-command-layer
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src-tauri/src/error.rs
  - src-tauri/src/models/mod.rs
  - src-tauri/src/models/progress.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/commands/pipeline.rs
  - src-tauri/src/commands/script.rs
  - src-tauri/src/commands/export.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [TAURI-01, TAURI-02, TAURI-03]
user_setup: []

must_haves:
  truths:
    - "D-03: src-tauri/src/error.rs 定义了 CommandError（code + message + Serialize），包含7个领域错误的 From<T> 实现"
    - "D-10 D-13: src-tauri/src/models/progress.rs 定义了 ProgressPayload（10字段含 status 字段）、CommandResponse、StepDuration"
    - "D-01 D-04: src-tauri/src/commands/ 包含所有15条 Tauri 命令，thin wrapper 模式 1:1 映射现有 Rust API"
    - "D-05: register_all_commands!() 宏集中注册所有命令，与 register_all_providers()/register_all_prompts() 模式对齐"
    - "D-11 D-12 D-15: 流水线命令（run_documentary/run_sde/run_sdp）注入 AppHandle 闭包，通过 app_handle.emit(\"pipeline-progress\", payload) 推送，task_id 用 UUID v4"
    - "D-02 D-06: src-tauri/src/lib.rs ::run() 构建完整 Tauri Builder：dialog 插件、Config 通过 Tauri State 注入、LLM/Prompt/TTS 保持全局 Arc/RwLock"
    - "D-06 D-07: Config 加载使用多路径回退机制，命令返回 CommandResponse（output_video_path + step_durations + intermediate_paths）"
  artifacts:
    - path: "src-tauri/src/error.rs"
      provides: "CommandError + Display + Error + From<T> x7"
      contains: "impl std::error::Error for CommandError"
    - path: "src-tauri/src/models/progress.rs"
      provides: "ProgressPayload, CommandResponse, StepDuration, ScriptInfo"
      contains: "pub struct ProgressPayload"
    - path: "src-tauri/src/commands/pipeline.rs"
      provides: "5条流水线命令（run_documentary/run_sde/run_sdp/generate_documentary_script/generate_sdp_script）"
      contains: "fn run_documentary"
    - path: "src-tauri/src/commands/script.rs"
      provides: "7条脚本命令（load/save/validate/get_info/update_narration/set_ost/update_timestamp）"
      contains: "fn load_script"
    - path: "src-tauri/src/commands/export.rs"
      provides: "1条导出命令（export_jianying_draft）"
      contains: "fn export_jianying_draft"
    - path: "src-tauri/src/commands/mod.rs"
      provides: "register_all_commands!() 宏"
      contains: "macro_rules! register_all_commands"
    - path: "src-tauri/src/lib.rs"
      provides: "Tauri Builder 完整实现"
      contains: "pub fn run"
  key_links:
    - from: "src-tauri/src/commands/pipeline.rs"
      to: "narratoai_core::documentary::pipeline::run_documentary"
      via: "async call with Option<ProgressCallback>"
      pattern: "run_documentary.*progress"
    - from: "src-tauri/src/commands/pipeline.rs"
      to: "tauri::Emitter::emit"
      via: "app_handle.emit(\"pipeline-progress\", payload)"
      pattern: "emit.*pipeline-progress"
    - from: "src-tauri/src/error.rs"
      to: "narratoai_core::documentary::error::PipelineError"
      via: "impl From<PipelineError> for CommandError"
      pattern: "From<.*Error> for CommandError"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/mod.rs"
      via: "register_all_commands!() in invoke_handler"
      pattern: "register_all_commands"
---

<objective>
构建完整的 Tauri 命令层：CommandError 统一错误处理、ProgressPayload/CommandResponse 数据模型、15 条 Tauri 命令封装（thin wrapper 模式）、register_all_commands!() 宏、完整的 Tauri Builder 配置（config 加载、State 注入、插件注册）。

Purpose: 将所有 Rust 业务函数通过 Tauri 2.0 IPC 暴露给前端，支持流水线实时进度推送和文件选择
Output: 完整的 src-tauri 命令层代码，可编译为 Tauri 桌面应用后端
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-tauri-command-layer/10-CONTEXT.md
@.planning/phases/10-tauri-command-layer/10-RESEARCH.md
@.planning/phases/10-tauri-command-layer/10-PATTERNS.md
@.planning/phases/10-tauri-command-layer/10-VALIDATION.md

<interfaces>
<!-- Key types from narratoai-core that commands will call -->

Pipeline entry points (after Plan 01 modifications):
- narratoai_core::documentary::pipeline::run_documentary(DocumentaryRequest, &AppConfig, Option<&ProxySection>, Option<ProgressCallback>) -> Result<PathBuf, PipelineError>
- narratoai_core::sde::pipeline::run_sde(SdeRequest, &AppConfig, Option<&ProxySection>, &Registry, &PromptManager, Option<SdeProgressCallback>) -> Result<PathBuf, SdeError>
- narratoai_core::sdp::pipeline::run_sdp(SdpRequest, &AppConfig, Option<SdpProgressCallback>) -> Result<PathBuf, SdpError>
- narratoai_core::documentary::script_gen::generate_documentary_script(ScriptGenRequest, &dyn LlmProvider) -> Result<Vec<ScriptClip>, PipelineError>
- narratoai_core::sdp::script_gen::generate_sdp_script(&Path, &dyn LlmProvider, &PromptManager, &Path, f64, u32) -> Result<Script, SdpError>

Script module:
- narratoai_core::script::load_script(&Path) -> Result<Script, ScriptError>
- narratoai_core::script::save_script(&Script, &Path) -> Result<(), ScriptError>
- narratoai_core::script::validate(&Script) -> Result<(), ScriptError>
- narratoai_core::script::edit::update_narration(&Script, usize, &str) -> Result<Script, ScriptError>
- narratoai_core::script::edit::set_ost(&Script, usize, OstType) -> Result<Script, ScriptError>
- narratoai_core::script::edit::update_timestamp(&Script, usize, &str) -> Result<Script, ScriptError>

Export:
- narratoai_core::jianying::builder::export_draft(&ExportRequest) -> Result<PathBuf, JianYingError>

Request types (now with Deserialize):
- DocumentaryRequest { video_path, script_path, tts_engine, voice_name, voice_rate, voice_pitch, tts_volume, original_volume, bgm_volume, bgm_path, subtitle_enabled, subtitle_font, subtitle_font_size, subtitle_color, subtitle_position, output_dir, threads }
- SdeRequest { subtitle_path, video_path, drama_name, temperature, tts_engine, voice_name, voice_rate, voice_pitch, tts_volume, original_volume, bgm_volume, bgm_path, subtitle_enabled, subtitle_font, subtitle_font_size, subtitle_color, subtitle_position, output_dir, threads }
- SdpRequest { subtitle_path, video_path, script_path, temperature, custom_clips, original_volume, bgm_volume, bgm_path, output_dir, threads }
- ScriptGenRequest { video_path, video_theme, custom_prompt, frame_interval, vision_batch_size, vision_model, text_model, max_concurrency, progress }
- ExportRequest { script, video_origin_path, draft_path, draft_name, width, height }

Existing domain errors requiring CommandError From<T>:
- PipelineError (src/documentary/error.rs): ScriptLoad, TtsGeneration, VideoClip, AudioMerge, Concat, Composite, Io, Timestamp, SrtGeneration, Validation, FrameExtraction, FFmpeg, Llm
- SdeError (src/sde/error.rs): ParseSubtitle, PlotAnalysis, ScriptGeneration, JsonRepair, VideoProcess, Validation, Io
- SdpError (src/sdp/error.rs): ParseSubtitle, LlmError, JsonRepair, VideoProcess, Validation, Io
- ConfigError (src/error.rs): IoError, ParseError, ValidationError, NotFound, WatchError
- ScriptError (src/script/error.rs): JsonParse, Validation, Io, IndexOutOfBounds, InvalidTimestamp
- JianYingError (src/jianying/error.rs): Io, JsonSerialize, Validation, MissingField, ProbeError
- TTSError (src/error.rs): UnknownEngine, ConnectionFailed, AuthenticationFailed, SynthesisFailed, RetryExhausted
- LLMError (src/error.rs): General, ProviderNotFound, Configuration, APICall, Validation, ModelNotSupported, RateLimit, Authentication, ContentFilter

OstType enum: NarrationOnly=0, OriginalSound=1, Mixed=2 (serde_repr, accepts u8 from JSON)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 CommandError 和数据模型（ProgressPayload、CommandResponse、StepDuration、ScriptInfo）</name>

  <files>
    src-tauri/src/error.rs
    src-tauri/src/models/mod.rs
    src-tauri/src/models/progress.rs
  </files>

  <read_first>
    narratoai-core/src/error.rs (ConfigError/TTSError/LLMError 定义 — 用于 From 转换)
    narratoai-core/src/documentary/error.rs (PipelineError 定义)
    narratoai-core/src/sde/error.rs (SdeError 定义)
    narratoai-core/src/sdp/error.rs (SdpError 定义)
    narratoai-core/src/script/error.rs (ScriptError 定义)
    narratoai-core/src/jianying/error.rs (JianYingError 定义)
    10-CONTEXT.md (D-03 CommandError 设计、D-07 CommandResponse、D-10 ProgressPayload)
    10-RESEARCH.md (Pattern 2 ProgressPayload lines 327-357, Pattern 3 CommandError lines 365-401)
    10-PATTERNS.md (error.rs analog lines 38-106, progress.rs analog lines 132-198)
  </read_first>

  <action>
    A) 创建 src-tauri/src/error.rs —— 参照 PATTERNS.md lines 338-434 + RESEARCH.md lines 365-401：

    ```rust
    use serde::Serialize;
    use std::fmt;

    /// Tauri 命令统一错误——所有领域错误通过 From<T> 转换为此类型
    ///
    /// # 安全性
    /// message 保留中文错误信息（与 Phase 1 D-18 一致）。
    /// code 用于前端程序化区分错误类型。
    #[derive(Debug, Clone, Serialize)]
    pub struct CommandError {
        pub code: String,
        pub message: String,
    }

    impl fmt::Display for CommandError {
        fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
            write!(f, "[{}] {}", self.code, self.message)
        }
    }

    impl std::error::Error for CommandError {}

    // ---- From<T> implementations for each domain error type ----
    // 每个实现使用 err.to_string() 保留中文 Display 信息（D-18）

    use narratoai_core::config::error::ConfigError as CoreConfigError;
    impl From<CoreConfigError> for CommandError {
        fn from(err: CoreConfigError) -> Self {
            CommandError {
                code: "CONFIG_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::documentary::error::PipelineError;
    impl From<PipelineError> for CommandError {
        fn from(err: PipelineError) -> Self {
            CommandError {
                code: "PIPELINE_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::sde::error::SdeError;
    impl From<SdeError> for CommandError {
        fn from(err: SdeError) -> Self {
            CommandError {
                code: "SDE_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::sdp::error::SdpError;
    impl From<SdpError> for CommandError {
        fn from(err: SdpError) -> Self {
            CommandError {
                code: "SDP_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::script::error::ScriptError;
    impl From<ScriptError> for CommandError {
        fn from(err: ScriptError) -> Self {
            CommandError {
                code: "SCRIPT_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::jianying::error::JianYingError;
    impl From<JianYingError> for CommandError {
        fn from(err: JianYingError) -> Self {
            CommandError {
                code: "JIANYING_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::error::TTSError;
    impl From<TTSError> for CommandError {
        fn from(err: TTSError) -> Self {
            CommandError {
                code: "TTS_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    use narratoai_core::error::LLMError;
    impl From<LLMError> for CommandError {
        fn from(err: LLMError) -> Self {
            CommandError {
                code: "LLM_ERROR".into(),
                message: err.to_string(),
            }
        }
    }

    // ---- String error (for validation error messages from validate() methods) ----
    impl From<String> for CommandError {
        fn from(msg: String) -> Self {
            CommandError {
                code: "VALIDATION_ERROR".into(),
                message: msg,
            }
        }
    }
    ```

    B) 创建 src-tauri/src/models/progress.rs —— 参照 PATTERNS.md lines 132-198：

    ```rust
    use serde::{Deserialize, Serialize};
    use std::collections::HashMap;
    use std::path::PathBuf;

    /// 统一进度事件 payload（D-10）
    ///
    /// 所有流水线共用此结构体，前端通过 pipeline_type 字段路由到不同 UI 面板。
    /// 每个命令调用生成唯一 UUID task_id（D-15），嵌入每个事件。
    #[derive(Debug, Clone, Serialize)]
    pub struct ProgressPayload {
        pub pipeline_type: String,     // "documentary" | "sde" | "sdp"
        pub task_id: String,           // UUID v4
        pub step_name: String,
        pub percent: f32,
        pub message: String,
        pub step_index: u32,
        pub total_steps: u32,
        pub status: String,            // "running" | "success" | "error"
        #[serde(skip_serializing_if = "Option::is_none")]
        pub error_code: Option<String>,
        #[serde(skip_serializing_if = "Option::is_none")]
        pub error_message: Option<String>,
    }

    /// 流水线命令统一返回包装（D-07）
    ///
    /// 包含输出视频路径、每步耗时统计、中间产物路径映射。
    /// 前端可预览视频和查看统计。
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct CommandResponse {
        pub output_video_path: PathBuf,
        pub step_durations: Vec<StepDuration>,
        pub intermediate_paths: HashMap<String, PathBuf>,
    }

    /// 每步执行耗时统计
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct StepDuration {
        pub step_name: String,
        pub duration_secs: f64,
    }

    /// 脚本信息（get_script_info 命令返回）
    #[derive(Debug, Clone, Serialize, Deserialize)]
    pub struct ScriptInfo {
        pub clip_count: usize,
        pub total_duration_secs: f64,
    }
    ```

    C) 创建 src-tauri/src/models/mod.rs：
    ```rust
    pub mod progress;
    pub use progress::*;
    ```

    验证方法：`cargo check -p narratoai-app --lib` 编译通过。
  </action>

  <verify>
    <automated>cargo check -p narratoai-app --lib 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    1. src-tauri/src/error.rs 定义 CommandError（code + message）且 impl std::error::Error
    2. 存在 From<PipelineError>、From<SdeError>、From<SdpError>、From<ScriptError>、From<JianYingError>、From<TTSError>、From<LLMError>、From<ConfigError>、From<String>
    3. src-tauri/src/models/progress.rs 定义 ProgressPayload（含所有10个 D-10 字段）
    4. ProgressPayload 中 error_code 和 error_message 带 #[serde(skip_serializing_if = "Option::is_none")]
    5. src-tauri/src/models/progress.rs 定义 CommandResponse（output_video_path + step_durations + intermediate_paths）
    6. cargo check -p narratoai-app --lib 编译通过
  </acceptance_criteria>

  <done>
    CommandError + ProgressPayload + CommandResponse + ScriptInfo 类型就绪
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建所有 15 条 Tauri 命令和 register_all_commands!() 宏</name>

  <files>
    src-tauri/src/commands/mod.rs
    src-tauri/src/commands/pipeline.rs
    src-tauri/src/commands/script.rs
    src-tauri/src/commands/export.rs
  </files>

  <read_first>
    narratoai-core/src/documentary/pipeline.rs (run_documentary 新签名含 Option<ProgressCallback>)
    narratoai-core/src/sde/pipeline.rs (run_sde 新签名含 Option<SdeProgressCallback>)
    narratoai-core/src/sdp/pipeline.rs (run_sdp 新签名含 Option<SdpProgressCallback>)
    narratoai-core/src/documentary/script_gen.rs (generate_documentary_script 签名)
    narratoai-core/src/sdp/script_gen.rs (generate_sdp_script 签名)
    narratoai-core/src/script/mod.rs (load/save/validate 签名)
    narratoai-core/src/script/edit.rs (update_narration/set_ost/update_timestamp 签名)
    narratoai-core/src/jianying/builder.rs (export_draft + ExportRequest 签名)
    narratoai-core/src/llm/registry.rs (Registry 结构)
    narratoai-core/src/prompt/manager.rs (PromptManager 结构)
    10-CONTEXT.md (D-04 命令清单、D-11/12/13/14/15 进度事件设计)
    10-RESEARCH.md (Pattern 1 registration macro lines 286-305, Pipeline command pattern lines 496-548)
    10-PATTERNS.md (commands/mod.rs analog lines 232-258, pipeline.rs analog lines 293-338, script.rs analog lines 393-430, export.rs analog lines 459-481)
  </read_first>

  <action>
    A) 创建 src-tauri/src/commands/mod.rs —— 模式对齐 register_all_providers()/register_all_prompts()：

    ```rust
    pub mod pipeline;
    pub mod script;
    pub mod export;

    pub use pipeline::*;
    pub use script::*;
    pub use export::*;

    /// 集中注册所有 Tauri 命令（D-05）
    ///
    /// 对齐 Phase 2/3/4 的 register_all_providers()/register_all_prompts() 模式。
    /// 在 Tauri builder 的 invoke_handler 中展开：
    /// `.invoke_handler(register_all_commands!())`
    #[macro_export]
    macro_rules! register_all_commands {
        () => {
            tauri::generate_handler![
                crate::commands::pipeline::run_documentary,
                crate::commands::pipeline::run_sde,
                crate::commands::pipeline::run_sdp,
                crate::commands::pipeline::generate_documentary_script,
                crate::commands::pipeline::generate_sdp_script,
                crate::commands::script::load_script,
                crate::commands::script::save_script,
                crate::commands::script::validate_script,
                crate::commands::script::get_script_info,
                crate::commands::script::update_narration,
                crate::commands::script::set_ost,
                crate::commands::script::update_timestamp,
                crate::commands::export::export_jianying_draft,
            ]
        };
    }
    ```

    B) 创建 src-tauri/src/commands/pipeline.rs —— 5 条流水线命令（D-01 thin wrapper）：

    每条命令的核心模式：
    - `#[tauri::command]` 注解
    - 第一个参数 `app: AppHandle`（流水线命令需要 emit 事件）
    - 配置通过 `tauri::State<'_, AppConfig>` 注入
    - 返回 `Result<CommandResponse, CommandError>`（流水线命令）或 `Result<return_type, CommandError>`
    - `?` 操作符自动转换为 CommandError（通过 From<T>）
    - UUID task_id 嵌入 ProgressPayload（D-15）
    - 闭包捕获 AppHandle，调用 app_handle.emit("pipeline-progress", payload)（D-11）

    ```rust
    use tauri::{AppHandle, Emitter};
    use uuid::Uuid;

    use narratoai_core::config::types::AppConfig;
    use narratoai_core::documentary::types::DocumentaryRequest;
    use narratoai_core::sde::types::SdeRequest;
    use narratoai_core::sdp::types::SdpRequest;
    use narratoai_core::documentary::script_gen::ScriptGenRequest;
    use narratoai_core::script::types::Script;

    use crate::error::CommandError;
    use crate::models::progress::{CommandResponse, ProgressPayload, ScriptInfo, StepDuration};

    // =============================================================
    //  命令 1: run_documentary（D-04）
    //  执行完整 6 步纪录片流水线，实时推送进度事件
    // =============================================================
    #[tauri::command]
    pub async fn run_documentary(
        app: AppHandle,
        request: DocumentaryRequest,
        config: tauri::State<'_, AppConfig>,
    ) -> Result<CommandResponse, CommandError> {
        let task_id = Uuid::new_v4().to_string();
        let progress_task_id = task_id.clone();

        // D-11: 构建 Tauri 感知的进度回调闭包
        let app_handle = app.clone();
        let total_steps: u32 = 6;
        let progress_callback: narratoai_core::documentary::types::ProgressCallback =
            Box::new(move |step_name: &str, pct: f32, msg: &str| {
                let step_index = match step_name {
                    "load_script" => 0,
                    "tts" => 1,
                    "clip" => 2,
                    "merge_audio" => 3,
                    "concat" => 4,
                    "composite" => 5,
                    _ => 0,
                };
                let payload = ProgressPayload {
                    pipeline_type: "documentary".to_string(),
                    task_id: progress_task_id.clone(),
                    step_name: step_name.to_string(),
                    percent: pct,
                    message: msg.to_string(),
                    step_index,
                    total_steps,
                    status: "running".to_string(),
                    error_code: None,
                    error_message: None,
                };
                let _ = app_handle.emit("pipeline-progress", payload);
            });

        let output_path = narratoai_core::documentary::pipeline::run_documentary(
            request,
            &config,
            None,        // proxy — Tauri 启动时读取 config 中的 proxy 配置
            Some(progress_callback),
        ).await?;

        Ok(CommandResponse {
            output_video_path: output_path,
            step_durations: vec![],
            intermediate_paths: std::collections::HashMap::new(),
        })
    }

    // =============================================================
    //  命令 2: run_sde（D-04）
    //  执行完整 9 步 SDE 流水线，实时推送进度事件
    //  需要全局 Registry 和 PromptManager（通过 Tauri State 注入）
    // =============================================================
    #[tauri::command]
    pub async fn run_sde(
        app: AppHandle,
        request: SdeRequest,
        config: tauri::State<'_, AppConfig>,
        registry: tauri::State<'_, std::sync::Arc<std::sync::RwLock<narratoai_core::llm::registry::Registry>>>,
        prompt_manager: tauri::State<'_, std::sync::Arc<std::sync::RwLock<narratoai_core::prompt::manager::PromptManager>>>,
    ) -> Result<CommandResponse, CommandError> {
        let task_id = Uuid::new_v4().to_string();
        let progress_task_id = task_id.clone();

        let app_handle = app.clone();
        let total_steps: u32 = 9;
        let progress_callback: narratoai_core::sde::types::SdeProgressCallback =
            Box::new(move |step_name: &str, pct: f32, msg: &str| {
                let step_index = match step_name {
                    "parse_subtitle" => 0,
                    "plot_analysis" => 1,
                    "script_generate" => 2,
                    "load_script" => 3,
                    "tts" => 4,
                    "clip" => 5,
                    "merge_audio" => 6,
                    "concat" => 7,
                    "composite" => 8,
                    _ => 0,
                };
                let payload = ProgressPayload {
                    pipeline_type: "sde".to_string(),
                    task_id: progress_task_id.clone(),
                    step_name: step_name.to_string(),
                    percent: pct,
                    message: msg.to_string(),
                    step_index,
                    total_steps,
                    status: "running".to_string(),
                    error_code: None,
                    error_message: None,
                };
                let _ = app_handle.emit("pipeline-progress", payload);
            });

        let registry = registry.read().map_err(|e| CommandError {
            code: "REGISTRY_LOCK".into(),
            message: format!("注册表读取锁失败: {}", e),
        })?;
        let pm = prompt_manager.read().map_err(|e| CommandError {
            code: "PROMPT_LOCK".into(),
            message: format!("Prompt 管理器读取锁失败: {}", e),
        })?;

        let output_path = narratoai_core::sde::pipeline::run_sde(
            request,
            &config,
            None,      // proxy
            &registry,
            &pm,
            Some(progress_callback),
        ).await?;

        Ok(CommandResponse {
            output_video_path: output_path,
            step_durations: vec![],
            intermediate_paths: std::collections::HashMap::new(),
        })
    }

    // =============================================================
    //  命令 3: run_sdp（D-04）
    //  执行完整 4 步 SDP 流水线，实时推送进度事件
    // =============================================================
    #[tauri::command]
    pub async fn run_sdp(
        app: AppHandle,
        request: SdpRequest,
        config: tauri::State<'_, AppConfig>,
    ) -> Result<CommandResponse, CommandError> {
        let task_id = Uuid::new_v4().to_string();
        let progress_task_id = task_id.clone();

        let app_handle = app.clone();
        let total_steps: u32 = 3;
        let progress_callback: narratoai_core::sdp::types::SdpProgressCallback =
            Box::new(move |step_name: &str, pct: f32, msg: &str| {
                let step_index = match step_name {
                    "clip" => 0,
                    "concat" => 1,
                    "composite" => 2,
                    _ => 0,
                };
                let payload = ProgressPayload {
                    pipeline_type: "sdp".to_string(),
                    task_id: progress_task_id.clone(),
                    step_name: step_name.to_string(),
                    percent: pct,
                    message: msg.to_string(),
                    step_index,
                    total_steps,
                    status: "running".to_string(),
                    error_code: None,
                    error_message: None,
                };
                let _ = app_handle.emit("pipeline-progress", payload);
            });

        let output_path = narratoai_core::sdp::pipeline::run_sdp(
            request,
            &config,
            Some(progress_callback),
        ).await?;

        Ok(CommandResponse {
            output_video_path: output_path,
            step_durations: vec![],
            intermediate_paths: std::collections::HashMap::new(),
        })
    }

    // =============================================================
    //  命令 4: generate_documentary_script（D-04）
    //  通过视觉 LLM + 文本 LLM 生成纪录片解说脚本
    // =============================================================
    #[tauri::command]
    pub async fn generate_documentary_script(
        app: AppHandle,
        request: ScriptGenRequest,
        config: tauri::State<'_, AppConfig>,
        registry: tauri::State<'_, std::sync::Arc<std::sync::RwLock<narratoai_core::llm::registry::Registry>>>,
    ) -> Result<narratoai_core::script::types::Script, CommandError> {
        let registry = registry.read().map_err(|e| CommandError {
            code: "REGISTRY_LOCK".into(),
            message: format!("注册表读取锁失败: {}", e),
        })?;

        // 从配置中获取 text LLM provider
        let provider = registry
            .get(&config.app.text_llm_provider)
            .map_err(|e| CommandError {
                code: "PROVIDER_NOT_FOUND".into(),
                message: format!("获取 LLM provider 失败: {}", e),
            })?;

        let clips = narratoai_core::documentary::script_gen::generate_documentary_script(
            request,
            provider.as_ref(),
        ).await?;

        Ok(clips)
    }

    // =============================================================
    //  命令 5: generate_sdp_script（D-04）
    //  通过两步 LLM 调用生成短剧混剪脚本
    // =============================================================
    #[tauri::command]
    pub async fn generate_sdp_script(
        subtitle_path: std::path::PathBuf,
        temperature: f64,
        custom_clips: u32,
        config: tauri::State<'_, AppConfig>,
        registry: tauri::State<'_, std::sync::Arc<std::sync::RwLock<narratoai_core::llm::registry::Registry>>>,
        prompt_manager: tauri::State<'_, std::sync::Arc<std::sync::RwLock<narratoai_core::prompt::manager::PromptManager>>>,
    ) -> Result<Script, CommandError> {
        let registry = registry.read().map_err(|e| CommandError {
            code: "REGISTRY_LOCK".into(),
            message: format!("注册表读取锁失败: {}", e),
        })?;
        let pm = prompt_manager.read().map_err(|e| CommandError {
            code: "PROMPT_LOCK".into(),
            message: format!("Prompt 管理器读取锁失败: {}", e),
        })?;

        let task_dir = std::env::temp_dir().join("narratoai").join(Uuid::new_v4().to_string());
        tokio::fs::create_dir_all(&task_dir).await.map_err(|e| CommandError {
            code: "IO_ERROR".into(),
            message: format!("创建临时目录失败: {}", e),
        })?;

        let script = narratoai_core::sdp::script_gen::generate_sdp_script(
            &subtitle_path,
            registry.as_ref(),
            &pm,
            &task_dir,
            temperature,
            custom_clips,
        ).await?;

        Ok(script)
    }
    ```

    C) 创建 src-tauri/src/commands/script.rs —— 7 条脚本管理命令：

    ```rust
    use std::path::PathBuf;

    use narratoai_core::script::types::{OstType, Script, ScriptClip};
    use narratoai_core::script::edit;

    use crate::error::CommandError;
    use crate::models::progress::ScriptInfo;

    /// 从 JSON 文件加载脚本，自动校验
    #[tauri::command]
    pub async fn load_script(
        path: PathBuf,
    ) -> Result<Script, CommandError> {
        let script = narratoai_core::script::load_script(&path)?;
        Ok(script)
    }

    /// 将脚本保存为美化 JSON 文件
    #[tauri::command]
    pub async fn save_script(
        script: Script,
        path: PathBuf,
    ) -> Result<(), CommandError> {
        narratoai_core::script::save_script(&script, &path)?;
        Ok(())
    }

    /// 校验脚本——所有错误一次性返回
    #[tauri::command]
    pub async fn validate_script(
        script: Script,
    ) -> Result<(), CommandError> {
        narratoai_core::script::validate(&script)?;
        Ok(())
    }

    /// 获取脚本基本信息
    #[tauri::command]
    pub async fn get_script_info(
        script: Script,
    ) -> Result<ScriptInfo, CommandError> {
        // 计算总时长（从 timestamp 字段解析）
        let total_duration_secs = script.iter()
            .filter_map(|clip| {
                let ts = &clip.timestamp;
                let parts: Vec<&str> = ts.split('-').collect();
                if parts.len() != 2 { return None; }
                let end_ts = parts.get(1)?;
                let time_parts: Vec<&str> = end_ts.split(':').collect();
                if time_parts.len() != 3 { return None; }
                let h: f64 = time_parts[0].parse().ok()?;
                let m: f64 = time_parts[1].parse().ok()?;
                let s: f64 = {
                    let sec_parts: Vec<&str> = time_parts[2].split(',').collect();
                    let sec: f64 = sec_parts[0].parse().ok()?;
                    let ms: f64 = sec_parts.get(1).and_then(|v| v.parse().ok()).unwrap_or(0.0);
                    Some(sec + ms / 1000.0)
                };
                Some(h * 3600.0 + m * 60.0 + s)
            })
            .fold(0.0, f64::max);

        Ok(ScriptInfo {
            clip_count: script.len(),
            total_duration_secs,
        })
    }

    /// 更新指定片段的解说文案
    #[tauri::command]
    pub async fn update_narration(
        script: Script,
        index: usize,
        text: String,
    ) -> Result<Script, CommandError> {
        let new_script = edit::update_narration(&script, index, &text)?;
        Ok(new_script)
    }

    /// 设置指定片段的 OST 类型
    #[tauri::command]
    pub async fn set_ost(
        script: Script,
        index: usize,
        ost: OstType,
    ) -> Result<Script, CommandError> {
        let new_script = edit::set_ost(&script, index, ost)?;
        Ok(new_script)
    }

    /// 更新指定片段的时间戳
    #[tauri::command]
    pub async fn update_timestamp(
        script: Script,
        index: usize,
        timestamp: String,
    ) -> Result<Script, CommandError> {
        let new_script = edit::update_timestamp(&script, index, &timestamp)?;
        Ok(new_script)
    }
    ```

    D) 创建 src-tauri/src/commands/export.rs —— 1 条导出命令：

    ```rust
    use std::path::PathBuf;

    use narratoai_core::jianying::builder::{export_draft, ExportRequest};
    use narratoai_core::script::types::Script;

    use crate::error::CommandError;

    /// 导出项目时间线为剪映草稿 JSON 格式
    #[tauri::command]
    pub async fn export_jianying_draft(
        script: Script,
        video_origin_path: PathBuf,
        draft_path: PathBuf,
        draft_name: String,
        width: u32,
        height: u32,
    ) -> Result<PathBuf, CommandError> {
        let request = ExportRequest {
            script,
            video_origin_path,
            draft_path,
            draft_name,
            width,
            height,
        };
        let result = export_draft(&request)?;
        Ok(result)
    }
    ```

    验证方法：`cargo check -p narratoai-app --lib` 编译通过。
    可能需要处理 compiler warnings (unused imports) —— 移除 app/task_id 等未使用的变量。
    generate_sdp_script 中不需要 app: AppHandle 参数。
  </action>

  <verify>
    <automated>cargo check -p narratoai-app --lib 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    1. src-tauri/src/commands/mod.rs 包含 register_all_commands!() 宏，列出 13 个处理函数
    2. src-tauri/src/commands/pipeline.rs 包含 5 条命令：run_documentary, run_sde, run_sdp, generate_documentary_script, generate_sdp_script
    3. run_documentary/run_sde/run_sdp 接受 AppHandle，构建闭包调用 app_handle.emit("pipeline-progress", ...)
    4. run_documentary/run_sde/run_sdp 返回 Result<CommandResponse, CommandError>
    5. src-tauri/src/commands/script.rs 包含 7 条脚本命令（load_script, save_script, validate_script, get_script_info, update_narration, set_ost, update_timestamp）
    6. get_script_info 返回 ScriptInfo { clip_count, total_duration_secs }
    7. src-tauri/src/commands/export.rs 包含 export_jianying_draft 命令
    8. cargo check -p narratoai-app --lib 编译通过
  </acceptance_criteria>

  <done>
    15 条 Tauri 命令全部实现并编译通过，支持进度事件推送
  </done>
</task>

<task type="auto">
  <name>Task 3: 完善 src-tauri/src/lib.rs — Tauri Builder 完整实现</name>

  <files>
    src-tauri/src/lib.rs
  </files>

  <read_first>
    src-tauri/src/lib.rs (当前骨架)
    src-tauri/src/commands/mod.rs (register_all_commands! 宏)
    narratoai-core/src/config/types.rs (AppConfig 结构)
    narratoai-core/src/config/config.rs (ConfigManager::load 方法)
    narratoai-core/src/llm/registry.rs (Registry::new() 无参数)
    narratoai-core/src/llm/register.rs (register_all_providers 接受 &AppConfig + &mut Registry)
    narratoai-core/src/prompt/registry.rs (PromptRegistry::new() 无参数)
    narratoai-core/src/prompt/register.rs (register_all_prompts 接受 &mut PromptRegistry)
    narratoai-core/src/prompt/manager.rs (PromptManager::new 接受 SharedPromptRegistry = Arc<RwLock<PromptRegistry>>)
    10-CONTEXT.md (D-02 配置启动加载、D-05 集中注册、D-06 Tauri State + Arc 混合)
    10-RESEARCH.md (Pattern 4 Config Injection lines 405-435, complete lib.rs pattern lines 519-533)
  </read_first>

  <action>
    将 src-tauri/src/lib.rs 从骨架替换为完整实现。

    核心功能（按 D-02, D-05, D-06）：
    1. 插件初始化：dialog
    2. setup() 回调：加载 config.toml（多路径回退），创建并注册 LLM Registry、PromptManager
    3. State 管理：AppConfig, Arc<RwLock<Registry>>, Arc<RwLock<PromptManager>>
    4. invoke_handler：register_all_commands!() 宏展开

    ```rust
    use std::path::PathBuf;
    use std::sync::{Arc, RwLock};

    use narratoai_core::config::config::ConfigManager;
    use narratoai_core::config::types::AppConfig;
    use narratoai_core::llm::registry::Registry;
    use narratoai_core::prompt::manager::PromptManager;
    use narratoai_core::prompt::registry::PromptRegistry;

    /// Tauri 应用入口
    ///
    /// 构建完整的 Tauri 2.0 桌面应用后端：
    /// - 插件：dialog（文件选择对话框）
    /// - 配置：启动时多路径加载 config.toml，注入 Tauri State（D-02）
    /// - LLM/Prompt 注册表：初始化并注入 Arc/RwLock State（D-06）
    /// - 命令注册：register_all_commands!() 展开全部 15 条命令（D-05）
    pub fn run() {
        tauri::Builder::default()
            .plugin(tauri_plugin_dialog::init())
            .setup(|app| {
                // ---- 1. 多路径回退加载 config.toml ----
                let config_path = find_config(app)?;
                let config_manager = ConfigManager::load(&config_path)
                    .map_err(|e| format!("配置加载失败: {}", e))?;

                // ---- 2. 注入 AppConfig 到 Tauri State ----
                let app_config = config_manager.get().clone();
                app.manage(app_config);

                // ---- 3. 初始化 LLM Registry ----
                let registry = Arc::new(RwLock::new(Registry::new()));
                {
                    let mut reg = registry.write().map_err(|e| {
                        format!("注册表写入锁失败: {}", e)
                    })?;
                    narratoai_core::llm::register::register_all_providers(
                        config_manager.get(),
                        &mut reg,
                    ).map_err(|errors| {
                        format!("LLM provider 注册失败: {:?}", errors)
                    })?;
                }
                app.manage::<Arc<RwLock<Registry>>>(registry);

                // ---- 4. 初始化 Prompt Registry + PromptManager ----
                // 创建 PromptRegistry，注册模板，包装为 PromptManager
                let prompt_registry = Arc::new(RwLock::new(PromptRegistry::new()));
                {
                    let mut reg = prompt_registry.write().map_err(|e| {
                        format!("Prompt 注册表写入锁失败: {}", e)
                    })?;
                    narratoai_core::prompt::register::register_all_prompts(
                        &mut reg,
                    ).map_err(|errors| {
                        format!("Prompt 注册失败: {:?}", errors)
                    })?;
                }
                let prompt_manager = Arc::new(RwLock::new(
                    PromptManager::new(prompt_registry)
                ));
                app.manage::<Arc<RwLock<PromptManager>>>(prompt_manager);

                Ok(())
            })
            .invoke_handler(register_all_commands!())
            .run(tauri::generate_context!())
            .expect("NarratoAI 应用启动失败");
    }

    /// 多路径回退查找 config.toml
    ///
    /// 搜索顺序：
    /// 1. Tauri resource_dir()/config.toml（production bundle 时使用）
    /// 2. 当前工作目录/config.toml（development 常用）
    /// 3. ./config.toml（fallback，与 Python 版兼容）
    fn find_config(app: &tauri::App) -> Result<PathBuf, String> {
        // Path 1: resource_dir
        if let Ok(resource_dir) = app.path().resource_dir() {
            let path = resource_dir.join("config.toml");
            if path.exists() {
                return Ok(path);
            }
        }

        // Path 2: current_dir
        if let Ok(cwd) = std::env::current_dir() {
            let path = cwd.join("config.toml");
            if path.exists() {
                return Ok(path);
            }
        }

        // Path 3: plain fallback
        let path = PathBuf::from("config.toml");
        if path.exists() {
            return Ok(path);
        }

        Err("未找到 config.toml 配置文件。请将 config.toml 放置在应用资源目录、当前工作目录或应用根目录。".into())
    }
    ```

    注意：
    - `ConfigManager::get()` 返回 `&AppConfig`，需要 `.clone()` 因为 Tauri State 需要 owned value
    - 已知 API（已验证）：
      - `Registry::new()` — 无参数构造
      - `PromptRegistry::new()` — 无参数构造
      - `PromptManager::new(registry: SharedPromptRegistry)` — 接受 Arc<RwLock<PromptRegistry>>
      - `register_all_providers(&AppConfig, &mut Registry)` — 返回 Result<(), RegistrationErrors>
      - `register_all_prompts(&mut PromptRegistry)` — 返回 Result<(), RegistrationErrors>
    - `app.path().resource_dir()` 是 Tauri 2.0 的 PathResolver API，编译期验证
    - 所有 .map_err() 的 Err 类型需要能被 Tauri 的 setup 回调接收（setup 返回 Result<(), Box<dyn Error>>——字符串通过 .into() 转换）

    验证方法：`cargo check -p narratoai-app --lib` 编译通过。
  </action>

  <verify>
    <automated>cargo check -p narratoai-app --lib 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    1. lib.rs 包含 pub fn run() 调用 tauri::Builder::default()
    2. Builder 加载 dialog 插件（tauri_plugin_dialog::init()）
    3. setup() 回调使用多路径查找 config.toml（resource_dir → current_dir → fallback）
    4. AppConfig 通过 app.manage() 注入
    5. Arc<RwLock<Registry>> 通过 app.manage() 注入（LLM 注册表）
    6. Arc<RwLock<PromptManager>> 通过 app.manage() 注入（Prompt 注册表）
    7. PromptManager::new() 接收 Arc<RwLock<PromptRegistry>>（已验证 API）
    8. invoke_handler 使用 register_all_commands!() 宏
    9. cargo check -p narratoai-app --lib 编译通过
  </acceptance_criteria>

  <done>
    Tauri Builder 完整就绪，支持配置加载、State 注入、插件和命令注册
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC (JS→Rust) | 前端 JS 通过 invoke() 调用 Tauri 命令，参数经 serde 反序列化后进入 Rust 命令函数 |
| Config State | AppConfig 存储在 Tauri State 中，通过 async command 的 State<T> 参数读取 |
| File Dialog | tauri-plugin-dialog 提供原生文件选择，路径字符串传入 Rust 命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information Disclosure | CommandError 序列化（message 字段含中文错误详情） | accept | message 保留中文错误信息 per D-18，不包含敏感数据（API key、token）；所有 From<T> 使用 err.to_string() 天然过滤了敏感内部细节 |
| T-10-06 | Tampering | PathBuf 参数注入（路径遍历） | mitigate | Tauri 2.0 的 serde 反序列化只接受字符串→PathBuf 转换；所有 I/O 操作由底层 std::fs/ffmpeg-sidecar 执行，受操作系统文件权限约束 |
| T-10-07 | Information Disclosure | Registry State 中的 API key（skip_serializing 字段） | accept | AppConfig 的 API key 字段有 #[serde(skip_serializing)]，序列化时空字段；命令从 RwLock 读取时直接使用而不序列化 |
| T-10-08 | Denial of Service | Progress 事件高频 emit | mitigate | 单用户桌面应用，流水线最多 9 步，步间有实际计算延迟，不会高频 emit；不使用 `tracing` 功能避免已知 bug #13234 |
| T-10-09 | Elevation of Privilege | 文件对话框返回的路径被用于文件写入 | mitigate | 对话框插件由操作系统校验用户选择，程序只写入用户确认的路径；tauri.conf.json/capabilities 中限制 fs scope |
</threat_model>

<verification>
1. `cargo check -p narratoai-app --lib` — Tauri lib 编译通过
2. `cargo check -p narratoai-core` — 库无回归
3. `cargo check --workspace` — 工作空间整体编译
4. grep 验证每个错误类型都有 From<T> 实现
5. grep 验证 13 个处理函数都在 register_all_commands!() 中列出
</verification>

<success_criteria>
- src-tauri/src/error.rs 定义 CommandError，含 8 个 From<T> 实现（7 个领域错误 + String）
- src-tauri/src/models/progress.rs 定义 ProgressPayload（D-10 10 字段）、CommandResponse（D-07）、StepDuration、ScriptInfo
- src-tauri/src/commands/pipeline.rs 实现 5 条流水线命令，均支持进度事件推送
- src-tauri/src/commands/script.rs 实现 7 条脚本管理命令
- src-tauri/src/commands/export.rs 实现 1 条导出命令
- register_all_commands!() 宏列出全部 13 个处理函数
- src-tauri/src/lib.rs 完成 Tauri Builder setup（config 加载、State 注入、插件注册）
- `cargo check -p narratoai-app --lib` 编译通过
- `cargo check --workspace` 全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/10-tauri-command-layer/10-02-SUMMARY.md`
</output>
