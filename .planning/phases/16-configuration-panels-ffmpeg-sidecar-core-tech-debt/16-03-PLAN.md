---
phase: 16-configuration-panels-ffmpeg-sidecar-core-tech-debt
plan: 03
type: execute
wave: 1
depends_on:
  - "16-01"
files_modified:
  - src/stores/llm.ts
  - src/stores/tts.ts
  - src/stores/bgm.ts
  - src/stores/export.ts
  - src/stores/mode.ts
  - src/composables/useConfig.ts
  - src/components/config/ProviderPresets.ts
  - src/types/index.ts
  - src/types/generated/AppConfig.ts
  - src/types/generated/AppSection.ts
  - src/types/generated/UiSection.ts
  - src/types/generated/ProxySection.ts
  - src/types/generated/FramesSection.ts
  - src/types/generated/AzureSection.ts
  - src/types/generated/TencentSection.ts
  - src/types/generated/SoulVoiceSection.ts
  - src/types/generated/TtsQwenSection.ts
  - src/types/generated/IndexTTS2Section.ts
  - src/types/generated/DoubaoTTSSection.ts
  - src/types/generated/AudioSection.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
user_setup: []

must_haves:
  truths:
    - "Pinia stores 扩展为完整字段（llm: vision+text 完整字段，tts: 7 引擎参数，bgm: 路径+模式，export: 路径+格式，mode: 模式参数）"
    - "useConfig composable 封装 get_config/save_config 调用"
    - "ProviderPresets 包含 11 个 LLM Provider 的推荐模型和默认 Base URL"
    - "ts-rs 生成类型从 narratoai-core 同步到 src/types/"
    - "AppConfig 相关 TypeScript 接口可在前端 import"
    - "mode store 扩展包含 params 对象（帧间隔/批处理/剧名/温度/片段数）"
  artifacts:
    - path: "src/stores/llm.ts"
      provides: "扩展的 LLM Store（完整字段+actions）"
      min_lines: 80
    - path: "src/stores/tts.ts"
      provides: "扩展的 TTS Store（7 引擎参数+voice列表缓存）"
      min_lines: 80
    - path: "src/composables/useConfig.ts"
      provides: "get_config/save_config 封装"
      min_lines: 60
    - path: "src/components/config/ProviderPresets.ts"
      provides: "Provider 推荐映射表"
      min_lines: 40
    - path: "src/types/index.ts"
      provides: "重新导出 generated 类型"
  key_links:
    - from: "useConfig.ts"
      to: "composables/useTauri.ts"
      via: "tauriInvoke('get_config') / tauriInvoke('save_config')"
      pattern: "tauriInvoke"
    - from: "stores/llm.ts"
      to: "types/AppConfig.ts"
      via: "AppConfig.app 字段映射"
---

<objective>
扩展 5 个 Pinia store 为完整字段实现 + 创建 useConfig.ts composable + ProviderPresets + 同步 ts-rs 生成类型

**Purpose:** 为 Wave 1 配置面板提供完整的状态管理层。stores 从骨架扩展为可存储/加载/重置完整配置字段的状态容器，useConfig.ts 提供与 Tauri 后端命令的接驳层。

**Output:** 可直接被面板组件调用的完整 Pinia stores + composable + 类型定义
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-RESEARCH.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-UI-SPEC.md

<interfaces>
**现有 Store 骨架:**

`src/stores/llm.ts` — visionConfig / textConfig（均为 {provider, model, apiKey, baseUrl}）+ updateVisionConfig / updateTextConfig

`src/stores/tts.ts` — engine (string) + engineConfigs (Record<string, any>) + setEngine / updateConfig

`src/stores/bgm.ts` — folder (string) + mode (BgmMode) + setFolder / setMode

`src/stores/export.ts` — outputDir (string) + format (string) + setOutputDir / setFormat

`src/stores/mode.ts` — currentMode + setMode（localStorage 持久化）

**ts-rs 已生成类型位置:** `narratoai-core/src/types/generated/`（需要复制到 `src/types/generated/`）

**tauriInvoke 封装:**
```typescript
export async function tauriInvoke<T>(command: string, args?: Record<string, unknown>): Promise<T>
```

**AppConfig 接口（从 ts-rs AppConfig.ts 推断）:**
```typescript
{ app: { vision_llm_provider, vision_openai_model_name, vision_openai_api_key, ... },
  ui: { tts_engine, edge_voice_name, edge_volume, ... },
  azure: { speech_key, speech_region },
  proxy: { http, https, enabled },
  frames: { frame_interval_input, vision_batch_size, ... },
  ... }
```

**ProviderPresets 结构（per UI-SPEC D-11 + RESEARCH.md Code Examples）:**
```typescript
const PROVIDER_PRESETS: Record<string, { recommendedModel: string; defaultBaseUrl: string }>
```

**D-31 data flow:** get_config 返回完整脱敏 AppConfig JSON → 前端解析分发到各 Pinia store → save_config 接收增量更新
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 同步 ts-rs 生成类型 + 创建 src/types/generated/ 目录</name>
  <files>
    src/types/index.ts
    src/types/generated/AppConfig.ts
    src/types/generated/AppSection.ts
    src/types/generated/UiSection.ts
    src/types/generated/ProxySection.ts
    src/types/generated/FramesSection.ts
    src/types/generated/AzureSection.ts
    src/types/generated/TencentSection.ts
    src/types/generated/SoulVoiceSection.ts
    src/types/generated/TtsQwenSection.ts
    src/types/generated/IndexTTS2Section.ts
    src/types/generated/DoubaoTTSSection.ts
    src/types/generated/AudioSection.ts
  </files>
  <read_first>
    narratoai-core/src/types/generated/ — ts-rs 已生成的 .ts 文件（路径相对于 narratoai-core crate）
    src/types/index.ts — 当前骨架类型文件
  </read_first>
  <action>
    Step 1 — 从 `narratoai-core/src/types/generated/` 复制全部 .ts 文件到 `src/types/generated/`：
    - AppConfig.ts, AppSection.ts, UiSection.ts, ProxySection.ts, FramesSection.ts
    - AzureSection.ts, TencentSection.ts, SoulVoiceSection.ts, TtsQwenSection.ts
    - IndexTTS2Section.ts, DoubaoTTSSection.ts, AudioSection.ts
    - 保持文件名和内容一致（import 相对路径需要调整以匹配新的目录）
    
    Step 2 — 修改 `src/types/generated/AppConfig.ts` 中的 import 路径：将 `./AppSection` 等 prefixes 改为 `./AppSection`（在子目录中应使用相对路径 `./`）。

    Step 3 — 更新 `src/types/index.ts`：
    - 添加 `export type { AppConfig } from './generated/AppConfig'`
    - 导出所有子类型：`export type { AppSection } from './generated/AppSection'` 等
    - 保留现有 VideoMeta, ProgressPayload, LLMConfig 接口不删除

    Step 4 — 运行 `npx vue-tsc --noEmit` 验证类型编译通过。
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `src/types/generated/` 包含所有 13 个 .ts 文件
    - `src/types/index.ts` 导出 AppConfig 及所有子类型
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>ts-rs 生成类型同步完成，前端可 import AppConfig</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 4 个配置 Pinia stores + mode store</name>
  <files>
    src/stores/llm.ts
    src/stores/tts.ts
    src/stores/bgm.ts
    src/stores/export.ts
    src/stores/mode.ts
  </files>
  <read_first>
    src/stores/llm.ts — 当前骨架（visionConfig + textConfig + 2 actions）
    src/stores/tts.ts — 当前骨架（engine + engineConfigs + 2 actions）
    src/stores/bgm.ts — 当前骨架（folder + mode + 2 actions）
    src/stores/export.ts — 当前骨架（outputDir + format + 2 actions）
    src/stores/mode.ts — 当前实现（currentMode + setMode + localStorage）
    src/stores/app.ts — 参考 localStorage 持久化模式
    src/types/generated/AppConfig.ts — AppConfig 接口
  </read_first>
  <action>
    **llm.ts 扩展（per D-09 ~ D-13）：**
    - 保留现有 visionConfig/textConfig（类型改为使用 LLMConfig）。
    - 添加 `loading` ref（boolean），`dirty` ref（boolean）。
    - 添加 action `async loadConfig(config: AppConfig)`: 从 get_config 返回的 AppConfig.app 中提取 vision_llm_provider, vision_openai_model_name, vision_openai_api_key, vision_openai_base_url 等字段填充 visionConfig/textConfig。需处理字段映射：后端字段名 `vision_llm_provider` → store 中 `visionConfig.provider`。
    - 将 AppSection 的字段映射进 visionConfig 和 textConfig。
    - 添加 action `async testConnection(which: 'vision' | 'text')`: 调用 tauriInvoke('test_llm_connection', { provider, model, apiKey, baseUrl })。
    - 添加 action `resetPanel(which: 'vision' | 'text')`: 设置为空字符串。
    - 添加 action `markClean()`: 设置 dirty=false。
    - 添加 action `markDirty()`: 设置 dirty=true。

    **tts.ts 扩展（per D-14 ~ D-15）：**
    - 保留现有 engine + engineConfigs。
    - 添加 `loading` ref，`voiceList` ref（Edge-TTS voice list 数组），`voicesLoading` ref。
    - 添加 `async loadConfig(config: AppConfig)`: 从 config.ui.tts_engine 设置 engine，从各引擎 section 设置 engineConfigs。
    - 定义明确的 EngineConfig 类型各引擎参数：edge（voiceName, volume, rate, pitch）、azure（voiceName, volume, rate, pitch, speechKey, speechRegion）、tencent（secretId, secretKey, region）、soulvoice（apiKey, voiceUri, apiUrl, model）、qwen（apiKey, apiUrl, modelName）、indextts2（apiUrl, referenceAudio, inferMode, temperature, topP, topK, doSample, numBeams）、doubao（ak, sk, appid, token, cluster, apiUrl, volume, pitch）。
    - 每个引擎参数从 AppConfig 对应 section 读取。
    - 添加 action `async loadVoices()`: 调用 tauriInvoke('get_edge_tts_voices') 缓存到 voiceList。
    - 添加 action `resetPanel()`: engineConfigs 全部清空。
    - 添加 `dirty` ref。

    **bgm.ts 扩展（per D-16 ~ D-17）：**
    - 保留 folder + mode (BgmMode)。
    - 添加 `audioFiles` ref（string[]），`loading` ref。
    - 添加 `async loadConfig(config: AppConfig)`: （BGM 配置不在当前 AppConfig 中——这是纯前端路径选择。设置默认空值，不做后端加载）。
    - 添加 action `resetPanel()`: folder='', mode='random', audioFiles=[]。
    - 添加 `dirty` ref。

    **export.ts 扩展（per D-16）：**
    - 保留 outputDir + format。
    - 添加 `loading` ref。
    - 添加 `async loadConfig(config: AppConfig)`: （Export 配置不在当前 AppConfig 中——这是运行时路径选择。设置默认空值）。
    - 添加 action `resetPanel()`: outputDir='', format='mp4'。
    - 添加 `dirty` ref（per D-21，用于"未保存的更改"检测）。

    **mode.ts 扩展（per CONF-05, RESEARCH.md Open Question 4）：**
    - 添加 `params` ref 对象：`{ frameInterval: number, visionBatchSize: number, dramaName: string, temperature: number, clipCount: number, minDuration: number, maxDuration: number }`
    - 添加 action `loadConfig(config: AppConfig)`: 从 config.frames 填充 frameInterval（frame_interval_input）、visionBatchSize（vision_batch_size）；dramaName/temperature/clipCount/minDuration/maxDuration 设置为默认值（这些不在 AppConfig 中——前端保持）。
    - 添加 action `resetParams()`: params 恢复默认值。
    - 设置默认值：frameInterval=3, visionBatchSize=10, dramaName='', temperature=1.0, clipCount=5, minDuration=2, maxDuration=10。
    - params 不持久化到 localStorage（草稿通过 useConfig.ts 管理）。

    所有 store 使用 Composition API 风格（`defineStore('name', () => { ... })`）。
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - llm.store 导出 visionConfig/textConfig（LLMConfig 类型）+ loadConfig/testConnection/resetPanel/markClean/markDirty
    - tts.store 导出 engine/engineConfigs/voiceList + loadConfig/loadVoices/resetPanel
    - bgm.store 导出 folder/mode/audioFiles + loadConfig/resetPanel
    - export.store 导出 outputDir/format + loadConfig/resetPanel
    - mode.store 导出 params（含 frameInterval/visionBatchSize/dramaName/temperature/clipCount/minDuration/maxDuration）+ loadConfig/resetParams
    - 所有 store 有 dirty ref
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>5 个 stores 扩展完成，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 useConfig.ts composable + ProviderPresets.ts</name>
  <files>
    src/composables/useConfig.ts
    src/components/config/ProviderPresets.ts
  </files>
  <read_first>
    src/composables/useTauri.ts — tauriInvoke 封装
    src/stores/app.ts — localStorage 持久化参考模式（watch + 立即写入）
    .planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md — D-31: get_config/save_config 数据流
    .planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-RESEARCH.md — ProviderPresets 代码示例
  </read_first>
  <action>
    **创建 `src/composables/useConfig.ts`**（per D-31）：
    - `loadFromBackend()` 函数：
      - 调用 `tauriInvoke<AppConfig>('get_config')` 获取配置
      - 从结果分发到各 store：`useLlmStore().loadConfig(config)`、`useTtsStore().loadConfig(config)`、`useModeStore().loadConfig(config)`
      - BGM 和 Export store 的 loadConfig 调用（不做后端加载，仅标记已加载）
      - 返回 AppConfig
      - 错误处理：try/catch，失败时 console.warn + 不影响 stores 现有状态
    - `saveToBackend(changes: Record<string, unknown>)` 函数：
      - 调用 `tauriInvoke<void>('save_config', changes)`
      - 返回 Promise<void>
    - `collectChangedFields()` 辅助函数：
      - 读取所有 store 的 dirty 状态，收集变更字段为扁平 JSON 对象
      - 用于 save_config 的增量更新参数
    - 返回 `{ loadFromBackend, saveToBackend, collectChangedFields }`

    **创建 `src/components/config/ProviderPresets.ts`**（per D-11 + RESEARCH.md Code Examples）：
    - 导出 `PROVIDER_PRESETS` 常量，类型为 `Record<string, { recommendedModel: string; defaultBaseUrl: string; label: string }>`
    - 包含以下 entry（label 为中文显示名，recommendedModel 为建议模型，defaultBaseUrl 为默认接口地址）：
      - openai: `推荐: gpt-4o`, `https://api.openai.com/v1`, OpenAI
      - deepseek: `推荐: deepseek-chat`, `https://api.deepseek.com/v1`, DeepSeek
      - gemini: `推荐: gemini-2.0-flash`, `https://generativelanguage.googleapis.com/v1beta/openai`, Gemini
      - qwen: `推荐: qwen-plus`, `https://dashscope.aliyuncs.com/compatible-mode/v1`, Qwen
      - siliconflow: `推荐: Qwen/Qwen3.5-122B-A10B`, `https://api.siliconflow.cn/v1`, SiliconFlow
      - moonshot: `推荐: moonshot-v1-8k`, `https://api.moonshot.cn/v1`, Moonshot
      - anthropic: `推荐: claude-sonnet-4-20250514`, `https://api.anthropic.com/v1`, Anthropic
      - cohere: `推荐: command-r-plus`, `https://api.cohere.com/v1`, Cohere
      - together: `推荐: meta-llama/Llama-4-17B`, `https://api.together.xyz/v1`, Together AI
      - openrouter: `推荐: openai/gpt-4o`, `https://openrouter.ai/api/v1`, OpenRouter
    - 导出 `PROVIDER_OPTIONS` 常量（v-select 直接使用的 items 数组），含 `{ title, value }` 格式，每个 preset 一个 + `{ title: '自定义', value: 'custom' }` 最后。
    - 导出 `getProviderPreset(provider: string)` 函数，返回对应 preset 或 null（custom 时）。
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `useConfig.ts` 导出 loadFromBackend（调用 get_config + 分发到 stores）、saveToBackend（调用 save_config + 增量更新）
    - `ProviderPresets.ts` 导出 PROVIDER_PRESETS（11 preset + label/recommendedModel/defaultBaseUrl）、PROVIDER_OPTIONS（12 项 v-select array）、getProviderPreset 函数
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>useConfig.ts + ProviderPresets.ts 创建完成，TypeScript 编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pinia state ← get_config IPC | 从后端读取配置到前端状态 |
| localStorage | 草稿数据明文存储 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Information Disclosure | ProviderPresets.ts 含默认 base URL | accept | URL 为公开 API 端点，非敏感信息 |
| T-16-09 | Information Disclosure | apiKey 在 Pinia store 内存中 | accept | D-24: 桌面单用户，安全风险低；get_config 返回已脱敏 key |
</threat_model>

<verification>
- `npx vue-tsc --noEmit` — TypeScript 编译通过
- 各 store 初始状态验证（手动检查 ref 默认值正确）
</verification>

<success_criteria>
1. 5 个 Pinia stores 扩展完成（llm/tts/bgm/export/mode），含完整字段和 actions
2. useConfig.ts 提供 loadFromBackend/saveToBackend 函数
3. ProviderPresets.ts 提供 11 个预设 Provider 的推荐映射
4. ts-rs 生成类型已从 narratoai-core 同步到 src/types/generated/
5. 类型通过 TypeScript 编译检查
</success_criteria>

<output>
完成后创建 `.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-03-SUMMARY.md`
</output>
