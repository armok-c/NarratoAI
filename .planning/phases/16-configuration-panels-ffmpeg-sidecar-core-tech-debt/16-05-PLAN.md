---
phase: 16-configuration-panels-ffmpeg-sidecar-core-tech-debt
plan: 05
type: execute
wave: 3
depends_on:
  - "16-04"
files_modified:
  - src/composables/useConfig.ts
  - src/App.vue
  - src/components/SettingsDrawer.vue
  - src/stores/llm.ts
  - src/stores/tts.ts
  - src/stores/bgm.ts
  - src/stores/export.ts
  - tests/stores/llm.test.ts
  - tests/stores/tts.test.ts
  - tests/composables/useConfig.test.ts
  - tests/components/SettingSection.test.ts
autonomous: true
requirements:
  - CONF-06
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
user_setup: []

must_haves:
  truths:
    - "SettingsDrawer 关闭时自动暂存未保存编辑到 localStorage('settingsDraft')"
    - "应用启动时（get_config 之后）自动从 localStorage 恢复草稿"
    - "全局保存成功后清除 localStorage 草稿"
    - "有草稿时 Footer 保存按钮旁显示'未保存的更改'橙色文字"
    - "保存时后端返回 N 项配置不完整 → 对应面板标题显示红色小圆点"
    - "模式切换时如果有未保存更改 → 弹出确认对话框"
    - "App.vue onMounted 中预加载 get_config() 到 Pinia stores"
    - "配置面板有 vitest 真实测试（store actions、panel rendering、draft save/restore）"
  artifacts:
    - path: "src/composables/useConfig.ts"
      provides: "完整 draft 管理（saveDraft/restoreDraft/clearDraft/hasDraft）+ 变更收集"
      min_lines: 120
    - path: "src/App.vue"
      provides: "get_config() 预加载 + 草稿恢复"
      contains: "loadFromBackend"
    - path: "tests/stores/llm.test.ts"
      provides: "LLM store 单元测试"
      min_lines: 30
    - path: "tests/composables/useConfig.test.ts"
      provides: "draft save/restore/clear 测试"
      min_lines: 30
  key_links:
    - from: "useConfig.ts"
      to: "localStorage('settingsDraft')"
      via: "setItem/getItem/removeItem"
      pattern: "settingsDraft"
    - from: "SettingsDrawer.vue"
      to: "useConfig.ts saveDraft"
      via: "watch 或 onBeforeUnmount"
    - from: "App.vue onMounted"
      to: "useConfig.ts loadFromBackend"
      via: "tauriInvoke('get_config')"
---

<objective>
完成 Local Draft 暂存策略 + 全局保存/验证/snackbar + 模式切换面板显隐 + 配置预加载 + vitest 测试

**Purpose:** CONF-06 Local Draft 模式、配置保存流、模式切换防丢失、启动预加载、所有配置面板的 vitest 测试。整合并精修所有 Wave 1/2 前端工作为完整体验。

**Output:** 功能完整的配置面板（保存/暂存/恢复/验证/重置）+ 覆盖核心逻辑的 vitest 测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-UI-SPEC.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-RESEARCH.md

<interfaces>
**useConfig.ts 当前状态（Plan 16-03 创建）:**
- loadFromBackend(): 调用 get_config → 分发到 stores
- saveToBackend(changes): 调用 save_config
- collectChangedFields(): 收集 dirty 字段

需要扩展：draft save/restore/clear/hasDraft, collectChangedFields → 完整配置聚合

**SettingsDrawer 当前状态（Plan 16-04 创建）:**
- 7 个面板 + Footer 含禁用状态的"保存设置"按钮 + "重置全部"链接 + 版本号
- 需要扩展：保存逻辑、draft 自动暂存、snackbar、dirty 检测、面板 badge、模式切换确认

**App.vue 当前状态:**
- 仅含 `<v-app><router-view /></v-app>` + 空的 `<script setup>`
- 需要扩展：onMounted 预加载 get_config + draft 恢复

**本地存储键名（D-21）:**
- `settingsDraft`: 草稿 JSON
- `settingsPanelStates`: 面板折叠状态（已在 Plan 16-04 实现）
- `lastMode`: 模式持久化（已在 mode.ts 实现）

**Draft JSON 结构（D-21）:**
```typescript
interface SettingsDraft {
  llmVision?: { provider, model, apiKey, baseUrl }
  llmText?: { provider, model, apiKey, baseUrl }
  tts?: { engine, engineConfigs }
  bgm?: { folder, mode }
  export?: { outputDir, format }
  modeParams?: { frameInterval, visionBatchSize, dramaName, temperature, clipCount, minDuration, maxDuration }
  networkProxy?: { enabled, http, https }
  updatedAt: string // ISO timestamp
}
```

**Test 框架: vitest 4.x + jsdom 环境**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 Local Draft 暂存 + App.vue 配置预加载</name>
  <files>
    src/composables/useConfig.ts
    src/App.vue
  </files>
  <read_first>
    src/composables/useConfig.ts — 当前 loadFromBackend / saveToBackend / collectChangedFields 实现
    src/App.vue — 当前根组件（仅含 <v-app><router-view /></v-app> + 空 script setup）
    src/stores/app.ts — localStorage 持久化参考模式
    .planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md — D-20 ~ D-24 draft 策略
  </read_first>
  <action>
    **扩展 `src/composables/useConfig.ts`**（per D-20 ~ D-24）:

    添加导出函数：
    1. `saveDraft()`:
       - 读取所有 store 当前值：useLlmStore().visionConfig/textConfig、useTtsStore().engine/engineConfigs、useBgmStore().folder/mode、useExportStore().outputDir/format、useModeStore().params、networkProxy from get_config proxy
       - 序列化为 SettingsDraft 对象，含 `updatedAt: new Date().toISOString()`
       - localStorage.setItem('settingsDraft', JSON.stringify(draft))
       - try/catch 静默处理 localStorage 满/禁用（D-24 静默降级）

    2. `restoreDraft()`:
       - localStorage.getItem('settingsDraft') → JSON.parse
       - 如果 parse 失败 → 静默跳过（D-24）
       - 如果 draft 不存在 → 无操作
       - 如果 draft.updatedAt 存在 → 覆盖到各 Pinia stores
       - 设置各 store 的 dirty ref 为 true
       - 返回 boolean（是否有草稿恢复）

    3. `clearDraft()`:
       - localStorage.removeItem('settingsDraft')
       - 所有 store 设置 dirty=false（markClean）

    4. `hasDraft()`:
       - return localStorage.getItem('settingsDraft') !== null

    5. 扩展 `collectChangedFields()` → `collectAllConfig()`:
       - 除收集 dirty 字段外，聚合所有配置为扁平 JSON
       - 将 store 字段名映射到后端 AppConfig 字段名（如 visionConfig.provider → vision_llm_provider）

    6. 添加 `draftStatus` ref: `{ hasDraft: boolean, updatedAt: string | null }`

    **扩展 `src/App.vue`**（per D-32）:
    - 在 `<script setup>` 中添加 `onMounted(async () => { ... })`
    - 调用 `loadFromBackend()` 从后端加载配置
    - 调用 `restoreDraft()` 恢复草稿（草稿覆盖显示 per D-22）
    - 如果未恢复成功（无草稿），不做额外操作
    - 加载失败：console.warn("配置加载失败，" + error)，不影响应用启动
    - 加载成功：设置 loaded=true（供子组件判断）
    - 使用 try/catch 包装所有操作，确保应用不因配置加载失败而崩溃
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - useConfig.ts 导出 saveDraft() / restoreDraft() / clearDraft() / hasDraft() / collectAllConfig()
    - saveDraft 写入 localStorage('settingsDraft') JSON
    - restoreDraft 从 localStorage 读取并覆盖 Pinia stores
    - clearDraft 移除 localStorage key + 重置 dirty 标志
    - App.vue onMounted 调用 loadFromBackend + restoreDraft
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>Draft 管理 + 配置预加载实现，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 全局保存/验证/snackbar + 模式切换面板显隐 + 重置逻辑</name>
  <files>
    src/components/SettingsDrawer.vue
  </files>
  <read_first>
    src/components/SettingsDrawer.vue — 当前手风琴布局 + 7 面板 + Footer 骨架
    src/composables/useConfig.ts — Task 1 扩展后的 draft/collEct API
    src/stores/llm.ts — dirty, markClean, resetPanel
    src/stores/tts.ts — dirty, resetPanel
    src/stores/bgm.ts — dirty, resetPanel
    src/stores/export.ts — dirty, resetPanel
    src/stores/mode.ts — params, resetParams
    .planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md — D-04 ~ D-06, D-08, D-18 保存流决策
    .planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-UI-SPEC.md — Section 5.2 Save Flow + Section 5.4 Draft Flow
  </read_first>
  <action>
    在 SettingsDrawer.vue 中添加以下功能和交互逻辑：

    **Step 1 — 保存状态和 dirty 检测:**
    - 添加 `isSaving` ref（保存中 loading 状态）
    - 添加 `hasUnsavedChanges` computed（遍历所有 store.dirty，如果任一为 true → true）
    - 添加 `panelErrors` ref（`Record<string, string[]>` 追踪各面板验证错误）

    **Step 2 — 全局保存按钮**（per D-04 ~ D-05, D-18）:
    - 将 Footer 中"保存设置"按钮的 `:disabled` 绑定为 `!hasUnsavedChanges`
    - 添加 `:loading="isSaving"`
    - 添加 `@click="handleSave"`
    - `handleSave()`:
      1. 设置 isSaving=true
      2. 调用 `collectAllConfig()` 聚合所有字段
      3. 调用 `saveToBackend(changes)` 发送增量更新
      4. 成功：
         - 调用 `clearDraft()`
         - 设置所有 store dirty=false
         - 显示 v-snackbar "设置已保存"（color="success", 3s, top）
      5. 失败（如后端返回验证错误）：
         - 解析错误 JSON 获取缺失/无效字段列表
         - 设置 panelErrors（如 {llmVision: ['API Key 为空']}）
         - 对应 SettingSection header badge 显示红色小圆点
         - 显示 snackbar "N 项配置不完整"（color="warning", 4s, top）
      6. 失败（网络/服务器错误）：
         - 显示 snackbar "配置保存失败: {error}"（color="error", 5s, top）
      7. 始终：isSaving=false

    **Step 3 — 未保存更改指示器**（per D-21）:
    - Footer 中保存按钮旁添加 `span.text-body-2.text-warning` "未保存的更改"
    - v-if="hasUnsavedChanges"

    **Step 4 — 草稿自动暂存**（per D-20）:
    - SettingsDrawer 关闭时触发（@update:model-value 事件，值从 true → false 时）
    - 调用 `saveDraft()` 自动暂存
    - 使用 `watch(() => props.modelValue, (newVal, oldVal) => { if (!newVal && hasUnsavedChanges) saveDraft() })`

    **Step 5 — 面板重置逻辑**（per D-06, D-23）:
    - Footer "重置全部"链接 @click="handleResetAll"
    - `handleResetAll()`:
      1. 弹出确认对话框（使用 Vuetify v-dialog 或 `confirm()`）
      2. 确认 → 调用所有 store 的 resetPanel/resetParams + clearDraft()
      3. 取消 → 无操作
    - per-panel 重置按钮已存在于各面板 header-actions，点击调用对应 store.resetPanel()
    - per-panel 重置不修改草稿（D-23）

    **Step 6 — 模式切换未保存检测**（per D-08）:
    - 通过 watcher 监听 modeStore.currentMode 变化
    - 如果 hasUnsavedChanges && 模式要切换：
      - 弹出确认对话框 "未保存的更改将丢失，是否继续？"
      - 确认 → 允许切换（调用 modeStore.setMode）
      - 取消 → 恢复当前模式（revert modeStore.currentMode）

    **Step 7 — 面板验证圆点 badge**:
    - 遍历 panelErrors，对每个 SettingSection 的 header 添加 `v-badge dot color="error"`
    - 使用 SettingSection 的 header-actions slot 或替换现有图标区域
    - Badge 显示错误数（数字 badge 或红点）

    **Step 8 — 冲突检测**（per D-22）:
    - 如果 save_config 返回 conflict 错误（配置文件被外部修改）：
      - 弹出确认对话框 "配置文件已被外部修改，是否覆盖？"
      - 确认 → 重新调用 save_config（带 force 标志或直接覆盖）
      - 取消 → 关闭对话框，不做任何保存

    导入所需的 composables 和 stores：
    ```typescript
    import { useConfig } from '@/composables/useConfig'
    import { useLlmStore } from '@/stores/llm'
    import { useTtsStore } from '@/stores/tts'
    import { useBgmStore } from '@/stores/bgm'
    import { useExportStore } from '@/stores/export'
    import { useModeStore } from '@/stores/mode'
    ```
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - "保存设置"按钮绑定 handleSave，调用 saveToBackend + draft 清理
    - 保存成功显示绿色 snackbar "设置已保存"（3s）
    - 验证错误显示橙色 snackbar + 面板 badge 红点
    - 保存失败显示红色 snackbar（5s）
    - "未保存的更改"橙色文字在 hasUnsavedChanges 时显示
    - 抽屉关闭时自动调用 saveDraft()
    - "重置全部"弹出确认对话框
    - 模式切换时未保存更改弹出确认对话框
    - 后端冲突时弹出覆盖确认对话框
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>全局保存/验证/snackbar + draft 暂存 + 模式切换保护实现</done>
</task>

<task type="auto">
  <name>Task 3: 配置面板 vitest 测试</name>
  <files>
    tests/stores/llm.test.ts
    tests/stores/tts.test.ts
    tests/composables/useConfig.test.ts
    tests/components/SettingSection.test.ts
  </files>
  <read_first>
    tests/useTauri.test.ts — 现有 vitest 测试参考（结构、mock 模式）
    vitest.config.ts — vitest 配置
  </read_first>
  <action>
    创建真实的 vitest 测试（per D-35），替换 Phase 15 创建的测试存根。

    **`tests/stores/llm.test.ts`**（CONF-01）：
    - 测试 store 初始状态（visionConfig 和 textConfig 均为空初始值）
    - 测试 updateVisionConfig 修改字段
    - 测试 resetPanel 清空字段
    - 使用 `describe`/`it`/`expect` 结构
    - Pinia 测试需要创建 pinia 实例：`import { createPinia, setActivePinia } from 'pinia'` + `beforeEach(() => setActivePinia(createPinia()))`

    **`tests/stores/tts.test.ts`**（CONF-02）：
    - 测试 engine 初始值 'edge_tts'
    - 测试 setEngine 切换引擎
    - 测试 updateConfig 保存/恢复各引擎参数

    **`tests/composables/useConfig.test.ts`**（CONF-06）：
    - 测试 saveDraft 写入 localStorage
    - 测试 restoreDraft 从 localStorage 读取
    - 测试 clearDraft 清除 localStorage
    - 测试 draft 恢复后覆盖 store 值
    - 使用 `beforeEach` 清理 localStorage + pinia 状态

    **`tests/components/SettingSection.test.ts`**：
    - 替换 Phase 15 创建的 8 行测试存根
    - 测试 SettingSection 渲染 title 和 icon
    - 测试 collapsible 模式的展开/折叠行为
    - 测试 loading 状态显示进度条
    - 使用 `mount` from `@vue/test-utils` + jsdom

    现有测试存根文件位于 `src/components/__tests__/` 和 `src/stores/__tests__/` 和 `src/layouts/__tests__/` 和 `src/views/__tests__/`，其中 Phase 15 创建的 8 个存根各有 8 行。本 task 创建的测试文件放在 `tests/` 目录（项目级测试目录），不修改 Phase 15 的测试存根（保留原存根）。

    运行 `npx vitest run` 确认新测试通过，不破坏现有测试。
  </action>
  <verify>
    <automated>npx vitest run tests/stores/llm.test.ts tests/stores/tts.test.ts tests/composables/useConfig.test.ts tests/components/SettingSection.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - llm.test.ts 通过（最小 3 个 test cases）
    - tts.test.ts 通过（最小 3 个 test cases）
    - useConfig.test.ts 通过（最小 3 个 test cases）
    - SettingSection.test.ts 通过（替换存根为真实测试）
    - `npx vitest run` 不破坏现有测试
  </acceptance_criteria>
  <done>4 个测试文件创建并全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage ←→ Pinia state | 草稿数据在存储和状态间流转 |
| IPC save_config → config.toml | 增量更新写入配置文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Information Disclosure | localStorage draft 明文存储 | accept | D-24: 桌面单用户环境风险低 |
| T-16-14 | Data Loss | 模式切换未保存丢失 | mitigate | D-08: 切换时确认对话框保护未保存编辑 |
| T-16-15 | Data Loss | 外部修改冲突 | mitigate | D-22: save_config 冲突检测 + 覆盖确认对话框 |
| T-16-16 | Tampering | localStorage 数据损坏 | mitigate | D-24: 恢复失败静默跳过，try/catch 包裹 JSON.parse |
</threat_model>

<verification>
- `npx vue-tsc --noEmit` — TypeScript 编译通过
- `npx vitest run` — 新测试全部通过
- SettingsDrawer 交互流整体检查
</verification>

<success_criteria>
1. Local Draft 暂存/恢复/清除完整生命周期
2. 全局保存调用 save_config + 成功后清除草稿 + snackbar 反馈
3. 验证错误时面板标题显示红色 badge
4. "未保存的更改"可见性控制
5. 模式切换未保存更改确认对话框
6. 配置预加载（App.vue onMounted 调用 get_config + 草稿恢复）
7. 4 个测试文件覆盖 store/composable/component 核心逻辑
</success_criteria>

<output>
完成后创建 `.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-05-SUMMARY.md`
</output>
