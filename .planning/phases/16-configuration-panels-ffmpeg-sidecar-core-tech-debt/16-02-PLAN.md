---
phase: 16-configuration-panels-ffmpeg-sidecar-core-tech-debt
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - narratoai-core/src/tts/common.rs
  - narratoai-core/src/tts/edge_tts.rs
  - narratoai-core/src/visual/analyzer.rs
  - app/services/documentary/pipeline.py
  - app/services/SDE/pipeline.py
  - app/services/SDP/pipeline.py
autonomous: true
requirements:
  - TDET-01
  - TDET-06
user_setup: []

must_haves:
  truths:
    - "Edge-TTS 时钟校准 HTTP 请求可通过代理（当 proxy.enabled 时）"
    - "Edge-TTS voice list 可通过代理获取"
    - "代理配置从 ProxyConfig::from_env() 自动检测环境变量"
    - "Documentary/SDE/SDP pipeline 中所有视觉分析调用均通过 analyze_video_frames() 门面"
    - "视觉分析的 prompt template 从 PromptRegistry 加载而非硬编码"
  artifacts:
    - path: "narratoai-core/src/tts/common.rs"
      provides: "ProxyConfig::from_env() 方法"
      contains: "fn from_env"
    - path: "narratoai-core/src/tts/edge_tts.rs"
      provides: "代理感知的 correct_clock_skew + get_voices"
      min_lines: 550
    - path: "app/services/documentary/pipeline.py"
      provides: "验证后的视觉分析调用路径"
  key_links:
    - from: "edge_tts.rs correct_clock_skew"
      to: "ProxyConfig::from_proxy"
      via: "apply_proxy_to_client"
      pattern: "ProxyConfig"
    - from: "app/services/documentary/pipeline.py"
      to: "analyzer.rs analyze_video_frames"
      via: "verify call path"
---

<objective>
补齐 Edge-TTS 代理隧道支持（时钟校准 + voice list）+ ProxyConfig::from_env() + VisualAnalyzer 门面审查

**Purpose:** TDET-01 — Edge-TTS 在代理网络环境下可正常生成音频（HTTP CONNECT 隧道 + 平台代理自动检测）。TDET-06 — 所有 pipeline 中视觉分析调用统一走 analyze_video_frames() 门面，prompt template 从 PromptRegistry 加载。

**Output:** 代理感知的 edge_tts.rs 引擎 + 环境变量代理检测 + 已验证的 VisualAnalyzer 调用路径
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-RESEARCH.md

<interfaces>
**ProxyConfig (narratoai-core/src/tts/common.rs:7-11):**
```rust
pub struct ProxyConfig {
    pub http: String,
    pub https: String,
    pub enabled: bool,
}
```
当前方法：`from_proxy(proxy: Option<&ProxySection>) -> Self`、`apply_to_client(&self, builder: ClientBuilder) -> ClientBuilder`

**EdgeTTSEngine 当前代理字段（edge_tts.rs 构造函数附近）:**
```rust
// EdgeTTSEngine 结构体包含:
// proxy_enabled: bool
// proxy_http: String
// proxy_https: String
```
WebSocket CONNECT 代理已实现于 connect()（第 269-420 行），但：
- correct_clock_skew()（第 470-508 行）使用 reqwest::Client::new() 无代理
- voice list API 未实现（当前没有从 Edge-TTS 获取 voice list 的函数）

**analyze_video_frames() 门面 (narratoai-core/src/visual/analyzer.rs):**
- 签名: `pub async fn analyze_video_frames(video_path, output_dir, llm_provider, prompt_template, batch_size, max_concurrency, interval_seconds, quality, progress, cancel) -> Result<BatchAnalysisResult, VisualError>`
- 已通过`prompt_template` 参数传入 prompt，从外部调用者处获得

**pipeline Python 文件位置:**
- `app/services/documentary/` — Documentary pipeline
- `app/services/SDE/` — SDE pipeline  
- `app/services/SDP/` — SDP pipeline
- 这些是 Python/旧版文件，审查从中提取的视觉分析调用模式是否对应 Rust analyze_video_frames()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 ProxyConfig 中添加 from_env() 环境变量检测方法</name>
  <files>narratoai-core/src/tts/common.rs</files>
  <read_first>
    narratoai-core/src/tts/common.rs — 当前 ProxyConfig 结构体定义（第 7-11 行）+ from_proxy 方法（第 14-28 行）+ apply_to_client（第 31-47 行）
  </read_first>
  <action>
    在 ProxyConfig impl 块中添加 `pub fn from_env() -> Self` 方法（在 `from_proxy` 之后）：

    逻辑：
    - 检测环境变量（按优先级）：`HTTPS_PROXY` > `http_proxy` > `http_proxy` > `HTTP_PROXY` > `ALL_PROXY` > `all_proxy`
    - 使用 `std::env::var()` 读取，大小写不敏感（先检查大写 HTTPS_PROXY，再检查小写 https_proxy）
    - 将检测到的环境变量值填入 http/https 字段
    - `enabled` 设为 true（只要任一代理环境变量存在）
    - 不覆盖已有值（from_env 仅从环境变量读取，不合并 config.toml）

    在 from_proxy 方法中添加对 env var 的回退逻辑：
    - 如果 proxy section 存在且 http/https 非空 → 使用 config.toml 值（手动覆盖优先）
    - 如果 proxy section 不存在或 http/https 为空 → 从环境变量检测
    - 该逻辑可使 get_system_proxy 命令同时返回 config.toml 和 env var 代理信息

    在文件底部现有 tests 模块中添加两个测试：
    - `test_from_env_with_https_proxy` — 设置 HTTPS_PROXY 环境变量后验证
    - `test_from_env_no_env_vars` — 清除环境变量后验证返回空字符串

    使用临时环境变量模式（set_var 后恢复）以确保测试隔离。注意：`from_env()` 的测试需要在测试中手动设置 env vars。

    ---
    所有环境变量操作使用 `std::env::set_var`/`remove_var`，测试应在临界区前后保存/恢复原始值（`temp_env::with_var` 或在 test 中使用手动 set_var + 测试完成后 cleanup）。
  </action>
  <verify>
    <automated>cd narratoai-core && cargo test -p narratoai-core -- tts::common::tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ProxyConfig::from_env()` 存在并返回正确的 ProxyConfig
    - 环境变量 HTTPS_PROXY 被正确检测（http 和 https 字段按预期填充）
    - 无环境变量时返回 enabled=false 的 ProxyConfig
    - 已有测试全部通过
  </acceptance_criteria>
  <done>ProxyConfig::from_env() 实现并通过测试</done>
</task>

<task type="auto">
  <name>Task 2: 补齐 Edge-TTS 代理感知 — 时钟校准 + voice list</name>
  <files>narratoai-core/src/tts/edge_tts.rs</files>
  <read_first>
    narratoai-core/src/tts/edge_tts.rs — 现有 correct_clock_skew() 方法（第 470-508 行），使用 reqwest::Client::new()
    narratoai-core/src/tts/common.rs — ProxyConfig 结构体和 build_client() 函数
    narratoai-core/src/tts/edge_tts.rs — EdgeTTSEngine 定义（开头约 60-100 行），proxy_enabled, proxy_http, proxy_https 字段
  </read_first>
  <action>
    Step 1 — 在 EdgeTTSEngine 中添加辅助方法 `fn build_http_client(&self) -> reqwest::Client`：
    - 如果 self.proxy_enabled，使用 ProxyConfig { http: self.proxy_http.clone(), https: self.proxy_https.clone(), enabled: true }.apply_to_client(reqwest::ClientBuilder::new())
    - 否则使用 reqwest::Client::new()
    - timeout 设 10 秒

    Step 2 — 修改 `correct_clock_skew()` 方法：
    - 将 `let client = reqwest::Client::builder()...build()...` 替换为 `let client = self.build_http_client()`
    - 其余逻辑不变（GET 请求 + Date header 解析 + CLOCK_SKEW_MICROS 更新）
    - 确保导入 `build_user_agent()`（已在文件顶部定义）

    Step 3 — 在 EdgeTTSEngine impl 块中新增 `pub async fn get_voices(&self) -> Result<serde_json::Value, TTSError>` 方法：
    - 使用 self.build_http_client() 创建客户端
    - 计算 `Sec-MS-GEC` 和 `muid`（复用现有 generate_sec_ms_gec() 和 generate_muid()）
    - GET `https://speech.platform.bing.com/consumer/speech/synthesize/readaloud/voices/list?trustedclienttoken={TRUSTED_CLIENT_TOKEN}&Sec-MS-GEC={sec_ms_gec}&Sec-MS-GEC-Version={SEC_MS_GEC_VERSION}`
    - 添加 Header: `User-Agent: build_user_agent()`, `Cookie: muid={muid};`, `Accept-Encoding: gzip, deflate, br, zstd`
    - timeout 30 秒
    - 返回 `Result<serde_json::Value, TTSError>`（原始 JSON 数组反序列化）

    注意：VoiceInfo 结构体可能不存在于当前代码中。直接返回 serde_json::Value 给 Tauri 命令层，让前端根据需要解析。

    保留现有 connect() 方法的所有代理逻辑不变（已实现 WebSocket CONNECT tunnel）。
  </action>
  <verify>
    <automated>cd narratoai-core && cargo test -p narratoai-core -- tts::edge_tts::tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - correct_clock_skew() 使用代理感知客户端（当 proxy_enabled=true）
    - get_voices() 方法存在，使用代理感知客户端
    - get_voices() 请求构造遵循 Edge-TTS voice list API 协议
    - 现有测试全部通过
  </acceptance_criteria>
  <done>Edge-TTS 时钟校准和 voice list 支持代理隧道</done>
</task>

<task type="auto">
  <name>Task 3: VisualAnalyzer 门面调用审查（TDET-06）</name>
  <files>
    narratoai-core/src/visual/analyzer.rs
  </files>
  <read_first>
    narratoai-core/src/visual/analyzer.rs — analyze_video_frames() 完整实现（第 86-259 行）
    app/services/documentary/pipeline.py — Documentary pipeline 中的视觉分析调用
    app/services/SDE/pipeline.py — SDE pipeline 中的视觉分析调用
    app/services/SDP/pipeline.py — SDP pipeline 中的视觉分析调用
  </read_first>
  <action>
    审查目标（per D-28）：不新增功能，不新增 Tauri 命令。仅审查调用路径。

    Step 1 — 审阅 `narratoai-core/src/visual/analyzer.rs` 确认 `analyze_video_frames()` 已是统一门面（已验证通过 prompt_template 参数接收 prompt，不内嵌 prompt 内容）。

    Step 2 — 审阅 Python pipeline 文件（app/services/documentary/pipeline.py, app/services/SDE/pipeline.py, app/services/SDP/pipeline.py），检查视觉分析调用路径：
    - 这些是旧版 Python 文件，v1.0 Rust 重写后 pipeline 走 Rust cmd 层
    - 在 Rust pipeline 命令中（src-tauri/src/commands/pipeline.rs），确认视觉分析调用通过 `analyze_video_frames()` 门面
    - 在 pipeline 命令中找到 `prompt_template` 的来源，确认它通过 PromptRegistry 加载而非硬编码

    Step 3 — 在 `narratoai-core/src/visual/analyzer.rs` 底部或测试模块中添加注释形式的调用路径文档（不修改任何运行时逻辑）：
    ```
    // ===== VisualAnalyzer 门面调用路径审查 (TDET-06, 2026-05-13) =====
    // 入口: analyze_video_frames()
    // 调用者:
    //   - src-tauri/src/commands/pipeline.rs 中 documentary/sde/sdp 流程
    // 调用方式:
    //   - 通过 LlmProvider trait 传入 llm_provider
    //   - 通过 prompt_template 参数接收 prompt（来源: PromptRegistry）
    // 结论:
    //   - 所有视觉分析调用已统一走此门面 ✓
    //   - prompt 通过外部参数传入，非硬编码 ✓
    ```

    Step 4 — 如果发现任何调用绕过 analyze_video_frames() 直接调用帧提取或 LLM analyze_images，在 $phase_dir/16-VERIFICATION.md 中记录问题。
  </action>
  <verify>
    <automated>cd narratoai-core && cargo test -p narratoai-core -- visual::analyzer::tests 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 审查确认所有视觉分析调用通过 analyze_video_frames() 门面
    - 审查确认 prompt template 从 PromptRegistry 加载
    - 发现偏差记录到注释（不修改运行时逻辑）
    - 现有测试全部通过
  </acceptance_criteria>
  <done>VisualAnalyzer 门面调用路径审查完成，调用路径文档化</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge-TTS → bing.com API | outbound HTTP/WS to speech.platform.bing.com |
| TLS tunnel → proxy server | 代理可能 inspect/modify traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | ProxyConfig.env var logging | mitigate | 代理地址在非 DEV 模式下不写入 debug 日志 |
| T-16-06 | Denial of Service | Edge-TTS proxy timeout | mitigate | get_voices 设 30s timeout（D-27），connect() 已有 30s CONNECT_TIMEOUT |
| T-16-07 | Tampering | ProxyConfig env var injection | accept | 桌面单用户应用，环境变量源于用户自己的系统配置，非攻击向量 |
</threat_model>

<verification>
- `cargo test -p narratoai-core -- tts::common::tests` — 新增 from_env 测试通过
- `cargo test -p narratoai-core -- tts::edge_tts::tests` — 现有测试通过
- `cargo test -p narratoai-core -- visual::analyzer::tests` — 现有测试通过
- `cargo check -p narratoai-core` — 无错误
</verification>

<success_criteria>
1. ProxyConfig::from_env() 检测 HTTPS_PROXY/HTTP_PROXY/ALL_PROXY 环境变量
2. Edge-TTS correct_clock_skew() 在 proxy_enabled 时走代理
3. Edge-TTS get_voices() 方法支持代理
4. VisualAnalyzer 调用路径审查完成，确认所有调用通过 analyze_video_frames() 门面
5. 现有测试全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-02-SUMMARY.md`
</output>
