---
phase: 16-configuration-panels-ffmpeg-sidecar-core-tech-debt
plan: 04
type: execute
wave: 2
depends_on:
  - "16-03"
files_modified:
  - src/components/SettingsDrawer.vue
  - src/components/config/LlmVisionPanel.vue
  - src/components/config/LlmTextPanel.vue
  - src/components/config/TtsPanel.vue
  - src/components/config/BgmPanel.vue
  - src/components/config/ExportPanel.vue
  - src/components/config/ModeParamsPanel.vue
  - src/components/config/NetworkProxyPanel.vue
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - MODE-03
user_setup: []

must_haves:
  truths:
    - "SettingsDrawer 显示 7 个 SettingSection 面板（LLM Vision/Text/TTS/BGM/Export/NetworkProxy/ModeParams）"
    - "每个 LLM 面板含 4 字段（Provider/Model/API Key/Base URL）+ 测试连接按钮"
    - "TTS 面板含引擎下拉 + 动态参数 + Edge-TTS voice 加载"
    - "BGM 面板含文件夹选择 + 随机/指定切换"
    - "Export 面板含输出目录选择 + 格式选择"
    - "模式参数面板根据 ModeStore.currentMode 动态显示不同字段"
    - "网络代理面板含启用开关 + HTTP/HTTPS 地址字段"
    - "API Key 字段使用 type=password + 显示/隐藏切换"
    - "LLM Vision 面板仅 Documentary 模式可见，TTS 面板在 SDP 模式隐藏"
  artifacts:
    - path: "src/components/SettingsDrawer.vue"
      provides: "带 7 个面板的手风琴布局 + Footer 保存/重置"
      min_lines: 200
    - path: "src/components/config/LlmVisionPanel.vue"
      provides: "Vision LLM 配置组件"
      min_lines: 80
    - path: "src/components/config/LlmTextPanel.vue"
      provides: "Text LLM 配置组件"
      min_lines: 80
    - path: "src/components/config/TtsPanel.vue"
      provides: "TTS 7 引擎配置组件"
      min_lines: 120
    - path: "src/components/config/BgmPanel.vue"
      provides: "BGM 配置组件（含文件夹选择）"
      min_lines: 60
    - path: "src/components/config/ExportPanel.vue"
      provides: "Export 配置组件（含文件夹选择）"
      min_lines: 60
    - path: "src/components/config/ModeParamsPanel.vue"
      provides: "模式专用参数动态组件"
      min_lines: 70
    - path: "src/components/config/NetworkProxyPanel.vue"
      provides: "网络代理配置组件（含启用开关）"
      min_lines: 60
  key_links:
    - from: "SettingsDrawer.vue"
      to: "各 config/*Panel.vue 组件"
      via: "import + 在 Content 区渲染"
---

<objective>
重写 SettingsDrawer.vue 手风琴布局 + 创建 7 个独立配置面板组件

**Purpose:** 完整的配置面板 UI — 用户可通过 SettingsDrawer 配置 LLM 双模型（CONF-01）、TTS 引擎（CONF-02）、BGM（CONF-03）、Export（CONF-04）、模式专用参数（CONF-05）、网络代理。遵循 D-01 至 D-19 全部决策。

**Output:** 可直接使用的 SettingsDrawer（含 7 个配置面板）+ 面板折叠状态持久化
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-CONTEXT.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-UI-SPEC.md
@.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-debt/16-RESEARCH.md

<interfaces>
**现有 SettingSection 组件 props（已验证于 src/components/SettingSection.vue）:**
```typescript
interface SettingSectionProps {
  title: string
  icon?: string
  collapsible?: boolean
  defaultExpanded?: boolean
  variant?: 'outlined' | 'flat' | 'elevated'
  loading?: boolean
}
```
Slots: default（内容区），`#header-actions`（header 右侧操作）

**现有 SettingsDrawer 结构（已验证）:**
- template: v-navigation-drawer (modelValue + right + temporary + 420px) → Header div → Content div → Footer div
- Content 当前仅含 1 个外观 SettingSection
- Footer 当前仅版本号

**Store 接口（Plan 16-03 之后）:**
- `useLlmStore()` → visionConfig, textConfig, loading, dirty, testConnection, resetPanel
- `useTtsStore()` → engine, engineConfigs, voiceList, loading, voicesLoading, loadVoices
- `useBgmStore()` → folder, mode, audioFiles
- `useExportStore()` → outputDir, format
- `useModeStore()` → currentMode, params
- `useConfig()` composable → loadFromBackend, saveToBackend

**面板排列顺序（D-03）:**
LLM Vision → LLM Text → TTS → BGM → Export → 网络代理 → 模式专用参数

**面板图标（D-07）:**
mdi-robot / mdi-chat-processing / mdi-text-to-speech / mdi-music-note / mdi-file-export / mdi-earth / mdi-tune
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 LLM 双模型面板（LlmVisionPanel + LlmTextPanel）+ ProviderPresets 使用</name>
  <files>
    src/components/config/LlmVisionPanel.vue
    src/components/config/LlmTextPanel.vue
  </files>
  <read_first>
    src/components/SettingSection.vue — SettingSection 组件接口（props: title, icon, collapsible, defaultExpanded, variant, loading; slots: default, header-actions）
    src/stores/llm.ts — llm store 接口（visionConfig, textConfig, testConnection, resetPanel）
    src/components/config/ProviderPresets.ts — PROVIDER_OPTIONS, getProviderPreset
    src/composables/useTauri.ts — tauriInvoke 函数
  </read_first>
  <action>
    两个面板结构完全相同，分别绑定到 visionConfig 和 textConfig。创建为独立组件文件（per D-09）。

    **模板结构（两个面板一致，仅 store 绑定区分）：**
    ```vue
    <SettingSection
      :icon="icon" :title="title" collapsible
      :default-expanded="isFirst" :loading="loading"
    >
      <template #header-actions>
        <v-btn variant="text" size="small" @click="resetPanel">重置</v-btn>
      </template>
      <template #default>
        <v-select v-model="config.provider" :items="PROVIDER_OPTIONS" label="提供商" variant="outlined" density="compact" />
        <div v-if="modelHint" class="text-caption text-medium-emphasis mt-1">{{ modelHint }}</div>
        <v-text-field v-model="config.model" label="模型" variant="outlined" density="compact" />
        <v-text-field v-model="config.apiKey" :type="showKey ? 'text' : 'password'" label="API 密钥" variant="outlined" density="compact" :append-inner-icon="showKey ? 'mdi-eye-off' : 'mdi-eye'" @click:append-inner="showKey = !showKey" />
        <v-text-field v-model="config.baseUrl" label="接口地址" placeholder="留空使用默认" variant="outlined" density="compact" />
        <v-btn variant="outlined" size="small" class="mt-2" :loading="testing" @click="handleTestConnection">测试连接</v-btn>
      </template>
    </SettingSection>
    ```

    **逻辑：**
    - 读取 `useLlmStore()` 中的 visionConfig/textConfig 和 loading
    - provider 改变时通过 `getProviderPreset()` 计算 `modelHint`（显示如"推荐: gpt-4o"的小字提示，per D-11），不自动填充 model 字段
    - `showKey` ref 控制 API Key 显示/隐藏（默认 type="password"，per D-08）
    - `handleTestConnection`：调用 `store.testConnection('vision'/'text')`，成功显示绿色 snackbar "连接成功"（3s），失败显示红色 snackbar 带错误详情（5s）
    - `resetPanel`：调用 `store.resetPanel('vision'/'text')`
    - 面板之间 12px gap（`class="mb-3"` 或 wrapper div with gap）

    LlmVisionPanel: icon="mdi-robot", title="视觉模型", defaultExpanded=true（第一个面板默认展开）
    LlmTextPanel: icon="mdi-chat-processing", title="文本模型", defaultExpanded=false

    字段垂直排列，全宽（per UI-SPEC Section 1.3）。
    不包含 form 元素，使用 pinia store 响应式。
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - LlmVisionPanel.vue 创建，绑定 visionConfig
    - LlmTextPanel.vue 创建，绑定 textConfig
    - 各面板包含 4 字段（Provider v-select / Model / API Key password / Base URL）+ 测试连接按钮
    - ProviderPresets 的 PROVIDER_OPTIONS 供 v-select 使用
    - modelHint 在 Provider 选择后显示
    - API Key 有显示/隐藏切换
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>LLM Vision/Text 面板创建完成，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 TTS 面板 + BGM 面板 + Export 面板</name>
  <files>
    src/components/config/TtsPanel.vue
    src/components/config/BgmPanel.vue
    src/components/config/ExportPanel.vue
  </files>
  <read_first>
    src/stores/tts.ts — engine, engineConfigs, voiceList, loading, voicesLoading, loadVoices
    src/stores/bgm.ts — folder, mode, audioFiles
    src/stores/export.ts — outputDir, format
    src/composables/useTauri.ts — tauriInvoke 函数
  </read_first>
  <action>
    **TtsPanel.vue**（per D-14 ~ D-15）:
    - 顶部 v-select（items: 7 个引擎中文标签列表 `[{title:'Edge-TTS',value:'edge_tts'},{title:'Azure',value:'azure'},{title:'腾讯云',value:'tencent'},{title:'SoulVoice',value:'soulvoice'},{title:'阿里通义',value:'tts_qwen'},{title:'IndexTTS2',value:'indextts2'},{title:'豆包',value:'doubaotts'}]`，label="TTS 引擎"）
    - v-model 绑定 `store.engine`
    - 引擎切换时保留各引擎参数（store.engineConfigs 内存缓存，per D-14）
    - 动态渲染区域（v-if chain 或计算属性根据 engine 渲染不同字段）：
      - edge_tts: voiceName (v-autocomplete 绑定 store.voiceList，loading=voicesLoading), volume (v-text-field type=number, 0-100), rate (v-text-field type=number, 0.5-2.0), pitch (v-text-field type=number, -20~20)
      - azure: speechKey (type=password), speechRegion, voiceName, volume, rate, pitch
      - tencent: secretId (type=password), secretKey (type=password), region
      - soulvoice: apiKey (type=password), voiceUri, apiUrl, model
      - tts_qwen: apiKey (type=password), modelName, apiUrl
      - indextts2: apiUrl, referenceAudio, inferMode(v-select: 普通推理/快速推理), temperature, topP, topK, doSample(v-switch), numBeams
      - doubaotts: ak (type=password), sk (type=password), appid, token (type=password), cluster, apiUrl
    - Edge-TTS 引擎选择时，通过 `watch(engine, 'edge_tts')` 调用 `store.loadVoices()`（懒加载+缓存）
    - voice 列表加载错误时显示 inline 错误 + 重试按钮
    - 面板标题: title="语音合成", icon="mdi-text-to-speech"
    - 各引擎字段的 label 使用简体中文（per D-19）
    
    **BgmPanel.vue**（per D-16 ~ D-17）:
    - 文件夹路径行：只读 v-text-field (model=store.folder, readonly) + v-btn "浏览" (@click=selectBgmFolder)
    - `selectBgmFolder`: `import { open } from '@tauri-apps/plugin-dialog'` → `open({ directory: true, title: '选择 BGM 文件夹' })` → 结果赋值给 store.folder
    - 动态导入 try/catch（非 Tauri 环境降级）
    - v-switch: label="随机" / "指定", model=store.mode
    - "指定"模式时显示 v-select（文件列表，disabled until folder selected）
    - 面板标题: title="背景音乐", icon="mdi-music-note"
    
    **ExportPanel.vue**（per D-16）:
    - 输出目录路径行：只读 v-text-field + "浏览"按钮（同 BGM 文件夹选择模式）
    - 格式选择: v-select items=['mp4', 'mkv', 'mov'], label="导出格式"
    - 面板标题: title="导出设置", icon="mdi-file-export"
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TtsPanel.vue 含引擎下拉 + 7 引擎动态参数 + Edge-TTS voice 列表加载
    - BgmPanel.vue 含文件夹选择 + 随机/指定切换
    - ExportPanel.vue 含目录选择 + 格式选择
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>TTS/BGM/Export 面板创建完成，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 ModeParamsPanel + NetworkProxyPanel + 重写 SettingsDrawer.vue</name>
  <files>
    src/components/config/ModeParamsPanel.vue
    src/components/config/NetworkProxyPanel.vue
    src/components/SettingsDrawer.vue
  </files>
  <read_first>
    src/stores/mode.ts — currentMode（3 个模式值）, params（frameInterval/visionBatchSize/dramaName/temperature/clipCount/minDuration/maxDuration）
    src/stores/llm.ts — visionConfig, textConfig
    src/stores/tts.ts — engine, engineConfigs
    src/stores/bgm.ts — folder, mode
    src/stores/export.ts — outputDir, format
    src/components/SettingsDrawer.vue — 当前实现（单一面板 + 版本号 footer）
    src/components/config/LlmVisionPanel.vue — Task 1 已创建
    src/components/config/LlmTextPanel.vue — Task 1 已创建
    src/components/config/TtsPanel.vue — Task 2 已创建
    src/components/config/BgmPanel.vue — Task 2 已创建
    src/components/config/ExportPanel.vue — Task 2 已创建
  </read_first>
  <action>
    **ModeParamsPanel.vue**（per D-17 + D-02）:
    - 标题和内容根据 `useModeStore().currentMode` 动态变化：
      - `documentary` → title="帧参数", 字段: 帧间隔 (v-text-field number, model=params.frameInterval), 视觉批处理大小 (v-text-field number, model=params.visionBatchSize)
      - `sde` → title="短剧参数", 字段: 剧名 (v-text-field, model=params.dramaName), 温度 (v-slider min=0 max=2 step=0.1, model=params.temperature)
      - `sdp` → title="混剪参数", 字段: 自定义片段数 (v-text-field number, model=params.clipCount), 最短时长 (v-text-field number, model=params.minDuration), 最长时长 (v-text-field number, model=params.maxDuration)
    - 图标: mdi-tune
    - 使用 v-if 根据 currentMode 切换字段组
    - 字段默认值从 store.params 读取

    **NetworkProxyPanel.vue**（per D-26）:
    - 顶部 v-switch "启用代理" (model=proxyEnabled)
    - HTTP 代理地址: v-text-field, placeholder="留空使用环境变量" (model=proxyHttp, :disabled=!proxyEnabled)
    - HTTPS 代理地址: v-text-field, placeholder="留空使用环境变量" (model=proxyHttps, :disabled=!proxyEnabled)
    - 开关关闭时字段灰化但不隐藏（方便临时关闭不丢失配置，per Claude's Discretion）
    - 图标: mdi-earth, title="网络代理"
    - 从 get_config 返回的 AppConfig.proxy 加载初始值

    **SettingsDrawer.vue 重写**（per D-01 ~ D-08）:
    - 保留现有三段式结构（Header/Content/Footer）和 v-navigation-drawer 配置
    - Header 保持不变（齿轮 + "设置" + 关闭按钮）
    
    **Content 区重写为手风琴布局**:
    - 按照 D-03 顺序渲染所有 7 个面板：
      1. LlmVisionPanel（仅 Documentary 模式可见，v-if="modeStore.currentMode === 'documentary'"）
      2. LlmTextPanel（始终可见）
      3. TtsPanel（SDP 模式隐藏，v-if="modeStore.currentMode !== 'sdp'"）
      4. BgmPanel（始终可见）
      5. ExportPanel（始终可见）
      6. NetworkProxyPanel（始终可见）
      7. ModeParamsPanel（始终可见）
    - 面板间 12px gap（`class="mb-3"`）
    - 面板折叠状态通过 `localStorage('settingsPanelStates')` 记忆
    - 使用 SettingSection 的 collapsible + defaultExpanded prop
    - ✅ Mode-aware visibility per MODE-03（从 Phase 15 延期）

    **Footer 区扩展**（per D-04）:
    - v-btn "保存设置" (color="primary" variant="elevated" block, 后续 Wave 2 Plan 16-05 接入保存逻辑，此处先渲染按钮样式，disabled 防止提交)
    - "重置全部" 链接文字（居中，`text-caption text-decoration-underline`，后续 Plan 16-05 接入确认对话框）
    - 版本号保留（`text-caption`, `opacity: 0.6`）

    引入所有 7 个 panel 组件：
    ```typescript
    import LlmVisionPanel from '@/components/config/LlmVisionPanel.vue'
    import LlmTextPanel from '@/components/config/LlmTextPanel.vue'
    import TtsPanel from '@/components/config/TtsPanel.vue'
    import BgmPanel from '@/components/config/BgmPanel.vue'
    import ExportPanel from '@/components/config/ExportPanel.vue'
    import ModeParamsPanel from '@/components/config/ModeParamsPanel.vue'
    import NetworkProxyPanel from '@/components/config/NetworkProxyPanel.vue'
    import { useModeStore } from '@/stores/mode'
    ```
  </action>
  <verify>
    <automated>npx vue-tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ModeParamsPanel 根据 currentMode 渲染不同字段（Documentary/SDE/SDP）
    - NetworkProxyPanel 含启用开关 + HTTP/HTTPS 地址字段
    - SettingsDrawer Content 区按 D-03 顺序渲染 7 个面板
    - LlmVisionPanel 仅 Documentary 可见（v-if）
    - TtsPanel 在 SDP 模式下隐藏（v-if）
    - Footer 含"保存设置"按钮（disabled）+ "重置全部"链接 + 版本号
    - `npx vue-tsc --noEmit` 通过
  </acceptance_criteria>
  <done>SettingsDrawer 重写 + 7 面板创建完成，TypeScript 编译通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsDrawer → Pinia stores | UI 输入直接写入 store 状态 |
| API Key 字段 type=password | 防止屏幕窥视 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Information Disclosure | API Key 可见性 | mitigate | D-08: type="password" + 显示/隐藏切换按钮 |
| T-16-11 | Tampering | Provider selection changes | accept | 用户主动修改自己的配置，非攻击 |
| T-16-12 | Information Disclosure | Edge-TTS voice list | accept | voice list 仅含音色名称/语言/性别，不包含用户数据 |
</threat_model>

<verification>
- `npx vue-tsc --noEmit` — TypeScript 编译通过
- SettingsDrawer 渲染 7 个面板，面板顺序符合 D-03
- LlmVisionPanel 在非 Documentary 模式隐藏（v-if computed check）
- TtsPanel 在 SDP 模式隐藏（v-if computed check）
</verification>

<success_criteria>
1. SettingsDrawer 手风琴布局包含 7 个 SettingSection 面板（顺序 per D-03）
2. LLM Vision/Text 面板各自独立，含 Provider v-select + Model + API Key(type=password) + Base URL + 测试连接按钮
3. TTS 面板含引擎下拉 + 7 引擎动态参数 + Edge-TTS voice autocomplete
4. BGM 面板含文件夹选择 + 随机/指定 v-switch
5. Export 面板含输出目录选择 + 格式 v-select
6. 模式参数面板根据 ModeStore.currentMode 切换
7. 网络代理面板含启用开关 + HTTP/HTTPS 地址
8. Footer 含"保存设置"按钮 + "重置全部" + 版本号
9. 面板可见性符合 D-02 规则
</success_criteria>

<output>
完成后创建 `.planning/phases/16-configuration-panels-ffmpeg-sidecar-core-tech-dept/16-04-SUMMARY.md`
</output>
