---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - tests/config_test.rs
  - tests/ffmpeg_test.rs
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - FFMP-01
  - FFMP-09

must_haves:
  truths:
    - "从真实 config.example.toml 文件加载配置成功，所有字段值与文件内容一致"
    - "从最小 TOML 文件加载配置成功，缺失 section 使用默认值"
    - "热加载: 文件变更后 ConfigManager.get() 返回更新后的配置"
    - "ffprobe 探测真实视频文件返回非零时长和有效分辨率"
    - "FFmpeg 裁剪真实视频文件产生有效的输出文件"
    - "硬件加速检测返回系统实际支持的编码器列表"
    - "spawn_blocking 包装的 FFmpeg 操作不阻塞 tokio runtime"
  artifacts:
    - path: "tests/config_test.rs"
      provides: "配置加载/校验/热加载集成测试"
      contains: "#[test]"
    - path: "tests/ffmpeg_test.rs"
      provides: "FFmpeg 操作/探测/硬件检测集成测试"
      contains: "#[test]"
  key_links:
    - from: "tests/config_test.rs"
      to: "narratoai_core::config"
      via: "use narratoai_core::config::ConfigManager"
      pattern: "ConfigManager"
    - from: "tests/ffmpeg_test.rs"
      to: "narratoai_core::ffmpeg"
      via: "use narratoai_core::ffmpeg"
      pattern: "narratoai_core::ffmpeg"
---

<objective>
编写集成测试，验证配置系统和 FFmpeg 操作在真实环境下的端到端功能。

Purpose: 用真实文件和真实 FFmpeg 进程验证 Phase 1 所有需求已满足。这是 Phase 1 的最终验收。
Output: tests/config_test.rs, tests/ffmpeg_test.rs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@config.example.toml

<interfaces>
<!-- Plan 01 和 Plan 02 创建的公共接口 -->

From src/config/mod.rs (created by Plan 01):
```rust
pub struct ConfigManager {
    // ...
}
impl ConfigManager {
    pub fn load(path: &Path) -> Result<Self, ConfigError>;
    pub fn start_watching(&mut self) -> Result<(), ConfigError>;
    pub fn get(&self) -> AppConfig;
    pub fn config(&self) -> Arc<RwLock<AppConfig>>;
}

pub fn version() -> &'static str;  // from lib.rs
```

From src/config/types.rs (created by Plan 01):
```rust
pub struct AppConfig { /* 10 sections */ }
impl AppConfig {
    pub fn validate(&self) -> Result<(), ConfigError>;
}
```

From src/ffmpeg/command.rs (created by Plan 02):
```rust
pub async fn clip_video(
    input: &Path, output: &Path, start: f64, duration: f64,
    progress: Option<ProgressCallback>,
) -> Result<(), FFmpegError>;

pub async fn run_ffmpeg<F, R>(blocking_fn: F) -> Result<R, FFmpegError>;
```

From src/ffmpeg/probe.rs (created by Plan 02):
```rust
pub struct VideoInfo { /* duration_secs, width, height, codec_name, format_name */ }
pub fn probe_video(path: &Path) -> Result<VideoInfo, FFmpegError>;
pub async fn probe_video_async(path: &Path) -> Result<VideoInfo, FFmpegError>;
```

From src/ffmpeg/hwaccel.rs (created by Plan 02):
```rust
pub enum HwAccelProfile { /* 5 variants */ }
pub fn detect_hw_encoders() -> Result<Vec<String>, FFmpegError>;
pub fn recommend_profile() -> HwAccelProfile;
pub fn get_profile(profile: HwAccelProfile) -> &'static FFmpegProfileInfo;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写配置系统集成测试 (tests/config_test.rs)</name>
  <files>tests/config_test.rs</files>
  <read_first>
    - config.example.toml（真实配置文件内容）
    - src/config/mod.rs（ConfigManager 接口）
    - src/config/types.rs（AppConfig 和所有子段结构体）
    - src/config/defaults.rs（Default trait 实现和默认值）
    - src/config/watcher.rs（ConfigWatcher 实现）
    - src/error.rs（ConfigError 定义）
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture 节)
  </read_first>
  <acceptance_criteria>
    - tests/config_test.rs 存在且包含至少 5 个 #[test] 函数
    - test_load_example_config: 从 config.example.toml 加载成功，app.project_version == "0.7.8"
    - test_load_minimal_config: 从只含 [app] section 的 TOML 加载成功
    - test_load_empty_config: 从空文件加载成功，所有 section 使用默认值
    - test_load_nonexistent: 返回 ConfigError::NotFound
    - test_default_values_match_example: AppConfig::default() 的关键字段值与 config.example.toml 一致
    - test_hot_reload: 修改文件后 ConfigManager.get() 返回更新后的值
    - cargo test --test config_test 通过
  </acceptance_criteria>
  <action>
    创建 `tests/config_test.rs`:

    1. 测试 `test_load_example_config`:
       - 使用项目根目录的 config.example.toml
       - ConfigManager::load(Path::new("config.example.toml"))
       - 验证关键值: app.project_version == "0.7.8", ui.tts_engine == "edge_tts", frames.frame_interval_input == 3
       - 验证所有 10 个 section 都存在

    2. 测试 `test_load_minimal_config`:
       - 用 tempfile 创建临时文件，内容只有 `[app]\nproject_version = "test"`
       - ConfigManager::load 成功
       - ui 使用默认值（tts_engine == "edge_tts"）

    3. 测试 `test_load_empty_config`:
       - 用 tempfile 创建空文件
       - ConfigManager::load 成功
       - AppConfig::default() 与加载结果一致

    4. 测试 `test_load_nonexistent`:
       - ConfigManager::load(Path::new("/nonexistent/config.toml"))
       - 结果是 Err(ConfigError::NotFound { .. })

    5. 测试 `test_default_values`:
       - 验证 AppConfig::default() 的关键字段:
         - app.llm_vision_timeout == 120
         - app.llm_text_timeout == 180
         - app.llm_max_retries == 3
         - ui.edge_voice_name == "zh-CN-XiaoyiNeural-Female"
         - frames.vision_batch_size == 10
         - proxy.enabled == false

    6. 测试 `test_hot_reload`（需要短暂 sleep 等待 notify 事件）:
       - 用 tempfile 创建临时配置文件
       - ConfigManager::load 然后 start_watching
       - 等待 100ms 让 watcher 启动
       - 修改文件内容（写入新的 project_version）
       - 等待 500ms 让 notify 检测到变更
       - 验证 ConfigManager.get().app.project_version 更新为新值

    注意事项:
    - 集成测试在 tests/ 目录，使用 `use narratoai_core::*` 导入
    - tempfile 创建的文件在 TempDir drop 时自动删除
    - 热加载测试可能需要调整等待时间，Windows 上 notify 事件延迟可能更高（用 1s 保险）
    - 如果 notify RC 版本有问题，此测试可以标记 #[ignore]
  </action>
  <verify>
    <automated>cd E:/git/NarratoAI && cargo test --test config_test 2>&1</automated>
  </verify>
  <done>
    - tests/config_test.rs 包含 6 个集成测试
    - 从真实 config.example.toml 加载验证所有字段
    - 空文件/部分文件/不存在文件三种边界场景覆盖
    - 默认值与 config.example.toml 一致
    - 热加载测试覆盖（可能标记 #[ignore] 如果 notify 不稳定）
  </done>
</task>

<task type="auto">
  <name>Task 2: 编写 FFmpeg 操作集成测试 (tests/ffmpeg_test.rs)</name>
  <files>tests/ffmpeg_test.rs</files>
  <read_first>
    - src/ffmpeg/command.rs（clip_video 函数）
    - src/ffmpeg/probe.rs（probe_video 函数）
    - src/ffmpeg/hwaccel.rs（detect_hw_encoders, recommend_profile）
    - src/error.rs（FFmpegError 定义）
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture 节)
  </read_first>
  <acceptance_criteria>
    - tests/ffmpeg_test.rs 存在且包含至少 4 个 #[test] 函数和 1 个 #[tokio::test] 函数
    - test_probe_system_video: 对系统中任意可用视频文件执行 probe_video 返回有效 VideoInfo
    - test_clip_video_async: 通过 clip_video 裁剪视频产生有效输出文件
    - test_hw_accel_detection: detect_hw_encoders 返回非空列表
    - test_recommend_profile: recommend_profile 返回 HwAccelProfile 枚举变体之一
    - test_profiles_completeness: 所有 5 个 profile 都有有效数据
    - cargo test --test ffmpeg_test 通过（需要系统 FFmpeg）
  </acceptance_criteria>
  <action>
    创建 `tests/ffmpeg_test.rs`:

    1. 辅助函数 `create_test_video() -> (TempDir, PathBuf)`:
       - 使用 ffmpeg-sidecar 或 std::process::Command 调用 ffmpeg 生成一个 2 秒的测试视频
       - **D-12 落实:** 优先使用 ffmpeg_sidecar::ffmpeg_path() 获取 ffmpeg 路径，确保使用 auto-download 的二进制
       - 命令: `ffmpeg -f lavfi -i testsrc=duration=2:size=320x240:rate=25 -f lavfi -i sine=frequency=440:duration=2 -c:v libx264 -c:a aac -y <output>`
       - 返回临时目录和视频文件路径

    2. 测试 `test_probe_system_video`:
       - 创建测试视频
       - 调用 probe_video(test_video_path)
       - 验证: duration_secs > 0, width > 0, height > 0, codec_name 非空
       - 验证 format_name 包含 "mov" 或 "mp4" 或 "matroska"

    3. 测试 `test_clip_video_async`（#[tokio::test]）:
       - 创建测试视频
       - 创建临时输出路径
       - 调用 clip_video(input, output, 0.0, 1.0, None).await
       - 验证: 结果 Ok(())
       - 验证: 输出文件存在且大小 > 0

    4. 测试 `test_hw_accel_detection`:
       - 调用 detect_hw_encoders()
       - 验证: 返回 Ok(_)
       - 验证: 列表至少包含 "libx264"（软件编码器总在）
       - 如果系统有 NVIDIA GPU，列表应包含 h264_nvenc

    5. 测试 `test_recommend_profile`:
       - 调用 recommend_profile()
       - 验证: 返回值是 5 个 HwAccelProfile 变体之一
       - 验证: get_profile(recommend_profile()) 返回有效数据

    6. 测试 `test_profiles_completeness`:
       - 遍历所有 5 个 HwAccelProfile 变体
       - 对每个调用 get_profile()
       - 验证: name 非空, encoder 非空, pixel_format == "yuv420p", compatibility_level 在 1-5 范围

    注意事项:
    - 这些测试依赖 FFmpeg（通过 ffmpeg-sidecar auto-download 获取，per D-12）
    - 如果 FFmpeg 不可用，测试应 panic 并给出清晰提示
    - 创建测试视频使用 libx264 编码器（确保可用）
    - 测试视频用 TempDir 管理生命周期
  </action>
  <verify>
    <automated>cd E:/git/NarratoAI && cargo test --test ffmpeg_test 2>&1</automated>
  </verify>
  <done>
    - tests/ffmpeg_test.rs 包含 5 个集成测试
    - ffprobe 对测试视频返回有效信息
    - clip_video 裁剪成功产生输出文件
    - 硬件加速检测工作正常
    - 所有 5 个 profile 数据完整
    - 所有测试 cargo test --test ffmpeg_test 通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试视频生成 -> FFmpeg | 测试代码生成命令参数调用 FFmpeg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | 集成测试占用系统资源 | accept | 测试创建小视频（2 秒 320x240），资源消耗极低 |

测试代码不引入新的安全边界，所有安全考量已在 Plan 01 和 Plan 02 中覆盖。
</threat_model>

<verification>
1. `cargo build` — 零错误
2. `cargo test --test config_test` — 配置集成测试通过
3. `cargo test --test ffmpeg_test` — FFmpeg 集成测试通过
4. `cargo test` — 全部测试通过（单元 + 集成）
5. `cargo test 2>&1 | grep -E "test result|running"` — 确认所有测试运行
</verification>

<success_criteria>
- cargo test 全部通过（零失败）
- 配置系统: 完整文件加载、空文件、部分文件、不存在文件、默认值、热加载 全部验证
- FFmpeg: 视频探测、视频裁剪、硬件检测、profile 推荐全部验证
- Phase 1 所有 6 个需求（CONF-01~04, FFMP-01, FFMP-09）有对应通过的测试
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
