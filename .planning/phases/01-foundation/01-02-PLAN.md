---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib.rs
  - src/ffmpeg/mod.rs
  - src/ffmpeg/command.rs
  - src/ffmpeg/probe.rs
  - src/ffmpeg/hwaccel.rs
autonomous: true
requirements:
  - FFMP-01
  - FFMP-09
  - CONF-04

must_haves:
  truths:
    - "通过 ffmpeg-sidecar FfmpegCommand 成功执行视频裁剪操作"
    - "通过 std::process::Command 调用 ffprobe 成功获取视频信息（时长、分辨率、编码）"
    - "FFmpeg 操作通过 spawn_blocking 执行，不阻塞 tokio 运行时"
    - "检测到系统 FFmpeg 支持的硬件加速编码器（h264_nvenc/h264_amf/h264_qsv/h264_videotoolbox）"
    - "无硬件编码器时回退到 UniversalSoftware profile"
    - "ffmpeg-sidecar auto-download feature 确保用户无需预装 FFmpeg（per D-12）"
  artifacts:
    - path: "src/ffmpeg/mod.rs"
      provides: "FFmpeg 模块入口和公共接口导出"
      contains: "pub mod command"
    - path: "src/ffmpeg/command.rs"
      provides: "FfmpegCommand 封装 + spawn_blocking 异步包装 + 进度回调"
      contains: "pub async fn clip_video"
    - path: "src/ffmpeg/probe.rs"
      provides: "ffprobe 视频信息探测（VideoInfo 结构体 + probe_video 函数）"
      contains: "pub struct VideoInfo"
    - path: "src/ffmpeg/hwaccel.rs"
      provides: "硬件加速检测 + Profile 枚举 + 推荐逻辑"
      contains: "pub enum HwAccelProfile"
  key_links:
    - from: "src/ffmpeg/command.rs"
      to: "src/error.rs"
      via: "Result<_, FFmpegError>"
      pattern: "FFmpegError"
    - from: "src/ffmpeg/command.rs"
      to: "ffmpeg-sidecar"
      via: "use ffmpeg_sidecar"
      pattern: "ffmpeg_sidecar::FfmpegCommand"
    - from: "src/ffmpeg/probe.rs"
      to: "src/error.rs"
      via: "Result<_, FFmpegError>"
      pattern: "FFmpegError"
    - from: "src/ffmpeg/hwaccel.rs"
      to: "src/error.rs"
      via: "Result<_, FFmpegError>"
      pattern: "FFmpegError"
    - from: "src/ffmpeg/probe.rs"
      to: "ffmpeg-sidecar"
      via: "通过 ffmpeg_sidecar::ffprobe_path() 获取 ffprobe 二进制路径"
      pattern: "ffprobe_path"
    - from: "src/ffmpeg/hwaccel.rs"
      to: "ffmpeg-sidecar"
      via: "通过 ffmpeg_sidecar::ffmpeg_path() 获取 ffmpeg 二进制路径"
      pattern: "ffmpeg_path"
---

<objective>
创建 FFmpeg 操作层——通过 ffmpeg-sidecar 执行视频裁剪，通过 ffprobe 探测视频信息，检测硬件加速编码器。

Purpose: 建立 FFmpeg 基础设施层，后续 Phase 6 流水线将大量使用这些操作。所有操作通过 spawn_blocking 异步化。FFmpeg 二进制通过 ffmpeg-sidecar auto-download 自动获取（per D-12）。
Output: src/ffmpeg/ 模块（mod.rs, command.rs, probe.rs, hwaccel.rs）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@app/config/ffmpeg_config.py

<interfaces>
<!-- src/error.rs 由 Plan 01 创建，Plan 02 需要使用其中的 FFmpegError -->

From src/error.rs (created by Plan 01):
```rust
#[derive(Error, Debug)]
pub enum FFmpegError {
    #[error("FFmpeg 进程启动失败: {0}")]
    SpawnFailed(String),
    #[error("FFmpeg 执行超时: {0}")]
    Timeout(String),
    #[error("FFmpeg 输出解析错误: {0}")]
    OutputParseError(String),
    #[error("FFmpeg 执行错误: {0}")]
    ExecutionError(String),
    #[error("未找到 FFmpeg 二进制文件")]
    BinaryNotFound,
}
```

From src/lib.rs (created by Plan 01 — 注意 Plan 01 不导出 ffmpeg 模块):
```rust
pub mod config;
pub mod error;

pub fn version() -> &'static str {
    env!("CARGO_PKG_VERSION")
}
```

**D-12 落实:** ffmpeg-sidecar 已在 Plan 01 的 Cargo.toml 中启用 `auto-download` feature。Plan 02 使用 `ffmpeg_sidecar::ffmpeg_path()` 和 `ffmpeg_sidecar::ffprobe_path()` 获取二进制路径，而非硬编码系统命令名。这确保 auto-download 下载的二进制被正确使用。

Python 版 FFmpegProfile 结构 (from app/config/ffmpeg_config.py):
- 5 个预定义 Profile: high_performance, compatibility, windows_nvidia, macos_videotoolbox, universal_software
- 每个 Profile 有: name, description, hwaccel_enabled, hwaccel_type, encoder, quality_preset, pixel_format, additional_args, compatibility_level
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 src/ffmpeg/mod.rs + 实现 command.rs（异步视频裁剪 + 进度回调）</name>
  <files>src/lib.rs, src/ffmpeg/mod.rs, src/ffmpeg/command.rs</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: FFmpeg 异步操作模式)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-11, D-12, D-14, D-15)
    - src/error.rs (FFmpegError 定义)
    - src/lib.rs (当前只导出 config 和 error，需添加 ffmpeg)
  </read_first>
  <behavior>
    - Test 1: clip_video 对不存在的输入文件返回 FFmpegError
    - Test 2: ProgressCallback 类型签名正确（Fn(Option<f64>, &str) + Send + Sync）
    - Test 3: spawn_blocking 闭包内使用 PathBuf（Send + 'static），不使用 &Path 引用
    - Test 4 (WARNING 2): spawn_blocking 包装的 FFmpeg 操作不阻塞 tokio runtime——在 async test 中同时运行多个 spawn_blocking 任务，验证它们并行执行而非串行
  </behavior>
  <action>
    1. 在 `src/lib.rs` 中添加 `pub mod ffmpeg;`（Plan 01 未添加此行，此处补上）:
       ```rust
       pub mod config;
       pub mod ffmpeg;
       pub mod error;
       ```

    2. 创建 `src/ffmpeg/mod.rs`:
       ```rust
       pub mod command;
       pub mod probe;
       pub mod hwaccel;
       ```

    3. 创建 `src/ffmpeg/command.rs`（per D-11, D-12, D-14, D-15）:
       - 定义进度回调类型:
         ```rust
         pub type ProgressCallback = Box<dyn Fn(Option<f64>, &str) + Send + Sync>;
         ```
       - 实现 `clip_video` 异步函数:
         ```rust
         pub async fn clip_video(
             input: &Path,
             output: &Path,
             start: f64,
             duration: f64,
             progress: Option<ProgressCallback>,
         ) -> Result<(), FFmpegError>
         ```
       - 核心逻辑:
         a. 将 input/output 转为 PathBuf（满足 Send + 'static）
         b. 将 progress 回调包装在 Option<Arc<ProgressCallback>> 中（Arc 满足 Send）
         c. 调用 `tokio::task::spawn_blocking(move || { ... })`
         d. 闭包内:
            - 创建 `ffmpeg_sidecar::FfmpegCommand::new()`
            - `.input(input_path)`, `.seek(start)`, `.duration(duration)`, `.output(output_path)`
            - `.overwrite()` 允许覆盖输出
            - 调用 `.iter()` 获取事件迭代器
            - 遍历事件:
              - `FfmpegEvent::Progress(p)` -> 调用 progress 回调，传入进度百分比和 "视频裁剪中" 描述
              - `FfmpegEvent::Error(e)` -> tracing::error! 记录
              - `FfmpegEvent::Done` -> 正常退出
         e. `.await` spawn_blocking 结果，map_err 转换 JoinError 为 FFmpegError

       - **D-12 落实:** FfmpegCommand::new() 会自动使用 auto-download 下载的 FFmpeg 二进制。无需手动指定路径。

       - 实现 `run_ffmpeg` 通用异步包装函数:
         ```rust
         pub async fn run_ffmpeg<F, R>(blocking_fn: F) -> Result<R, FFmpegError>
         where
             F: FnOnce() -> Result<R, FFmpegError> + Send + 'static,
             R: Send + 'static,
         ```
         此函数是通用的 spawn_blocking 包装器，后续 Phase 6 可复用。

       - 单元测试:
         - test_progress_callback_type: 验证 ProgressCallback 可以被 Box 化
         - test_clip_video_invalid_input: 对不存在的文件调用返回 SpawnFailed 错误
         - test_spawn_blocking_non_blocking: 验证 spawn_blocking 不阻塞 tokio runtime（per WARNING 2）——创建两个并发的 spawn_blocking 任务（各 sleep 100ms），验证总耗时不显著超过 100ms（并行执行）

    注意事项:
    - ffmpeg-sidecar 的 FfmpegEvent 枚举变体名以实际 crate 2.5.1 API 为准
    - Progress 事件中的 time 字段可能是 Option<Duration>，需要检查实际 API
    - 闭包内不能捕获 &Path 引用，必须提前转为 PathBuf
    - Windows 路径: ffmpeg-sidecar 内部处理路径转换，无需手动处理反斜杠
  </action>
  <verify>
    <automated>cd E:/git/NarratoAI && cargo test --lib ffmpeg::command 2>&1</automated>
  </verify>
  <done>
    - src/ffmpeg/mod.rs 创建并包含三个子模块声明
    - src/lib.rs 添加 `pub mod ffmpeg` 导出
    - src/ffmpeg/command.rs 包含 clip_video async 函数
    - clip_video 使用 tokio::task::spawn_blocking 包装同步 ffmpeg-sidecar 调用
    - ProgressCallback 类型定义正确
    - 闭包仅捕获 Send + 'static 类型（PathBuf, Arc）
    - spawn_blocking 不阻塞 tokio runtime 的测试通过
    - cargo test --lib ffmpeg::command 通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 probe.rs (ffprobe 探测) + hwaccel.rs (硬件检测)</name>
  <files>src/ffmpeg/probe.rs, src/ffmpeg/hwaccel.rs</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5 硬件检测, Pitfall 3 ffprobe 限制)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12 内置 FFmpeg 二进制, D-13 硬件检测方式)
    - app/config/ffmpeg_config.py (Python 版 PROFILES 定义和推荐逻辑)
    - src/error.rs (FFmpegError 定义)
  </read_first>
  <behavior>
    - Test 1: VideoInfo 结构体 Debug/Clone 正确
    - Test 2: detect_hw_encoders() 返回包含 "libx264" 或硬件编码器的 Vec<String>
    - Test 3: recommend_profile() 在有 nvenc 时返回 HwAccelProfile::WindowsNvidia
    - Test 4: HwAccelProfile 的 5 个变体与 Python 版 PROFILES 字典的 5 个 profile 一一对应
  </behavior>
  <action>
    1. 创建 `src/ffmpeg/probe.rs`（per RESEARCH Pitfall 3, D-12）:
       - 定义 `VideoInfo` 结构体:
         ```rust
         #[derive(Debug, Clone)]
         pub struct VideoInfo {
             pub duration_secs: f64,
             pub width: u32,
             pub height: u32,
             pub codec_name: String,
             pub format_name: String,
         }
         ```
       - 实现 `probe_video(path: &Path) -> Result<VideoInfo, FFmpegError>`:
         a. **D-12 落实:** 使用 `ffmpeg_sidecar::ffprobe_path()` 获取 ffprobe 二进制路径（而非硬编码 "ffprobe" 字符串），然后通过 `std::process::Command::new(ffprobe_path)` 调用
         b. 参数: `["-v", "quiet", "-print_format", "json", "-show_format", "-show_streams"]`
         c. arg(path) 传入视频文件路径
         d. 检查 exit status，失败则返回 OutputParseError
         e. 使用 serde_json::from_slice 解析 stdout 为 serde_json::Value
         f. 提取:
            - duration: json["format"]["duration"] (String -> parse f64)
            - video_stream: streams 数组中 codec_type == "video" 的第一个
            - width/height: video_stream["width"]/"height"] (u64 -> u32)
            - codec_name: video_stream["codec_name"]
            - format_name: json["format"]["format_name"]
         g. 所有字段缺失时使用默认值（0.0, 0, "unknown"），不 panic

         **WARNING 1 说明（合理偏离 D-11）:** probe.rs 使用 `std::process::Command` 而非 ffmpeg-sidecar API 调用 ffprobe，这是合理的——ffmpeg-sidecar 不提供完整的 ffprobe 封装（仅有 `ffprobe_version()` 和 `ffprobe_path()`）。使用 `ffmpeg_sidecar::ffprobe_path()` 获取路径后通过 `std::process::Command` 调用，兼顾了 D-12（auto-download 获取的二进制被使用）和功能完整性。

       - 实现 `probe_video_async(path: &Path) -> Result<VideoInfo, FFmpegError>`:
         使用 spawn_blocking 包装 probe_video，路径转为 PathBuf 满足 Send

    2. 创建 `src/ffmpeg/hwaccel.rs`（per D-12, D-13, 对齐 Python 版 ffmpeg_config.py）:
       - 定义 `HwAccelProfile` 枚举（与 Python 版 5 个 PROFILES 对应）:
         ```rust
         #[derive(Debug, Clone, Copy, PartialEq, Eq)]
         pub enum HwAccelProfile {
             HighPerformance,      // h264_nvenc/h264_amf — 高性能（NVIDIA/AMD）
             Compatibility,        // libx264 — 兼容性回退
             WindowsNvidia,        // h264_nvenc — Windows NVIDIA 专用
             MacosVideotoolbox,    // h264_videotoolbox — macOS
             UniversalSoftware,    // libx264 — 通用软件编码
         }
         ```

       - 定义 `FFmpegProfileInfo` 结构体（对应 Python 版 FFmpegProfile dataclass）:
         ```rust
         pub struct FFmpegProfileInfo {
             pub name: &'static str,
             pub description: &'static str,
             pub hwaccel_enabled: bool,
             pub hwaccel_type: Option<&'static str>,
             pub encoder: &'static str,
             pub quality_preset: &'static str,
             pub pixel_format: &'static str,
             pub additional_args: &'static [&'static str],
             pub compatibility_level: u8,
         }
         ```

       - 实现 `PROFILES: Map<HwAccelProfile, FFmpegProfileInfo>`（5 个预定义 profile）:
         精确对应 Python 版 PROFILES 字典的值:
         - HighPerformance: hwaccel_enabled=true, hwaccel_type=Some("auto"), encoder="auto", preset="fast", pixel_format="yuv420p", additional_args=["-preset", "fast"], compat_level=2
         - Compatibility: hwaccel_enabled=false, encoder="libx264", preset="medium", pixel_format="yuv420p", additional_args=["-preset", "medium", "-crf", "23"], compat_level=5
         - WindowsNvidia: hwaccel_enabled=true, hwaccel_type=Some("nvenc_pure"), encoder="h264_nvenc", preset="medium", pixel_format="yuv420p", additional_args=["-preset", "medium", "-cq", "23"], compat_level=3
         - MacosVideotoolbox: hwaccel_enabled=true, hwaccel_type=Some("videotoolbox"), encoder="h264_videotoolbox", preset="medium", pixel_format="yuv420p", additional_args=["-q:v", "65"], compat_level=3
         - UniversalSoftware: hwaccel_enabled=false, encoder="libx264", preset="medium", pixel_format="yuv420p", additional_args=["-preset", "medium", "-crf", "23"], compat_level=5

       - 实现 `detect_hw_encoders() -> Result<Vec<String>, FFmpegError>`:
         a. **D-12 落实:** 使用 `ffmpeg_sidecar::ffmpeg_path()` 获取 ffmpeg 二进制路径（而非硬编码 "ffmpeg"），然后 `std::process::Command::new(ffmpeg_path)` with args `["-encoders", "-hide_banner"]`
         b. 解析 stdout，检测以下编码器字符串:
            - h264_nvenc, hevc_nvenc (NVIDIA)
            - h264_amf, hevc_amf (AMD)
            - h264_qsv, hevc_qsv (Intel Quick Sync)
            - h264_videotoolbox (macOS)
         c. 返回检测到的编码器名称列表

       - 实现 `recommend_profile() -> HwAccelProfile`（对齐 Python 版 get_recommended_profile）:
         a. 调用 detect_hw_encoders()
         b. 检测逻辑:
            - 有 nvenc -> 检查 OS: Windows -> WindowsNvidia, 其他 -> HighPerformance
            - 有 videotoolbox -> MacosVideotoolbox
            - 有 amf 或 qsv -> HighPerformance
            - 无硬件编码器 或 检测失败 -> UniversalSoftware

       - 实现 `get_profile(profile: HwAccelProfile) -> &'static FFmpegProfileInfo`:
         从 PROFILES map 中获取 profile 信息

       - 单元测试:
         - test_profile_variants: 验证 5 个 HwAccelProfile 变体存在
         - test_detect_encoders_format: 验证 detect_hw_encoders 返回合法的编码器名称列表
         - test_recommend_returns_valid_profile: recommend_profile 返回值是 5 个变体之一
         - test_profiles_data: 验证每个 profile 的 FFmpegProfileInfo 字段非空
  </action>
  <verify>
    <automated>cd E:/git/NarratoAI && cargo test --lib ffmpeg::probe ffmpeg::hwaccel 2>&1</automated>
  </verify>
  <done>
    - probe_video 通过 ffmpeg_sidecar::ffprobe_path() 获取路径后调用 ffprobe 获取时长、分辨率、编码、格式（per D-12）
    - probe_video_async 使用 spawn_blocking 包装
    - detect_hw_encoders 通过 ffmpeg_sidecar::ffmpeg_path() 获取路径后调用 ffmpeg -encoders 检测硬件编码器（per D-12）
    - HwAccelProfile 枚举包含 5 个变体，与 Python 版一一对应
    - FFmpegProfileInfo 包含 Python 版 FFmpegProfile 的所有字段
    - recommend_profile 根据检测结果推荐 profile
    - 无硬件编码器时回退 UniversalSoftware
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 视频文件路径 -> FFmpeg CLI | 用户提供的文件路径传入 FFmpeg 子进程，可能包含特殊字符 |
| ffprobe JSON 输出 -> VideoInfo | 外部进程输出解析，格式可能变化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | FFmpeg 文件路径参数 | mitigate | ffmpeg-sidecar builder API 正确转义参数，不拼接字符串命令 |
| T-02-02 | Tampering | ffprobe 输出解析 | mitigate | serde_json 反序列化为 Value 后安全提取字段，缺失字段使用默认值不 panic |
| T-02-03 | Denial of Service | spawn_blocking 长时间阻塞 | accept | FFmpeg 操作本身可能耗时，spawn_blocking 使用独立线程池，不阻塞 tokio runtime |
</threat_model>

<verification>
1. `cargo build` — 零错误
2. `cargo test --lib ffmpeg` — FFmpeg 模块单元测试通过
3. 系统有 FFmpeg 8.0.1 + NVIDIA GPU，detect_hw_encoders 应检测到 h264_nvenc
4. ffprobe 对真实视频文件返回正确信息（集成测试在 Plan 03 中）
5. probe.rs 和 hwaccel.rs 都通过 ffmpeg-sidecar 路径函数获取二进制（per D-12）
</verification>

<success_criteria>
- clip_video 使用 spawn_blocking 包装 ffmpeg-sidecar 调用
- probe_video 通过 ffmpeg_sidecar::ffprobe_path() + std::process::Command 调用 ffprobe 解析 JSON（per D-12）
- detect_hw_encoders 通过 ffmpeg_sidecar::ffmpeg_path() + std::process::Command 检测硬件编码器（per D-12）
- 硬件加速检测工作并推荐正确的 Profile
- 所有 FFmpeg 操作不阻塞 tokio 运行时
- spawn_blocking 不阻塞测试通过（per WARNING 2）
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
