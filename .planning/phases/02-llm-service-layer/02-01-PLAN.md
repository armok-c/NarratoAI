---
phase: 02-llm-service-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/error.rs
  - src/llm/types.rs
  - src/llm/provider.rs
autonomous: true
requirements:
  - LLM-07
user_setup: []

must_haves:
  truths:
    - async-openai, image, base64, futures, reqwest, async-trait 依赖声明在 Cargo.toml 中
    - LLMError 枚举包含 9 种变体且编译通过
    - LlmProvider trait 定义了 generate_text / generate_text_stream / analyze_images 三个方法
    - VisionBatchConfig 和 LlmResponseFormat 类型存在于 types.rs
    - LLMError 的中文错误消息可通过 Display trait 读取
  artifacts:
    - path: "Cargo.toml"
      provides: "Phase 2 依赖声明"
      content_check: "async-openai, image, base64, futures, reqwest, async-trait in [dependencies]; wiremock in [dev-dependencies]"
    - path: "src/error.rs"
      provides: "LLMError 枚举（9 变体）"
      exports: ["LLMError"]
      min_variants: 9
    - path: "src/llm/types.rs"
      provides: "VisionBatchConfig 和 LlmResponseFormat 类型"
    - path: "src/llm/provider.rs"
      provides: "LlmProvider trait 定义"
      exports: ["LlmProvider"]
      min_methods: 3
  key_links:
    - from: "src/llm/provider.rs"
      to: "src/error.rs"
      via: "LlmProvider 方法签名中 Result<_, LLMError>"
    - from: "src/llm/provider.rs"
      to: "src/llm/types.rs"
      via: "analyze_images 参数中 batch_size / max_concurrency 使用 VisionBatchConfig 模式"
---

<objective>
建立 LLM 服务层的核心基础设施——依赖声明、领域错误类型、LLM 数据类型和 Provider trait 定义。

Purpose: 为 Phase 2 后续的 Provider 实现和注册提供编译基础设施。先定义契约（接口和错误类型），后实现。
Output: Cargo.toml 依赖更新、LLMError 枚举（9 变体）、VisionBatchConfig/LlmResponseFormat 类型、LlmProvider trait。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!--
Phase 1 已建立的模式：
- src/error.rs: ConfigError + FFmpegError 用 thiserror 派生，中文 #[error("...")] 消息
- Cargo.toml: 已有 tokio / serde / thiserror / tracing / toml / ffmpeg-sidecar 等依赖
- src/lib.rs: 已有 pub mod config; pub mod ffmpeg; pub mod error;
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 添加 Cargo.toml 依赖并扩展 LLMError 到 src/error.rs</name>
  <files>
    Cargo.toml
    src/error.rs
  </files>

  <read_first>
    Cargo.toml — 读取现有 [dependencies] 和 [dev-dependencies] 确定插入位置
    src/error.rs — 读取现有的 ConfigError / FFmpegError 模式和测试结构
  </read_first>

  <action>
    **步骤 A — 修改 Cargo.toml**

    在 `[dependencies]` 区块（serde_json 行之后）追加以下依赖：
    ```toml
    async-openai = { version = "0.36", features = ["chat-completion"] }
    async-trait = "0.1"
    image = "0.25"
    base64 = "0.22"
    futures = "0.3"
    reqwest = "0.12"
    ```

    在 `[dev-dependencies]` 区块（tempfile 行之后）追加：
    ```toml
    wiremock = "0.6"
    ```

    **步骤 B — 在 src/error.rs 的 FFmpegError 枚举和 impl From 之间插入 LLMError 枚举**

    在 `src/error.rs` 的 `FFmpegError` 枚举（行 38）和 `impl From<notify::Error> for ConfigError`（行 42）之间，插入 LLMError 枚举。具体在 `FFmpegError::BinaryNotFound` 变体（行 38）之后、空行（行 40）之后插入。

    LLMError 枚举包含 9 种变体，完全对齐 Python `app/services/llm/exceptions.py`（D-14）：

    ```rust
    /// LLM 服务领域错误
    #[derive(Error, Debug)]
    pub enum LLMError {
        #[error("LLM 调用失败: {0}")]
        General(String),

        #[error("未找到大模型供应商: {0}")]
        ProviderNotFound(String),

        #[error("配置错误: {0}")]
        Configuration(String),

        #[error("API 调用失败: {0}")]
        APICall(String),

        #[error("输出验证失败: {0}")]
        Validation(String),

        #[error("不支持的模型: {0}")]
        ModelNotSupported(String),

        #[error("API 速率限制: {0}")]
        RateLimit(String),

        #[error("API 认证失败: {0}")]
        Authentication(String),

        #[error("内容被安全过滤器阻止: {0}")]
        ContentFilter(String),
    }

    /// 将 async-openai 的 OpenAIError 映射到 LLMError
    impl From<async_openai::error::OpenAIError> for LLMError {
        fn from(err: async_openai::error::OpenAIError) -> Self {
            LLMError::APICall(err.to_string())
        }
    }
    ```

    **步骤 C — 在 src/error.rs 末尾（tests 模块内）追加 LLMError 测试用例**

    在 `src/error.rs` 的 `#[cfg(test)] mod tests` 内，在最后一个测试函数之后追加以下 9 个测试函数：

    ```rust
    #[test]
    fn test_llm_error_general_message_chinese() {
        let err = LLMError::General("test".into());
        let msg = err.to_string();
        assert!(msg.contains("LLM 调用失败"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_provider_not_found_message_chinese() {
        let err = LLMError::ProviderNotFound("unknown_provider".into());
        let msg = err.to_string();
        assert!(msg.contains("未找到大模型供应商"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_configuration_message_chinese() {
        let err = LLMError::Configuration("bad config".into());
        let msg = err.to_string();
        assert!(msg.contains("配置错误"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_api_call_message_chinese() {
        let err = LLMError::APICall("timeout".into());
        let msg = err.to_string();
        assert!(msg.contains("API 调用失败"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_validation_message_chinese() {
        let err = LLMError::Validation("invalid output".into());
        let msg = err.to_string();
        assert!(msg.contains("输出验证失败"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_model_not_supported_message_chinese() {
        let err = LLMError::ModelNotSupported("gpt-5".into());
        let msg = err.to_string();
        assert!(msg.contains("不支持的模型"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_rate_limit_message_chinese() {
        let err = LLMError::RateLimit("too many requests".into());
        let msg = err.to_string();
        assert!(msg.contains("API 速率限制"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_authentication_message_chinese() {
        let err = LLMError::Authentication("invalid key".into());
        let msg = err.to_string();
        assert!(msg.contains("API 认证失败"), "消息应包含中文: {}", msg);
    }

    #[test]
    fn test_llm_error_content_filter_message_chinese() {
        let err = LLMError::ContentFilter("blocked content".into());
        let msg = err.to_string();
        assert!(msg.contains("内容被安全过滤器阻止"), "消息应包含中文: {}", msg);
    }
    ```

    测试必须与现有测试风格一致：`use super::*;` + 断言中文错误消息。
  </action>

  <verify>
    <automated>cd E:/GitLib/NarratoAI && cargo check 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check` 编译通过，无错误
    - `src/error.rs` 包含 `pub enum LLMError` 声明
    - `src/error.rs` 包含 9 个 LLMError 变体
    - `src/error.rs` 包含 `impl From<async_openai::error::OpenAIError> for LLMError`
    - `Cargo.toml` [dependencies] 包含 `async-openai`、`image`、`base64`、`futures`、`reqwest`、`async-trait`
    - `Cargo.toml` [dev-dependencies] 包含 `wiremock`
    - `cargo test error::tests::test_llm_error_general_message_chinese 2>&1` 通过
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 创建 src/llm/types.rs（数据类型）+ src/llm/provider.rs（LlmProvider trait）</name>
  <files>
    src/llm/types.rs
    src/llm/provider.rs
  </files>

  <read_first>
    src/llm/ (空目录 — 需创建)
    src/ffmpeg/hwaccel.rs — 现有 struct/enum 定义模式（`#[derive(Debug, Clone)]`）
    src/error.rs — 本次任务新添加的 LLMError 枚举
    Python 参考: app/services/llm/base.py — 对应 LlmProvider trait 的 VisionModelProvider / TextModelProvider 抽象
  </read_first>

  <action>
    **步骤 A — 创建 src/llm/types.rs**

    定义 LLM 服务层专用的辅助数据类型。文件内容：

    ```rust
    use serde::{Deserialize, Serialize};

    /// LLM 响应格式选项
    #[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
    pub enum LlmResponseFormat {
        /// 普通文本格式
        Text,
        /// JSON 格式（触发 D-19 response_format=json_object 回退逻辑）
        Json,
    }

    /// 视觉分析批量处理配置
    #[derive(Debug, Clone)]
    pub struct VisionBatchConfig {
        /// 每批最多图片数，默认 10
        pub batch_size: usize,
        /// 最大并发批次数，默认 1
        pub max_concurrency: usize,
    }

    impl Default for VisionBatchConfig {
        fn default() -> Self {
            Self {
                batch_size: 10,
                max_concurrency: 1,
            }
        }
    }
    ```

    使用 `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]` 对齐 Phase 1 现有枚举模式（如 HwAccelProfile）。

    不依赖 `async-openai` 类型——保持 `types.rs` 纯净，避免上游 crate API 变动影响。

    **步骤 B — 创建 src/llm/provider.rs**

    定义 `LlmProvider` trait。使用 `#[async_trait]` 因为 trait 方法都是 async fn，需要在运行时通过 `&dyn LlmProvider` 动态分派（D-04, D-05）。

    ```rust
    use std::path::PathBuf;
    use std::pin::Pin;
    use futures::stream::Stream;
    use crate::error::LLMError;
    use crate::llm::types::{LlmResponseFormat, VisionBatchConfig};

    /// LLM Provider 统一 trait
    ///
    /// 单一 trait 包含三个方法——文本生成（非流式）、文本生成（流式）、图片视觉分析。
    /// 底层都是 `/v1/chat/completions` 端点，只是 content 格式不同（D-04）。
    #[async_trait::async_trait]
    pub trait LlmProvider: Send + Sync {
        /// 非流式文本生成
        ///
        /// 返回完整响应文本。当 response_format 为 Some(Json) 时，
        /// 实现应尝试 D-19 JSON 回退逻辑。
        async fn generate_text(
            &self,
            prompt: &str,
            system_prompt: Option<&str>,
            temperature: Option<f32>,
            max_tokens: Option<u32>,
            response_format: Option<LlmResponseFormat>,
        ) -> Result<String, LLMError>;

        /// 流式文本生成
        ///
        /// 返回 token 文本流。每个 item 是从 ChatCompletionChunk delta.content 提取的 String 片段（D-09）。
        /// 流中途错误立即终止并返回 Err，不做重试（D-10）。
        async fn generate_text_stream(
            &self,
            prompt: &str,
            system_prompt: Option<&str>,
            temperature: Option<f32>,
            max_tokens: Option<u32>,
        ) -> Result<Pin<Box<dyn Stream<Item = Result<String, LLMError>> + Send>>, LLMError>;

        /// 视觉分析
        ///
        /// 接收图片文件路径列表 + 文本 prompt。内部完成图片预处理（thumbnail→JPEG→base64→data URL），
        /// 按 batch_size 分批，使用 Semaphore 控制并发（D-17, D-18），每批发送 vision 消息给 LLM。
        /// 返回按批次顺序排列的分析结果列表。
        async fn analyze_images(
            &self,
            images: &[PathBuf],
            prompt: &str,
            batch_size: Option<usize>,
            max_concurrency: Option<usize>,
        ) -> Result<Vec<String>, LLMError>;
    }
    ```

    注意：
    - 使用 `#[async_trait::async_trait]` 而非 `pub use` — 减少引用路径混乱
    - `Send + Sync` 超限 trait 确保 trait object 是线程安全的（D-05: `Arc<dyn LlmProvider>`）
    - `Stream` 来自 `futures::stream::Stream`，`Pin` 来自 `std::pin::Pin`
    - `LlmResponseFormat` 用于控制是否启用 JSON response_format fallback（D-19）

    不需要内附 `#[cfg(test)]` 测试模块——此文件定义 trait 接口，测试在 `tests/llm_test.rs` 中实现。
  </action>

  <verify>
    <automated>cd E:/GitLib/NarratoAI && cargo check 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `cargo check` 编译通过
    - `src/llm/types.rs` 包含 `LlmResponseFormat` enum（Text / Json 两个变体）
    - `src/llm/types.rs` 包含 `VisionBatchConfig` struct
    - `VisionBatchConfig::default().batch_size == 10`
    - `VisionBatchConfig::default().max_concurrency == 1`
    - `src/llm/provider.rs` 包含 `pub trait LlmProvider` 声明
    - `LlmProvider` trait 包含 `generate_text`、`generate_text_stream`、`analyze_images` 三个方法
    - `LlmProvider` trait 应用 `#[async_trait]` 并约束 `Send + Sync`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Security Context

ASVS Level 1 生效。本 Plan 定义基础设施，不涉及运行时的数据流，但仍需防范：

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | LLMError::Configuration(String) | mitigate | Configuration 错误消息不应包含 API key 明文——在 Plan 02 的错误映射中确保截断 |
| T-02-01-02 | Spoofing | async-openai Client (Cargo.toml) | transfer | 依赖声明指定版本号，由 cargo 的 checksum 和 registry 安全性保障包完整性 |

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo.toml → crates.io | 依赖拉取时的完整性边界——由 cargo 锁定文件保障，超出 ASVS L1 范围 |
</threat_model>

<verification>
1. `cargo check` 编译通过
2. `cargo test 2>&1` 确保 Phase 1 测试不受影响（LLMError 新增不破坏现有 `#[cfg(test)] mod tests`）
3. 手动验证 `src/error.rs` 中 LLMError 的 9 个变体名称和中文消息对齐 D-14
</verification>

<success_criteria>
1. Cargo.toml 包含所有 7 个新依赖声明
2. src/error.rs 包含 LLMError 枚举（9 自描述变体 + 1 From 转换）
3. src/llm/types.rs 包含 LlmResponseFormat 和 VisionBatchConfig
4. src/llm/provider.rs 包含 LlmProvider trait（3 方法 + async-trait + Send+Sync）
5. `cargo test` 全部通过（含 LLMError 中文消息测试）
</success_criteria>

<output>
After completion, create `.planning/phases/02-llm-service-layer/02-01-SUMMARY.md`
</output>
