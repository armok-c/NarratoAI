---
phase: 14-foundation-frontend-scaffold
plan: 14-03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - E:\GitLib\NarratoAI\narratoai-core\Cargo.toml
  - E:\GitLib\NarratoAI\src-tauri\Cargo.toml
  - E:\GitLib\NarratoAI\narratoai-core\src\documentary\types.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\sde\types.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\sdp\types.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\script\types.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\config\types.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\llm\mod.rs
  - E:\GitLib\NarratoAI\narratoai-core\src\tts\mod.rs
  - E:\GitLib\NarratoAI\src-tauri\src\models\progress.rs
  - E:\GitLib\NarratoAI\src-tauri\src\error.rs
  - E:\GitLib\NarratoAI\src\types\generated\.gitkeep
autonomous: true
requirements: [FNDT-06, TDET-03]
user_setup: []

must_haves:
  truths:
    - "ts-rs crate 依赖配置在 narratoai-core 和 src-tauri 的 Cargo.toml 中"
    - "所有 Tauri 命令请求/响应类型（DocumentaryRequest, SdeRequest, SdpRequest, ProgressPayload, CommandResponse）都标注了 #[derive(TS)] 和 #[ts(export)]"
    - "ts-rs 导出目标路径指向 src/types/generated/"
    - "src/types/generated/ 目录存在"
    - "TDET-03 已验证 — 代码库中不存在 assert!(true) 测试桩"
  artifacts:
    - path: "E:\\GitLib\\NarratoAI\\narratoai-core\\Cargo.toml"
      provides: "ts-rs dependency added with serde-compat feature"
      contains: "ts-rs"
    - path: "E:\\GitLib\\NarratoAI\\src-tauri\\Cargo.toml"
      provides: "ts-rs dependency added"
      contains: "ts-rs"
    - path: "E:\\GitLib\\NarratoAI\\narratoai-core\\src\\documentary\\types.rs"
      provides: "DocumentaryRequest with #[derive(TS)] #[ts(export, export_to = ...)]"
    - path: "E:\\GitLib\\NarratoAI\\src-tauri\\src\\models\\progress.rs"
      provides: "ProgressPayload, CommandResponse, ScriptInfo with #[derive(TS)]"
    - path: "E:\\GitLib\\NarratoAI\\src\\types\\generated\\.gitkeep"
      provides: "Placeholder directory for ts-rs generated .ts type files"
  key_links:
    - from: "Cargo.toml (both)"
      to: "ts-rs crate"
      via: "dependencies.ts-rs"
    - from: "Rust structs (all)"
      to: "frontend src/types/generated/"
      via: "#[ts(export_to = \"../src/types/generated/\")]"
    - from: "narratoai-core types"
      to: "src-tauri types"
      via: "shared frontend type import space"

---

<objective>
集成 ts-rs TypeScript 类型自动导出系统，完成 TDET-03 集成测试桩验证

Purpose: ts-rs 在 Rust 编译时自动从 #[derive(TS)] 结构体生成 `.ts` 类型定义文件，消除前后端类型不同步的手动维护成本。TDET-03 确认 v1.0 遗留的 `assert!(true)` 测试桩已清理完毕。

Output: narratoai-core 和 src-tauri 的 Cargo.toml 新增 ts-rs 依赖，关键 IPC 结构体标注 #[derive(TS)] + #[ts(export)]，src/types/generated/ 目录就绪，TDET-03 验证通过文档记录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-RESEARCH.md
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md（D-09/D-10/D-11 ts-rs 决策）
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md（D-12/D-13 TDET-03 决策）

<interfaces>
<!-- ts-rs export target paths -->

Active crates and their CANONICAL export paths:
- narratoai-core (Cargo.toml at E:/GitLib/NarratoAI/narratoai-core/Cargo.toml):
  export_to = "../src/types/generated/" → resolves to E:/GitLib/NarratoAI/src/types/generated/
- src-tauri (Cargo.toml at E:/GitLib/NarratoAI/src-tauri/Cargo.toml):
  export_to = "../src/types/generated/" → resolves to E:/GitLib/NarratoAI/src/types/generated/
- Both crates export to the SAME directory — fine because ts-rs creates one .ts file per type
</interfaces>

<context_files>
Current Cargo.toml files:
@E:\GitLib\NarratoAI\narratoai-core\Cargo.toml
@E:\GitLib\NarratoAI\src-tauri\Cargo.toml

Rust structs needing #[derive(TS)]:
@E:\GitLib\NarratoAI\narratoai-core\src\documentary\types.rs (DocumentaryRequest)
@E:\GitLib\NarratoAI\narratoai-core\src\sde\types.rs (SdeRequest)
@E:\GitLib\NarratoAI\narratoai-core\src\sdp\types.rs (SdpRequest)
@E:\GitLib\NarratoAI\narratoai-core\src\script\types.rs (ScriptClip, OstType)
@E:\GitLib\NarratoAI\src-tauri\src\models\progress.rs (ProgressPayload, CommandResponse, ScriptInfo)
@E:\GitLib\NarratoAI\src-tauri\src\error.rs (CommandError)
</context_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 集成 ts-rs — Cargo.toml 依赖 + #[derive(TS)] 标注 + 导出目录</name>
  <read_first>
    - E:\GitLib\NarratoAI\narratoai-core\Cargo.toml（当前状态，添加依赖）
    - E:\GitLib\NarratoAI\src-tauri\Cargo.toml（当前状态，添加依赖）
    - E:\GitLib\NarratoAI\narratoai-core\src\documentary\types.rs（添加 TS derive）
    - E:\GitLib\NarratoAI\narratoai-core\src\sde\types.rs
    - E:\GitLib\NarratoAI\narratoai-core\src\sdp\types.rs
    - E:\GitLib\NarratoAI\narratoai-core\src\script\types.rs
    - E:\GitLib\NarratoAI\narratoai-core\src\config\types.rs
    - E:\GitLib\NarratoAI\src-tauri\src\models\progress.rs
    - E:\GitLib\NarratoAI\src-tauri\src\error.rs
  </read_first>
  <files>
    E:\GitLib\NarratoAI\narratoai-core\Cargo.toml
    E:\GitLib\NarratoAI\src-tauri\Cargo.toml
    E:\GitLib\NarratoAI\narratoai-core\src\documentary\types.rs
    E:\GitLib\NarratoAI\narratoai-core\src\sde\types.rs
    E:\GitLib\NarratoAI\narratoai-core\src\sdp\types.rs
    E:\GitLib\NarratoAI\narratoai-core\src\script\types.rs
    E:\GitLib\NarratoAI\narratoai-core\src\config\types.rs
    E:\GitLib\NarratoAI\narratoai-core\src\llm\mod.rs
    E:\GitLib\NarratoAI\narratoai-core\src\tts\mod.rs
    E:\GitLib\NarratoAI\src-tauri\src\models\progress.rs
    E:\GitLib\NarratoAI\src-tauri\src\error.rs
    E:\GitLib\NarratoAI\src\types\generated\.gitkeep
  </files>
  <action>
    按 D-09/D-10/D-11 集成 ts-rs 类型导出系统：

    **1. narratoai-core/Cargo.toml** — 添加依赖：
    ```toml
    [dependencies]
    ts-rs = { version = "11", features = ["serde-compat", "format"] }
    ```
    保持其他所有依赖不变。

    **2. src-tauri/Cargo.toml** — 添加依赖：
    ```toml
    [dependencies]
    ts-rs = { version = "11", features = ["serde-compat", "format"] }
    ```
    保持其他所有依赖不变。

    **3. narratoai-core 结构体标注** — 添加 `#[derive(TS)]` 在已有 `#[derive(Debug, Clone, Serialize, Deserialize)]` 行之后，加 `#[ts(export, export_to = "../src/types/generated/")]` 属性。
    
    具体标注的结构体清单（D-10 导出范围）：
    
    - `narratoai-core/src/documentary/types.rs`:
      - `DocumentaryRequest` — 加 `#[derive(TS)]` + `#[ts(export, export_to = "../src/types/generated/")]`
    
    - `narratoai-core/src/sde/types.rs`:
      - `SdeRequest` — 相同模式
    
    - `narratoai-core/src/sdp/types.rs`:
      - `SdpRequest` — 相同模式
    
    - `narratoai-core/src/script/types.rs`:
      - `ScriptClip` — 相同模式
      - `OstType`（如果存在且是 enum 或 struct）— 相同模式
    
    - `narratoai-core/src/config/types.rs`:
      - `AppConfig` — 相同模式
      - 各配置段类型（`LLMConfig`, `TtsSetting`, `AudioConfig`, `BgmConfig`, `ExportConfig`, `SubtitleConfig` 等，按实际文件内容覆盖）
    
    - `narratoai-core/src/llm/mod.rs`:
      - LLM 相关配置类型（`LlmConfig`, `ModelConfig` 等，按实际文件覆盖）
    
    - `narratoai-core/src/tts/mod.rs`:
      - `TtsConfig`、TTS 引擎枚举（如果存在 `enum TtsEngine`）— 相同模式
    
    **4. src-tauri 结构体标注**：
    
    - `src-tauri/src/models/progress.rs`:
      - `ProgressPayload` — 加 `#[derive(TS)]` + `#[ts(export, export_to = "../src/types/generated/")]`
      - `CommandResponse` — 相同模式
      - `ScriptInfo` — 相同模式
    
    - `src-tauri/src/error.rs`:
      - `CommandError` — 相同模式

    **关键规则**：
    - 每个结构体保持原有 `#[derive(...)]` 不变，仅在尾部追加 `TS`
    - `#[ts(...)]` 属性在 derive 行之后独立一行
    - `PathBuf` 字段：ts-rs 自动映射为 `string`，无需特殊处理
    - `serde(skip_serializing_if)` 等 serde 属性保持不动，ts-rs 的 `serde-compat` feature 会读取它们

    **5. 创建输出目录占位**：
    - 创建 `src/types/generated/` 目录
    - 放入 `.gitkeep` 保持目录在 git 中
    - 创建 `src/types/generated/tsconfig.json`（可选，让 IDE 将其识别为模块）：
      `{ "compilerOptions": { "module": "ESNext", "moduleResolution": "bundler" }, "include": ["*.ts"] }`

    **6. 触发类型生成验证**：
    运行 `cd E:/GitLib/NarratoAI && cargo test` 触发 ts-rs 的类型收集。注意：
    - ts-rs 在 `#[test]` 阶段收集所有 `#[ts(export)]` 标注的类型并生成 `.ts` 文件
    - 如果 `cargo test` 因为现有测试失败而报错，ts-rs 生成可能仍在常规构建阶段完成
    - 更可靠的方式：运行 `cargo build` — ts-rs 作为 proc-macro 在编译时处理 derive，但实际文件写需要 ts-rs 的 `export`  feature 激活
    
    **注意：ts-rs 在 11.x 中 export 机制**：
    - 为触发实际文件写入，可能需要添加一个 bin target 或 test 来收集所有类型
    - 最简单方案：创建一个独立的测试文件 `narratoai-core/tests/ts_export_test.rs`，内容为 `#[test] fn trigger_ts_export() { ts_rs::export_all!(); }` — 但 ts-rs 11 的 API 不同
    - 替代方案：在 narratoai-core 或 src-tauri 中添加 `[features] ts-export = ["ts-rs/export"]`，暂不激活，Phase 14 仅完成依赖和标注配置
    - **Phase 14 交付范围**：依赖 + 标注 → `cargo build` 编译通过。实际 .ts 文件生成留到后续 Phase（构建脚本集成时触发）
  </action>
  <acceptance_criteria>
    - `narratoai-core/Cargo.toml` 和 `src-tauri/Cargo.toml` 均包含 `ts-rs = { version = "11", features = ["serde-compat", "format"] }`
    - `narratoai-core/src/documentary/types.rs` 中 DocumentaryRequest 的 derive 包含 TS
    - `narratoai-core/src/sde/types.rs` 中 SdeRequest 的 derive 包含 TS
    - `narratoai-core/src/sdp/types.rs` 中 SdpRequest 的 derive 包含 TS
    - `narratoai-core/src/script/types.rs` 中 ScriptClip 的 derive 包含 TS
    - `src-tauri/src/models/progress.rs` 中 ProgressPayload、CommandResponse、ScriptInfo 的 derive 包含 TS
    - 所有标注的结构体都有 `#[ts(export, export_to = "../src/types/generated/")]`
    - `src/types/generated/.gitkeep` 存在
    - `cargo build` 在 narratoai-core 和 src-tauri 都编译通过（或至少 narratoai-core 编译通过）
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && cargo build -p narratoai-core 2>&1 | tail -10</automated>
  </verify>
  <done>ts-rs 依赖配置完成，关键结构体标注完成，cargo build 通过</done>
</task>

<task type="auto">
  <name>Task 2: TDET-03 集成测试桩验证</name>
  <read_first>
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-RESEARCH.md（TDET-03 Verification 小节）
    - E:\GitLib\NarratoAI\.planning\STATE.md（Pending Todos — 集成测试桩替换）
  </read_first>
  <files>
    （无代码文件修改 — 仅验证和文档记录）
  </files>
  <action>
    按 D-12/D-13 执行 TDET-03 验证。此任务**不修改任何代码**，仅确认已清理状态。

    执行步骤：

    1. 运行 `grep -r 'assert!(true)' E:/GitLib/NarratoAI/narratoai-core/tests/ --include='*.rs'` 确认无匹配
    2. 运行 `grep -r 'assert!(true)' E:/GitLib/NarratoAI/src-tauri/ --include='*.rs'` 确认无匹配
    3. 运行 `grep -r 'assert!(true)' E:/GitLib/NarratoAI/ --include='*.rs'` 确认无匹配（排除 node_modules）
    4. 在根和工作区: `grep -r 'assert!.*true' --include='*.rs' E:/GitLib/NarratoAI/narratoai-core/tests/` — 确认零匹配
    5. 如果以上全部返回零匹配 → TDET-03 完成，无需操作
    6. 如果有任何残留 `assert!(true)` → 替换为真实断言，无法脱离外部依赖的测试标记 `#[ignore = "需要 FFmpeg/网络"]`

    验证结论记录在此 PLAN.md 的 TDET-03 验证结果中（不创建单独文件）：

    ```
    ## TDET-03 验证结果
    
    **验证时间**: 2026-05-12
    **验证命令**: grep -r 'assert!(true)' narratoai-core/tests/ src-tauri/ --include='*.rs'
    **结果**: 零匹配 — 代码库中已无 assert!(true) 桩
    **处理**: TDET-03 标记为已完成，无需代码修改
    
    **验证人**: Phase 14 Planner (automated)
    **状态**: COMPLETE
    ```

    任务完成后，在 SUMMARY 文件中记录此验证结果。
  </action>
  <acceptance_criteria>
    - 执行 grep 命令确认 assert!(true) 零匹配
    - 若发现残留桩：替换为真实断言
    - TDET-03 验证记录存在于 Phase 14 文档中
  </acceptance_criteria>
  <verify>
    <automated>grep -r 'assert!.*true' --include='*.rs' E:/GitLib/NarratoAI/narratoai-core/tests/ E:/GitLib/NarratoAI/src-tauri/src/ 2>/dev/null | wc -l | xargs -I{} echo "assert matches: {}"</automated>
  </verify>
  <done>TDET-03 验证完成：确认代码库无 assert!(true) 桩，标记为已完成</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ts-rs build-time macro -> filesystem | ts-rs proc-macro 在编译时读取结构体定义并写入 .ts 文件 |
| Cargo.toml dependency | ts-rs 作为编译时依赖，不产生运行时影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | ts-rs export_to path | mitigate | export_to 路径限制在 `src/types/generated/` 目录内，不包含 `..` 遍历；代码审查确保路径正确 |
| T-14-07 | Information Disclosure | ts-rs generated .ts files | accept | 生成文件中不含运行时敏感数据（API keys 等）；结构体定义中敏感字段使用 `#[serde(skip_serializing)]` 已在 Rust 端处理 |
</threat_model>

<verification>
- [ ] `cargo build -p narratoai-core` 编译通过
- [ ] `cargo build -p narratoai-app` 编译通过（或 narratoai-core 编译通过即可，Tauri 构建可能依赖前端打包）
- [ ] grep 确认零 `assert!(true)` 匹配
- [ ] `src/types/generated/` 目录存在
</verification>

<success_criteria>
Phase 14 ts-rs 集成和 TDET-03 完成：
1. ts-rs 依赖就绪，#[derive(TS)] + export 标注在所有关键 IPC 类型上
2. `cargo build` 通过（含 ts-rs）
3. `src/types/generated/` 目录就绪
4. TDET-03 验证通过 — 确认 assert!(true) 零残留
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation-frontend-scaffold/14-03-SUMMARY.md`
</output>
