---
phase: 14-foundation-frontend-scaffold
plan: 14-02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - E:\GitLib\NarratoAI\src\composables\useTauri.ts
  - E:\GitLib\NarratoAI\src\composables\useTheme.ts
  - E:\GitLib\NarratoAI\src\utils\logger.ts
  - E:\GitLib\NarratoAI\src\types\index.ts
  - E:\GitLib\NarratoAI\src\stores\app.ts
  - E:\GitLib\NarratoAI\src\stores\mode.ts
  - E:\GitLib\NarratoAI\src\stores\video.ts
  - E:\GitLib\NarratoAI\src\stores\pipeline.ts
  - E:\GitLib\NarratoAI\src\stores\script.ts
  - E:\GitLib\NarratoAI\src\stores\llm.ts
  - E:\GitLib\NarratoAI\src\stores\tts.ts
  - E:\GitLib\NarratoAI\src\stores\bgm.ts
  - E:\GitLib\NarratoAI\src\stores\export.ts
  - E:\GitLib\NarratoAI\src\stores\index.ts
  - E:\GitLib\NarratoAI\src\api\pipelineApi.ts
  - E:\GitLib\NarratoAI\src\api\scriptApi.ts
  - E:\GitLib\NarratoAI\src\api\configApi.ts
  - E:\GitLib\NarratoAI\src\api\exportApi.ts
autonomous: true
requirements: [FNDT-04, FNDT-05]
user_setup: []

must_haves:
  truths:
    - "9 个 Pinia 3 stores（app/mode/video/pipeline/script/llm/tts/bgm/export）全部可导入并返回正确的初始状态"
    - "useAppStore().theme 初始值为 localStorage.getItem('theme') 或 'light'"
    - "tauriInvoke() 正确序列化 BigInt、过滤敏感字段（apiKey/password/token/secret/api_key/accessToken）"
    - "tauriInvoke() 调试日志仅在 DEV 模式或 VITE_DEBUG=true 时输出"
    - "API 模块的每个函数使用 tauriInvoke 调用对应 Tauri 命令"
    - "src/stores/index.ts 集中 re-export 所有 9 个 use*Store"
  artifacts:
    - path: "E:\\GitLib\\NarratoAI\\src\\composables\\useTauri.ts"
      provides: "tauriInvoke wrapper with BigInt serialization, sensitive field filtering, debug logging"
      contains: "tauriInvoke, normalizeTauriArgs, filterSensitiveArgs, isDebugMode, debugLog"
    - path: "E:\\GitLib\\NarratoAI\\src\\composables\\useTheme.ts"
      provides: "useTheme composable — 主题切换逻辑（读写 app store + Vuetify theme）"
    - path: "E:\\GitLib\\NarratoAI\\src\\utils\\logger.ts"
      provides: "loglevel wrapper for client-side logging"
    - path: "E:\\GitLib\\NarratoAI\\src\\types\\index.ts"
      provides: "Manual supplementary types (VideoMeta, pipeline step types)"
    - path: "E:\\GitLib\\NarratoAI\\src\\stores\\*.ts"
      provides: "9 Pinia store skeletons with defineStore('name', () => { ... }) Composition API"
    - path: "E:\\GitLib\\NarratoAI\\src\\api\\*.ts"
      provides: "Domain-split Tauri IPC command modules using tauriInvoke"
  key_links:
    - from: "src/stores/pipeline.ts"
      to: "src/composables/useTauri.ts"
      via: "import { tauriInvoke } from '@/composables/useTauri'"
    - from: "src/api/pipelineApi.ts"
      to: "src/composables/useTauri.ts"
      via: "import { tauriInvoke } from '@/composables/useTauri'"
    - from: "src/api/*.ts functions"
      to: "src-tauri/src/commands/"
      via: "tauriInvoke('command_name', args) -> Tauri IPC"
    - from: "src/stores/index.ts"
      to: "src/stores/*.ts"
      via: "export { useAppStore } from './app' (and all 8 others)"

---

<objective>
创建 tauriInvoke 封装层、9 个 Pinia Store 骨架和 API 命令模块

Purpose: 实现 Phase 14 核心架构层 — tauriInvoke 是前端与 Tauri 后端的唯一 IPC 通道（BigInt 安全/敏感字段过滤/调试日志）；9 个 Pinia Store 骨架定义了完整的状态模型和最小 action 接口；API 模块按领域分组 Tauri 命令调用。

Output: useTauri.ts（直接移植 SmartEdit）、useTheme.ts、logger.ts、src/types/index.ts、9 个 store 文件 + index.ts、4 个 API 模块
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-UI-SPEC.md

<interfaces>
<!-- Pinia Store interface contracts from UI-SPEC and CONTEXT D-04/D-05/D-06 -->

Store contracts (skeleton level — Phase 14 has types + initial state + basic actions only):

1. app: theme: 'light'|'dark', toggleTheme(), setTheme(val)
   - Read theme from localStorage on init, write on toggle

2. mode: currentMode: 'documentary'|'sde'|'sdp', setMode(val), resetPipeline()

3. video: videos: VideoMeta[], addVideo(v), removeVideo(id), clear()
   - VideoMeta: { id: string, name: string, path: string, status: string }

4. pipeline: status: 'idle'|'running'|'error', progress: ProgressPayload|null, start(), stop(), reset(), updateProgress(p)
   - ProgressPayload: { pipeline_type, task_id, step_name, percent, message, step_index, total_steps, status }

5. script: content: string|null, load(id), save(), validate(), update(text)

6. llm: visionConfig: LLMConfig, textConfig: LLMConfig, updateVisionConfig(c), updateTextConfig(c)
   - LLMConfig: { provider: string, model: string, apiKey: string, baseUrl: string }

7. tts: engine: string, engineConfigs: Record<string, any>, setEngine(e), updateConfig(e, c)

8. bgm: folder: string, mode: 'random'|'specified', setFolder(p), setMode(m)

9. export: outputDir: string, format: string, setOutputDir(p), setFormat(f)

All stores use: defineStore('name', () => { const state = ref(...); function action() {...}; return { state, action } })
</interfaces>

<context_files>
SmartEdit useTauri.ts to direct port:
@E:\GitLib\SmartEdit\src\composables\useTauri.ts

SmartEdit store pattern:
@E:\GitLib\SmartEdit\src\stores\app.ts

Existing project scaffold (created by Plan 14-01):
@E:\GitLib\NarratoAI\package.json

Tauri backend commands (API modules invoke these):
@E:\GitLib\NarratoAI\src-tauri\src\commands\mod.rs
@E:\GitLib\NarratoAI\src-tauri\src\commands\pipeline.rs
@E:\GitLib\NarratoAI\src-tauri\src\commands\script.rs
@E:\GitLib\NarratoAI\src-tauri\src\commands\export.rs
</context_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 composables（useTauri.ts 直接移植 + useTheme.ts + logger.ts）</name>
  <read_first>
    - E:\GitLib\SmartEdit\src\composables\useTauri.ts（直接移植源）
    - E:\GitLib\SmartEdit\src\stores\app.ts（主题模式参考）
  </read_first>
  <files>
    E:\GitLib\NarratoAI\src\composables\useTauri.ts
    E:\GitLib\NarratoAI\src\composables\useTheme.ts
    E:\GitLib\NarratoAI\src\utils\logger.ts
    E:\GitLib\NarratoAI\src\types\index.ts
  </files>
  <action>
    创建 4 个独立文件（全部新建）：

    1. `src/composables/useTauri.ts` — **直接移植** SmartEdit 的 useTauri.ts：
       - 保持全部现有函数：`isDebugMode()`、`filterSensitiveArgs()`、`debugLog()`、`debugError()`、`serializeBigIntArg()`、`normalizeTauriArgs()`、`tauriInvoke<T>()`
       - `import { invoke } from '@tauri-apps/api/core'`
       - BigInt 序列化使用 `Number.MAX_SAFE_INTEGER` / `Number.MIN_SAFE_INTEGER` 检查
       - 敏感字段列表：`['apiKey', 'password', 'token', 'secret', 'api_key', 'accessToken']`
       - 调试日志：DEV 模式或 `VITE_DEBUG=true` 时输出
       - 函数体、参数名、错误消息中文提示、export 与 SmartEdit 完全一致
       - 移除 SmartEdit 特定的 eslint-disable 注释（NarratoAI 不使用 eslint）

    2. `src/composables/useTheme.ts`：
       - 导入 `useAppStore`（创建跨 store 引用包装，但不实际引用 Vue 实例）
       - 导出 `useTheme()` composable，返回 `{ theme: computed(() => store.theme), toggleTheme: store.toggleTheme }`
       - 使用 Vuetify 的 `useTheme()` 同步 Material Design 3 主题
       - 注意：这是骨架，Phase 15 会完善。当前只需返回 app store 的 theme 引用。

    3. `src/utils/logger.ts`：
       - `import log from 'loglevel'`
       - 根据 `import.meta.env.DEV` 设置日志级别：DEV 时 `log.setLevel('debug')`，生产时 `log.setLevel('warn')`
       - 导出 `logger` 对象绑定了 `log` 实例的方法：`logger.debug()`, `logger.info()`, `logger.warn()`, `logger.error()`
       - 命名空间前缀: `'[NarratoAI]'` 自动附加

    4. `src/types/index.ts`：
       - 定义 `VideoMeta` 接口：`{ id: string; name: string; path: string; status: string; duration?: number }`
       - 定义 `ProgressPayload` 接口：`{ pipeline_type: string; task_id: string; step_name: string; percent: number; message: string; step_index: number; total_steps: number; status: string }` — 与 Rust ProgressPayload 一致
       - 定义 `LLMConfig` 接口：`{ provider: string; model: string; apiKey: string; baseUrl: string }`
       - 添加 `// TODO:: Phase 14 skeleton — replace with ts-rs generated types from src/types/generated/`
       - 使用 `import type` 从 `@/types/generated` 的导入注释为预留，不加实际导入

    关键约束：useTauri.ts 是全局 IPC 单例，不涉及 store 状态；直接移植确保后续 Phase 无需修正模式差异。
  </action>
  <acceptance_criteria>
    - `src/composables/useTauri.ts` 包含 `export async function tauriInvoke<T>(command: string, args?: Record<string, unknown>): Promise<T>`
    - `src/composables/useTauri.ts` 包含 `const sensitiveFields = ['apiKey', 'password', 'token', 'secret', 'api_key', 'accessToken']`
    - `src/composables/useTauri.ts` 注释/消息使用中文
    - `src/utils/logger.ts` export 包含 `logger.debug`, `logger.info`, `logger.warn`, `logger.error`
    - `src/types/index.ts` 定义 `VideoMeta`, `ProgressPayload`, `LLMConfig` 接口
    - `npm run typecheck` 通过零错误
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>useTauri.ts 移植完成，useTheme/logger/types 创建完成，typecheck 通过</done>
</task>

<task type="auto">
  <name>Task 2: 创建 9 个 Pinia 3 Store 骨架 + index.ts</name>
  <read_first>
    - E:\GitLib\SmartEdit\src\stores\app.ts（Composition API 模式参考）
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md（D-04/D-05/D-06 store 规格）
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-UI-SPEC.md（store 接口合约表）
  </read_first>
  <files>
    E:\GitLib\NarratoAI\src\stores\app.ts
    E:\GitLib\NarratoAI\src\stores\mode.ts
    E:\GitLib\NarratoAI\src\stores\video.ts
    E:\GitLib\NarratoAI\src\stores\pipeline.ts
    E:\GitLib\NarratoAI\src\stores\script.ts
    E:\GitLib\NarratoAI\src\stores\llm.ts
    E:\GitLib\NarratoAI\src\stores\tts.ts
    E:\GitLib\NarratoAI\src\stores\bgm.ts
    E:\GitLib\NarratoAI\src\stores\export.ts
    E:\GitLib\NarratoAI\src\stores\index.ts
  </files>
  <action>
    按 CONTEXT.md D-04/D-05/D-06 要求创建 9 个 store 文件加一个 index.ts re-export。所有 store 使用 Composition API 模式：`defineStore('name', () => { ... })`。

    **注意**: REQUIREMENTS.md FNDT-04 列举了 8 个 store，UI-SPEC 合约表列出了 9 个（含 export）。本计划创建全部 9 个。

    1. `src/stores/app.ts`：
       - `defineStore('app', () => { const theme = ref<'light'|'dark'>((localStorage.getItem('theme') as ...) || 'light')`
       - `function toggleTheme() { theme.value = theme.value === 'light' ? 'dark' : 'light'; localStorage.setItem('theme', theme.value); }`
       - `function setTheme(val: 'light'|'dark') { theme.value = val; localStorage.setItem('theme', val); }`
       - return { theme, toggleTheme, setTheme }

    2. `src/stores/mode.ts`：
       - `type WorkMode = 'documentary' | 'sde' | 'sdp'`
       - `const currentMode = ref<WorkMode>('documentary')`
       - `function setMode(val: WorkMode) { currentMode.value = val; }`
       - `function resetPipeline() { /* Phase 17: full implementation */ }`
       - return { currentMode, setMode, resetPipeline }

    3. `src/stores/video.ts`：
       - `import type { VideoMeta } from '@/types'`
       - `const videos = ref<VideoMeta[]>([])`
       - `function addVideo(v: VideoMeta) { videos.value.push(v); }`
       - `function removeVideo(id: string) { videos.value = videos.value.filter(v => v.id !== id); }`
       - `function clear() { videos.value = []; }`
       - return { videos, addVideo, removeVideo, clear }

    4. `src/stores/pipeline.ts`：
       - `import type { ProgressPayload } from '@/types'`
       - `type PipelineStatus = 'idle' | 'running' | 'error'`
       - `const status = ref<PipelineStatus>('idle')`
       - `const progress = ref<ProgressPayload | null>(null)`
       - `function start() { status.value = 'running'; }`
       - `function stop() { status.value = 'idle'; }`
       - `function reset() { status.value = 'idle'; progress.value = null; }`
       - `function updateProgress(p: ProgressPayload) { progress.value = p; }`
       - return { status, progress, start, stop, reset, updateProgress }

    5. `src/stores/script.ts`：
       - `const content = ref<string | null>(null)`
       - 4 个 skeleton actions 各带 TODO 注释：load, save, validate, update
       - return { content, load, save, validate, update }

    6. `src/stores/llm.ts`：
       - `import type { LLMConfig } from '@/types'`
       - `const visionConfig = ref<LLMConfig>({ provider: '', model: '', apiKey: '', baseUrl: '' })`
       - `const textConfig = ref<LLMConfig>({ provider: '', model: '', apiKey: '', baseUrl: '' })`
       - `function updateVisionConfig(c: Partial<LLMConfig>) { Object.assign(visionConfig.value, c); }`
       - `function updateTextConfig(c: Partial<LLMConfig>) { Object.assign(textConfig.value, c); }`
       - return { visionConfig, textConfig, updateVisionConfig, updateTextConfig }

    7. `src/stores/tts.ts`：
       - `const engine = ref<string>('edge_tts')`
       - `const engineConfigs = ref<Record<string, any>>({})`
       - `function setEngine(e: string) { engine.value = e; }`
       - `function updateConfig(engineName: string, config: any) { engineConfigs.value[engineName] = config; }`
       - return { engine, engineConfigs, setEngine, updateConfig }

    8. `src/stores/bgm.ts`：
       - `type BgmMode = 'random' | 'specified'`
       - `const folder = ref<string>('')`
       - `const mode = ref<BgmMode>('random')`
       - `function setFolder(p: string) { folder.value = p; }`
       - `function setMode(m: BgmMode) { mode.value = m; }`
       - return { folder, mode, setFolder, setMode }

    9. `src/stores/export.ts`：
       - `defineStore('export', () => { const outputDir = ref(''); const format = ref('mp4'); })`
       - `function setOutputDir(p: string) { outputDir.value = p; }`
       - `function setFormat(f: string) { format.value = f; }`
       - return { outputDir, format, setOutputDir, setFormat }

    10. `E:\GitLib\NarratoAI\src\stores\index.ts`：
       - 集中 re-export 所有 9 个 use*Store：
         - `export { useAppStore } from './app'`
         - `export { useModeStore } from './mode'`
         - `export { useVideoStore } from './video'`
         - `export { usePipelineStore } from './pipeline'`
         - `export { useScriptStore } from './script'`
         - `export { useLlmStore } from './llm'`
         - `export { useTtsStore } from './tts'`
         - `export { useBgmStore } from './bgm'`
         - `export { useExportStore } from './export'`

    关键约束：
    - D-06: Phase 14 仅创建骨架（类型+初始状态+基础 actions），完整逻辑在后续 Phase
    - 不引入 `pinia-plugin-persistedstate`（app store 手动 localStorage）
    - 不使用 Options API 模式
    - pipeline store 使用本地定义的 ProgressPayload 类型（非 ts-rs 生成），标注 TODO 替换
  </action>
  <acceptance_criteria>
    - 9 个 store 文件全部创建（app/mode/video/pipeline/script/llm/tts/bgm/export），每个使用 `defineStore('name', () => { ... })`
    - `src/stores/index.ts` re-export 所有 9 个 use*Store
    - app store 从 `localStorage` 读取初始 theme
    - pipeline store 包含完整 ProgressPayload 类型的初始状态
    - `npm run typecheck` 通过零错误
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>9 个 Pinia store 骨架创建完成，index.ts re-export 完成，typecheck 通过</done>
</task>

<task type="auto">
  <name>Task 3: 创建 API 命令模块（pipelineApi/scriptApi/configApi/exportApi）</name>
  <read_first>
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md（D-08 API 模块规格）
    - E:\GitLib\NarratoAI\src-tauri\src\commands\mod.rs（Tauri 命令注册表）
    - E:\GitLib\NarratoAI\src-tauri\src\commands\pipeline.rs（流水线命令）
    - E:\GitLib\NarratoAI\src-tauri\src\commands\script.rs（脚本命令）
    - E:\GitLib\NarratoAI\src-tauri\src\commands\export.rs（导出命令）
  </read_first>
  <files>
    E:\GitLib\NarratoAI\src\api\pipelineApi.ts
    E:\GitLib\NarratoAI\src\api\scriptApi.ts
    E:\GitLib\NarratoAI\src\api\configApi.ts
    E:\GitLib\NarratoAI\src\api\exportApi.ts
  </files>
  <action>
    按 CONTEXT.md D-08 创建 4 个 API 命令模块，每个模块使用 `tauriInvoke<T>()` 封装对应 Tauri 命令。全部新建。

    1. `src/api/pipelineApi.ts`：
       - `import { tauriInvoke } from '@/composables/useTauri'`
       - 导出函数：
         - `runDocumentary(request: DocumentaryRequest): Promise<CommandResponse>` — 调用 `tauriInvoke<CommandResponse>('run_documentary', { request })`
         - `runSde(request: SdeRequest): Promise<CommandResponse>` — 调用 `tauriInvoke<CommandResponse>('run_sde', { request })`
         - `runSdp(request: SdpRequest): Promise<CommandResponse>` — 调用 `tauriInvoke<CommandResponse>('run_sdp', { request })`
         - `stopPipeline(taskId: string): Promise<void>` — 调用 `tauriInvoke<void>('stop_pipeline', { taskId })`
       - 参数类型使用 `any` 暂代替（TODO: 替换为 ts-rs 生成类型的导入）

    2. `src/api/scriptApi.ts`：
       - `import { tauriInvoke } from '@/composables/useTauri'`
       - 导出函数：
         - `loadScript(path: string): Promise<string>` — 调用 `tauriInvoke<string>('load_script', { path })`
         - `saveScript(path: string, content: string): Promise<void>` — 调用 `tauriInvoke<void>('save_script', { path, content })`
         - `validateScript(path: string): Promise<{ valid: boolean; errors: string[] }>` — 调用 `tauriInvoke('validate_script', { path })`
         - `updateNarration(path: string, clipIndex: number, narration: string): Promise<void>` — 调用 `tauriInvoke<void>('update_narration', { path, clipIndex, narration })`
         - `setOst(path: string, clipIndex: number, ostType: number): Promise<void>` — 调用 `tauriInvoke<void>('set_ost', { path, clipIndex, ostType })`

    3. `src/api/configApi.ts`：
       - `import { tauriInvoke } from '@/composables/useTauri'`
       - 导出函数：
         - `getConfig(): Promise<any>` — 调用 `tauriInvoke<any>('get_config')`
         - `setConfig(config: any): Promise<void>` — 调用 `tauriInvoke<void>('set_config', { config })`
       - 配置类型使用 `any` 暂代替（TODO: 替换为 ts-rs 生成的 AppConfig）

    4. `src/api/exportApi.ts`：
       - `import { tauriInvoke } from '@/composables/useTauri'`
       - 导出函数：
         - `exportJianyingDraft(scriptPath: string, outputDir: string): Promise<string>` — 调用 `tauriInvoke<string>('export_jianying_draft', { scriptPath, outputDir })`

    关键约束：
    - 所有函数使用 `tauriInvoke<T>()`，不直接使用 `invoke()`（D-07 强制）
    - 命令名称字符串必须与 `src-tauri/src/commands/` 中实际注册的命令匹配
    - 所有类型参数使用 `any` 或本地接口（Phase 16 完成时替换为 ts-rs 生成类型）
    - 不添加额外业务逻辑（验证、缓存、重试等），仅做 IPC 调用转发
  </action>
  <acceptance_criteria>
    - `src/api/pipelineApi.ts` 导出 runDocumentary, runSde, runSdp, stopPipeline 四个函数
    - `src/api/scriptApi.ts` 导出 loadScript, saveScript, validateScript, updateNarration, setOst 五个函数
    - `src/api/configApi.ts` 导出 getConfig, setConfig 两个函数
    - `src/api/exportApi.ts` 导出 exportJianyingDraft 一个函数
    - 所有函数使用 `tauriInvoke` 而非 `invoke`
    - `npm run typecheck` 通过零错误
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>4 个 API 模块创建完成，全部 typecheck 通过，所有函数使用 tauriInvoke 而非原始 invoke</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API module -> Tauri IPC | 前端 API 函数调用 Tauri 命令 |
| tauriInvoke -> invoke() | tauriInvoke 加工参数后转发到 invoke |
| localStorage -> app store | 主题偏好持久化到浏览器 localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | tauriInvoke debugLog | mitigate | `filterSensitiveArgs` 在日志输出前移除 apiKey/password/token/secret/api_key/accessToken；debugLog 仅在 DEV/VITE_DEBUG 时输出 |
| T-14-04 | Tampering | normalizeTauriArgs BigInt | mitigate | `serializeBigIntArg` 检查 BigInt 是否在 `Number.MIN_SAFE_INTEGER` 到 `Number.MAX_SAFE_INTEGER` 范围内，超范围抛异常 |
| T-14-05 | Information Disclosure | localStorage theme | accept | localStorage 仅存 theme 偏好（light/dark），不含敏感数据 |
</threat_model>

<verification>
- [ ] `npm run typecheck` 通过零错误
- [ ] 手动检查 `src/composables/useTauri.ts` 包含 filterSensitiveArgs 敏感字段列表
- [ ] 手动检查 `src/stores/` 下 9 个 store 文件都存在且使用 Composition API
- [ ] 手动检查 `src/api/*.ts` 所有函数使用 `tauriInvoke()` 而非 `invoke()`
</verification>

<success_criteria>
Phase 14 API 层和状态管理层全部就位：
1. tauriInvoke 封装（BigInt 序列化 + 敏感字段过滤 + 调试日志）移植完毕
2. 9 个 Pinia 3 Store 骨架就位，类型定义正确
3. 4 个 API 命令模块按领域拆分，全部使用 tauriInvoke
4. TypeScript 编译通过零错误
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation-frontend-scaffold/14-02-SUMMARY.md`
</output>
