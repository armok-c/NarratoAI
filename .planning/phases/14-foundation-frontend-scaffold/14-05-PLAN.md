---
phase: 14-foundation-frontend-scaffold
plan: 05
type: execute
wave: 1
depends_on: [14-04]
files_modified:
  - tests/audio_integration.rs (deleted)
  - tests/youtube_integration.rs (deleted)
  - narratoai-core/tests/audio_lufs_integration.rs (new)
  - narratoai-core/tests/youtube_integration.rs (new)
autonomous: true
gap_closure: true
requirements: [TDET-03]

must_haves:
  truths:
    - "集成测试中所有 assert!(true) 桩替换为真实断言"
  artifacts:
    - path: "narratoai-core/tests/audio_lufs_integration.rs"
      provides: "音频 LUFS 标准化和 RMS 集成测试"
      contains: "normalize_lufs"
    - path: "narratoai-core/tests/youtube_integration.rs"
      provides: "YouTube yt-dlp 集成测试"
      contains: "yt-dlp"
  key_links:
    - from: "narratoai-core/tests/audio_lufs_integration.rs"
      to: "narratoai-core/src/audio/normalizer.rs"
      via: "use narratoai_core::audio::normalizer"
      pattern: "normalize_lufs|get_audio_rms"
    - from: "narratoai-core/tests/youtube_integration.rs"
      to: "narratoai-core/src/youtube/downloader.rs"
      via: "使用 yt-dlp CLI 直接调用验证"
      pattern: "yt-dlp"
---

<objective>
关闭 Phase 14 验证中 SC5 的最后一个 gap：根目录 tests/ 下 3 个 assert!(true) 桩。

根目录 Cargo.toml 仅有 [workspace] 定义（无 [package]），导致根 tests/ 下的集成测试永远不会被 cargo test 执行。正确做法是将测试移入 narratoai-core/tests/ 并替换桩为真实断言。

Purpose: 满足 SC5（集成测试中所有 assert!(true) 桩替换为真实断言）和 TDET-03。
Output: 2 个新的集成测试文件替代根目录死代码文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-foundation-frontend-scaffold/14-VERIFICATION.md
@narratoai-core/src/audio/normalizer.rs
@narratoai-core/src/youtube/downloader.rs
@narratoai-core/tests/audio_normalize_test.rs
@tests/audio_integration.rs
@tests/youtube_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 替换音频集成测试桩</name>
  <files>tests/audio_integration.rs (delete), narratoai-core/tests/audio_lufs_integration.rs (new)</files>
  <read_first>
    narratoai-core/src/audio/normalizer.rs
    narratoai-core/tests/audio_normalize_test.rs
    tests/audio_integration.rs
  </read_first>
  <acceptance_criteria>
    - tests/audio_integration.rs 已删除
    - narratoai-core/tests/audio_lufs_integration.rs 存在
    - 文件中无 assert!(true)
    - cargo test -p narratoai-core --test audio_lufs_integration 编译通过
    - test_get_audio_rms_with_real_file 通过（不依赖 FFmpeg）
  </acceptance_criteria>
  <action>
    步骤 1 — 创建 narratoai-core/tests/audio_lufs_integration.rs：

    此文件包含 2 个测试函数，替代根目录的死代码桩。

    ```rust
    //! 音频 LUFS 标准化和 RMS 集成测试
    //!
    //! 覆盖 normalizer.rs 中 normalize_lufs 和 get_audio_rms 的集成场景。

    use std::f64::consts::PI;
    use std::path::Path;
    use narratoai_core::audio::normalizer::{normalize_lufs, get_audio_rms, analyze_lufs};

    fn generate_sine_wave(
        output_dir: &Path,
        name: &str,
        frequency: f64,
        duration_secs: f64,
        sample_rate: u32,
        amplitude: f64,
    ) -> std::path::PathBuf {
        let path = output_dir.join(name);
        let num_samples = (sample_rate as f64 * duration_secs) as usize;
        let data_size = (num_samples * 2) as u32;
        let file_size = 36 + data_size;
        let mut buffer = Vec::with_capacity(44 + data_size as usize);
        buffer.extend_from_slice(b"RIFF");
        buffer.extend_from_slice(&file_size.to_le_bytes());
        buffer.extend_from_slice(b"WAVE");
        buffer.extend_from_slice(b"fmt ");
        buffer.extend_from_slice(&16u32.to_le_bytes());
        buffer.extend_from_slice(&1u16.to_le_bytes());
        buffer.extend_from_slice(&1u16.to_le_bytes());
        buffer.extend_from_slice(&sample_rate.to_le_bytes());
        buffer.extend_from_slice(&(sample_rate * 2).to_le_bytes());
        buffer.extend_from_slice(&2u16.to_le_bytes());
        buffer.extend_from_slice(&16u16.to_le_bytes());
        buffer.extend_from_slice(b"data");
        buffer.extend_from_slice(&data_size.to_le_bytes());
        for i in 0..num_samples {
            let t = i as f64 / sample_rate as f64;
            let sample = (amplitude * (2.0 * PI * frequency * t).sin() * 32767.0) as i16;
            buffer.extend_from_slice(&sample.to_le_bytes());
        }
        std::fs::write(&path, &buffer).expect("写入 WAV 文件失败");
        path
    }

    fn ffmpeg_available() -> bool {
        std::process::Command::new("ffmpeg")
            .arg("-version")
            .output()
            .map(|o| o.status.success())
            .unwrap_or(false)
    }
    ```

    **test_normalize_lufs_two_pass** (#[ignore] — 需要 FFmpeg)：
    - 调用 ffmpeg_available() 检查，不可用时 return（而非 panic）
    - 在 test_output/audio_lufs/ 下生成 2 秒 440Hz 0.8 振幅正弦波 WAV（44100 Hz 单声道）
    - 调用 `normalize_lufs(&input, &output, -23.0, -1.0, 44100, 1)`
    - 断言 output 文件存在
    - 调用 `analyze_lufs(&output)` 获取标准化后的 LUFS 数据
    - 断言 `measured_I()` 在 [-26, -20] 范围内（-23 ± 3 LUFS 容差）

    **test_get_audio_rms_with_real_file**（无 #[ignore] — symphonia 纯 Rust，不需要 FFmpeg）：
    - 在临时目录下生成 2 秒 440Hz 0.8 振幅正弦波 WAV
    - 调用 `get_audio_rms(&path)`
    - 断言返回 Ok
    - 断言 RMS dB 在 [-10, 0] 范围内（0.8 振幅正弦波 RMS ≈ -4.95 dB，给 ±5 dB 容差）

    步骤 2 — 删除根目录 tests/audio_integration.rs
  </action>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && ! grep -r 'assert!(true)' tests/ narratoai-core/tests/ src-tauri/tests/ 2>/dev/null && cargo test -p narratoai-core --test audio_lufs_integration -- --include-ignored 2>&1 | tail -5</automated>
  </verify>
  <done>
    - tests/audio_integration.rs 不存在
    - narratoai-core/tests/audio_lufs_integration.rs 存在，包含 normalize_lufs 和 get_audio_rms 的真实断言
    - cargo test -p narratoai-core --test audio_lufs_integration 编译通过
    - grep -r 'assert!(true)' 根目录 tests/ 零匹配
  </done>
</task>

<task type="auto">
  <name>Task 2: 替换 YouTube 集成测试桩</name>
  <files>tests/youtube_integration.rs (delete), narratoai-core/tests/youtube_integration.rs (new)</files>
  <read_first>
    narratoai-core/src/youtube/downloader.rs
    tests/youtube_integration.rs
  </read_first>
  <acceptance_criteria>
    - tests/youtube_integration.rs 已删除
    - narratoai-core/tests/youtube_integration.rs 存在
    - 文件中无 assert!(true)
    - cargo test -p narratoai-core --test youtube_integration 编译通过
  </acceptance_criteria>
  <action>
    步骤 1 — 创建 narratoai-core/tests/youtube_integration.rs：

    注意：`get_video_formats` 是私有函数（非 pub），集成测试无法直接调用。
    替代方案：直接调用 yt-dlp CLI 并解析 JSON 输出，验证格式获取功能正常。
    这与 get_video_formats 的内部实现逻辑一致（都是调用 yt-dlp --dump-json）。

    ```rust
    //! YouTube 下载集成测试
    //!
    //! 需要系统安装 yt-dlp 和网络连接。使用 `cargo test -- --ignored` 运行。

    use std::process::Command;

    fn find_ytdlp() -> String {
        if let Ok(output) = Command::new("yt-dlp").arg("--version").output() {
            if output.status.success() {
                return "yt-dlp".to_string();
            }
        }
        panic!("yt-dlp 不可用。请访问 https://github.com/yt-dlp/yt-dlp/releases 下载安装。");
    }
    ```

    **test_ytdlp_version**（#[ignore]，保留原有实现 — 已有真实断言，无需修改）：
    - 不变，已有 assert!(output.status.success()) 和 assert!(!version.is_empty())

    **test_get_video_formats**（#[ignore]）— 替换桩：
    - 调用 find_ytdlp() 确认 yt-dlp 可用
    - 使用 yt-dlp --dump-json --flat-playlist 对一个已知公开视频 URL 调用
    - 推荐 URL：`https://www.youtube.com/watch?v=jNQXAC9IVRw`（"Me at the zoo"，YouTube 第一个视频，永久公开）
    - 设置 60 秒超时（避免网络慢时长时间挂起）
    - 解析 stdout 为 serde_json::Value
    - 断言 JSON 中 "formats" 字段存在且为数组
    - 断言 formats 数组长度 > 0
    - 断言至少一个格式包含 "format_id" 非空字符串
    - 如果 yt-dlp 命令失败（网络/URL 问题），断言 stderr 非空且包含可读错误信息

    步骤 2 — 删除根目录 tests/youtube_integration.rs
  </action>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && ! grep -r 'assert!(true)' tests/ narratoai-core/tests/ src-tauri/tests/ 2>/dev/null && echo "OK: no assert!(true) stubs" || echo "FAIL: stubs remain"</automated>
  </verify>
  <done>
    - tests/youtube_integration.rs 不存在
    - narratoai-core/tests/youtube_integration.rs 存在，包含 yt-dlp 格式获取的真实断言
    - cargo test -p narratoai-core --test youtube_integration 编译通过
    - grep -r 'assert!(true)' 根目录 tests/ 零匹配
  </done>
</task>

</tasks>

<verification>
1. grep -r 'assert!(true)' tests/ narratoai-core/tests/ src-tauri/tests/ — 零匹配
2. cargo test -p narratoai-core --test audio_lufs_integration -- --include-ignored — 全部通过
3. cargo test -p narratoai-core --test youtube_integration — 编译通过（#[ignore] 测试不自动执行）
4. cargo test -p narratoai-core — 整体测试不受影响
</verification>

<success_criteria>
- SC5 满足：整个代码库中 grep -r 'assert!(true)' 返回零匹配
- 3 个桩全部替换为真实断言
- 根目录 tests/ 下的死代码文件已删除，测试移入正确的 crate 目录
- TDET-03 完全满足
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation-frontend-scaffold/14-05-SUMMARY.md`
</output>
