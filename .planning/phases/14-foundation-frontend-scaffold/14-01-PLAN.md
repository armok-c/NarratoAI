---
phase: 14-foundation-frontend-scaffold
plan: 14-01
type: execute
wave: 1
depends_on: []
files_modified:
  - E:\GitLib\NarratoAI\package.json
  - E:\GitLib\NarratoAI\index.html
  - E:\GitLib\NarratoAI\vite.config.ts
  - E:\GitLib\NarratoAI\tsconfig.json
  - E:\GitLib\NarratoAI\tsconfig.node.json
  - E:\GitLib\NarratoAI\src\env.d.ts
  - E:\GitLib\NarratoAI\src\main.ts
  - E:\GitLib\NarratoAI\src\App.vue
  - E:\GitLib\NarratoAI\src\styles\main.css
  - E:\GitLib\NarratoAI\src\plugins\vuetify.ts
  - E:\GitLib\NarratoAI\src\router\index.ts
  - E:\GitLib\NarratoAI\src\layouts\DefaultLayout.vue
  - E:\GitLib\NarratoAI\src\views\HomeView.vue
  - E:\GitLib\NarratoAI\src\views\NotFoundView.vue
  - E:\GitLib\NarratoAI\src-tauri\tauri.conf.json
autonomous: false
requirements: [FNDT-01, FNDT-02, FNDT-03]
user_setup: []

must_haves:
  truths:
    - "Vite 8 dev server 启动无错误，HMR 工作"
    - "Vuetify 4 组件正常渲染，zhHans 中文界面生效"
    - "@/ 路径别名在 Vite 和 TypeScript 两端都正确解析"
    - "Hash 路由 / 跳转到 HomeView 显示欢迎内容，未知路由跳转到 NotFoundView 显示 404"
    - "npm run typecheck 通过无类型错误"
    - "tauri.conf.json devUrl 指向 http://localhost:5173，beforeDevCommand 配置为 npm run dev"
  artifacts:
    - path: "E:\\GitLib\\NarratoAI\\package.json"
      provides: "Project dependencies at confirmed versions"
    - path: "E:\\GitLib\\NarratoAI\\vite.config.ts"
      provides: "Vite 8 build config with @/ alias, manualChunks, port 5173, Tauri host"
    - path: "E:\\GitLib\\NarratoAI\\tsconfig.json"
      provides: "TypeScript 6 strict mode config with @/ path alias"
    - path: "E:\\GitLib\\NarratoAI\\src\\main.ts"
      provides: "App entry: createApp + Pinia + Router + Vuetify"
    - path: "E:\\GitLib\\NarratoAI\\src\\App.vue"
      provides: "Root component: v-app wrapper with router-view"
    - path: "E:\\GitLib\\NarratoAI\\src\\plugins\\vuetify.ts"
      provides: "Vuetify 4 plugin with zhHans locale, light/dark theme colors (per UI-SPEC)"
    - path: "E:\\GitLib\\NarratoAI\\src\\router\\index.ts"
      provides: "Hash routing with HomeView '/' and 404 catch-all"
    - path: "E:\\GitLib\\NarratoAI\\src-tauri\\tauri.conf.json"
      provides: "Updated build config for Vite integration"
  key_links:
    - from: "vite.config.ts"
      to: "src/"
      via: "@ path alias"
      pattern: "alias:.*'@'.*'src'"
    - from: "main.ts"
      to: "plugins/vuetify.ts"
      via: "import vuetify from './plugins/vuetify'"
    - from: "main.ts"
      to: "router/index.ts"
      via: "import router from './router'"
    - from: "App.vue"
      to: "layouts/DefaultLayout.vue"
      via: "router-view renders DefaultLayout for route /"
    - from: "tauri.conf.json"
      to: "Vite dev server"
      via: "devUrl: http://localhost:5173 + beforeDevCommand: npm run dev"

---

<objective>
创建 NarratoAI 前端项目脚手架和 Vue 3 核心基础设施

Purpose: 建立 Vite 8 + Vue 3.5 + TypeScript 6.0 项目骨架，集成 Vuetify 4 组件库、vue-router 5 Hash 路由、@mdi/font 图标系统，配置 Tauri 2.0 dev server 集成。Phase 14 所有后续计划依赖此基础。

Output: 完整的前端项目脚手架（package.json, Vite/TS 配置, 入口文件, Vuetify 插件, 路由, 布局外壳, 占位视图, Tauri 配置更新）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-RESEARCH.md
@E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from SmartEdit reference for direct porting -->

From SmartEdit src/plugins/vuetify.ts:
  - createVuetify() with components, directives, locale (zhHans), icons (mdi), theme (light/dark)
  - Light theme: primary #2563EB, background #F8FAFC, surface #FFFFFF
  - Dark theme: primary #3B82F6, background #0F172A, surface #1E293B

From SmartEdit src/router/index.ts:
  - createRouter with createWebHashHistory(import.meta.env.BASE_URL)
  - routes: / -> DefaultLayout (with children / -> HomeView), /:pathMatch(.*)* -> NotFoundView
  - Lazy-loaded components via () => import('@/...')

From SmartEdit src/main.ts:
  - createApp(App).use(createPinia()).use(router).use(vuetify).mount('#app')

From SmartEdit vite.config.ts:
  - vue() plugin, @/ alias, manualChunks (vuetify/vue-vendor/tauri)
  - server: port 5173, strictPort true, host 127.0.0.1, watch.ignored ['**/src-tauri/**']

From SmartEdit tsconfig.json:
  - strict: true, @/ path alias, moduleResolution: bundler, target: ES2022

From SmartEdit package.json (version reference):
  - Dependencies: vue, vuetify, pinia, vue-router, @mdi/font, @tauri-apps/api, loglevel
  - DevDependencies: vite, @vitejs/plugin-vue, typescript, vue-tsc, @tauri-apps/cli
</interfaces>

<context_files>
Current tauri.conf.json at:
@E:\GitLib\NarratoAI\src-tauri\tauri.conf.json

Current must remain:
- productName: "NarratoAI"
- identifier: "com.narratoai.app"
- window title: "NarratoAI", 1200x800
- CSP security policy
- dialog plugin

Must change:
- devUrl: from "http://localhost:1420" to "http://localhost:5173"
- beforeDevCommand: from "" to "npm run dev"
- beforeBuildCommand: from "" to "npm run build"
</context_files>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认 Vite/TypeScript/Vuetify/Pinia/vue-router 版本选择</name>
  <read_first>
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-CONTEXT.md (D-14)
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-RESEARCH.md (Standard Stack 版本对比)
    - E:\GitLib\NarratoAI\.planning\REQUIREMENTS.md (FNDT-01)
  </read_first>
  <decision>选择前端技术栈版本 — CONTEXT.md D-14 锁定版本与 npm registry 最新稳定版不一致</decision>
  <context>
    CONTEXT.md D-14 锁定了 Vite 6 / TypeScript ~5.7 / Vuetify 3.5+ / Pinia 2.1+ / vue-router 4.x，
    但 npm registry 和 REQUIREMENTS.md FNDT-01 显示已有更新的主版本。

    版本对比：

    | 包名 | CONTEXT D-14 | npm registry 最新 | REQUIREMENTS.md |
    |------|-------------|-------------------|-----------------|
    | Vite | 6 | 8.0.12 | 8 |
    | TypeScript | ~5.7 | 6.0.3 | 6.0 |
    | Vuetify | 3.5+ | 4.0.7 | 3 (未指定版本) |
    | Pinia | 2.1+ | 3.0.4 | 3 |
    | vue-router | 4.x (对应"5") | 5.0.6 | 5 |

    关键决策因素：
    - SmartEdit 项目的 `package.json` 使用已验证的生产组合，但不含最新版本
    - REQUIREMENTS.md FNDT-01 明确引用 "Vite 8 + TypeScript 6.0"（已与 npm registry 一致）
    - Vuetify 4 兼容 Vue 3.5，Composition API 无需变更
    - Pinia 3 的 `defineStore('id', () => {})` Composition API 向后兼容
    - vue-router 5 使用 `createWebHashHistory` API 不变

    Plan 推荐：采用 npm registry 最新稳定版（与 REQUIREMENTS.md 一致），所有已验证的 SmartEdit 模式（vite.config.ts manualChunks、tsconfig strict、router Hash 模式、Pinia Composition API）在最新版本中均兼容。
  </context>
  <options>
    <option id="latest-stable">
      <name>使用 npm registry 最新稳定版（推荐）</name>
      <pros>
        - 与 REQUIREMENTS.md FNDT-01 完全一致（Vite 8 + TypeScript 6.0）
        - Vuetify 4 拥有最新特性、性能和类型支持
        - Pinia 3 和 vue-router 5 保持 Composition API 兼容
        - 避免 npm install 废弃警告
      </pros>
      <cons>
        - 与 CONTEXT.md D-14 原始锁定版本不一致（需更新决策绑定）
        - Vuetify 4 对比 3.x 可能存在少量 API 变更
      </cons>
    </option>
    <option id="d14-original">
      <name>严格遵循 CONTEXT.md D-14 版本</name>
      <pros>
        - 保持原始决策绑定不变
      </pros>
      <cons>
        - 使用已过时版本（Vite 6、Pinia 2.x），npm install 有废弃警告
        - 与 REQUIREMENTS.md FNDT-01 直接矛盾
        - vue-router 4.x 与 RESEARCH 中的 "vue-router 5 Hash 路由" 标注不一致
      </cons>
    </option>
  </options>
  <resume-signal>Select: latest-stable 或 d14-original</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 创建项目脚手架文件</name>
  <read_first>
    - E:\GitLib\SmartEdit\package.json (依赖版本参考)
    - E:\GitLib\SmartEdit\vite.config.ts (Vite 配置模式)
    - E:\GitLib\SmartEdit\tsconfig.json (TypeScript 严格模式配置)
  </read_first>
  <files>
    E:\GitLib\NarratoAI\package.json
    E:\GitLib\NarratoAI\index.html
    E:\GitLib\NarratoAI\vite.config.ts
    E:\GitLib\NarratoAI\tsconfig.json
    E:\GitLib\NarratoAI\tsconfig.node.json
    E:\GitLib\NarratoAI\src\env.d.ts
    E:\GitLib\NarratoAI\src\styles\main.css
  </files>
  <action>
    使用 Task 1 确认的版本，基于 SmartEdit 已验证模式创建以下文件。全部文件为新建。

    1. `package.json`：
       - type: "module"
       - scripts: dev (vite), build (vue-tsc --noEmit && vite build), preview (vite preview), typecheck (vue-tsc --noEmit)
       - dependencies: vue@^3.5, vuetify@^4, pinia@^3, vue-router@^5, @mdi/font@^7, @tauri-apps/api@^2, loglevel@^1
       - devDependencies: vite@^8, @vitejs/plugin-vue@^6, typescript@^6, vue-tsc@^2, @tauri-apps/cli@^2
       - 排除 eslint/prettier/axios/naive-ui/element-plus — v2.0 Scope 不需要

    2. `index.html`：
       - Vite SPA 入口，含 `<div id="app">`、`<script type="module" src="/src/main.ts">`
       - lang="zh-CN"、title="NarratoAI"
       - viewport meta 标签（适用于 Vuetify 响应式）

    3. `vite.config.ts`：
       - import { defineConfig } from 'vite'，vue() 插件
       - resolve.alias: '@' -> resolve(__dirname, 'src')
       - server: port 5173, strictPort true, host '127.0.0.1', watch.ignored ['**/src-tauri/**']
       - clearScreen: false (Tauri 集成需要)
       - build.chunkSizeWarningLimit: 600
       - build.rollupOptions.output.manualChunks:
         - id.includes('vuetify') -> 'vuetify'
         - id.includes('vue') || id.includes('pinia') || id.includes('vue-router') -> 'vue-vendor'
         - id.includes('@tauri-apps') -> 'tauri'
       - 函数形式 manualChunks（保持与 SmartEdit 一致）

    4. `tsconfig.json`：
       - compilerOptions: target "ES2022", module "ESNext", moduleResolution "bundler", strict true
       - paths: { "@/*": ["./src/*"] }
       - include: ["src/**/*.ts", "src/**/*.d.ts", "src/**/*.tsx", "src/**/*.vue"]
       - vue-tsc 需要的 vue 类型引用

    5. `tsconfig.node.json`：
       - 针对 vite.config.ts 的 Node 环境配置
       - compilerOptions: composite true, module "ESNext", moduleResolution "bundler"
       - include: ["vite.config.ts"]

    6. `src/env.d.ts`：
       - vue 模块声明：`declare module '*.vue' { import type { DefineComponent } from 'vue'; const component: DefineComponent<{}, {}, any>; export default component; }`
       - Vite 环境变量类型声明 (ImportMetaEnv)

    7. `src/styles/main.css`：
       - 全局重置样式（保持最小，Vuetify 提供大部分）
       - body 背景色和字体基础设置

    创建文件后执行 `npm install` 确认所有依赖解析成功无报错。
  </action>
  <acceptance_criteria>
    - `package.json` 包含所有声明的依赖，version 字段使用确认的版本
    - `npm install` 退出码 0，无警告或错误
    - `vite.config.ts` 包含 @/ 别名、manualChunks 三块分包、端口 5173 strict
    - `tsconfig.json` 包含 strict:true 和 paths["@/*"]
    - `index.html` 包含 `<div id="app">` 和指向 `src/main.ts` 的 script 标签
    - `src/env.d.ts` 包含 `declare module '*.vue'` 声明
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && npm install && node -e "const p = require('./package.json'); console.log('vue:', p.dependencies.vue); console.log('vite:', p.devDependencies.vite)" && ls src/env.d.ts src/styles/main.css</automated>
  </verify>
  <done>所有脚手架文件创建完毕，npm install 成功，依赖版本为 Task 1 确认的版本</done>
</task>

<task type="auto">
  <name>Task 3: 创建 Vue 核心文件 + Vuetify 插件 + 路由 + 布局/视图 + 更新 Tauri 配置</name>
  <read_first>
    - E:\GitLib\SmartEdit\src\main.ts (入口模式)
    - E:\GitLib\SmartEdit\src\App.vue (根组件)
    - E:\GitLib\SmartEdit\src\plugins\vuetify.ts (Vuetify 插件，直接移植)
    - E:\GitLib\SmartEdit\src\router\index.ts (Hash 路由模式，直接移植)
    - E:\GitLib\NarratoAI\src-tauri\tauri.conf.json (当前配置，需修改)
    - E:\GitLib\NarratoAI\.planning\phases\14-foundation-frontend-scaffold\14-UI-SPEC.md (HomeView 占位文案、NotFoundView 文案、颜色 Token)
  </read_first>
  <files>
    E:\GitLib\NarratoAI\src\main.ts
    E:\GitLib\NarratoAI\src\App.vue
    E:\GitLib\NarratoAI\src\plugins\vuetify.ts
    E:\GitLib\NarratoAI\src\router\index.ts
    E:\GitLib\NarratoAI\src\layouts\DefaultLayout.vue
    E:\GitLib\NarratoAI\src\views\HomeView.vue
    E:\GitLib\NarratoAI\src\views\NotFoundView.vue
    E:\GitLib\NarratoAI\src-tauri\tauri.conf.json
  </files>
  <action>
    基于 SmartEdit 已验证模式创建/修改以下文件：

    1. `src/main.ts`：
       - import { createApp } from 'vue', { createPinia } from 'pinia', App.vue, router, vuetify
       - const app = createApp(App); app.use(createPinia()); app.use(router); app.use(vuetify); app.mount('#app')
       - 与 SmartEdit 入口完全一致

    2. `src/App.vue`：
       - `<template><v-app><router-view /></v-app></template>`
       - `<script setup lang="ts">` 空
       - 与 SmartEdit 根组件一致

    3. `src/plugins/vuetify.ts`：
       - import 'vuetify/styles', '@mdi/font/css/materialdesignicons.css'
       - import { createVuetify } from 'vuetify', * as components from 'vuetify/components', * as directives from 'vuetify/directives'
       - import { zhHans } from 'vuetify/locale'
       - createVuetify 配置：
         - components, directives
         - locale: { locale: 'zhHans', messages: { zhHans } }
         - icons: { defaultSet: 'mdi' }
         - theme.defaultTheme: 'light'
         - theme.themes.light: { colors: { primary: '#2563EB', secondary: '#64748B', background: '#F8FAFC', surface: '#FFFFFF', 'surface-variant': '#F1F5F9', error: '#DC2626', 'on-background': '#334155', 'on-surface': '#1E293B', info: '#0284C7', success: '#16A34A', warning: '#D97706' } }
         - theme.themes.dark: { colors: { primary: '#3B82F6', secondary: '#94A3B8', background: '#0F172A', surface: '#1E293B', 'surface-variant': '#334155', error: '#EF4444', 'on-background': '#E2E8F0', 'on-surface': '#F1F5F9', info: '#0EA5E9', success: '#22C55E', warning: '#F59E0B' } }
       - export default vuetify
       - 与 SmartEdit 一致，颜色值按 UI-SPEC 更新

    4. `src/router/index.ts`：
       - import { createRouter, createWebHashHistory } from 'vue-router'
       - const router = createRouter({ history: createWebHashHistory(import.meta.env.BASE_URL), routes: [...] })
       - 路由表：
         - path: '/', component: () => import('@/layouts/DefaultLayout.vue'), children: [{ path: '', name: 'home', component: () => import('@/views/HomeView.vue') }]
         - path: '/:pathMatch(.*)*', name: 'not-found', component: () => import('@/views/NotFoundView.vue')
       - export default router

    5. `src/layouts/DefaultLayout.vue`：
       - `<template><v-app-bar><v-app-bar-title>NarratoAI</v-app-bar-title></v-app-bar><v-main><router-view /></v-main></template>`
       - `<script setup lang="ts">` 空
       - 最简单的三区域外壳，Phase 15 将替换为完整布局

    6. `src/views/HomeView.vue`：
       - 占位欢迎页面，文案严格按 UI-SPEC Copywriting Contract：
         - `v-container` 居中布局
         - `<h1 class="text-h5">欢迎使用 NarratoAI</h1>`
         - `<p class="text-body-2">AI 驱动的视频解说与自动化剪辑工具。选择工作模式并配置参数后，即可开始制作。</p>`
         - `<p class="text-body-2">请从右上角设置菜单开始配置。</p>`
         - `<p class="text-body-2">支持纪录片解说、短剧解说和短剧混剪三种模式。</p>`

    7. `src/views/NotFoundView.vue`：
       - 404 占位页面，文案严格按 UI-SPEC：
         - `<v-container>` 居中布局
         - 大号 "404" 显示
         - `<h1 class="text-h5">页面未找到</h1>`
         - `<p class="text-body-2">您访问的页面不存在，可能已被移除或地址输入有误。</p>`
         - `<router-link to="/">返回首页</router-link>`

    8. `src-tauri/tauri.conf.json`（修改已有文件）：
       - build.devUrl: "http://localhost:5173"（从 :1420 改为 :5173）
       - build.beforeDevCommand: "npm run dev"（从空字符串改为 npm run dev）
       - build.beforeBuildCommand: "npm run build"（从空字符串改为 npm run build）
       - 其他字段保持不变（productName、identifier、window、CSP、dialog plugin）

    关键约束：
    - 所有组件使用 `<script setup lang="ts">`，不写额外 TypeScript 逻辑
    - HomeView 和 NotFoundView 是纯展示组件，无状态无交互
    - DefaultLayout 使用 Vuetify 组件（v-app-bar, v-main），不使用自定义 CSS
    - 不创建 Phase 15+ 的组件：AppHeader、SettingsDrawer、VideoTable、LogPanel 等
  </action>
  <acceptance_criteria>
    - `src/main.ts` 包含 createApp + use Pinia + use Router + use Vuetify 的四步调用
    - `src/App.vue` 包含 `<v-app>` 和 `<router-view />`
    - `src/plugins/vuetify.ts` 配置了 zhHans locale，mdi icon set，light 和 dark 两组主题颜色（颜色值匹配 UI-SPEC）
    - `src/router/index.ts` 使用 `createWebHashHistory`，包含 "/" 和 "/:pathMatch(.*)*" 两条路由
    - `src/layouts/DefaultLayout.vue` 包含 v-app-bar 和包含 router-view 的 v-main
    - `src/views/HomeView.vue` 包含文案 "欢迎使用 NarratoAI"
    - `src/views/NotFoundView.vue` 包含文案 "页面未找到" 和 "返回首页" router-link
    - `src-tauri/tauri.conf.json` 中 devUrl 为 "http://localhost:5173"，beforeDevCommand 为 "npm run dev"
    - `npm run typecheck` 通过零错误
  </acceptance_criteria>
  <verify>
    <automated>cd E:/GitLib/NarratoAI && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <done>所有 Vue 核心文件创建完毕，Vuetify 插件/路由/布局/视图均通过 typecheck，tauri.conf.json 已更新</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite dev server -> filesystem | dev server 读取本地源文件 |
| tauri.conf.json -> Tauri runtime | 构建配置影响 Tauri 安全策略 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | tauri.conf.json beforeDevCommand | mitigate | beforeDevCommand 限定为 "npm run dev"，不允许任意命令注入；配置文件通过 git 版本控制 |
| T-14-02 | Information Disclosure | Vite dev server host config | mitigate | server.host 固定为 127.0.0.1（仅本地访问），不开放到 0.0.0.0 |
</threat_model>

<verification>
- [ ] `npm install` 完成无错误
- [ ] `npm run typecheck` 通过零错误
- [ ] `npm run dev` 启动后访问 http://localhost:5173 显示 HomeView 欢迎内容（手动验证）
- [ ] 访问 http://localhost:5173/#/nonexistent 显示 NotFoundView 404 页面（手动验证）
- [ ] tauri.conf.json 与 git diff 确认 devUrl 和 beforeDevCommand 正确更新
</verification>

<success_criteria>
Phase 14 基础脚手架全部就位：
1. Vite 8 dev server 启动无错误
2. Vuetify 4 组件正常渲染
3. Hash 路由正确导航
4. TypeScript 编译通过零错误
5. Tauri 构建配置指向 Vite dev server
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation-frontend-scaffold/14-01-SUMMARY.md`
</output>
