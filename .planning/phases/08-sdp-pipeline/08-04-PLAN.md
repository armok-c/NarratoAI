---
phase: 08-sdp-pipeline
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - src/sdp/clip.rs
  - src/sdp/pipeline.rs
  - src/sdp/mod.rs
  - src/lib.rs
autonomous: true
requirements: [SDP-01, SDP-02]
user_setup: []

must_haves:
  truths:
    - "sdp_step_clip() 复用 SDE 的 sde_step_clip() 完成视频裁剪（全部 OST=1）"
    - "run_sdp() 4 步流水线编排：clip → concat → composite"
    - "step_concat 使用 FFmpeg concat demuxer 拼接裁剪后的片段"
    - "step_composite 使用 FFmpeg filter_complex 合成输出（原始音频 + 可选 BGM，无字幕烧录）"
    - "进度回调在每步完成后上报（clip=30%, concat=60%, composite=90%→100%）"
    - "输入校验：所有片段 OST=1，视频文件存在，脚本非空"
    - "lib.rs 导出 pub mod sdp"
    - "sdp/mod.rs 包含 clip 和 pipeline 模块声明"
  artifacts:
    - path: "src/sdp/clip.rs"
      provides: "sdp_step_clip() — 包装 sde_step_clip() 的 OST=1 裁剪"
      contains: "sdp_step_clip"
    - path: "src/sdp/pipeline.rs"
      provides: "run_sdp() 主编排 + step_concat + step_composite"
      contains: "run_sdp"
  key_links:
    - from: "src/sdp/clip.rs"
      to: "crate::sde::clip::sde_step_clip"
      via: "复用 OST=1 精确时间戳校正 + clip_all_videos"
    - from: "src/sdp/pipeline.rs"
      to: "src/sdp/types.rs"
      via: "SdpPipelineState, SdpRequest, SdpProgressStep"
    - from: "src/sdp/pipeline.rs"
      to: "crate::ffmpeg::command::run_ffmpeg"
      via: "FFmpeg concat / composite 调用"
---

<objective>
创建 SDP 视频处理流水线（clip + pipeline）+ 更新 lib.rs 和 sdp/mod.rs。

Purpose: 实现 `run_sdp()` 4 步流水线编排（D-06）：step_clip → step_concat → step_composite，复用 SDE 裁剪逻辑和 documentary FFmpeg 模式。

核心设计（D-06, D-08, D-09）：
- clip: 复用 `sde_step_clip()`（OST=1 精确时间戳校正 → clip_all_videos）
- concat: FFMpeg concat demuxer (-c copy)
- composite: FFmpeg filter_complex + amix（原始音频音量控制 + 可选 BGM，无 TTS/字幕）

Output:
- `src/sdp/clip.rs` — SDP 裁剪包装
- `src/sdp/pipeline.rs` — 4 步流水线 + 输入校验 + 进度回调
- `src/sdp/mod.rs` — 追加 clip/pipeline 模块声明
- `src/lib.rs` — 新增 `pub mod sdp;`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-sdp-pipeline/08-CONTEXT.md
@.planning/phases/08-sdp-pipeline/08-RESEARCH.md

<interfaces>
From src/sde/clip.rs (function to reuse):
```rust
pub async fn sde_step_clip(
    state: &mut SdePipelineState,
    video_path: &Path,
) -> Result<(), SdeError> {
    // OST=1 precise timestamp correction (find_precise_range)
    // → clip_all_videos (documentary FFmpeg clip)
}
```
注：SdePipelineState 中的 subtitle_segments 字段可用于时间戳校正，
但 SDP 的 SdpPipelineState 也包含 subtitle_segments，二者结构兼容。

From src/documentary/clip.rs:
```rust
pub async fn clip_all_videos(
    video_path: &Path,
    script_clips: &[ScriptClip],
    tts_results: &HashMap<i64, TtsResult>,
    task_dir: &Path,
) -> Result<HashMap<i64, PathBuf>, PipelineError>
```
注：SDP 全部 OST=1，不需要 tts_results —— 但 clip_all_videos 的接口要求 HashMap<i64, TtsResult>。
解决方案：传入空 HashMap（因为 OST=1 分支不读取 tts_results）。

From src/sdp/types.rs (created in Plan 03):
```rust
pub struct SdpRequest {
    pub subtitle_path: PathBuf, pub video_path: PathBuf, pub script_path: Option<PathBuf>,
    pub temperature: f64, pub custom_clips: u32, pub original_volume: f64,
    pub bgm_volume: f64, pub bgm_path: Option<PathBuf>,
    pub output_dir: Option<PathBuf>, pub threads: u32,
}
pub struct SdpPipelineState { /* ... script, video_clips, combined_video_path, output_video_path, total_duration, progress */ }
pub enum SdpProgressStep { Clip, Concat, Composite }
pub type SdpProgressCallback = Box<dyn Fn(SdpProgressStep, f32, &str) + Send + Sync>;
```

From src/sdp/mod.rs (created in Plan 03, BEFORE this plan adds clip/pipeline):
```rust
pub mod error;
pub mod types;
pub mod script_gen;

pub use error::SdpError;
pub use types::{SdpRequest, SdpPipelineState, SdpProgressStep, SdpProgressCallback};
pub use script_gen::generate_sdp_script;
```

FFmpeg patterns from src/documentary/pipeline.rs:
- Concat: `ffmpeg -f concat -safe 0 -i <list.txt> -c copy -y <output>`
- Composite (SDP simplified: no TTS audio, no subtitle burning):
  - `ffmpeg -i <merged_video> [-i <bgm>] -filter_complex "[0:a]volume=<vol>[orig];[1:a]aloop=loop=-1:size=2e+09,atrim=0:<dur>,volume=<bgm_vol>,afade=t=out:st=<fade>:d=3[bgm];[orig][bgm]amix=inputs=2:duration=longest[aout]" -map 0:v -map "[aout]" -c:v libx264 -c:a aac -y <output>`

Script type:
```rust
use crate::script::types::{Script, ScriptClip, OstType};
pub fn load_script(path: &Path) -> Result<Script, ScriptError>
pub fn validate(script: &Script) -> Result<(), ScriptError>
```
</interfaces>

Build command:
```bash
cargo build -p narratoai-core 2>&1 | head -40
Test:
cargo test -p narratoai-core -- sdp 2>&1 | tail -20
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 sdp_step_clip() 包装</name>

  <files>
    src/sdp/clip.rs
  </files>

  <read_first>
    src/sde/clip.rs — sde_step_clip() 实现模式
    src/sdp/types.rs — SdpPipelineState 定义
    src/sde/types.rs — SdePipelineState 用于对比字段映射
  </read_first>

  <action>
    创建 `src/sdp/clip.rs`：

    ```rust
    use std::path::Path;
    use crate::documentary::types::TtsResult;
    use crate::sdp::error::SdpError;
    use crate::sdp::types::SdpPipelineState;

    /// SDP 视频裁剪——复用 SDE 的 sde_step_clip() 模式
    ///
    /// 1. OST=1 精确时间戳校正（find_precise_range）
    /// 2. 调用 documentary clip_all_videos 执行 FFmpeg 裁剪
    /// 3. 计算每个片段的 duration 和 time_range
    ///
    /// 与 SDE 裁剪的区别：
    /// - SDE 的 SdePipelineState 有 TTS 相关字段，SDP 无需
    /// - SDP 全部 OST=1，timezone 校正逻辑相同
    /// - clip_all_videos 需要 tts_results 参数，SDP 传空 HashMap
    pub async fn sdp_step_clip(
        state: &mut SdpPipelineState,
        video_path: &Path,
    ) -> Result<(), SdpError> {
        // OST=1 精确时间戳校正（等价于 sde_step_clip 中 OST=1 分支）
        use crate::subtitle::timestamp::find_precise_range;
        use crate::subtitle::types::SubtitleSegment;
        use crate::script::types::OstType;

        for clip in &mut state.script {
            if clip.ost == OstType::OriginalSound {
                if let Some((precise_start, precise_end)) =
                    find_precise_range(&clip.timestamp, &state.subtitle_segments)
                {
                    let start_str = secs_to_timestamp_str(precise_start);
                    let end_str = secs_to_timestamp_str(precise_end);
                    clip.timestamp = format!("{}-{}", start_str, end_str);
                }
                // fallback: 如果找不到精确匹配，保留 LLM 原始时间戳（不崩溃）
            }
        }

        // 调用 documentary 裁剪（传空 tts_results 因为 SDP 全部 OST=1，不读取 tts）
        let empty_tts: std::collections::HashMap<i64, TtsResult> = std::collections::HashMap::new();
        let video_clips = crate::documentary::clip::clip_all_videos(
            video_path,
            &state.script,
            &empty_tts,
            &state.task_dir,
        )
        .await
        .map_err(|e| SdpError::VideoProcess(e))?;

        state.video_clips = video_clips;

        // 计算每个片段的 duration 和 source_time_range/edited_time_range
        // 参考 src/sde/pipeline.rs L220-L253 的 cumulative_time 计算逻辑
        let mut cumulative_time = 0.0f64;
        for clip in &mut state.script {
            if let Some(clip_path) = state.video_clips.get(&clip._id) {
                clip.video = Some(clip_path.clone());
            }

            // 计算片段时长：从 timestamp 中解析起止秒数
            let ts_parts: Vec<&str> = clip.timestamp.splitn(2, '-').collect();
            if ts_parts.len() == 2 {
                let start_secs = crate::subtitle::timestamp::parse_srt_timestamp(ts_parts[0])
                    .unwrap_or(0.0);
                let end_secs = crate::subtitle::timestamp::parse_srt_timestamp(ts_parts[1])
                    .unwrap_or(0.0);
                let clip_duration = (end_secs - start_secs).max(0.0);

                if clip_duration > 0.0 {
                    clip.duration = Some(clip_duration);
                    clip.source_time_range = Some(format!(
                        "{}-{}",
                        secs_to_timestamp_str(start_secs),
                        secs_to_timestamp_str(end_secs)
                    ));
                    clip.edited_time_range = Some(format!(
                        "{}-{}",
                        secs_to_timestamp_str(cumulative_time),
                        secs_to_timestamp_str(cumulative_time + clip_duration)
                    ));
                    cumulative_time += clip_duration;
                }
            }
        }

        state.total_duration = cumulative_time;
        Ok(())
    }

    /// 将秒数转换为 SRT 时间格式 "HH:MM:SS,mmm"
    fn secs_to_timestamp_str(secs: f64) -> String {
        if secs < 0.0 { return "00:00:00,000".to_string(); }
        let secs = secs.min(86399.999);
        let total_ms = (secs * 1000.0).round() as u64;
        let ms = total_ms % 1000;
        let total_secs = total_ms / 1000;
        let s = total_secs % 60;
        let total_mins = total_secs / 60;
        let m = total_mins % 60;
        let h = total_mins / 60;
        format!("{:02}:{:02}:{:02},{:03}", h, m, s, ms)
    }
    ```

    单元测试：
    - test_secs_to_timestamp_str_zero
    - test_secs_to_timestamp_str_normal
    - test_secs_to_timestamp_str_negative
    - test_sdp_step_clip_ost1_precise_correction（模拟 OST=1 片段和字幕段落，验证时间戳校正）
    - test_sdp_step_clip_nonexistent_match（找不到匹配时保留原始时间戳）
  </action>

  <verify>
    <automated>cargo build -p narratoai-core 2>&1 | head -20</automated>
    <automated>cargo test -p narratoai-core -- sdp::clip 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/sdp/clip.rs 存在
    - 包含 `pub async fn sdp_step_clip(` 函数
    - 调用 `find_precise_range` 进行 OST=1 精确时间戳校正
    - 调用 `crate::documentary::clip::clip_all_videos` 执行 FFmpeg 裁剪
    - 包含 `fn secs_to_timestamp_str` 辅助函数
    - `cargo test -p narratoai-core -- sdp::clip 2>&1 | tail -3` 包含 "test result: ok"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建 pipeline.rs — run_sdp() 主编排 + step_concat + step_composite</name>

  <files>
    src/sdp/pipeline.rs
  </files>

  <read_first>
    src/sde/pipeline.rs — run_sde() 主编排模式（SDP 4 步简化版参考）
    src/documentary/pipeline.rs — concat + composite FFmpeg 命令模式
    src/documentary/timestamp.rs — parse_time_to_secs, secs_to_ffmpeg_time
    src/ffmpeg/command.rs — run_ffmpeg 模式
    src/script/mod.rs — load_script
  </read_first>

  <action>
    创建 `src/sdp/pipeline.rs`，包含以下内容：

    **模块导入：**
    ```rust
    use std::path::{Path, PathBuf};
    use crate::config::types::AppConfig;
    use crate::documentary::error::PipelineError;
    use crate::documentary::timestamp::{parse_time_to_secs, secs_to_ffmpeg_time};
    use crate::ffmpeg::command::run_ffmpeg;
    use crate::sdp::error::SdpError;
    use crate::sdp::types::{SdpPipelineState, SdpProgressStep, SdpRequest};
    use crate::script::types::OstType;
    ```

    **`run_sdp()` 主编排函数：**
    ```rust
    /// SDP 流水线主入口——4 步顺序编排
    ///
    /// 步骤：Clip(0%→30%) → Concat(30%→60%) → Composite(60%→90%→100%)
    ///
    /// 参数：
    /// - request: SdpRequest（含 video_path, script_path, bgm_path, volume 等）
    /// - config: 全局配置
    ///
    /// 返回最终输出视频的 PathBuf。
    pub async fn run_sdp(
        request: SdpRequest,
        config: &AppConfig,
    ) -> Result<PathBuf, SdpError> {
        // 0. 参数校验（D-21）
        request.validate().map_err(|e| SdpError::Validation { details: e })?;

        // 1. 创建任务目录
        let task_id = uuid::Uuid::new_v4().to_string();
        let task_dir = request.output_dir.clone()
            .unwrap_or_else(|| std::env::temp_dir().join("narratoai").join(&task_id));
        std::fs::create_dir_all(&task_dir)?;

        // 2. 初始化流水线状态
        let mut state = SdpPipelineState::new(task_id, task_dir);

        // 3. 加载脚本（D-15）
        let script_path = request.script_path.clone()
            .unwrap_or_else(|| state.task_dir.join("merged_subtitle.json"));
        state.script = crate::script::load_script(&script_path)?;

        // G4: OST 校验——所有片段必须为 OST=1（D-21）
        if state.script.iter().any(|c| c.ost != OstType::OriginalSound) {
            return Err(SdpError::Validation {
                details: "SDP 仅支持 OST=1（原声）片段，请检查脚本中的 OST 值".into(),
            });
        }

        // ============================================================
        //  步骤 1: 视频裁剪 (Clip)  —  0% → 30%
        // ============================================================
        state.emit_progress(SdpProgressStep::Clip, 0.0, "正在裁剪视频片段...");
        crate::sdp::clip::sdp_step_clip(&mut state, &request.video_path).await?;
        state.emit_progress(SdpProgressStep::Clip, 30.0, "视频裁剪完成");

        // ============================================================
        //  步骤 2: 片段拼接 (Concat)  —  30% → 60%
        // ============================================================
        state.emit_progress(SdpProgressStep::Concat, 30.0, "正在拼接视频片段...");
        sdp_step_concat(&mut state).await?;
        state.emit_progress(SdpProgressStep::Concat, 60.0, "视频拼接完成");

        // ============================================================
        //  步骤 3: 最终合成 + BGM (Composite)  —  60% → 90% → 100%
        // ============================================================
        state.emit_progress(SdpProgressStep::Composite, 60.0, "正在最终合成...");
        sdp_step_composite(&mut state, &request).await?;
        state.emit_progress(SdpProgressStep::Composite, 100.0, "最终合成完成");

        // 返回结果
        state.output_video_path.ok_or_else(|| SdpError::Validation {
            details: "流水线完成但未生成输出视频".into(),
        })
    }
    ```

    **`sdp_step_concat()` — 视频拼接：**
    ```rust
    /// SDP 步骤 2：视频片段拼接
    ///
    /// 使用 FFmpeg concat demuxer 合并裁剪后的片段。
    /// 与 documentary step_concat 模式一致（D-09）。
    async fn sdp_step_concat(
        state: &mut SdpPipelineState,
    ) -> Result<(), SdpError> {
        let concat_list_path = state.task_dir.join("concat_list.txt");
        let mut concat_content = String::new();

        for clip in &state.script {
            if let Some(ref video_path) = clip.video {
                let path_str = video_path
                    .to_string_lossy()
                    .replace('\\', "/")
                    .replace("'", "'\\''");
                concat_content.push_str(&format!("file '{}'\n", path_str));
            } else {
                return Err(SdpError::Validation {
                    details: format!("片段 {} 缺少视频文件", clip._id),
                });
            }
        }

        std::fs::write(&concat_list_path, &concat_content)?;

        let combined_path = state.task_dir.join("combined_concat.mp4");
        let concat_path_str = concat_list_path.to_string_lossy().to_string();
        let combined_path_str = combined_path.to_string_lossy().to_string();

        run_ffmpeg(move || {
            let mut cmd = ffmpeg_sidecar::command::FfmpegCommand::new();
            cmd.arg("-f").arg("concat")
                .arg("-safe").arg("0")
                .arg("-i").arg(&concat_path_str)
                .arg("-c").arg("copy")
                .arg("-y")
                .output(&combined_path_str);

            let mut child = cmd.spawn()
                .map_err(|e| crate::error::FFmpegError::SpawnFailed(e.to_string()))?;
            let iter = match child.iter() {
                Ok(iter) => iter,
                Err(e) => { let _ = child.kill(); let _ = child.wait();
                    return Err(crate::error::FFmpegError::SpawnFailed(e.to_string())); }
            };
            let mut had_errors = false;
            for event in iter {
                if let ffmpeg_sidecar::event::FfmpegEvent::Error(e) = event {
                    tracing::error!("SDP Concat error: {}", e);
                    had_errors = true;
                }
            }
            let status = child.wait()
                .map_err(|e| crate::error::FFmpegError::ExecutionError(e.to_string()))?;
            if had_errors || !status.success() {
                return Err(crate::error::FFmpegError::ExecutionError(
                    "SDP Concat failed".into()
                ));
            }
            Ok(())
        }).await.map_err(|e: crate::error::FFmpegError| {
            SdpError::VideoProcess(PipelineError::FFmpeg { source: e })
        })?;

        state.combined_video_path = Some(combined_path);
        Ok(())
    }
    ```

    **`sdp_step_composite()` — 最终合成 + BGM：**
    ```rust
    /// SDP 步骤 3：最终合成 + BGM 混音（D-20）
    ///
    /// 与 documentary composite 的区别（D-06）：
    /// - 无 TTS 音频输入（SDP 全部 OST=1，原始音频直接取自视频 clip）
    /// - 无字幕烧录（无 subtitles filter）
    /// - 仍支持 BGM 混音（aloop + atrim + volume + afade + amix）
    /// - 仍需要原始音频音量控制
    async fn sdp_step_composite(
        state: &mut SdpPipelineState,
        request: &SdpRequest,
    ) -> Result<(), SdpError> {
        let merged_video = state.combined_video_path.as_ref()
            .ok_or_else(|| SdpError::Validation { details: "缺少拼接视频".into() })?;

        let output_path = state.task_dir.join("combined_sdp.mp4");
        let video_str = merged_video.to_string_lossy().to_string();
        let output_str = output_path.to_string_lossy().to_string();
        let has_bgm = request.bgm_path.is_some();

        run_ffmpeg(move || {
            let mut cmd = ffmpeg_sidecar::command::FfmpegCommand::new();
            cmd.arg("-i").arg(&video_str);

            let mut filter_complex_parts: Vec<String> = Vec::new();
            let mut amix_count = 0usize;

            // 原始音频（音量调整）
            filter_complex_parts.push(
                format!("[0:a]volume={:.2}[orig]", request.original_volume)
            );
            amix_count += 1;

            if let Some(ref bgm_path) = request.bgm_path {
                cmd.arg("-i").arg(bgm_path.to_string_lossy().to_string());
                let total_dur = state.total_duration.max(0.0);
                let fade_start = if total_dur > 3.0 { total_dur - 3.0 } else { 0.0 };
                filter_complex_parts.push(format!(
                    "[1:a]aloop=loop=-1:size=2e+09,atrim=0:{:.3},asetpts=PTS-STARTPTS,volume={:.2},afade=t=out:st={:.1}:d=3[bgm]",
                    total_dur, request.bgm_volume, fade_start
                ));
                amix_count += 1;
            }

            // 构建 amix
            let mix_inputs = if has_bgm { "[orig][bgm]" } else { "[orig]" };
            filter_complex_parts.push(format!(
                "{}amix=inputs={}:duration=longest[aout]",
                mix_inputs, amix_count
            ));

            if !filter_complex_parts.is_empty() {
                cmd.arg("-filter_complex")
                    .arg(&filter_complex_parts.join(";"));
            }

            cmd.arg("-map").arg("0:v")
                .arg("-map").arg("[aout]")
                .codec_video("libx264")
                .arg("-preset").arg("medium")
                .arg("-pix_fmt").arg("yuv420p")
                .codec_audio("aac")
                .arg("-y")
                .output(&output_str);

            // Standard run_ffmpeg child spawn/wait pattern
            let mut child = cmd.spawn()
                .map_err(|e| crate::error::FFmpegError::SpawnFailed(e.to_string()))?;
            let iter = match child.iter() {
                Ok(iter) => iter,
                Err(e) => { let _ = child.kill(); let _ = child.wait();
                    return Err(crate::error::FFmpegError::SpawnFailed(e.to_string())); }
            };
            let mut had_errors = false;
            for event in iter {
                if let ffmpeg_sidecar::event::FfmpegEvent::Error(e) = event {
                    tracing::error!("SDP Composite error: {}", e);
                    had_errors = true;
                }
            }
            let status = child.wait()
                .map_err(|e| crate::error::FFmpegError::ExecutionError(e.to_string()))?;
            if had_errors || !status.success() {
                return Err(crate::error::FFmpegError::ExecutionError(
                    "SDP Composite failed".into()
                ));
            }
            Ok(())
        }).await.map_err(|e: crate::error::FFmpegError| {
            SdpError::VideoProcess(PipelineError::FFmpeg { source: e })
        })?;

        state.output_video_path = Some(output_path);
        Ok(())
    }
    ```

    **单元测试：**
    - test_run_sdp_invalid_script（传空/无效脚本应报 Validation 错误）
    - test_run_sdp_ost_validation_only（用非 OST=1 脚本测试校验逻辑，无需实际 FFmpeg）
    - test_sdp_concat_no_video_clips（video_clips 为空时 step_concat 校验失败）
    - test_sdp_state_progress_sequence（ProgressStep 顺序：Clip→Concat→Composite）
    - test_sdp_pipeline_state_new（状态初始化检查）
  </action>

  <verify>
    <automated>cargo build -p narratoai-core 2>&1 | head -20</automated>
    <automated>cargo test -p narratoai-core -- sdp::pipeline 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/sdp/pipeline.rs 存在
    - 包含 `pub async fn run_sdp(` 函数
    - 包含 `async fn sdp_step_concat(` 函数（私有）
    - 包含 `async fn sdp_step_composite(` 函数（私有）
    - run_sdp 中在加载脚本后执行 OST 全为 1 的校验
    - step_composite 使用 filter_complex，包含 amix 混音（无字幕烧录相关 filter）
    - step_composite 参数包含 original_volume + bgm_volume（无 tts_volume 相关参数）
    - `cargo test -p narratoai-core -- sdp::pipeline 2>&1 | tail -3` 包含 "test result: ok"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 更新 lib.rs + sdp/mod.rs — 注册 SDP 模块 + 添加 clip/pipeline 声明</name>

  <files>
    src/lib.rs
    src/sdp/mod.rs
  </files>

  <read_first>
    src/lib.rs — 当前模块声明列表
    src/sdp/mod.rs — 当前模块入口（来自 Plan 03，未含 clip/pipeline）
  </read_first>

  <action>
    **Action 1: 更新 src/lib.rs — 添加 `pub mod sdp;`**

    当前模块列表（需要保持字母顺序）：
    ```
    pub mod config;
    pub mod error;
    pub mod tts;
    pub mod llm;
    pub mod ffmpeg;
    pub mod jianying;
    pub mod script;
    pub mod prompt;
    pub mod subtitle;  // 已在 Plan 01 添加
    pub mod visual;
    pub mod audio;
    pub mod youtube;
    pub mod material;
    pub mod documentary;
    pub mod sde;
    pub mod sdp;       // 新增此行
    ```

    注意：确认 `pub mod subtitle;` 已在 Plan 01 中添加，`pub mod sdp;` 放在 sde 之后（按字母顺序 subtitle → sde → sdp）。

    **Action 2: 更新 src/sdp/mod.rs — 添加 clip/pipeline 模块声明**

    Plan 03 创建的 mod.rs 仅包含 `pub mod error; pub mod types; pub mod script_gen;`。现在 clip.rs 和 pipeline.rs 已创建，只需在 mod.rs 中追加以下内容（保留现有内容，添加新行）：
    ```rust
    pub mod clip;
    pub mod pipeline;

    pub use pipeline::run_sdp;
    ```
  </action>

  <verify>
    <automated>cargo build -p narratoai-core 2>&1 | head -20</automated>
    <automated>grep -n "pub mod sdp;" src/lib.rs</automated>
    <automated>grep -c "pub mod clip;" src/sdp/mod.rs</automated>
    <automated>grep -c "pub mod pipeline;" src/sdp/mod.rs</automated>
  </verify>

  <acceptance_criteria>
    - src/lib.rs 包含 `pub mod sdp;`
    - src/sdp/mod.rs 包含 `pub mod clip;` 和 `pub mod pipeline;`
    - `cargo build -p narratoai-core` 零错误
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script file → run_sdp | Untrusted script content validated before FFmpeg processing |
| FFmpeg CLI arguments | Path strings incorporating user file paths |
| BGM file path | Optional user-provided audio file path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-006 | Tampering | concat step | mitigate | Paths escaped with `replace('\\', "/")` in concat_list.txt; FFmpeg uses -safe 0 but does not execute shell |
| T-08-007 | DoS | composite filter | mitigate | `total_duration` bounded check, BGM loop size limited by `size=2e+09` in aloop |
| T-08-008 | Tampering | OST validation | mitigate | G4 guard in run_sdp() rejects non-OST=1 clips with clear error message |
</threat_model>

<verification>
1. `cargo build -p narratoai-core` 通过
2. `cargo test -p narratoai-core -- sdp` 全部测试通过
3. `cargo test -p narratoai-core -- sde` 仍通过（未引入回归）
4. `cargo test -p narratoai-core -- subtitle` 仍通过（未引入回归）
5. `cargo test -p narratoai-core -- documentary` 仍通过（未引入回归）
</verification>

<success_criteria>
- sdp_step_clip() 正确复用 SDE 裁剪逻辑
- run_sdp() 4 步流水线可编译，各步进度回调绑定
- step_concat 使用正确的 FFmpeg concat 命令
- step_composite 使用 filter_complex 实现原始音频 + 可选 BGM 混音
- sdp/mod.rs 包含 clip 和 pipeline 模块声明
- 所有模块测试通过
</success_criteria>
