---
phase: 08-sdp-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # New files
  - src/subtitle/mod.rs
  - src/subtitle/error.rs
  - src/subtitle/types.rs
  - src/subtitle/parser.rs
  - src/subtitle/timestamp.rs
  # Modified files
  - src/sde/types.rs
  - src/sde/timestamp.rs
  - src/sde/error.rs
  - src/sde/mod.rs
  - src/sde/clip.rs
  - src/sde/pipeline.rs
  - src/sde/script_gen.rs
  - src/lib.rs
autonomous: true
requirements: [SDP-01]
user_setup: []

must_haves:
  truths:
    - "SDE 和 SDP 共享同一个字幕解析模块，无重复代码"
    - "字幕解析功能可从 SDE 模块中提取为独立的公共模块"
    - "src/subtitle/ 包含 error/types/parser/timestamp 四个子模块"
    - "SDE 所有引用已更新为 crate::subtitle::* 路径"
    - "lib.rs 导出 pub mod subtitle"
  artifacts:
    - path: "src/subtitle/mod.rs"
      provides: "公共字幕模块入口，pub use 所有公共类型和函数"
      min_lines: 15
    - path: "src/subtitle/error.rs"
      provides: "SubtitleError 枚举（thiserror，中文错误信息）"
      contains: "SubtitleError"
    - path: "src/subtitle/types.rs"
      provides: "SubtitleSegment 数据结构"
      contains: "SubtitleSegment"
    - path: "src/subtitle/parser.rs"
      provides: "parse_subtitle_file, detect_encoding, normalize_subtitle_text 等解析函数"
      contains: "parse_subtitle_file"
    - path: "src/subtitle/timestamp.rs"
      provides: "parse_srt_timestamp, find_precise_range 时间戳工具函数"
      contains: "parse_srt_timestamp"
  key_links:
    - from: "src/sde/subtitle.rs"
      to: "删除"
      via: "实现移至 src/subtitle/parser.rs"
    - from: "src/sde/pipeline.rs"
      to: "crate::subtitle::parse_subtitle_file"
      via: "导入路径变更"
    - from: "src/lib.rs"
      to: "pub mod subtitle;"
      via: "新增模块导出"
---

<objective>
将字幕解析功能从 SDE 模块提取为独立公共模块 `src/subtitle/`。

Purpose: Phase 8 SDP 流水线需要字幕解析功能，Phase 7 SDE 已拥有完整的字幕解析实现。按 D-02 决策，将 `src/sde/subtitle.rs` 提取为 `src/subtitle/` 公共模块，使得 Phase 7 和 Phase 8 共享。

同时提取 `parse_srt_timestamp` 和 `find_precise_range`（当前位于 `src/sde/timestamp.rs`）到 `src/subtitle/timestamp.rs`，因为它们是字幕处理的基础工具。

Output:
- 新建 `src/subtitle/` 模块（error.rs, types.rs, parser.rs, timestamp.rs, mod.rs）
- `src/sde/subtitle.rs` 删除（代码已迁移）
- `src/sde/timestamp.rs` 中的 parse_srt_timestamp/find_precise_range 迁移（保留 SDE 兼容 re-export）
- 所有 SDE 模块导入路径更新
- `src/lib.rs` 新增 `pub mod subtitle;`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-sdp-pipeline/08-CONTEXT.md
@.planning/phases/08-sdp-pipeline/08-RESEARCH.md

<interfaces>
<!-- Key types from existing code that the executor needs -->

From src/sde/types.rs:
```rust
pub struct SubtitleSegment {
    pub index: usize,
    pub start_secs: f64,
    pub end_secs: f64,
    pub text: String,
}
```

From src/sde/error.rs (current):
```rust
pub enum SdeError {
    ParseSubtitle { details: String },
    PlotAnalysis { details: String },
    ScriptGeneration { details: String },
    JsonRepair { details: String },
    VideoProcess(PipelineError),
    Validation { details: String },
    Io { source: std::io::Error },
}
```

From src/sde/timestamp.rs (functions to move):
```rust
pub fn parse_srt_timestamp(input: &str) -> Result<f64, SdeError>
pub fn find_precise_range(llm_timestamp: &str, segments: &[SubtitleSegment]) -> Option<(f64, f64)>
```

Usage locations of `crate::sde::subtitle::parse_subtitle_file`:
- src/sde/pipeline.rs L18: `use crate::sde::subtitle::parse_subtitle_file;`
- src/sde/pipeline.rs L698: `parse_subtitle_file(&subtitle_path_clone)`

Usage of `crate::sde::timestamp::parse_srt_timestamp`:
- src/sde/subtitle.rs L8: `use crate::sde::timestamp::parse_srt_timestamp;`

Usage of `crate::sde::timestamp::find_precise_range`:
- src/sde/clip.rs L5: `use crate::sde::timestamp::find_precise_range;`
- src/sde/pipeline.rs L19: `use crate::sde::timestamp::find_precise_range;`
</interfaces>

Build verification:
```bash
cargo build -p narratoai-core 2>&1 | head -40
Test:
cargo test -p narratoai-core -- subtitle 2>&1 | tail -10
cargo test -p narratoai-core -- sde 2>&1 | tail -10
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建公共字幕模块 src/subtitle/</name>

  <files>
    src/subtitle/mod.rs
    src/subtitle/error.rs
    src/subtitle/types.rs
    src/subtitle/parser.rs
    src/subtitle/timestamp.rs
  </files>

  <read_first>
    src/sde/subtitle.rs — 全部解析代码需迁移
    src/sde/timestamp.rs — parse_srt_timestamp 和 find_precise_range 需迁移
    src/sde/error.rs — 当前 SdeError 错误类型定义
    src/sde/types.rs — SubtitleSegment 定义
    src/sde/mod.rs — 当前模块入口模式
  </read_first>

  <action>
    <!-- Action 1: Create src/subtitle/error.rs -->
    创建独立 SubtitleError 枚举，使用 thiserror 派生，中文错误信息：
    ```rust
    use thiserror::Error;

    #[derive(Error, Debug)]
    pub enum SubtitleError {
        #[error("字幕解析失败: {details}")]
        ParseSubtitle { details: String },
        #[error("IO 操作失败: {source}")]
        Io { source: std::io::Error },
    }

    impl From<std::io::Error> for SubtitleError {
        fn from(source: std::io::Error) -> Self { SubtitleError::Io { source } }
    }
    ```
    包含单元测试覆盖中文错误消息（参考 src/sde/error.rs 的测试模式）。

    <!-- Action 2: Create src/subtitle/types.rs -->
    将 SubtitleSegment 结构体定义从 src/sde/types.rs 移入此文件，更新导入路径。
    ```rust
    #[derive(Debug, Clone)]
    pub struct SubtitleSegment {
        pub index: usize,
        pub start_secs: f64,
        pub end_secs: f64,
        pub text: String,
    }
    ```

    <!-- Action 3: Create src/subtitle/timestamp.rs -->
    从 src/sde/timestamp.rs 迁移以下 public 函数：
    - `pub fn parse_srt_timestamp(input: &str) -> Result<f64, SubtitleError>` — 将返回类型从 SdeError 改为 SubtitleError
    - `pub fn find_precise_range(llm_timestamp: &str, segments: &[SubtitleSegment]) -> Option<(f64, f64)>` — SubtitleSegment 引用改为从 crate::subtitle::types 导入

    保留 src/sde/timestamp.rs 作为 re-export 包装（见 Task 2）。

    <!-- Action 4: Create src/subtitle/parser.rs -->
    从 src/sde/subtitle.rs 迁移所有 public 和 private 函数：
    - `pub fn detect_encoding(data: &[u8]) -> Result<String, SubtitleError>` — SdeError → SubtitleError
    - `pub fn normalize_subtitle_text(text: &str) -> String`
    - `pub fn parse_subtitle_file(path: &Path) -> Result<(Vec<SubtitleSegment>, String, String), SubtitleError>` — SdeError → SubtitleError
    - 所有 private 辅助函数（has_srt_timecodes, has_meaningful_content, decode_utf16le, decode_utf16be, looks_like_utf16le, parse_srt_blocks, split_ass_fields, normalize_ass_timestamp, parse_ass_dialogues, extract_text_from_srt, extract_text_from_ass, decode_bytes）
    - 所有 #[cfg(test)] 测试

    更新导入：
    - `use crate::sde::error::SdeError` → `use crate::subtitle::error::SubtitleError`
    - `use crate::sde::timestamp::parse_srt_timestamp` → `use crate::subtitle::timestamp::parse_srt_timestamp`
    - `use crate::sde::types::SubtitleSegment` → `use crate::subtitle::types::SubtitleSegment`

    <!-- Action 5: Create src/subtitle/mod.rs -->
    ```rust
    pub mod error;
    pub mod types;
    pub mod parser;
    pub mod timestamp;

    pub use error::SubtitleError;
    pub use types::SubtitleSegment;
    pub use parser::{parse_subtitle_file, detect_encoding, normalize_subtitle_text};
    pub use timestamp::{parse_srt_timestamp, find_precise_range};
    ```
  </action>

  <verify>
    <automated>test -f src/subtitle/mod.rs && test -f src/subtitle/error.rs && test -f src/subtitle/types.rs && test -f src/subtitle/parser.rs && test -f src/subtitle/timestamp.rs</automated>
    <automated>grep -c "pub enum SubtitleError" src/subtitle/error.rs</automated>
    <automated>grep -c "pub struct SubtitleSegment" src/subtitle/types.rs</automated>
    <automated>grep -c "pub fn parse_subtitle_file" src/subtitle/parser.rs</automated>
    <automated>grep -c "pub fn parse_srt_timestamp" src/subtitle/timestamp.rs</automated>
  </verify>

  <acceptance_criteria>
    - src/subtitle/mod.rs 文件存在，内容包含 pub mod error, types, parser, timestamp
    - src/subtitle/error.rs 文件存在，内容包含 pub enum SubtitleError
    - src/subtitle/types.rs 文件存在，内容包含 pub struct SubtitleSegment
    - src/subtitle/parser.rs 文件存在，内容包含 pub fn parse_subtitle_file(
    - src/subtitle/timestamp.rs 文件存在，内容包含 pub fn parse_srt_timestamp(
    - 所有辅助函数均已从 sde/subtitle.rs 搬入 parser.rs（has_srt_timecodes, has_meaningful_content, decode_utf16le, decode_utf16be, looks_like_utf16le, parse_srt_blocks, split_ass_fields, normalize_ass_timestamp, parse_ass_dialogues, extract_text_from_srt, extract_text_from_ass, decode_bytes）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 SDE 模块使用新路径 + 删除旧文件</name>

  <files>
    src/sde/mod.rs
    src/sde/error.rs
    src/sde/types.rs
    src/sde/timestamp.rs
    src/sde/clip.rs
    src/sde/pipeline.rs
    src/sde/script_gen.rs
    src/sde/subtitle.rs
    src/lib.rs
  </files>

  <read_first>
    src/sde/mod.rs — 当前模块声明列表（含 pub mod subtitle）
    src/sde/error.rs — 需添加 From<SubtitleError> 实现
    src/sde/types.rs — 需删除 SubtitleSegment 定义，改为 re-export
    src/sde/timestamp.rs — 需改为 re-export
    src/sde/clip.rs — 需更新 import 路径
    src/sde/pipeline.rs — 需更新 import 路径
    src/sde/subtitle.rs — 待删除
    src/lib.rs — 需添加 pub mod subtitle
  </read_first>

  <action>
    <!-- Action 1: Update src/sde/types.rs -->
    删除 SubtitleSegment 结构体定义。
    添加 re-export（保持向后兼容）：
    ```rust
    pub use crate::subtitle::types::SubtitleSegment;
    ```

    <!-- Action 2: Update src/sde/timestamp.rs -->
    删除 parse_srt_timestamp 和 find_precise_range 函数的实现。
    添加 re-export（保持向后兼容）：
    ```rust
    pub use crate::subtitle::timestamp::{parse_srt_timestamp, find_precise_range};
    ```
    保留 SdeProgressStep 的单元测试（不受影响）。

    <!-- Action 3: Update src/sde/error.rs -->
    添加 From<SubtitleError> 实现：
    ```rust
    impl From<crate::subtitle::error::SubtitleError> for SdeError {
        fn from(e: crate::subtitle::error::SubtitleError) -> Self {
            match e {
                crate::subtitle::error::SubtitleError::ParseSubtitle { details } =>
                    SdeError::ParseSubtitle { details },
                crate::subtitle::error::SubtitleError::Io { source } =>
                    SdeError::Io { source },
            }
        }
    }
    ```

    <!-- Action 4: Update src/sde/clip.rs -->
    更新 import：
    - `use crate::sde::timestamp::find_precise_range;` → 保持不变（sde/timestamp.rs 已 re-export）
    - 实际上`use crate::sde::types::SubtitleSegment;` → 保持不变（sde/types.rs 已 re-export）

    <!-- Action 5: Update src/sde/pipeline.rs -->
    更新 import（Line 18）：
    - `use crate::sde::subtitle::parse_subtitle_file;` → `use crate::subtitle::parser::parse_subtitle_file;`
    - `use crate::sde::timestamp::find_precise_range;` → 保持不变（re-export 可用）
    更新 Line 698（独立 API 函数中）同样使用 crate::subtitle::parser::parse_subtitle_file。

    <!-- Action 6: Update src/sde/script_gen.rs -->
    检查是否有使用 subtitle 模块的 import（当前没有直接引用 subtitle 模块，不需要改动）。

    <!-- Action 7: Delete src/sde/subtitle.rs -->
    删除此文件（整个文件，所有内容已迁移到 src/subtitle/parser.rs）。

    <!-- Action 8: Update src/sde/mod.rs -->
    删除 `pub mod subtitle;` 行（此模块不再属于 SDE）。

    <!-- Action 9: Update src/lib.rs -->
    新增 `pub mod subtitle;`（按字母顺序排列在 script 之前或之后，保持整洁）。
  </action>

  <verify>
    <automated>cargo build -p narratoai-core 2>&1 | head -20</automated>
    <automated>cargo test -p narratoai-core -- sde 2>&1 | tail -10</automated>
    <automated>cargo test -p narratoai-core -- subtitle 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/sde/mod.rs 不再包含 pub mod subtitle;
    - src/sde/subtitle.rs 文件已被删除
    - src/sde/types.rs 包含 pub use crate::subtitle::types::SubtitleSegment;
    - src/sde/timestamp.rs 包含 pub use crate::subtitle::timestamp::{parse_srt_timestamp, find_precise_range};
    - src/sde/error.rs 包含 impl From<SubtitleError> for SdeError
    - src/sde/pipeline.rs 使用 crate::subtitle::parser::parse_subtitle_file
    - src/lib.rs 包含 pub mod subtitle;
    - `cargo build -p narratoai-core` 零错误
    - `cargo test -p narratoai-core -- sde 2>&1 | tail -5` 包含 "test result: ok"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| subtitle file input → parser | Untrusted file content crosses into Rust processing |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-001 | Tampering | subtitle parser | mitigate | `parse_subtitle_file` only reads text content, does not execute or eval |
| T-08-002 | DoS | subtitle parser | mitigate | `parse_srt_blocks` uses bounded loops, `decode_utf16le` handles empty data gracefully |
</threat_model>

<verification>
1. `cargo build -p narratoai-core` 通过
2. `cargo test -p narratoai-core -- subtitle` 通过
3. `cargo test -p narratoai-core -- sde` 全部已有测试通过
</verification>

<success_criteria>
- subtitle 模块作为独立公共模块存在，不依赖 SDE 模块
- SDE 模块所有功能不受影响（import 通过 re-export 或直接更新）
- 测试全部通过
</success_criteria>
