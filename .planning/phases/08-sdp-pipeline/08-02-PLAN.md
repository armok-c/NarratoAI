---
phase: 08-sdp-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prompt/templates/short_drama_editing/subtitle_analysis_v2.0.md
  - src/prompt/templates/short_drama_editing/plot_extraction_v2.0.md
  - src/prompt/register.rs
autonomous: true
requirements: [SDP-01]
user_setup: []

must_haves:
  truths:
    - "SDP 专用的 subtitle_analysis prompt 模板在 short_drama_editing 分类下注册"
    - "SDP 专用的 plot_extraction prompt 模板参数已更新为 SDP 格式（含 plot_summary + plot_titles）"
    - "两个模板均在 register.rs 中注册"
  artifacts:
    - path: "src/prompt/templates/short_drama_editing/subtitle_analysis_v2.0.md"
      provides: "SDP 第一步 LLM prompt：分析字幕提取叙事结构和关键情节点"
      min_lines: 50
    - path: "src/prompt/templates/short_drama_editing/plot_extraction_v2.0.md"
      provides: "SDP 第二步 LLM prompt：精确定位关键剧情时间段（含 plot_summary + plot_titles 参数）"
      min_lines: 50
    - path: "src/prompt/register.rs"
      provides: "subtitle_analysis prompt 注册 + plot_extraction 参数更新"
      contains: "subtitle_analysis"
  key_links:
    - from: "register.rs"
      to: "subtitle_analysis_v2.0.md"
      via: "include_str! 包含模板"
    - from: "register.rs"
      to: "plot_extraction_v2.0.md"
      via: "include_str! 包含模板"
---

<objective>
创建 SDP 专属 LLM Prompt 模板。

Purpose: SDP 脚本生成需要两步 LLM 调用（D-10）。第一步使用 `subtitle_analysis` prompt 分析字幕叙事结构，第二步使用 `plot_extraction` prompt 精确定位关键剧情时间段。两个 prompt 都注册在 `short_drama_editing` 分类下。

subtitle_analysis 是全新的 Rust prompt 模板，翻译自 Python 版 `app/services/prompts/short_drama_editing/subtitle_analysis.py`。plot_extraction 已有注册但参数不完整（缺少 plot_summary 和 plot_titles），需更新参数定义和模板内容。

SDP 版 plot_extraction 的参数不同于现有版本：从 `["subtitle_content", "language"]` 变为 `["subtitle_content", "plot_summary", "plot_titles"]`，且参数说明需包含 `custom_clips` 变量。

Output:
- 新文件：`subtitle_analysis_v2.0.md`
- 更新文件：`plot_extraction_v2.0.md`
- 修改文件：`src/prompt/register.rs`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-sdp-pipeline/08-CONTEXT.md
@.planning/phases/08-sdp-pipeline/08-RESEARCH.md

<interfaces>
From src/prompt/register.rs — existing registration pattern:
```rust
fn register_short_drama_editing_prompts(registry: &mut PromptRegistry, errors: &mut Vec<PromptError>) {
    let prompt = Prompt {
        metadata: PromptMetadata {
            name: "plot_extraction".into(), category: "short_drama_editing".into(), version: "v2.0".into(),
            model_type: ModelType::Text, output_format: OutputFormat::PlotAnalysis,
            tags: vec!["short-drama-editing".into(), "plot-extraction".into(), "text".into()],
            parameters: vec![
                ParameterDef { name: "subtitle_content".into(), required: true, default: None, description: "字幕内容".into() },
                ParameterDef { name: "language".into(), required: false, default: Some("zh-CN".into()), description: "输出语言".into() },
            ],
        },
        content: include_str!("templates/short_drama_editing/plot_extraction_v2.0.md").into(),
    };
    if let Err(e) = registry.register(prompt, true) { errors.push(e); }
}
```

From src/prompt/types.rs — ParameterDef:
```rust
pub struct ParameterDef {
    pub name: String,
    pub required: bool,
    pub default: Option<String>,
    pub description: String,
}
```

Python reference files:
- app/services/prompts/short_drama_editing/subtitle_analysis.py — full prompt with ${subtitle_content} and ${custom_clips} placeholders
- app/services/prompts/short_drama_editing/plot_extraction.py — full prompt with ${subtitle_content}, ${plot_summary}, ${plot_titles} placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 subtitle_analysis_v2.0.md 模板</name>

  <files>
    src/prompt/templates/short_drama_editing/subtitle_analysis_v2.0.md
  </files>

  <read_first>
    app/services/prompts/short_drama_editing/subtitle_analysis.py — Python 版完整 prompt 内容
  </read_first>

  <action>
    从 Python 版 `subtitle_analysis.py` 翻译该 prompt。使用 Rust PromptTemplate 格式（`${variable_name}` 占位符），变量名与 `register.rs` 的 `ParameterDef` 字段一一对应。

    系统提示语不在模板中（由 script_gen.rs 调用时通过 system_prompt 参数传递）。模板内容仅包含用户 prompt 文本。

    关键变量：
    - `${subtitle_content}` — 完整字幕文本内容
    - `${custom_clips}` — 建议的片段数量（默认 5）
    - `${language}` — 输出语言（默认 zh-CN）

    必须包含：
    1. **任务目标** — 分析短剧字幕，提取连贯完整故事线的关键情节点
    2. **叙事结构分析要求** — 开端/发展/高潮/结局四阶段识别
    3. **关键情节点选择原则** — 连贯性原则（最重要）、覆盖性原则、戏剧性原则
    4. **严格时间顺序排列要求**
    5. **JSON 输出格式定义** — 包含 summary, narrative_structure, plot_titles, plot_connections, analysis_details
    6. **质量检查清单** — 5 项自检
    7. **输出限制** — 只输出 JSON，不要任何说明文字

    系统提示语（由调用方传入，非模板内容）：
    "你是一名资深短剧编剧和剪辑师，精通叙事结构和剧情节奏把控，擅长从字幕中提取能够形成完整故事线的关键情节。"
  </action>

  <verify>
    src/prompt/templates/short_drama_editing/subtitle_analysis_v2.0.md 存在
    文件包含 ${subtitle_content} 占位符
    文件包含 ${custom_clips} 占位符
    文件包含 JSON 输出格式定义
  </verify>

  <acceptance_criteria>
    - 文件存在
    - 内容包含 `${subtitle_content}` 和 `${custom_clips}` 占位符
    - 内容包含 "连贯性" 和 "叙事结构" 关键词
    - 内容包含 "narrative_structure" 和 "plot_titles" JSON 输出字段定义
    - 内容包含 "质量检查清单"
    - 内容包含 "只输出JSON" 或类似限制声明
    - 文件行数 >= 50（确保内容完整）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 plot_extraction_v2.0.md 为 SDP 专用版本</name>

  <files>
    src/prompt/templates/short_drama_editing/plot_extraction_v2.0.md
  </files>

  <read_first>
    app/services/prompts/short_drama_editing/plot_extraction.py — Python 版 SDP 专用 prompt
    src/prompt/templates/short_drama_editing/plot_extraction_v2.0.md — 当前 Rust 版（SDE 通用版，需替换）
  </read_first>

  <action>
    将当前 Rust 版 plot_extraction_v2.0.md（SDE 通用版，仅含 subtitle_content + language 参数）替换为 SDP 专用版。参照 Python 版 `plot_extraction.py` 内容。

    关键变量（与现有不同）：
    - `${subtitle_content}` — 完整字幕文本内容（保留）
    - `${plot_summary}` — 剧情梗概（来自 subtitle_analysis 步骤的输出）
    - `${plot_titles}` — 情节点标题列表（来自 subtitle_analysis 步骤的输出）

    必须包含：
    1. **任务目标** — 为每个关键情节点精确定位时间段
    2. **输入信息** — plot_summary、plot_titles、完整字幕内容
    3. **时间段选择原则** — 连贯性原则、技术规范（时间戳格式、不重叠、按顺序）、内容完整性原则、过渡片段策略
    4. **JSON 输出格式** — plot_points[]（含 sequence, timestamp, title, narrative_function, picture, context_before, context_after, transition_note）+ editing_notes
    5. **质量检查清单** — 时间戳检查、连贯性检查、完整性检查
    6. **重要限制** — 不虚构时间戳、不重叠、不颠倒顺序

    系统提示语（由调用方传入）：
    "你是一名专业短剧剪辑师，精通视频叙事节奏和剧情连贯性，擅长选择能够流畅衔接的视频片段。"

    删除模板中的 `${language}` 变量（SDP 版不需要）。
  </action>

  <verify>
    src/prompt/templates/short_drama_editing/plot_extraction_v2.0.md 存在
    文件包含 ${plot_summary} 和 ${plot_titles} 占位符
    文件包含 ${subtitle_content} 占位符
    文件不包含 ${language} 占位符
  </verify>

  <acceptance_criteria>
    - 文件存在
    - 内容包含 `${subtitle_content}`、`${plot_summary}`、`${plot_titles}` 占位符
    - 内容**不包含** `${language}` 占位符
    - 内容包含 "plot_points" JSON 数组输出定义
    - 内容包含 "严禁时间重叠"
    - 内容包含 "时间段选择原则" 或 "连贯性原则"
    - 文件行数 >= 50
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 更新 prompt/register.rs — 添加 subtitle_analysis 注册 + 更新 plot_extraction 参数</name>

  <files>
    src/prompt/register.rs
  </files>

  <read_first>
    src/prompt/register.rs — 现有注册逻辑
    src/prompt/types.rs — Prompt, PromptMetadata, ParameterDef 类型定义
  </read_first>

  <action>
    在 `register_short_drama_editing_prompts()` 函数中：

    1. **新增 subtitle_analysis v2.0 prompt**：
    ```rust
    let subtitle_analysis = Prompt {
        metadata: PromptMetadata {
            name: "subtitle_analysis".into(),
            category: "short_drama_editing".into(),
            version: "v2.0".into(),
            model_type: ModelType::Text,
            output_format: OutputFormat::Json,
            tags: vec!["short-drama-editing".into(), "subtitle-analysis".into(), "sdp".into()],
            parameters: vec![
                ParameterDef { name: "subtitle_content".into(), required: true, default: None, description: "字幕内容（含时间码）".into() },
                ParameterDef { name: "custom_clips".into(), required: false, default: Some("5".into()), description: "建议提取的片段数量".into() },
            ],
        },
        content: include_str!("templates/short_drama_editing/subtitle_analysis_v2.0.md").into(),
    };
    if let Err(e) = registry.register(subtitle_analysis, true) { errors.push(e); }
    ```

    2. **更新现有 plot_extraction v2.0 prompt 的参数**：
    将 parameters 从 `["subtitle_content", "language"]` 改为：
    ```rust
    parameters: vec![
        ParameterDef { name: "subtitle_content".into(), required: true, default: None, description: "字幕内容（含时间码）".into() },
        ParameterDef { name: "plot_summary".into(), required: true, default: None, description: "剧情梗概（来自第一步 analysis）".into() },
        ParameterDef { name: "plot_titles".into(), required: true, default: None, description: "情节点标题列表".into() },
    ],
    ```
    更新 tags 加入 "sdp"：
    ```rust
    tags: vec!["short-drama-editing".into(), "plot-extraction".into(), "text".into(), "sdp".into()],
    ```

    3. **更新 `test_register_all_prompts_full` 测试**：
    将 `short_drama_editing` 分类的 prompt 数量断言从 1 改为 2：
    ```rust
    assert_eq!(registry.list_prompts("short_drama_editing").len(), 2);
    ```
    添加 subtitle_analysis 查询断言：
    ```rust
    assert!(registry.get("short_drama_editing", "subtitle_analysis", Some("v2.0")).is_ok());
    ```
  </action>

  <verify>
    cargo build -p narratoai-core 通过
    cargo test -p narratoai-core -- prompt::register 通过
  </verify>

  <acceptance_criteria>
    - src/prompt/register.rs 包含 subtitle_analysis 的注册调用
    - subtitle_analysis 的 metadata 包含 name="subtitle_analysis", category="short_drama_editing", version="v2.0"
    - subtitle_analysis 参数包含 subtitle_content (required) 和 custom_clips (optional, default "5")
    - plot_extraction 参数包含 subtitle_content (required)、plot_summary (required)、plot_titles (required)
    - plot_extraction 参数不包含 language
    - `cargo test -p narratoai-core -- prompt::register` 通过，测试包含 subtitle_analysis 查询断言
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| prompt template content | Static file, no user input in template itself |
| parameter injection via render vars | Variable substitution handled by template::render, no shell execution |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-003 | Tampering | prompt rendering | accept | Prompt templates are static files embedded at compile time via include_str!, no runtime modification possible |
</threat_model>

<verification>
1. 两个模板文件存在且包含正确占位符
2. `cargo build -p narratoai-core` 通过（验证 include_str! 路径有效）
3. `cargo test -p narratoai-core -- prompt::register` 通过
</verification>

<success_criteria>
- subtitle_analysis_v2.0.md 创建并注册
- plot_extraction_v2.0.md 更新为 SDP 专用版本（参数：subtitle_content + plot_summary + plot_titles）
- 所有测试通过
- Prompt 系统可成功获取 subtitle_analysis 模板并渲染
</success_criteria>
