---
phase: 08-sdp-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sdp/error.rs
  - src/sdp/clip.rs
autonomous: true
requirements:
  - SDP-01
  - SDP-02
gap_closure: true

must_haves:
  truths:
    - "cargo build -p narratoai-core 编译成功，无 E0277 错误"
    - "cargo test -- sdp 全部通过"
    - "cargo test -- subtitle 全部通过（无回归）"
    - "cargo test -- sde 全部通过（无回归）"
    - "cargo test -- prompt::register 全部通过（无回归）"
    - "cargo test -- documentary 全部通过（无回归）"
    - "src/sdp/error.rs 包含 impl From<ScriptError> for SdpError"
    - "src/sdp/clip.rs 不包含未使用的 SubtitleSegment 导入"
  artifacts:
    - path: "src/sdp/error.rs"
      provides: "From<ScriptError> 实现将 ScriptError 5 个变体映射到 SdpError"
      contains: "impl From<crate::script::error::ScriptError> for SdpError"
    - path: "src/sdp/clip.rs"
      provides: "干净导入——移除未使用的 SubtitleSegment 引用"
      not_contains: "use crate::sde::types::SubtitleSegment;（不在测试模块之外的顶层作用域中）"
  key_links:
    - from: "src/sdp/error.rs"
      to: "src/script/error.rs"
      via: "impl From<ScriptError> for SdpError"
      pattern: "From<crate::script::error::ScriptError>"
    - from: "src/sdp/pipeline.rs:43"
      to: "src/sdp/error.rs"
      via: "? 运算符将 ScriptError 转换为 SdpError"
      pattern: "crate::script::load_script"  #行 43 使用 ? 运算符需 From 实现

---

<objective>
修复 Phase 8 的 BLOCKER 编译错误——为 `SdpError` 实现 `From<ScriptError>` trait，使 `run_sdp()` 中的 `?` 运算符能自动转换 ScriptError 为 SdpError。同时清除 clip.rs 中的未使用导入警告。

Purpose: 这是 Phase 8 的最后一步——前面 4 个 plan 已实现全部 SDP 模块代码，但缺少这一个 From 实现导致整个 `cargo build -p narratoai-core` 无法通过编译。只需添加 ~20 行代码即可解锁所有测试和验证。

Output: `src/sdp/error.rs` 新增一个 `impl From<ScriptError>` 代码块；`src/sdp/clip.rs` 删除一行未使用 import。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-sdp-pipeline/08-VERIFICATION.md（gap 详情见第 14-23 行的 missing 字段和第 163-182 行的精确代码修复）

# 源文件概览

## src/sdp/error.rs（当前行 105）

当前有 From<io::Error>（行 28-32）和 From<SubtitleError>（行 34-43）。需要在 From<SubtitleError> 块之后（行 43 之后）插入新的 From<ScriptError> 块。

ScriptError 5 个变体（src/script/error.rs 行 6-20）：
- `JsonParse(serde_json::Error)` — 映射到 `SdpError::JsonRepair { details: je.to_string() }`
- `Validation(Vec<ValidationError>)` — 映射到 `SdpError::Validation { details: format!("{:?}", errors) }`
- `Io(std::io::Error)` — 映射到 `SdpError::Io { source }`
- `IndexOutOfBounds` — 映射到 `SdpError::Validation { details: "索引越界".into() }`
- `InvalidTimestamp(String)` — 映射到 `SdpError::Validation { details: format!("时间戳格式无效: {}", ts) }`

## src/sdp/clip.rs 行 5

当前：`use crate::sde::types::SubtitleSegment;`
这行在 main 代码体未使用。测试模块（行 106）有自己的 `use crate::sde::types::SubtitleSegment;`。
→ 仅删除行 5 的 import，保留测试模块内的 import。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 From<ScriptError> 实现 + 移除未使用导入</name>

  <files>
    src/sdp/error.rs
    src/sdp/clip.rs
  </files>

  <read_first>
    src/sdp/error.rs（读取全文件，确认插入位置在 From<SubtitleError> 块之后、#[cfg(test)] 之前）
    src/script/error.rs（确认 ScriptError 5 个变体的签名）
    src/sdp/clip.rs 行 5（确认未使用的 SubtitleSegment 导入）
  </read_first>

  <action>
    ## 修改 1：src/sdp/error.rs

    在 `impl From<crate::subtitle::error::SubtitleError> for SdpError` 代码块（当前行 34-43）之后、
    `#[cfg(test)]` 属性（当前行 45）之前，插入以下新代码块：

    ```rust
    impl From<crate::script::error::ScriptError> for SdpError {
        fn from(e: crate::script::error::ScriptError) -> Self {
            match e {
                crate::script::error::ScriptError::JsonParse(je) =>
                    SdpError::JsonRepair { details: je.to_string() },
                crate::script::error::ScriptError::Validation(errors) =>
                    SdpError::Validation { details: format!("{:?}", errors) },
                crate::script::error::ScriptError::Io(source) =>
                    SdpError::Io { source },
                crate::script::error::ScriptError::IndexOutOfBounds =>
                    SdpError::Validation { details: "索引越界".into() },
                crate::script::error::ScriptError::InvalidTimestamp(ts) =>
                    SdpError::Validation { details: format!("时间戳格式无效: {}", ts) },
            }
        }
    }
    ```

    注意：对 `Validation` 变体使用 `format!("{:?}", errors)` 而非 `errors.iter()` —— 因为 ValidationError 不实现 Display，
    只有 Debug（derive 自动生成）。对 `IndexOutOfBounds` 和 `InvalidTimestamp` 使用 `SdpError::Validation`，
    复用已有的 Validation 变体，不新增 SdpError 变体。

    ## 修改 2：src/sdp/clip.rs

    删除第 5 行：`use crate::sde::types::SubtitleSegment;`

    仅删除这一行。保留测试模块（`#[cfg(test)] mod tests`）内第 106 行的 `use crate::sde::types::SubtitleSegment;`，
    该引用在测试代码中使用。
  </action>

  <verify>
    <automated>
      cd E:\GitLib\NarratoAI && cargo build -p narratoai-core 2>&1
    </automated>
    编译通过（exit code 0），无 E0277 类型转换错误。
  </verify>

  <acceptance_criteria>
    1. `cargo build -p narratoai-core` 退出码为 0（无编译错误，无警告）
    2. `src/sdp/error.rs` 包含 `impl From<crate::script::error::ScriptError> for SdpError {`
    3. `src/sdp/error.rs` 包含分支：`ScriptError::JsonParse(je)`、`ScriptError::Validation(errors)`、`ScriptError::Io(source)`、`ScriptError::IndexOutOfBounds`、`ScriptError::InvalidTimestamp(ts)`
    4. `src/sdp/clip.rs` 第 5 行不再包含 `use crate::sde::types::SubtitleSegment;`
    5. `cargo test -- sdp` 全部通过
    6. `cargo test -- subtitle` 全部通过（无回归）
    7. `cargo test -- sde` 全部通过（无回归）
    8. `cargo test -- prompt::register` 全部通过（无回归）
    9. `cargo test -- documentary` 全部通过（无回归）
  </acceptance_criteria>

  <done>
    单个 fix 完成：`cargo build -p narratoai-core` 零错误 + 所有 5 个测试套件全部通过。
    原 BLOCKER 编译失败（E0277）被修复，Phase 8 全部 22 个 must-haves 已验证通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| From&lt;ScriptError&gt; 转换 | ScriptError 到 SdpError 的类型转换层，位于 sdp 模块边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | E (Elevation of Privilege) | From&lt;ScriptError&gt; 实现 | mitigate | 使用穷尽 match 覆盖全部 5 个 ScriptError 变体，无 fallback，编译器确保模式完整性 |
| T-08-05-02 | I (Information Disclosure) | format!("{:?}", errors) | accept | V ec<ValidationError> 仅包含 clip_index/field/message 字段，无敏感信息 |
</threat_model>

<verification>
1. `cd E:\GitLib\NarratoAI && cargo build -p narratoai-core 2>&1 && echo "BUILD OK"` — 输出应为 "BUILD OK"
2. `cd E:\GitLib\NarratoAI && cargo test -- sdp 2>&1 && echo "TESTS OK"` — 所有 SDP 测试通过
3. `cd E:\GitLib\NarratoAI && cargo test -- subtitle 2>&1 && echo "SUB OK"` — 字幕测试无回归
4. `cd E:\GitLib\NarratoAI && cargo test -- sde 2>&1 && echo "SDE OK"` — SDE 测试无回归
5. `cd E:\GitLib\NarratoAI && cargo test -- documentary 2>&1 && echo "DOC OK"` — 纪录片测试无回归
6. `cd E:\GitLib\NarratoAI && cargo test -- prompt::register 2>&1 && echo "PRMP OK"` — Prompt 注册测试无回归
</verification>

<success_criteria>
1. `cargo build -p narratoai-core` 零错误（之前 E0277）
2. `cargo test -- sdp` 全部通过
3. `cargo test -- subtitle` 全部通过
4. `cargo test -- sde` 全部通过
5. `cargo test -- prompt::register` 全部通过
6. `cargo test -- documentary` 全部通过
7. Phase 8 全部 22 个 must-haves 已验证通过
8. 两个 REQUIREMENTS.md 需求（SDP-01, SDP-02）从 BLOCKED 转为 VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/08-sdp-pipeline/08-05-SUMMARY.md` with verification results.
</output>
