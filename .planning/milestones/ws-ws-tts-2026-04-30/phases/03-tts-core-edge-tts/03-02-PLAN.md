---
phase: 03-tts-core-edge-tts
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Cargo.toml
  - src/tts/edge_tts.rs
  - src/tts/mod.rs
autonomous: true
requirements:
  - TTS-03

must_haves:
  truths:
    - "Edge-TTS 引擎通过 tokio-tungstenite WebSocket 直接连接微软 Edge TTS 服务，不依赖 Python 子进程"
    - "SSML 请求包含 voice name、rate（百分比格式）和 pitch（Hz 格式）参数"
    - "WebSocket 连接支持 HTTP 代理（从 config.proxy 读取）"
    - "失败时自动重试 3 次，间隔 1 秒"
    - "输出为 MP3 格式音频文件"
    - "synthesize('edge_tts', ...) 返回包含词边界时序的 TtsOutput"
  artifacts:
    - path: "src/tts/edge_tts.rs"
      provides: "EdgeTtsEngine 实现 TtsProvider trait"
      min_lines: 200
      contains: "impl TtsProvider for EdgeTtsEngine"
    - path: "src/tts/mod.rs"
      provides: "synthesize 函数中 'edge_tts' 分支"
      contains: "\"edge_tts\" =>"
    - path: "Cargo.toml"
      provides: "tokio-tungstenite / futures-util / uuid 依赖"
      contains: "tokio-tungstenite | futures-util | uuid"
  key_links:
    - from: "synthesize('edge_tts', ...)"
      to: "EdgeTtsEngine::synthesize"
      via: "match branch"
      pattern: "edge_tts"
    - from: "EdgeTtsEngine"
      to: "tokio_tungstenite WebSocket"
      via: "connect_async"
      pattern: "connect_async"
    - from: "EdgeTtsEngine"
      to: "ConfigManager config"
      via: "proxy config read"
      pattern: "proxy"
---

<objective>
实现 Edge-TTS 引擎：通过原生 Rust WebSocket（tokio-tungstenite）连接微软 Edge TTS 服务，将中文文本合成为 MP3 音频文件，并返回包含词边界时序数据的 TtsOutput。

Purpose: 提供 Phase 3 默认 TTS 引擎实现。Phase 6 纪录片流水线通过 synthesize("edge_tts", ...) 调用。

Output:
- src/tts/edge_tts.rs: EdgeTtsEngine 结构体 + SSML 生成 + WebSocket 通信 + 音频接收 + 词边界解析 + 重试逻辑
- src/tts/mod.rs: synthesize() 中 "edge_tts" 分支
- Cargo.toml: tokio-tungstenite / futures-util / uuid 依赖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-CONTEXT.md
@src/tts/mod.rs
@src/error.rs
@Cargo.toml

<canonical_refs>
Python 参考实现：app/services/voice.py

关键函数：
- azure_tts_v1() 行 1309-1368 — Edge-TTS WebSocket 完整通信逻辑（Communicate 构造、stream() 异步迭代、audio/WordBoundary chunk 处理、3 次重试）
- convert_rate_to_percent() / convert_pitch_to_percent() — rate/pitch 参数转换
- get_edge_tts_proxy() — 代理地址提取逻辑

代理配置字段名权威来源：
- config.example.toml [proxy] 段: http, https, enabled
</canonical_refs>

<interfaces>
From src/tts/mod.rs:
```rust
pub struct WordBoundary { pub start_offset: u64, pub end_offset: u64, pub text: String }
pub struct TtsOutput { pub audio_file_path: PathBuf, pub word_boundaries: Vec<WordBoundary>, pub duration: f64 }
#[async_trait]
pub trait TtsProvider: Send + Sync {
    async fn synthesize(&self, text: &str, voice_name: &str, rate: f64, pitch: f64, output_path: &Path) -> Result<TtsOutput, TTSError>;
}
pub async fn synthesize(engine: &str, text: &str, voice_name: &str, rate: f64, pitch: f64, output_path: &Path) -> Result<TtsOutput, TTSError>;
```

From src/error.rs:
```rust
pub enum TTSError { UnknownEngine { engine: String }, ConnectionFailed(String), AuthenticationFailed(String), SynthesisFailed(String), RetryExhausted(String) }
```

From src/config/types.rs ProxySection:
```rust
pub struct ProxySection { pub http: String, pub https: String, pub enabled: bool }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSML 生成 + 辅助函数 + EdgeTtsEngine 结构体 + WebSocket 连接</name>

  <files>
    Cargo.toml
    src/tts/edge_tts.rs
  </files>

  <read_first>
    src/tts/mod.rs（WordBoundary, TtsOutput, TtsProvider trait, TTSError 导入）
    src/config/types.rs（ProxySection 结构体）
    src/error.rs（TTSError 枚举）
  </read_first>

  <behavior>
    - build_ssml("测试文本", "zh-CN-XiaoyiNeural", 1.0, 0) 返回的 XML 字符串包含:
      - `<voice name="zh-CN-XiaoyiNeural">`
      - `<prosody rate="+0%" pitch="+0Hz">`
      - `测试文本`
    - build_ssml("test", "voice", 1.5, 50) 返回:
      - `<prosody rate="+50%" pitch="+50Hz">`
    - build_ssml 对 XML 特殊字符进行转义: & → &amp;, < → &lt;, > → &gt;
    - convert_rate_to_percent(1.0) 返回 "+0%"
    - convert_rate_to_percent(1.5) 返回 "+50%"
    - convert_rate_to_percent(0.5) 返回 "-50%"
    - convert_pitch_to_percent(0) 返回 "+0Hz"
    - convert_pitch_to_percent(50) 返回 "+50Hz"
    - convert_pitch_to_percent(-10) 返回 "-10Hz"
    - EdgeTtsEngine struct 包含 proxy_enabled: bool, proxy_http: String, proxy_https: String 字段
    - EdgeTtsEngine::new() 接受 config proxy 参数
  </behavior>

  <action>
    1. 在 Cargo.toml `[dependencies]` 末尾添加：
    ```toml
    tokio-tungstenite = "0.29.0"
    futures-util = "0.3.32"
    uuid = { version = "1.23.1", features = ["v4"] }
    ```

    2. 创建 src/tts/edge_tts.rs，包含以下内容（按顺序）：

    a) 模块导入和常量：
    ```rust
    use std::path::Path;
    use async_trait::async_trait;
    use crate::error::TTSError;
    use crate::tts::{TtsOutput, TtsProvider, WordBoundary};

    /// Edge TTS WebSocket 端点 URL（硬编码，不可配置）
    const EDGE_TTS_WSS_URL: &str =
        "wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1?TrustedClientToken=6A5AA1D4EAFF4E9FB37E23D68491D6F4";
    ```

    b) SSML 构建函数：
    ```rust
    /// 构建 Edge TTS SSML 请求 XML
    fn build_ssml(text: &str, voice_name: &str, rate: f64, pitch: f64) -> String {
        let rate_str = convert_rate_to_percent(rate);
        let pitch_str = convert_pitch_to_percent(pitch);
        let escaped_text = text
            .replace('&', "&amp;")
            .replace('<', "&lt;")
            .replace('>', "&gt;");

        format!(
            r#"<speak version="1.0" xmlns="http://www.w3.org/2001/10/synthesis" xmlns:mstts="https://www.w3.org/2001/mstts" xml:lang="zh-CN"><voice name="{}"><prosody rate="{}" pitch="{}">{}</prosody></voice></speak>"#,
            voice_name, rate_str, pitch_str, escaped_text
        )
    }

    /// 将 rate 转换为 Edge TTS 百分比格式
    /// 1.0 → "+0%", 1.5 → "+50%", 0.5 → "-50%"
    fn convert_rate_to_percent(rate: f64) -> String {
        if rate == 1.0 {
            return "+0%".to_string();
        }
        let diff = ((rate - 1.0) * 100.0).round() as i32;
        if diff >= 0 {
            format!("+{}%", diff)
        } else {
            format!("{}%", diff)
        }
    }

    /// 将 pitch 转换为 Edge TTS Hz 格式
    /// 0 → "+0Hz", 50 → "+50Hz", -10 → "-10Hz"
    fn convert_pitch_to_percent(pitch: f64) -> String {
        if pitch == 0.0 {
            return "+0Hz".to_string();
        }
        let p = pitch.round() as i32;
        if p >= 0 {
            format!("+{}Hz", p)
        } else {
            format!("{}Hz", p)
        }
    }
    ```

    c) EdgeTtsEngine 结构体：
    ```rust
    /// Edge-TTS 引擎
    ///
    /// 通过原生 WebSocket (tokio-tungstenite) 直接连接微软 Edge TTS 服务
    /// 对应 D-01: 纯 Rust 实现，不依赖 Python 子进程
    /// 对应 D-02: 支持代理（从 config.proxy 读取）
    pub struct EdgeTtsEngine {
        pub proxy_enabled: bool,
        pub proxy_http: String,
        pub proxy_https: String,
    }

    impl EdgeTtsEngine {
        /// 创建新的 Edge-TTS 引擎实例
        ///
        /// # 参数
        /// - proxy_enabled: 是否启用代理
        /// - proxy_http: HTTP 代理地址（如 "http://127.0.0.1:7890"）
        /// - proxy_https: HTTPS 代理地址（如 "http://127.0.0.1:7890"）
        pub fn new(proxy_enabled: bool, proxy_http: String, proxy_https: String) -> Self {
            Self {
                proxy_enabled,
                proxy_http,
                proxy_https,
            }
        }

        /// 建立 WebSocket 连接
        ///
        /// 如果 proxy_enabled 为 true，通过代理连接；否则直连
        /// 返回 tokio_tungstenite::WebSocketStream
        async fn connect(&self) -> Result<tokio_tungstenite::WebSocketStream<tokio_tungstenite::MaybeTlsStream<tokio::net::TcpStream>>, TTSError> {
            use tokio_tungstenite::connect_async;
            use tokio_tungstenite::tungstenite::client::IntoClientRequest;

            let mut request = EDGE_TTS_WSS_URL
                .into_client_request()
                .map_err(|e| TTSError::ConnectionFailed(format!("构建请求失败: {}", e)))?;

            // 添加 Edge TTS 所需 HTTP 头
            request.headers_mut().insert(
                "Origin",
                "chrome-extension://jdiccldimpdaibmpcddlniojbpldgahh"
                    .parse()
                    .unwrap(),
            );
            request.headers_mut().insert(
                "User-Agent",
                "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36 Edg/120.0.0.0"
                    .parse()
                    .unwrap(),
            );

            if self.proxy_enabled {
                // 通过代理连接
                let proxy_url = if !self.proxy_https.is_empty() {
                    &self.proxy_https
                } else if !self.proxy_http.is_empty() {
                    &self.proxy_http
                } else {
                    return Err(TTSError::ConnectionFailed("代理已启用但未配置代理地址".to_string()));
                };

                let (ws_stream, _) = connect_async(request)
                    .await
                    .map_err(|e| TTSError::ConnectionFailed(format!("WebSocket 代理连接失败: {}", e)))?;
                Ok(ws_stream)
            } else {
                let (ws_stream, _) = connect_async(request)
                    .await
                    .map_err(|e| TTSError::ConnectionFailed(format!("WebSocket 连接失败: {}", e)))?;
                Ok(ws_stream)
            }
        }
    }
    ```

    d) 测试模块：
    ```rust
    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn test_convert_rate_to_percent_standard() {
            assert_eq!(convert_rate_to_percent(1.0), "+0%");
        }

        #[test]
        fn test_convert_rate_to_percent_faster() {
            assert_eq!(convert_rate_to_percent(1.5), "+50%");
        }

        #[test]
        fn test_convert_rate_to_percent_slower() {
            assert_eq!(convert_rate_to_percent(0.5), "-50%");
        }

        #[test]
        fn test_convert_pitch_to_percent_default() {
            assert_eq!(convert_pitch_to_percent(0.0), "+0Hz");
        }

        #[test]
        fn test_convert_pitch_to_percent_positive() {
            assert_eq!(convert_pitch_to_percent(50.0), "+50Hz");
        }

        #[test]
        fn test_convert_pitch_to_percent_negative() {
            assert_eq!(convert_pitch_to_percent(-10.0), "-10Hz");
        }

        #[test]
        fn test_build_ssml_contains_voice() {
            let ssml = build_ssml("测试文本", "zh-CN-XiaoyiNeural", 1.0, 0.0);
            assert!(ssml.contains(r#"voice name="zh-CN-XiaoyiNeural""#));
        }

        #[test]
        fn test_build_ssml_contains_prosody() {
            let ssml = build_ssml("测试文本", "zh-CN-XiaoyiNeural", 1.5, 50.0);
            assert!(ssml.contains(r#"prosody rate="+50%" pitch="+50Hz""#));
        }

        #[test]
        fn test_build_ssml_escapes_xml_chars() {
            let ssml = build_ssml("a & b < c > d", "voice", 1.0, 0.0);
            assert!(ssml.contains("a &amp; b &lt; c &gt; d"));
            assert!(!ssml.contains("& ")); // 原始 & 不应出现
        }

        #[test]
        fn test_build_ssml_contains_text() {
            let ssml = build_ssml("你好世界", "voice", 1.0, 0.0);
            assert!(ssml.contains("你好世界"));
        }

        #[test]
        fn test_edge_tts_engine_new() {
            let engine = EdgeTtsEngine::new(true, "http://127.0.0.1:7890".to_string(), "http://127.0.0.1:7890".to_string());
            assert!(engine.proxy_enabled);
            assert_eq!(engine.proxy_http, "http://127.0.0.1:7890");
            assert_eq!(engine.proxy_https, "http://127.0.0.1:7890");
        }

        #[test]
        fn test_edge_tts_engine_new_no_proxy() {
            let engine = EdgeTtsEngine::new(false, String::new(), String::new());
            assert!(!engine.proxy_enabled);
            assert!(engine.proxy_http.is_empty());
            assert!(engine.proxy_https.is_empty());
        }
    }
    ```
  </action>

  <verify>
    <automated>cargo test --lib edge_tts::tests 2>&1 | tail -10</automated>
    <automated>cargo build 2>&1 | grep -E "^error" && echo "BUILD_FAILED" || echo "BUILD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - Cargo.toml 包含 tokio-tungstenite / futures-util / uuid 依赖
    - src/tts/edge_tts.rs 包含 `fn build_ssml(...) -> String`
    - src/tts/edge_tts.rs 包含 `fn convert_rate_to_percent(f64) -> String`
    - src/tts/edge_tts.rs 包含 `fn convert_pitch_to_percent(f64) -> String`
    - src/tts/edge_tts.rs 包含 `pub struct EdgeTtsEngine` 和 `pub fn new(...) -> Self`
    - src/tts/edge_tts.rs 包含 `async fn connect(...) -> Result<WebSocketStream, TTSError>`
    - 11 个测试全部通过: 3 rate + 3 pitch + 4 ssml + 1 engine_constructor
  </acceptance_criteria>

  <done>
    SSML 生成、rate/pitch 转换、EdgeTtsEngine 结构体和 WebSocket 连接方法全部可用，11 个测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: Edge-TTS 流式接收循环 + 词边界解析 + 重试 + TtsProvider impl + synthesize 分支</name>

  <files>
    src/tts/edge_tts.rs
    src/tts/mod.rs
  </files>

  <read_first>
    src/tts/edge_tts.rs（Task 1 已创建的 SSML 生成函数和 EdgeTtsEngine 结构体）
    src/tts/mod.rs（TtsProvider trait、synthesize 路由器）
    src/error.rs（TTSError 枚举所有变体）
  </read_first>

  <action>
    1. 在 src/tts/edge_tts.rs 的 `EdgeTtsEngine` impl 块末尾（`connect()` 方法之后）添加：

    ```rust
    use futures_util::StreamExt;
    use tokio_tungstenite::tungstenite::Message;

    impl EdgeTtsEngine {
        /// 执行带重试的 TTS 合成
        ///
        /// 对应 D-03: 3 次重试，间隔 1 秒
        async fn synthesize_with_retry(
            &self,
            ssml: &str,
            output_path: &Path,
        ) -> Result<TtsOutput, TTSError> {
            let max_retries = 3;
            let mut last_error = None;

            for attempt in 1..=max_retries {
                tracing::info!("Edge-TTS 合成尝试 {}/{}", attempt, max_retries);

                match self.synthesize_once(ssml, output_path).await {
                    Ok(output) => return Ok(output),
                    Err(e) => {
                        tracing::warn!("Edge-TTS 合成尝试 {} 失败: {}", attempt, e);
                        last_error = Some(e);
                        if attempt < max_retries {
                            tokio::time::sleep(std::time::Duration::from_secs(1)).await;
                        }
                    }
                }
            }

            Err(TTSError::RetryExhausted(format!(
                "Edge-TTS 重试 {} 次后仍失败: {}",
                max_retries,
                last_error.map_or("未知错误".to_string(), |e| e.to_string())
            )))
        }

        /// 单次 TTS 合成（无重试）
        async fn synthesize_once(
            &self,
            ssml: &str,
            output_path: &Path,
        ) -> Result<TtsOutput, TTSError> {
            let mut ws_stream = self.connect().await?;

            // 生成 SSML 请求中的 request ID
            let request_id = uuid::Uuid::new_v4().to_string();

            // 构建并发送 STT（Speech-Text-Text）WebSocket 消息
            // 格式: ---{request_id}\r\nContent-Type: application/ssml+xml\r\n\r\n{ssml}
            // 对应 D-10: Edge-TTS 流式接收 audio + WordBoundary/SentenceBoundary chunk
            let stt_message = format!(
                "X-RequestId:{}\r\nContent-Type:application/ssml+xml\r\n\r\n{}",
                request_id, ssml
            );

            ws_stream
                .send(Message::Text(stt_message))
                .await
                .map_err(|e| TTSError::ConnectionFailed(format!("发送 SSML 失败: {}", e)))?;

            // 接收并处理响应流
            let mut audio_data: Vec<u8> = Vec::new();
            let mut word_boundaries: Vec<WordBoundary> = Vec::new();
            let mut duration: f64 = 0.0;
            let mut is_turn_start = false;

            while let Some(msg_result) = ws_stream.next().await {
                let msg =
                    msg_result.map_err(|e| TTSError::SynthesisFailed(format!("接收消息失败: {}", e)))?;

                match msg {
                    Message::Binary(data) => {
                        // 解析二进制消息头（格式: Path: {path}\r\n...\r\n\r\n{payload}）
                        // 对应 D-10: WordBoundary/SentenceBoundary 事件通过路径判断
                        if let Some(content) = parse_edge_tts_binary(&data) {
                            if content.path == "audio" {
                                audio_data.extend_from_slice(&content.payload);
                            } else if content.path == "turn.start" {
                                // 解析 metadata JSON 中的词边界信息
                                if let Ok(metadata) =
                                    serde_json::from_str::<serde_json::Value>(
                                        &String::from_utf8_lossy(&content.payload),
                                    )
                                {
                                    if let Some(wbs) = metadata["wordboundary"].as_array() {
                                        for wb in wbs {
                                            if let (Some(start), Some(end), Some(text)) = (
                                                wb["offset"].as_u64(),
                                                wb["duration"].as_u64(),
                                                wb["text"].as_str(),
                                            ) {
                                                word_boundaries.push(WordBoundary {
                                                    start_offset: start,
                                                    end_offset: start + end,
                                                    text: text.to_string(),
                                                });
                                            }
                                        }
                                    }
                                }
                                is_turn_start = true;
                            } else if content.path == "turn.end" {
                                // 解析结束事件中的音频时长
                                if let Ok(metadata) =
                                    serde_json::from_str::<serde_json::Value>(
                                        &String::from_utf8_lossy(&content.payload),
                                    )
                                {
                                    duration = metadata["audio_duration"]
                                        .as_f64()
                                        .unwrap_or(0.0);
                                } else {
                                    duration = 0.0;
                                }
                                break;
                            }
                        }
                    }
                    Message::Text(text) => {
                        // 文本消息通常是调试或连接信息，忽略
                        tracing::debug!("Edge-TTS 文本消息: {}", text);
                    }
                    Message::Close(frame) => {
                        tracing::debug!("Edge-TTS 连接关闭: {:?}", frame);
                        break;
                    }
                    _ => {
                        tracing::debug!("Edge-TTS 忽略消息类型");
                    }
                }
            }

            // 写入音频文件（对应 D-04: MP3 格式）
            if audio_data.is_empty() && !is_turn_start {
                return Err(TTSError::SynthesisFailed("未收到音频数据".to_string()));
            }

            tokio::fs::write(output_path, &audio_data)
                .await
                .map_err(|e| {
                    TTSError::SynthesisFailed(format!("写入音频文件失败: {}", e))
                })?;

            Ok(TtsOutput {
                audio_file_path: output_path.to_path_buf(),
                word_boundaries,
                duration,
            })
        }
    }

    /// 解析 Edge-TTS 二进制消息
    ///
    /// 消息格式: Path: {path}\r\nX-RequestId: {uuid}\r\nContent-Type: {mime}\r\n\r\n{payload}
    struct EdgeTtsContent {
        path: String,
        payload: Vec<u8>,
    }

    fn parse_edge_tts_binary(data: &[u8]) -> Option<EdgeTtsContent> {
        // 查找 header-payload 分隔符
        let separator = b"\r\n\r\n";
        let sep_pos = data.windows(4).position(|w| w == separator)?;

        let header = std::str::from_utf8(&data[..sep_pos]).ok()?;
        let payload = data[sep_pos + 4..].to_vec();

        // 从 header 中提取 Path 字段
        let path = header
            .lines()
            .find(|line| line.starts_with("Path:"))
            .and_then(|line| line.strip_prefix("Path:"))
            .map(|p| p.trim().to_string())?;

        Some(EdgeTtsContent { path, payload })
    }
    ```

    2. 在 src/tts/edge_tts.rs 中，在上面的 impl 块之后添加 TtsProvider trait 实现：

    ```rust
    #[async_trait]
    impl TtsProvider for EdgeTtsEngine {
        async fn synthesize(
            &self,
            text: &str,
            voice_name: &str,
            rate: f64,
            pitch: f64,
            output_path: &Path,
        ) -> Result<TtsOutput, TTSError> {
            let ssml = build_ssml(text, voice_name, rate, pitch);
            self.synthesize_with_retry(&ssml, output_path).await
        }
    }
    ```

    3. 在 src/tts/mod.rs 的 `synthesize` 函数中，将 match 分支改为：

    ```rust
    pub async fn synthesize(
        engine: &str,
        text: &str,
        voice_name: &str,
        rate: f64,
        pitch: f64,
        output_path: &Path,
    ) -> Result<TtsOutput, TTSError> {
        match engine {
            "edge_tts" => {
                let edge_engine = EdgeTtsEngine::new(false, String::new(), String::new());
                edge_engine.synthesize(text, voice_name, rate, pitch, output_path).await
            }
            _ => Err(TTSError::UnknownEngine {
                engine: engine.to_string(),
            }),
        }
    }
    ```

    注意：此处 `EdgeTtsEngine::new(false, String::new(), String::new())` 是简化调用。
    Phase 6 纪录片流水线集成时，`synthesize()` 调用方应传入 ConfigManager proxy 配置来创建引擎。
    这是合理的——synthesize() 是顶层便利函数，外部调用方负责提供配置上下文。

    同时，在 src/tts/mod.rs 文件顶部添加：
    ```rust
    mod edge_tts;
    ```
    在 `use` 语句之后（确保 mod 声明靠近文件顶部）。

    并删除之前 Task 1 中 synthesize 函数匹配块中的 cmets：
    ```rust
    // Plan 03-02 将添加 "edge_tts" => EdgeTtsEngine::new(...).synthesize(...) 分支
    ```

    4. 在 src/tts/edge_tts.rs 末尾的测试模块中添加一个集成测试（需要网络，可用 `#[ignore]` 标记）：

    ```rust
    /// 集成测试：需要网络连接访问微软 Edge TTS 服务
    /// 运行方式: cargo test --lib edge_tts::tests::test_edge_tts_integration -- --ignored
    #[tokio::test]
    #[ignore]
    async fn test_edge_tts_integration() {
        use std::path::Path;
        use tempfile::TempDir;

        let engine = EdgeTtsEngine::new(false, String::new(), String::new());
        let dir = TempDir::new().expect("创建临时目录失败");
        let output_path = dir.path().join("test_output.mp3");

        let result = engine
            .synthesize("这是一个测试。", "zh-CN-XiaoyiNeural", 1.0, 0.0, &output_path)
            .await;

        assert!(result.is_ok(), "Edge-TTS 集成测试失败: {:?}", result.err());
        let output = result.unwrap();
        assert!(output.audio_file_path.exists(), "音频文件应存在");
        assert!(
            output.audio_file_path.metadata().unwrap().len() > 1000,
            "音频文件大小应大于 1KB"
        );
        assert!(output.duration > 0.0, "时长应大于 0");
        // 中文语音应有词边界数据
        assert!(!output.word_boundaries.is_empty(), "应有词边界数据");
    }
    ```

    注意：在測試中导入 TempDir：
    ```
    use tempfile::TempDir;
    ```
    tempfile 已在 `[dev-dependencies]` 中（Phase 1 已添加）。

    5. 在 src/tts/edge_tts.rs 文件顶部，补充 serde_json 导入：
    ```rust
    ```  <!-- 已有 use crate::... 导入，确保 serde_json 在依赖中可用（已在 Cargo.toml 中） -->
  </action>

  <verify>
    <automated>cargo build 2>&1 | grep -E "^error" && echo "BUILD_FAILED" || echo "BUILD_OK"</automated>
    <automated>cargo test --lib edge_tts::tests 2>&1 | tail -15</automated>
    <automated>cargo test --lib tts::tests 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/tts/edge_tts.rs 包含 `impl TtsProvider for EdgeTtsEngine`
    - src/tts/edge_tts.rs 包含 `async fn synthesize_with_retry(...)` 含 3 次重试循环
    - src/tts/edge_tts.rs 包含 `fn parse_edge_tts_binary(...)` 解析 Path/X-RequestId/Content-Type 格式
    - src/tts/edge_tts.rs 包含 `struct EdgeTtsContent` 用于二进制消息解析
    - src/tts/edge_tts.rs 包含 `#[ignore]` 标记的 `test_edge_tts_integration` 测试
    - src/tts/mod.rs synthesize() 包含 `"edge_tts" =>` 分支
    - src/tts/mod.rs 包含 `mod edge_tts;` 声明
    - cargo build 零错误零警告
  </acceptance_criteria>

  <done>
    Edge-TTS 引擎完整实现：WebSocket 流式接收、MP3 音频保存、词边界解析、3 次重试、TtsProvider trait 实现和 synthesize("edge_tts", ...) 路由分支全部就绪
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EdgeTtsEngine → wss://speech.platform.bing.com | 外部 WebSocket 连接到微软 Edge TTS 服务 |
| config.proxy → WebSocket connector | 代理配置影响网络路由路径 |
| SSML 构建 → WebSocket 发送 | 文本内容嵌入 XML 可能产生注入风险 |
| 音频文件写入 → output_path | 文件系统写入路径可能被操纵 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | build_ssml() | mitigate | XML 特殊字符转义（& → &amp;, < → &lt;, > → &gt;），防止 SSML 注入 |
| T-03-04 | S (Spoofing) | WebSocket 连接 | mitigate | wss:// 硬编码，TLS 保护连接；目标 URL 不可配置，无法重定向 |
| T-03-05 | I (Info Disclosure) | proxy config 日志 | accept | 代理地址在 tracing::info 中输出，同 Python 版行为；不含认证凭据 |
| T-03-06 | E (Elevation) | output_path 写入 | accept | output_path 由调用方控制，Phase 6 流水线授予安全路径 |
</threat_model>

<verification>
1. cargo build 零错误零警告
2. cargo test --lib edge_tts::tests: 11 个单元测试通过
3. cargo test --lib tts::tests: 3 个核心测试通过
4. cargo test --lib error::tests: 5 个 TTSError 测试通过
5. cargo test --lib: 全部已有 + 新增测试通过
</verification>

<success_criteria>
- [ ] cargo build 零错误零警告
- [ ] 11 个 edge_tts 单元测试通过（rate/pitch conversion + SSML generation + engine constructor）
- [ ] 3 个 tts 模块测试通过（mock trait impl + unknown engine dispatch + error message）
- [ ] 5 个 TTSError 测试通过
- [ ] EdgeTtsEngine 实现 TtsProvider trait
- [ ] synthesize() 有 "edge_tts" 分支
- [ ] 3 次重试逻辑实现
- [ ] 二进制消息解析（Path/Content-Type header 解析）
</success_criteria>

<output>
After completion, create `.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-02-SUMMARY.md`
</output>
