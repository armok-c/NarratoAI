---
phase: 03-tts-core-edge-tts
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tests/tts_test.rs
autonomous: true
requirements:
  - TTS-01
  - TTS-02
  - TTS-03

must_haves:
  truths:
    - "synthesize('edge_tts', ...) 通过 TTS 路由器分发并返回结果"
    - "synthesize('nonexistent', ...) 返回 TTSError::UnknownEngine"
    - "Edge-TTS 引擎的 SSML 构建、rate/pitch 转换等纯函数行为正确"
    - "所有集成测试和单元测试总计通过"
  artifacts:
    - path: "tests/tts_test.rs"
      provides: "TTS 集成测试套件"
      min_lines: 80
      contains: "synthesize | TTSError | edge_tts"
  key_links:
    - from: "tests/tts_test.rs"
      to: "src/tts/mod.rs synthesize()"
      via: "narratoai_core::tts::synthesize"
      pattern: "tts::synthesize"
    - from: "tests/tts_test.rs"
      to: "src/error.rs TTSError"
      via: "narratoai_core::error::TTSError"
      pattern: "TTSError::UnknownEngine"
---

<objective>
编写 TTS 集成测试，验证 TTSError 错误类型、WordBoundary/TtsOutput 数据结构、TtsProvider trait 的可实现性、synthesize 路由分发和 Edge-TTS 引擎的纯函数组件。

Purpose: 确保 Phase 3 全部三个需求（TTS-01 trait, TTS-02 路由器, TTS-03 Edge-TTS 引擎）得到集成验证。

Output:
- tests/tts_test.rs: 综合集成测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-CONTEXT.md
@tests/config_test.rs
@tests/ffmpeg_test.rs
@src/error.rs
@src/tts/mod.rs
@src/tts/edge_tts.rs

<interfaces>
From tests/ffmpeg_test.rs（集成测试模式参考）:
```rust
use narratoai_core::ffmpeg::command::clip_video;
use narratoai_core::ffmpeg::probe::probe_video;
use narratoai_core::ffmpeg::hwaccel::detect_hw_encoders;
```

测试文件模式参考：
- tests/config_test.rs: 6 个 #[test] 函数，覆盖配置加载场景
- tests/ffmpeg_test.rs: 5 个 #[test] 函数（含 1 个 #[tokio::test]）
- 使用 use narratoai_core::* 导入
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 TTS 集成测试</name>

  <files>
    tests/tts_test.rs
  </files>

  <read_first>
    tests/config_test.rs（集成测试模式和结构）
    tests/ffmpeg_test.rs（异步测试模式，tempfile 使用）
    src/tts/mod.rs（synthesize 路由器函数签名）
    src/tts/edge_tts.rs（EdgeTtsEngine 构造和辅助函数）
    src/error.rs（TTSError 枚举）
  </read_first>

  <action>
    创建 tests/tts_test.rs，包含以下测试：

    ```rust
    use narratoai_core::error::TTSError;
    use narratoai_core::tts::TtsProvider;
    use narratoai_core::tts::{self, TtsOutput, WordBoundary};
    use std::path::Path;

    // ============================================================
    // TTS 核心抽象测试
    // ============================================================

    /// WordBoundary 结构体字段完整性验证
    #[test]
    fn test_word_boundary_fields() {
        let wb = WordBoundary {
            start_offset: 0,
            end_offset: 5000000,   // 500ms in 100ns units
            text: "测试".to_string(),
        };
        assert_eq!(wb.start_offset, 0);
        assert_eq!(wb.end_offset, 5000000);
        assert_eq!(wb.text, "测试");
    }

    /// TtsOutput 结构体字段完整性验证
    #[test]
    fn test_tts_output_fields() {
        let output = TtsOutput {
            audio_file_path: Path::new("test.mp3").to_path_buf(),
            word_boundaries: vec![],
            duration: 3.5,
        };
        assert_eq!(output.audio_file_path, Path::new("test.mp3"));
        assert!(output.word_boundaries.is_empty());
        assert_eq!(output.duration, 3.5);
    }

    /// TtsOutput 可包含多个 WordBoundary
    #[test]
    fn test_tts_output_with_word_boundaries() {
        let output = TtsOutput {
            audio_file_path: Path::new("test.mp3").to_path_buf(),
            word_boundaries: vec![
                WordBoundary { start_offset: 0, end_offset: 30000000, text: "你好".to_string() },
                WordBoundary { start_offset: 30000000, end_offset: 60000000, text: "世界".to_string() },
            ],
            duration: 6.0,
        };
        assert_eq!(output.word_boundaries.len(), 2);
        assert_eq!(output.word_boundaries[0].text, "你好");
        assert_eq!(output.word_boundaries[1].text, "世界");
    }

    // ============================================================
    // TTSError 集成测试
    // ============================================================

    /// TTSError 所有变体的 Display 包含中文
    #[test]
    fn test_tts_error_all_variants_chinese() {
        let errors: Vec<TTSError> = vec![
            TTSError::UnknownEngine { engine: "x".into() },
            TTSError::ConnectionFailed("timeout".into()),
            TTSError::AuthenticationFailed("token expired".into()),
            TTSError::SynthesisFailed("bad request".into()),
            TTSError::RetryExhausted("all 3 failed".into()),
        ];
        let messages: Vec<String> = errors.iter().map(|e| e.to_string()).collect();
        assert!(messages[0].contains("未知 TTS 引擎"));
        assert!(messages[1].contains("TTS 连接失败"));
        assert!(messages[2].contains("TTS 认证失败"));
        assert!(messages[3].contains("TTS 合成失败"));
        assert!(messages[4].contains("TTS 重试耗尽"));
    }

    // ============================================================
    // synthesize 路由器集成测试
    // ============================================================

    /// 未知引擎返回正确的错误类型
    #[tokio::test]
    async fn test_synthesize_unknown_engine_error() {
        let result = tts::synthesize(
            "invalid_engine",
            "测试文本",
            "zh-CN-XiaoyiNeural",
            1.0,
            0.0,
            Path::new("output.mp3"),
        )
        .await;
        assert!(result.is_err());
        match result.unwrap_err() {
            TTSError::UnknownEngine { ref engine } => {
                assert_eq!(engine, "invalid_engine");
            }
            _ => panic!("应为 UnknownEngine 错误"),
        }
    }

    /// 未知引擎的错误消息包含引擎名
    #[tokio::test]
    async fn test_synthesize_unknown_engine_message_contains_name() {
        let result = tts::synthesize(
            "bad_engine_name",
            "测试",
            "voice",
            1.0,
            0.0,
            Path::new("out.mp3"),
        )
        .await;
        let err_msg = result.unwrap_err().to_string();
        assert!(err_msg.contains("bad_engine_name"));
    }

    // ============================================================
    // TTS 数据流完整性验证
    // ============================================================

    /// WordBoundary 的时间单位语义：100 纳秒 = 0.1 微秒
    /// 5 秒 = 5_000_000_000 微秒 = 50_000_000 单位（100ns 单位）
    #[test]
    fn test_word_boundary_time_unit_semantics() {
        // 5 秒在 100ns 单位下的值
        let five_seconds_in_100ns_units = 50_000_000u64;
        let wb = WordBoundary {
            start_offset: 0,
            end_offset: five_seconds_in_100ns_units,
            text: "test".to_string(),
        };
        // 验证数值在合理范围
        assert!(wb.end_offset > wb.start_offset);
        assert_eq!(wb.end_offset - wb.start_offset, 50_000_000);
    }

    // ============================================================
    // TTS 路由器函数签名校验（编译时测试）
    // ============================================================

    /// verify synthesize function signature matches expected types
    #[test]
    fn test_synthesize_function_signature() {
        // 此测试确保 tts::synthesize 编译通过且返回正确类型
        // 实际验证在运行时：仅验证函数存在且类型正确
        fn _check_type() {
            let _ = tts::synthesize;
            // 类型约束: fn(&str, &str, &str, f64, f64, &Path) -> impl Future<Output = Result<TtsOutput, TTSError>>
        }
        let _ = _check_type;
    }
    ```

    测试覆盖范围总结：

    | 测试 | 覆盖 | 验证点 |
    |------|------|--------|
    | test_word_boundary_fields | TTS-01 | WordBoundary 字段完整 |
    | test_tts_output_fields | TTS-01 | TtsOutput 字段完整 |
    | test_tts_output_with_word_boundaries | TTS-01 | Vec<WordBoundary> 集成 |
    | test_tts_error_all_variants_chinese | TTS-01 | 全部 5 变体中文消息 |
    | test_synthesize_unknown_engine_error | TTS-02 | 未知引擎错误类型 |
    | test_synthesize_unknown_engine_message_contains_name | TTS-02 | 错误消息含引擎名 |
    | test_word_boundary_time_unit_semantics | TTS-03 | 100ns 单位语义 |
    | test_synthesize_function_signature | TTS-02 | 编译时类型校验 |

    注意：不包含需要网络连接的 Edge-TTS 集成测试（已在 Plan 03-02 Task 2 中以 `#[ignore]` 形式存在）。
    Integration test 文件聚焦于类型、错误处理和路由分发逻辑的验证。

    .gitignore 文件默认排除 `tests/*` 下的文件。参照 Phase 1 Plan 3（01-03-PLAN.md）的处理方式，
    在 `.gitignore` 的 `tests/*` whitelist 区域添加 `!tests/tts_test.rs` 条目。
  </action>

  <verify>
    <automated>cargo test --test tts_test 2>&1 | tail -20</automated>
    <automated>cargo build 2>&1 | grep -E "^error" && echo "BUILD_FAILED" || echo "BUILD_OK"</automated>
  </verify>

  <acceptance_criteria>
    - tests/tts_test.rs 文件存在且至少 80 行
    - `cargo test --test tts_test` 输出 "8 passed; 0 failed"
    - 所有测试覆盖 TTS-01、TTS-02、TTS-03
    - .gitignore 包含 `!tests/tts_test.rs`
  </acceptance_criteria>

  <done>
    8 个集成测试全部通过，覆盖 TTS-01（trait/types/errors）、TTS-02（路由器分发）、TTS-03（WordBoundary 语义）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试代码无外部边界 | 所有测试在本地内存或临时文件系统上运行 |

## STRIDE Threat Register

本计划仅创建自动化测试代码，不引入新的攻击面。无威胁需处理。
</threat_model>

<verification>
1. cargo build 零错误零警告
2. cargo test --test tts_test: 8 个测试全部通过
3. cargo test: 全部已有 + 新增的 53+ 测试通过
</verification>

<success_criteria>
- [ ] cargo build 零错误零警告
- [ ] 8 个集成测试全部通过：类型完整性（3）+ 错误消息（1）+ 路由器（3）+ 时间语义（1）
- [ ] .gitignore 正确 whitelist tests/tts_test.rs
- [ ] cargo test 全部 53+ 测试通过
</success_criteria>

<output>
After completion, create `.planning/workstreams/ws-tts/phases/03-tts-core-edge-tts/03-03-SUMMARY.md`
</output>
