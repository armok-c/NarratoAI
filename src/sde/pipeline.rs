use crate::sde::types::SdePipelineState;

/// SDE 流水线主入口——在 07-04-PLAN.md 实现完整 9 步编排
pub async fn run_sde(_state: &mut SdePipelineState) -> Result<(), crate::sde::error::SdeError> {
    unimplemented!("SDE pipeline will be implemented in plan 07-04")
}
